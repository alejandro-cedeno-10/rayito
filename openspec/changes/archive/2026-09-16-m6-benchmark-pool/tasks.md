## 0. [pre-flight] Account, budget and baseline

- [x] 0.1 `aws sts get-caller-identity` with `AWS_PROFILE=<tu-perfil> AWS_REGION=us-east-1`; `aws lambda-microvms list-microvm-images` and `list-microvms --image-identifier <rayito-base arn>`: no `RUNNING|SUSPENDED|PENDING` MicroVM of `rayito-base` left over from M5; note every image in the account that is not `rayito-base` / `rayito-m0-probe` (design D12 item 6)
- [x] 0.2 Run the design D12 Cost Explorer query for `Start=2026-09-14,End=2026-09-17`; save the raw JSON as `docs/benchmarks/raw/2026-09-cost-explorer-<date>.json`; confirm which days are still `Estimated` (re-query in task 5.1)

## 1. [image-variant] Sidecar warm-up flag and `--variant` in the scripts

- [x] 1.1 `kernel-sidecar/ipython/startup/0004_warmup.py`: read the sibling `warmup_variant` marker; content `slim` → return before any import; docstring updated (design D7)
- [x] 1.2 `kernel-sidecar/ipython/startup/0001_charts.py`: register `_repr_e2b_chart_` with `for_type_by_name("matplotlib.figure", "Figure", …)` instead of patching `Figure` (no `matplotlib` import at kernel start); `test_startup_scripts_produce_chart_data_and_png` still green
- [x] 1.3 (amendment 2: `kernel-sidecar/tests/test_variant.py`; amendment 12: plus `kernel-sidecar/tests/test_formatters.py`, in-process `InteractiveShell` + `select_figure_formats`, runs in the non-kernel CI job and on Windows) `kernel-sidecar/tests/test_kernel.py`: `test_slim_variant_starts_without_the_stack` (temporary sidecar root with the marker: `numpy`/`pandas`/`matplotlib` absent from `sys.modules` after `ready`; a `plt.plot` cell still yields `e2b/chart` + `image/png`); `cd kernel-sidecar && uv run pytest && uv run ruff check . && uv run ruff format --check . && uv run mypy src` clean
- [x] 1.4 `scripts/image_zip.py`: `--variant full|slim` (argparse, positional `image_dir` and `destination` kept); `slim` writes the synthetic entry `kernel-sidecar/ipython/startup/warmup_variant` = `slim\n` (0644, fixed date) into the zip only; docstring updated
- [x] 1.5 `scripts/publish_image.py`: `--variant full|slim`; default image name `rayito-base` / `rayito-base-slim`; the artifact's marker must match the flag or `SystemExit` before upload; `--image-name` still overrides; docstring updated; `IMAGE_HOOKS` and `--memory-mib` untouched
- [x] 1.6 `Makefile`: `image-zip-slim` (`image/rayito-image-slim.zip`), `image-publish-slim`, `clean` removes the slim zip; `.github/workflows/ci.yml`: after the existing `image_zip.py` step, build the slim zip and assert (python one-liner) the marker entry is present in it and absent in `image/rayito-image.zip`
- [x] 1.7 (amendment 2: `scripts/tests/test_image_variant.py`, run by `make test-scripts` and by the CI step "scripts/ unit tests" after the Python client job) `clients/python/tests/unit/test_scripts_image_variant.py` (adds `scripts/` to `sys.path`): slim zip has the marker with `slim\n`, full zip does not, exclusions unchanged; `publish_image` default names per variant; `--variant slim` refuses a full zip and `--variant full` refuses a slim zip; `uvx ruff check scripts` clean

## 2. [bench] `scripts/bench_cold_start.py` and its unit tests

- [x] 2.1 CLI and plan (design D1): `--template`, `--template-slim`, `--sequential`, `--bursts`, `--burst-modes`, `--resume-cycles`, `--phases`, `--out`, `--execution-role-arn`, `--budget-usd`, `--dry-run`, `--report <json>`; `plan_from_args` yields the batch order sdk 5, raw 5, sdk 10, raw 10, sdk 20, raw 20; `--dry-run` prints the plan and the estimate and exits 0 (or 2 over budget) without any AWS call
- [x] 2.2 Launch helper (design D2): `build_launch_plan(... timeout=600 ...)` → launcher → `create-microvm-auth-token` (8080, 60 min) → `HealthProbe` → `Sandbox.connect(microvm_id, access_token=plan.access_token, control_plane=plane)` → cells → `terminate-microvm` in `finally`; `LaunchSample` with every field of D2/D9; `error` samples for `ClientError` (name + `retry_after`) and `not_ready` (120 s, one `get-microvm` for `state`/`stateReason`)
- [x] 2.3 Launchers (design D3): shared `LambdaMicrovmsControlPlane.from_session(region)` (`sdk`) and a one-attempt boto3 client (`raw`, `user_agent_extra="rayito-bench"`); bursts via `ThreadPoolExecutor(max_workers=N)` with `batch_t0`, `batch_wall_s`, `kernel_ready_from_batch_s`, `throttled`, `failed`; 30 s between batches, batch VMs terminated first
- [x] 2.4 (amendment 10: bench-owned `TransportSettings(options=PROBE_CHANNEL_OPTIONS)` with `initial`/`max` reconnect backoff 100 ms and `min` 500 ms, `Health(timeout=0.5)`, per-poll return timestamps → `agent_ready_gap_s`, `kernel_ready_gap_s`, `resume_gap_s`; validated on AWS, run 201841Z) `HealthProbe` (design D4): bench-owned channel (`TransportSettings().open_channel`, `ProxyAuthPlugin(store, port=8080, access_token=None)`, `TokenStore`), `HealthServiceStub.Health(timeout=1.0)`, 100 ms fixed interval, `is_not_yet_reachable` → not yet, `RESOURCE_EXHAUSTED` → `throttled_polls` + 500 ms, other → error; `wait_ready()` returns `agent_ready_s`, `kernel_ready_s`, `agent_uptime_ms_at_ready`, poll counts; `wait_generation_above(seen)` for phase c; `rtt_ms()` = median of five round trips
- [x] 2.5 Phase a (sequential) and phase e (slim): N launches with `first_cell_s` (`1+1` → `"2"`) and `stack_cell_s` (the D6 stack cell → `int(text) > 0`) via `run_code(timeout=60)`; images block from `get-microvm-image-version` + `list-microvm-image-builds` + `get-microvm-image-build`
- [x] 2.6 Phase c (design D5): VM A `idle=None` explicit cycles (`pause()` timed, dwell 5 s, `resume(wait=False)` + `wait_generation_above`, `get_health()`, `run_code("x")` timed with `kernel_alive`); VM B `IdlePolicy(max_idle_seconds=60, suspended_duration_seconds=600, auto_resume=True)` auto cycles (`commands.run("echo back")` timed, `run_code("x")`); the two VMs in two threads; `x = 42` and a warm `1+1` before the cycles; `not_suspended` error rule
- [x] 2.7 (amendment 11: any exception leaving the phases → sweep + JSON with `meta.aborted: true` before the survivor listing; `terminate-microvm` failures of any type leave the VM pending for the sweep and are printed at exit; image lookup guarded with `meta.images.<variant>.error`) Guardrails (design D8): `maximumDurationInSeconds=600` everywhere; `VM_WALL_BUDGET_SECONDS=300` watchdog at phase boundaries and exit (`over_budget` flag); pre-flight `list-microvms` > 10 live → refuse; exit sweep of tracked VMs + survivor listing for both images; `estimate_cost` with the D8 formula and `--budget-usd` refusal; `KeyboardInterrupt` → sweep + JSON with `meta.aborted`; `boto3`/`grpc` loggers at WARNING; no token/JWE/payload ever printed
- [x] 2.8 Output (design D9/D10): `docs/benchmarks/raw/2026-09-cold-start-<runid>.json` with `schema`, `meta` (incl. `client_rtt_ms`, versions, images), `phases`, `cost`; `summarize` (median + nearest-rank p95, min, max, n over error-free samples); `render_tables` prints the D10 Markdown tables; `--report <json>` renders an existing file without AWS
- [x] 2.9 (amendment 2: `scripts/tests/test_bench_cold_start.py`; amendment 12: plus `scripts/tests/test_bench_guardrails.py` — fake `ControlPlane`, fake `HealthServiceStub`, fake clock: poll status branches and gaps, `wait_ready` timings and 120 s cap, `boot_vm` `not_ready` and throttled raw launch, `enforce_budget` flag + terminate, sweep under a failing terminate, guarded image block, Ctrl-C and crash exit paths writing `meta.aborted: true`; both files run by `make test-scripts` and by the CI step "scripts/ unit tests" after the Python client job) `clients/python/tests/unit/test_scripts_bench.py` (design D14): `percentile`, `summarize`, `estimate_cost` (defaults ≈ $1.5; budget refusal), `plan_from_args`, `render_tables`, JSON key round-trip, watchdog selection with a fake clock; no network
- [x] 2.10 `Makefile`: `bench-cold-start` (refuses without `RAYITO_E2E=1` and `RAYITO_TEMPLATE`; `RAYITO_TEMPLATE_SLIM` optional; `BENCH_ARGS` passthrough; runs from `clients/python` with `uv run python ../../scripts/bench_cold_start.py`); `uvx ruff check scripts` clean; `cd clients/python && uv run pytest tests/unit && uv run ruff check . && uv run ruff format --check . && uv run mypy src` clean

## 3. [image] Publish the two subjects and gate the full one

- [x] 3.1 (superseded by design amendment 1: the full subject is the already published `rayito-base` 10.0, no 11.0 built) ~~`make image-publish` → the next `rayito-base` version (**11.0** below; renumber if Track A published first) from the current tree (`rayd` unchanged, sidecar with tasks 1.1–1.2); three-state gate; record `snapshotBuild` sizes, `chipsetGeneration`, build seconds and the artifact key next to 10.0's (919 146 496 / 1 290 379 264 / 37 462 016 B, 215.8 s)~~
- [x] 3.2 (done by hand from the 10.0 artifact, amendment 1; two versions, amendment 9: 1.0 = 750 694 400 / 1 291 976 704 / 35 332 096 B, gen 4, 195.4 s, `rayd-68b9bb092c3e.zip`; 2.0 = 693 841 920 / 1 289 314 304 / 31 604 736 B, gen 3, 174.8 s, `rayd-a4b0266acee7.zip`; code install within 0.2 % of 10.0) `make image-publish-slim` → `rayito-base-slim` **1.0**; three-state gate; record the same fields; confirm `memorySnapshotSizeInBytes` dropped (expected ≈ 570 MB, Q34) and `codeInstallSizeInBytes` matches 11.0
- [x] 3.3 (not applicable: no 11.0; the lazy formatters were verified with a real-kernel unit test and a manual `plt.show()` check on `rayito-base-slim` 2.0, report §10) ~~`RAYITO_E2E=1 RAYITO_TEMPLATE=<rayito-base arn> make test-e2e` green on 11.0 (M1–M5 suites; `results[0].png` and `e2b/chart` prove the lazy chart registration on real AWS); if red because of `0001_charts.py`, apply the design "Risks" fallback and republish before continuing~~

## 4. [bench] The run (real AWS)

- [x] 4.1 `--dry-run` with both templates: plan and estimate printed, estimate ≤ $5, no AWS call made; then repeat task 0.1's `list-microvms` for both images (no live VM); paste the dry-run output into the report appendix
- [x] 4.2 `RAYITO_E2E=1 RAYITO_TEMPLATE=<rayito-base arn> RAYITO_TEMPLATE_SLIM=<rayito-base-slim arn> make bench-cold-start` (defaults: 20 sequential, bursts 5,10,20 × sdk,raw, 10 + 10 cycles, slim sequential 20); run in one sitting from the usual client (≈ 93 ms RTT); JSON written under `docs/benchmarks/raw/`
- [x] 4.3 After the run: `list-microvms` for both images shows no `PENDING|RUNNING|SUSPENDING|SUSPENDED`; `phases.*.samples` contain no `over_budget`; every `kernel_alive` is `true`; any `error` samples explained (raw-burst `ThrottlingException` is expected data, anything else is investigated and, if needed, the phase rerun with `--phases`)
- [x] 4.4 Note the wall time of the run and the sum of `vm_alive_s`, `launches`, `suspends`, `resumes`, expected snapshot GB (the `cost` block) for task 5
- [x] 4.5 (amendment 10) Validation run of the corrected probe: `--phases a,e --sequential 5` → `raw/2026-09-cold-start-20260916T201841Z.json` (10 launches, ≈ $0.015, every VM `TERMINATED`; a first attempt with `min_reconnect_backoff_ms=100` gave 5 × `not_ready` and one VM terminated by hand); the report §1 states the resolution of the main runs from their raw data (mean poll period per sample) and the `*_gap_s` p50/p95 of this run; §6 and §9 qualified accordingly

## 5. [cost] Cost Explorer verification (the day after 4.2)

- [ ] 5.1 (pending 2026-09-17: all days still `Estimated` on the bench day, report §8) **Deferred at acceptance (2026-09-16 21:10 UTC, `raw/2026-09-cost-explorer-2026-09-16T2115Z.json`): the 2026-09-16 line is still `Estimated` and identical to the 16:56 UTC query (761 GB-s, 16,49 GB read, no write line: only the hours before the bench), 2026-09-17 empty; nothing to attribute yet. Owner: whoever re-runs the D12 query on 2026-09-17 (and 2026-09-23 for the storage minimum); the target sections are `AWS_API_NOTES.md` §12 "Pendiente (2026-09-17)", report §8 and Q22.** Re-run the design D12 query with `End=<bench day + 2>`; confirm 2026-09-15/16 are no longer `Estimated`; attribute only the five `Lambda-MicroVM-*` usage types; check `list-microvm-images` / `list-microvms` for foreign images active on the bench day and note them
- [x] 5.2 (from the 2026-09-15 lines, all five within 0.21 %, §12 updated) Unit prices: recompute the implied price of each usage type from the final numbers; record the five usage-type names and the verified prices in `AWS_API_NOTES.md` §12
- [ ] 5.3 (pending 2026-09-17; the expected GB per launch/resume and the run's counts are in the report §7; same deferral as 5.1) Per-launch read: `Snapshot-Read-GB(bench day) / (launches + resumes)` weighted by variant vs `memorySnapshotSizeInBytes` (GB vs GiB, extra pages?); real $ per launch for 11.0 and slim 1.0 → §12, report §7/§8, Q22 "Medida"
- [ ] 5.4 (same deferral as 5.1; partial: M0's four suspends of 2026-09-15 give 0.635 GB per suspend ≈ the memory snapshot; §12 rewritten with the 0.919 GB cycle at $0.0049 and the ≈ 140 s break-even; bench-day confirmation pending 2026-09-17) Per-suspend write: `Snapshot-Write-GB(bench day) / suspends`; $ per pause/resume cycle at 2 GB with the measured sizes; the `max_idle_seconds` break-even recomputed → §12 (replaces the 2 GB / $0.0107 assumption)
- [ ] 5.5 (pending 2026-09-17; ratio 1.87–1.89 recorded in §12; same deferral as 5.1) Compute ratio and VM-seconds: memory GB-s ÷ vCPU-s on the bench day vs the JSON's `vm_seconds`; state which quantity is rounded and by how much → §12
- [x] 5.6 (builds leave no write line; storage baseline 67.5 GB-h recorded as shared-account usage; dated re-check notes for 2026-09-17 and 2026-09-23 in §12 and Q22) Builds and storage (Q22): whether the two builds of task 3 appear in any MicroVM line; storage GB-h delta the day after publishing vs the sum of `memory + codeInstall + disk` of 11.0 + slim 1.0; the 2026-09-14 baseline explained or recorded as shared-account usage; leave a dated note to re-check the storage line seven days after publishing
- [x] 5.7 Q8 (1 h active + 7 h suspended, 2 GB) derived from the verified prices and measured sizes, formula shown → §16 Q8 "Medida"

## 6. [report] `docs/benchmarks/2026-09-cold-start.md` and the decision

- [x] 6.1 Write sections 1–7 and 10 of design D10 from the JSON (`--report` tables pasted, every value traceable to a summary key; M0 Q2/Q3, M4 Q35, M5 Q39/Q40 comparisons; the two-point Δ per 100 MB stated as an interpolation)
- [x] 6.2 Section 8 from task 5 (amend the report the day after the run; keep the run-day estimate next to the actual lines)
- [x] 6.3 Section 9 DECISION per design D11: quote `B20` (sdk 20-burst `kernel_ready_s` p95) and `R` (explicit `resume_s` p95), apply the rule verbatim; if no pool: the suspended-pool sketch with its constraints (8 h cap incl. suspended time, `runHookPayload` token handoff as the open point, pool-wide `envs`, memory quota, `list()` visibility, storage cost per parked VM); if pool: ADR-008 in `ARCHITECTURE.md` (context, decision, consequences, follow-up change name) and `SPEC.md` §4 updated

## 7. [docs] Numbers into the repo

- [x] 7.1 `SPEC.md` §7: "Coste real" and "Cold start" bullets rewritten with the measured numbers and the report path; §4 pool non-goal line per the decision
- [x] 7.2 `README.md`: "Coste y latencia (medido 2026-09)" table (create → `kernel_ready` p50/p95 sequential and 20-burst, `resume()` p50/p95, auto-resume p50, first cell p50, $ per launch / per cycle / per hour at 2 GB) linking the report; the `rayito-base ≥ 10.0` note becomes `≥ 11.0` only if the e2e of task 3.3 required a change
- [x] 7.3 `MILESTONES.md` M6: the benchmark bullet replaced by the measured outcome (image versions and sizes, p50/p95s, decision, report path) and a "Track D — aceptado <date>" line with the run cost
- [x] 7.4 `AWS_API_NOTES.md`: §12 (tasks 5.2–5.6), §15 one sentence on full vs slim, §16 Q8 and Q22 filled, two rows added in the table's style with the exact figures — cold start / burst / resume / first cell (11.0 vs slim 1.0) and raw burst 5/10/20 throttling — numbered Q42/Q43 if this track lands first, after the other tracks' rows otherwise
- [x] 7.5 `ARCHITECTURE.md`: Capa 1 "Variantes de imagen" paragraph (marker, `--variant`, `rayito-base-slim` is a measurement image), sidecar warm-up paragraph with the build flag and the measured cost/benefit of the warm-up; ADR-008 only if task 6.3 says pool
- [x] 7.6 (done at acceptance 2026-09-16: Track C had not applied it; docstring now reads "≈ $0,0049 a 2 GB (0,92 GB de snapshot escritos y leídos, SPEC.md §7) ≈ 140 s de cómputo … menor que ≈ 150 s no ahorra"; `docs/site/docs/cost.md` aligned with the same numbers; `uv run pytest tests/unit` 616 passed, `mkdocs build --strict` OK) (handed to Track C, owner of `clients/python/`: replace the `IdlePolicy` cost note with "un ciclo suspend/resume cuesta ≈ $0,0049 a 2 GB (0,92 GB de snapshot escritos y leídos) ≈ 140 s de cómputo, así que un `max_idle_seconds` menor que ≈ 150 s no ahorra dinero") `clients/python/src/rayito/_models.py`: the `IdlePolicy` docstring's cost note ("un ciclo suspend/resume cuesta ≈ 5 min de cómputo") rewritten with the measured per-cycle cost and break-even from task 5.4 (docstring only; no surface change); `uv run pytest tests/unit` still green
- [x] 7.7 `openspec validate m6-benchmark-pool --strict` still passes after any design amendment made during the run (amend `design.md`, never silently deviate)

## 8. [acceptance] Close Track D

- [x] 8.1 (2026-09-16: `171432Z` schema `rayito.bench.cold-start/1`, `meta.aborted` false, phases `sequential`/`bursts`/`resume`/`sequential_slim`, 130 samples, 0 `over_budget`, 0 `error`, `kernel_alive` 20/20; `173200Z` and `201841Z` likewise (20 and 10 samples, 0 errors); report §9 quotes `B20` = 8,567 s (JSON 8.5674) and `R` = 0,400 s (JSON 0.4001) and applies D11 verbatim → suspended pool, ADR-008; SPEC §4/§7, README, MILESTONES, AWS_API_NOTES §12/§16, ARCHITECTURE Capa 1 + ADR-008 checked; gates: sidecar tests per 1.3 recorded by the implementer, `scripts/tests` 44 passed, `uv run pytest tests/unit` 616 passed + ruff/format/mypy clean, `uvx ruff check scripts` clean, `gen_limits.py --check` OK; task 3.3 not applicable (no 11.0); `list-microvms`: 0 live for every image) Acceptance agent re-reads the JSON (`schema`, `meta.aborted: false`, phases present, no `over_budget`, `kernel_alive` all true), the report (every number traceable, DECISION applies the D11 rule to the quoted `B20`/`R`), the docs of section 7, and `list-microvms` for both images (no live VM); confirms the unit/lint gates of tasks 1.3, 1.7, 2.10 and the e2e of task 3.3
- [x] 8.2 (2026-09-16: archived as `2026-09-16-m6-benchmark-pool` with tasks 5.1/5.3/5.4/5.5 explicitly deferred to 2026-09-17; `rayito-base-slim` deleted entirely (1.0 and 2.0) rather than set `INACTIVE`, `make image-publish-slim` rebuilds it) `openspec archive m6-benchmark-pool --yes`; `rayito-base-slim` 1.0 may be set `INACTIVE` afterwards (storage minimum already incurred)
