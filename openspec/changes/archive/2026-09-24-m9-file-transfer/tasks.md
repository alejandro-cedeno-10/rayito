## 0. [infra] Ground truth, measurements before code, IAM

- [x] 0.0 Check the live numbering before writing anything: next free `AWS_API_NOTES.md` §16 rows (this change writes Q70–Q75), `SECURITY.md` threat row (T16), ADR number (ADR-010) and the `StreamError` comment (merge with `m9-server-timeout`'s `sandbox_timeout` if it landed); record the mapping in the task note and use the real numbers everywhere
- [x] 0.1 `AWS_API_NOTES.md` §18 "Transferencias por URLs prefirmadas" exactly as design D20 (every call, every parameter, the source URL per row, the limits and behaviours list, the `rayd` raw-HTTP contract, the sentence that these are the only S3 operations and parameters the SDKs and `rayd` may use for transfers); verify each JavaScript name (`getSignedUrl` options, `createPresignedPost` input, `Upload` options, `S3ClientConfig.requestChecksumCalculation`) against the AWS SDK for JavaScript v3 docs and each Python name against botocore 1.43.94's model and signatures, and record the URLs; verify the `s3:signatureversion` condition key in the S3 condition-key reference before it enters `infra/README.md`
- [x] 0.2 `AWS_API_NOTES.md` §16 rows Q70–Q75 with the question and the docs answer, "Medida" empty: Q70 presigned GET of a missing key with and without prefix-scoped `s3:ListBucket` (404 `NoSuchKey` vs 403 `AccessDenied`), the expired-URL body, `ExpiredToken`; Q71 PUT-complete → visible-inside-the-VM latency with a 1 s poll (p50/p95, 20 samples); Q72 `@aws-sdk/client-s3` presigned `PutObject` with the default checksum setting vs `WHEN_REQUIRED` against real S3 with a non-empty body; Q73 `user.*` xattrs on the guest (`stat -f -c %T /home/user`, `os.setxattr`/`listxattr` as uid 1000 on a file, on a temp renamed over a name, on a symlink); Q74 `grpc-encoding`/`grpc-accept-encoding` through the proxy and gzip throughput; Q75 `rayd` import/export MB/s (10 MiB, 50 MB, 200 MB, multipart) and the binary size delta
- [x] 0.3 [AWS, ≈ $0.02] Measure Q70 (from the dev box with two caller roles or a policy simulation plus a real GET), Q71 and Q73 (on a `rayito-base` 20.0 sandbox via `commands.run` Python snippets, no execution role) and Q72 (a scratch Node script under the session scratchpad, never in the repo); fill the rows with placeholders only (`amzn-s3-demo-bucket`, `microvm-<id>`, `<tu-perfil>`); if Q73 shows `user.*` xattrs are not supported on `/home/user`, stop and report (design D17)
- [x] 0.4 `spike/m0/iam.yaml` per design D20 (`TransferBucket`, `TransferPrefix` with the pattern, `Rules.TransferPrefixIsDisjoint`, `HasTransferBucket`, the two conditional `CallerPolicy` statements; `ExecutionRole` untouched); `scripts/tests/test_iam_template.py` gains the three tests of D20; `uvx cfn-lint==1.56.3 spike/m0/iam.yaml` clean
- [x] 0.5 [AWS, maintainer] Update stack `rayito-m0-iam` with `TransferBucket=<bucket>` and `TransferPrefix=<prefix>`; `aws iam simulate-principal-policy` for `s3:PutObject` on `…/<prefix>/x` (allowed), on `…/rayito/x` via the new statements (not granted by them) and `s3:ListBucket` with `s3:prefix=<prefix>/*` (allowed), outputs in the task note with placeholders
- [x] 0.6 `limits.json` gains every key of design D22; `python scripts/gen_limits.py` regenerates `_limits.py` and `limits.ts`; `--check` passes

## 1. [rayd] Contract (applied by the M9 Contract agent, verified here)

- [x] 1.1 Confirm `proto/rayito/v1/common.proto` and `filesystem.proto` carry exactly design D1 (field numbers, names, the `StartImportRequest.metadata = 11` delta, comments); if they differ, stop and ask the Contract agent — never hand-edit the proto or generated code here
- [x] 1.2 `buf lint` clean and `buf breaking --against <pre-change copy of proto/>` clean (commands and output in the task note); `cargo build -p rayito-proto` regenerates Rust; the Python `filesystem_pb2*`/`common_pb2*` and TypeScript `filesystem_pb.ts`/`common_pb.ts` are the regenerated ones

## 2. [rayd] Domain (`rayd-core`, no new dependency)

- [x] 2.1 `filesystem/metadata.rs`: `FileMetadata` per design D17 with its table test; `WriteMessage.metadata` and `WriteSession::accept` refusing it without `path`; `FileSystem::read_metadata`, `FileSystem::open_snapshot` + `SnapshotFile`, `WriteSink::set_metadata` port additions; the fake filesystem implements them; `EntryInfo` mapping carries metadata
- [x] 2.2 `transfer/url_policy.rs`: `S3ObjectSpec`, `validate_request`, `is_forbidden_address` per design D6, with the D23 tables
- [x] 2.3 `transfer/poll.rs`, `transfer/s3_error.rs`, `transfer/import.rs` (classification table and `ImportPlan::admit`), `transfer/export.rs` (plan checks, part lengths), with the D23 tests
- [x] 2.4 `transfer/registry.rs` (ids, phases, bounds 16/2, retention 64/30 min on the `Clock` port, idempotent cancel, empty id `NOT_FOUND`, snapshots) and `transfer/barrier.rs` (selection), with the D23 tests
- [x] 2.5 `transfer/ports.rs` (`SignedHttp`, `RequestBody`, `ResponseBody`, `SignedRequest` with `Zeroizing<String>`, `HttpHead`, `HttpError{kind}`), `transfer/error.rs` (unary kind vs `StreamError{code, "<reason>: <frase>"}` per design D13, messages path-free and URL-free: a test asserts no `/`, `http` or `amazonaws` in any message), `transfer/fake.rs` (test-only fake `SignedHttp`), `transfer/mod.rs` constants and `transfer_limits_match_limits_json`
- [x] 2.6 `cargo test -p rayd-core` green on Windows; `cargo clippy --workspace --all-targets -- -D warnings` clean; `crates/rayd-core/Cargo.toml` unchanged

## 3. [rayd] Adapters, managers, gRPC, build

- [x] 3.1 Workspace pins of design D7 (`hyper-util`, `hyper-rustls`, `rustls` exact, `hyper` `client` + `http1`, tonic `gzip`) added to `crates/rayd/Cargo.toml`; `cargo tree -d` shows no new duplicate; `cargo deny check` → four `ok` with no allowlist change
  - hallazgo de revisión `Cargo.lock`/`zlib-rs` (2026-09-23): el workspace ya fija `flate2 = { version = "=1.1.10", default-features = false, features = ["rust_backend"] }`, pero `tonic` 0.14.6 (feature `gzip`) pide `flate2` con sus features por defecto; `runtime_detection` (`zlib-rs?/std`) es una feature débil que Cargo anota en el lockfile aunque no active la dependencia, y las features son aditivas, así que ningún ajuste del workspace quita `zlib-rs` 0.6.8 de `Cargo.lock`. No se enlaza: `cargo tree --locked -i zlib-rs --target all` → "nothing to print", y `cargo tree -i flate2 -e features` muestra `miniz_oxide` vía `rust_backend`. Se aplicó la otra opción del hallazgo: nota en `crates/rayd/CHANGELOG.md` (sección Dependencies de Unreleased); la frase equivalente en ADR-010 queda para 7.1. `cargo deny` no está instalado ni en el host ni en la VM, así que `cargo deny check` no se re-ejecutó aquí (lo cubre el job `cargo-deny` de CI).
- [x] 3.2 `adapters/signed_http.rs`: `HyperSignedHttp` + `FilteringResolver` per design D7; test fixtures under `crates/rayd/tests/fixtures/tls/` (test-only CA and leaf generated once with `openssl`, command in the task note); the unit tests of D23
- [x] 3.3 `adapters/std_filesystem.rs`: `set_metadata` (`fsetxattr` on the temp fd before rename), `read_metadata` (`llistxattr`/`lgetxattr`), `open_snapshot` (`O_NOFOLLOW`, regular only, `fstat`, `pread`); `cfg(unix)` tests of D23; errno mapping of D17
- [x] 3.4 `transfer/manager.rs`, `transfer/import.rs`, `transfer/export.rs`, `transfer/barrier.rs`: tasks, permits (semaphore of 2), poll wake-ups, requeue, DELETE cleanup, multipart ETags, sha256 with per-part clone, `SuspendSignal` subscription (suspend requeue ≤ 1 s, resume wake, terminate cancel), log fields of D21
- [x] 3.5 `grpc/transfer.rs` (the five RPCs, phase gate, `/run` gate, `SuspendableStream` + `KeepAlive` on `WatchTransfer`) delegated from `grpc/filesystem.rs`; metadata on `Write`, `Stat`, `ListDir`; `grpc/compression.rs` `CompressionOptInLayer` on the server builder; `accept_compressed`/`send_compressed` gzip on `FilesystemServiceServer` only; barrier calls in `grpc/filesystem.rs` (`Read`, `Stat`, `ListDir`), `grpc/process.rs` (`Start`), `grpc/code.rs` (`Execute`), `grpc/pty.rs` (`Create`); `main.rs` wiring
- [x] 3.6 `crates/rayd/tests/m9_transfer.rs` with every integration case of design D23 (including zero fake-HTTP calls on policy failures and the log test); existing suites untouched and green
- [x] 3.7 Baseline and delta: `cargo auditable zigbuild --release --locked --target aarch64-unknown-linux-musl -p rayd` before (from the commit before 3.1) and after, sizes in the task note (Q75); `python scripts/check_auditable.py` OK
- [x] 3.8 `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace --locked` on Windows; `cargo clippy -p rayd --all-targets -- -D warnings` and `cargo test -p rayd` under Linux (Docker) for the `cfg(unix)` suites; outputs in the task note

## 4. [sidecar] No change

- [x] 4.1 Confirm nothing under `kernel-sidecar/` changes (transfers, gzip and metadata live in `rayd`); note it in the task note

## 5. [python] SDK surface

- [x] 5.1 `_models.py`: `S3Staging` (+ `from_env`), `UploadTicket`, `AsyncUploadTicket`, `DownloadLink`, `TransferStatus`, `EntryInfo.metadata`; `exceptions.py`: `TransferException(code, reason)`, `FileUploadException`, `UnimplementedError(feature, reason, doc=None)`; `rayito.e2b.exceptions.UnimplementedError` re-exports the native class with `doc=COMPAT_DOC_PATH` at the shim's call sites; exports from `rayito/__init__.py`; `test_models.py`/`test_exceptions.py` cases
  Verificado aquí (2026-09-23, macOS arm64): los modelos y excepciones nativos existen y se exportan; `rayito.e2b.exceptions.UnimplementedError is rayito.UnimplementedError` es `True` y `rayito/e2b/_compat.py` la construye con `doc=COMPAT_DOC_PATH`. Se añadieron los casos que faltaban en `test_models.py` (`DownloadLink` sin URL en `repr` ni pickle, `EntryInfo.metadata` vacío y de sólo lectura, valores por defecto de `TransferStatus`/`S3Staging`) y en `test_exceptions.py` (`UnimplementedError` con y sin `doc`, fuera de `SandboxException`): 92 passed.
- [x] 5.2 `_transfer_base.py` per design D14 with `test_transfer_base.py` (every D23 Python base case, incl. the SigV4/regional-host presign test with fake credentials and the 604 801 clamp)
- [x] 5.3 `_filesystem_base.py` and `_transport.py`: metadata validation and request fields, gzip call options, `stream_idle_timeout` validation, the new `StreamError` codes, transfer state → exception mapping (D13)
- [x] 5.4 `sandbox_sync/transfer.py`, `sandbox_async/transfer.py`, `sandbox_{sync,async}/filesystem.py` (`upload_url`, `download_url`, routing of D15, `gzip`, `metadata`, `stream_idle_timeout`, `use_octet_stream`), `sandbox_{sync,async}/main.py` (`transfer=` on `create`/`connect`, `sbx.transfer`, the persist-overlap check, native `Sandbox.upload_url`/`download_url`, capability probe cache)
- [x] 5.5 `rayito/e2b/_sync.py`, `_async.py`, `_compat.py`: `upload_url`/`download_url` per design D14 (out of the `UnimplementedError` list, `SIGNED_URL_REASON` removed), shim `files.write`/`files.read` pass `gzip`, `metadata`, `use_octet_stream`, `stream_idle_timeout`
  Verificado aquí (2026-09-23, macOS arm64, `clients/python`, reintento): `upload_url`/`download_url` del shim delegan en el `UploadTicket`/`DownloadLink` nativos (`path=None` → `InvalidArgumentException`, `use_signature` ignorado, `use_signature_expiration` → `expires_in`, `<= 0` rechazado por el nativo); `SIGNED_URL_REASON` ya no existe.
  `rayito.e2b.exceptions.UnimplementedError` es el nativo re-exportado (D13), con `doc=COMPAT_DOC_PATH` en cada raise del shim. `files.write`/`write_files`/`read` pasan `gzip`, `metadata`, `use_octet_stream` y `stream_idle_timeout`.
  Tests: `test_e2b_v2_sync.py::test_upload_and_download_urls_through_the_shim` (S3 falso, incluye `use_signature_expiration=-1`), `test_upload_url_without_staging_is_the_native_unimplemented`, `test_files_forward_the_transfer_keywords` y sus gemelos async (`test_async_upload_and_download_urls_delegate_to_the_native`, `test_async_files_forward_the_transfer_keywords`).
  Mutaciones: URLs que vuelven a lanzar `UnimplementedError` → 2 fallos (sync) y 1 (async); `UnimplementedError` del shim como subclase → 2 fallos en `test_e2b_v2_exports.py`; quitar `gzip`/`stream_idle_timeout` del paso → 1 fallo cada uno.
- [x] 5.6 `tests/unit/fake_transfer.py`, `tests/unit/fake_s3.py`, `test_transfer_sync.py`, `test_transfer_async.py`, additions to `test_files_{sync,async}.py`, `test_e2b_compat_{sync,async,base}.py`, the parity test; every D23 Python case
  Nota del reintento del shim: la parte e2b de esta tarea ya está (ver 5.5).
  Sigue sin marcar (2026-09-23, reintento de specs-docs), contra lo que decía la revisión: `test_transfer_async.py::test_async_request_timeout_bounds_a_stalled_s3_download` falla 3 de 3 veces (`assert stalled_s3.body_closed.is_set()`) al correr juntos `test_transfer_{base,sync,async}.py`, `test_files_{sync,async}.py` y `test_e2b_compat_{base,sync,async}.py` (1 failed, 330 passed), y pasa solo, en su fichero (20 passed), con los tres de transferencia (145 passed) y en la corrida completa (2090 passed): depende de la carga o del orden. Es del área python; hasta que lo arreglen, los casos D23 de este fichero no son deterministas.
  Fixes de revisión (2026-09-23): (a) `LambdaMicrovmsControlPlane` guarda la sesión de `from_session` (propiedad `session`) y `_bind_transfer` (sync y async) cae a ella con `control_plane_session(...)` cuando `session=` es `None`, así las URLs se firman con el mismo principal que el plano, como TS `s3ClientOverridesFor(plane)`. Tests: `test_transfer_{sync,async}.py::test_*a_plane_built_from_a_session_signs_with_that_session` (la credencial de la URL es la de esa sesión) y `test_aws.py::test_from_session_keeps_the_session_for_signing`; rojo en una copia con `self._session = session` (2 fallos: `None is not Session`), verde en el árbol. (b) La spec `sdk-file-transfer`, D15 y la fila de §18 de `AWS_API_NOTES.md` dicen ya lo que hace el código: `get_object` por trozos de 262 144 bytes en todos los formatos, acotado por el plazo de la operación y por `stream_idle_timeout`; `download_fileobj` no se usa. `openspec validate m9-file-transfer --strict` válido.
  Verificado aquí (2026-09-23, macOS arm64, `clients/python`): el fallo intermitente era de la prueba, no del código: `ObjectFetch.cancel` cierra el cuerpo en el hilo de trabajo después de que el plazo ya levantó el error, así que `assert stalled_s3.body_closed.is_set()` corría contra ese hilo. Ahora es `assert stalled_s3.body_closed.wait(5)` en `test_transfer_{sync,async}.py` (descarga por `request_timeout` y por `stream_idle_timeout`). `pytest --timeout 60 tests/unit/test_transfer_*.py tests/unit/test_files_*.py tests/unit/test_e2b_compat_*.py` 3 de 3 veces → 331 passed. Mutación (restaurada): `cancel()` sin `chunks.close()` → fallan `test_request_timeout_bounds_a_stalled_s3_download` y `test_async_request_timeout_bounds_a_stalled_s3_download` (`assert False`). La parte e2b está (5.5) y la suite completa pasa: `pytest tests/unit --timeout 60` → 2090 passed; `ruff check .` limpio; `ruff format --check .` 198 ficheros; `mypy src tests` sin errores en 196.
- [x] 5.7 `cd clients/python && uv run pytest tests/unit && uv run ruff check . && uv run ruff format --check . && uv run mypy src tests` green; `clients/python/CHANGELOG.md` `[Unreleased]` entry
  Gate de Python (2026-09-23, macOS arm64, reintento de specs-docs): `uv run --with pytest-timeout pytest tests/unit --timeout 60` da 2090 passed en 321 s, exit 0; `ruff check .` limpio; `ruff format --check .` 198 ficheros (el `README.md` ya formateado); `mypy src tests` sin errores en 196 ficheros. La entrada `[Unreleased]` del CHANGELOG está (transferencias, ficheros grandes, gzip/metadata).
  Corrida anterior (superada): `pytest tests/unit --timeout 60` 2015 passed y 1 failed (`test_packaging.py::test_e2b_all_matches_the_proposal_list`, el `__all__` del shim en curso), `mypy src tests` limpio (196 ficheros), `ruff format --check .` limpio (198), `ruff check .` limpio. Los tests nativos de transferencia (`test_transfer_{base,sync,async}.py`, `test_pool_{sync,async}.py`) están verdes. La entrada del CHANGELOG está, con una frase nueva sobre el plazo de la pata S3 y la sesión del pool. 5.6 sigue abierta sólo por `test_e2b_compat_{sync,async,base}.py` (5.5, del shim): la parte nativa está completa.
  Corrida del fixer python (2026-09-23, macOS arm64, tras los fixes de revisión): `uv run --with pytest-timeout pytest tests/unit --timeout 60` 2090 passed, 0 failed (332 s; los `is_running` ya no fallan); `ruff check .` limpio; `mypy src tests` sin errores en 196 ficheros; `ruff format --check .` sólo marca `clients/python/README.md` (bloques de código de la doc, área de docs, en edición por otro agente). Queda sin marcar hasta que ese fichero pase el formato.

## 6. [ts] SDK surface

- [x] 6.1 `pnpm add` the four packages of design D19 (never npm/yarn); `package.json` caret floors and `pnpm-lock.yaml`; versions in the task note
- [x] 6.2 `src/errors.ts` (`TransferError`, `FileUploadError`, `DiskFullError`, `UnimplementedError`, new `StreamError` codes), `src/models.ts` (`EntryInfo.metadata`, `S3Staging`, `TransferStatus`), `src/sandbox/transfer.ts` (`UploadTicket`, `DownloadLink`, presign and routing, dynamic imports), `src/sandbox/filesystem.ts` (`uploadUrl`, `downloadUrl`, `format: "blob"`, `gzip`, `metadata`, `streamIdleTimeoutMs`, `useOctetStream`), `src/sandbox/sandbox.ts`/`launch.ts`/`core.ts` (`transfer` option and getter, `uploadUrl`/`downloadUrl`, capability probe), `src/transport/transport.ts` (lazy gzip transport on the unary session manager), exports in `index.ts`; `limits.ts` regenerated
- [x] 6.3 Fake `rayd` transfer servicer + fake S3 under `tests/unit/fake`; `transfer.test.ts`, `filesystem.test.ts`, `sandbox.test.ts`, `errors.test.ts` cases of design D23
- [x] 6.4 `cd clients/typescript && pnpm lint && pnpm typecheck && pnpm test && pnpm build && pnpm pack:check` green; `clients/typescript/CHANGELOG.md` entry
  - verificado aquí: Gate completo del paquete el 2026-09-23 en este árbol: `pnpm lint` (biome, 114 ficheros, 0 errores), `pnpm typecheck` exit 0, `pnpm test` 35 ficheros / 762 passed, `pnpm build` ok y `pnpm pack:check` ok (lista `package/dist/index.{mjs,cjs,d.mts,d.cts}`). El bloqueo de la review (TS2742 en `control-plane.ts`) ya está resuelto: `clientConfig` devuelve el tipo explícito `SdkClientConfig`.
  - hallazgo de la review (`transfer.ts`, `S3Access` sin las credenciales ni el proxy del plano): `LambdaMicrovmsControlPlane` guarda `awsClientSettings` (`credentials`, `proxy`) desde `fromRegion`, `LaunchObserver` del pool los reenvía, y `s3ClientOverridesFor(plane)` los pasa a los dos clientes S3 (`credentials` y un `NodeHttpHandler` con `ProxyTunnelAgent`); `requestChecksumCalculation: "WHEN_REQUIRED"` sigue sólo en el de prefirmar. Test nuevo `tests/unit/transfer-credentials.test.ts` (3 tests): rojo antes del cambio (`awsClientSettingsOf`/`s3ClientOverridesFor` inexistentes y `uploadUrl` fallando con `AuthenticationError` por la cadena por defecto), verde después; la URL prefirmada lleva `X-Amz-Credential=<id de las credenciales del plano>`. Entrada en `CHANGELOG.md` `[Unreleased]`.
  - reverificado (2026-09-23, reintento): `pnpm lint` (biome, 123 ficheros, 0 errores), `pnpm typecheck` exit 0, `pnpm test` 38 ficheros / 797 passed, `pnpm build` exit 0 y `pnpm pack:check` exit 0.
  - hallazgo de la review (D15/D18, la mitad TS de `requestTimeout` sobre la pata S3): `importUpload` corre `S3Access.upload` (ahora con el `abortController` de `@aws-sdk/lib-storage`) dentro de `OperationDeadline.budgetMs(size)` (sin `requestTimeoutMs` y con un stream de tamaño desconocido no se acota, como `budget` de Python); al vencer aborta la subida, borra el objeto de staging y lanza `TimeoutError("…plazo…")`. `streamExported` recibe el plazo en las lecturas `bytes`/`text`/`blob` (no en `stream`, como Python): el `GetObject` y cada chunk corren contra `remainingMs`, abortan el `signal` y borran el objeto. Fake S3 con `stalled` (métodos firmados por cabecera que nunca responden). Tests nuevos en `transfer.test.ts` (`requestTimeoutMs bounds a stalled S3 upload…` y `…download…`): rojo antes (ambos `Test timed out in 20000ms`), verde después (41/41). Gate: `pnpm lint` (biome, 123 ficheros, 0 errores), `pnpm typecheck` exit 0, `pnpm test` 38 ficheros / 831 passed, `pnpm build` exit 0 y `pnpm pack:check` exit 0.

## 7. [infra] Documentation, security, architecture
- [x] 7.1 `ARCHITECTURE.md`: `FilesystemService` row (transfer RPCs, gzip, metadata), hexagonal tables (`transfer` domain module, `SignedHttp` port, `HyperSignedHttp` adapter, the new `FileSystem`/`WriteSink` methods), **ADR-010** per design D2 with the measured numbers once 8.x ran
  Verificado aquí (2026-09-23, macOS arm64, reintento de specs-docs): ADR-010 escrita con contexto, decisión, alternativas descartadas y consecuencias de D2; fila `FilesystemService` con los cinco RPCs, gzip opcional y metadatos; tablas hexagonales con `transfer`, `filesystem::metadata`, el puerto `SignedHttp`, `WriteSink::set_metadata`, `FileSystem::read_metadata`/`open_snapshot` y el adaptador `HyperSignedHttp`; la pila del SDK nombra los paquetes de S3. Los números medidos de ADR-010 (Q71–Q75) quedan como *pendiente de aceptación en AWS* (8.4). Puerta: `scripts/tests/test_m9_docs.py::test_the_m9_adrs_are_written` rojo antes de editar (`no se encontró el encabezado «## ADR-010»`) y verde después.
- [x] 7.2 `SECURITY.md` **T16** and the logging paragraph per design D20/D21; `docs/site/docs/security.md` mirrors T16; `openspec/project.md` tech-stack sentence on TLS clients names the transfer adapter
  Verificado aquí (2026-09-23, reintento de specs-docs): fila T16 (URLs al portador, guardia SSRF de `rayd`, IMDS fuera de alcance, sin execution role, C-05/C-06/T7/T11), el párrafo de higiene de logging gana transferencias y metadatos, `CallerPolicy` de la sección IAM nombra `TransferBucket`, y la fila T9 los campos permitidos; `docs/site/docs/security.md` lleva la fila T16; `openspec/project.md` nombra `HyperSignedHttp` como segundo cliente TLS. `test_m9_docs.py::test_the_threat_model_names_the_m9_surfaces` rojo → verde; `test_security_docs.py` sigue verde (184 passed en `scripts/tests`).
- [x] 7.3 `docs/site/docs/files.md` (new) per design D20, `mkdocs.yml` nav after `persistence.md`, `e2b-compat.md` rows moved and added, `limits.md` new limits, `README.md` short section, `infra/README.md` "Transferencias de ficheros" (lifecycle, bucket policy, CORS, KMS; placeholders only); `crates/rayd/CHANGELOG.md` entry; `mkdocs build --strict` green
  Verificado aquí (2026-09-23, reintento de specs-docs): `files.md` con la superficie, `S3Staging` y sus variables, ejemplos `requests`/`curl -T`/`fetch` y el `POST`, la barrera y `wait()`, la ruta de ficheros grandes (Q59 medido; Q71/Q74/Q75 *pendiente de aceptación en AWS*), gzip, metadatos, `stream_idle_timeout`, IAM y "Diferencias con E2B"; nav "Ficheros y transferencias" tras "Persistencia"; `e2b-compat.md` mueve `upload_url`/`download_url` a "Se mapea" y añade `gzip`, `metadata`, `stream_idle_timeout`, `use_octet_stream`; filas nuevas en `limits.md`; sección M9 del `README.md`; `infra/README.md` "Transferencias de ficheros" (stack, ciclo de vida de 1 día, política SigV4 + TLS, CORS, SSE-KMS; sólo `amzn-s3-demo-bucket` y `<tu-perfil>`); entrada `[Unreleased]` en `crates/rayd/CHANGELOG.md`. `cd clients/python && uv run --group docs mkdocs build -f ../../docs/site/mkdocs.yml --strict` → built, sin avisos; `test_m9_docs.py::test_the_site_navigates_to_the_new_pages` y `test_operator_guidance_for_transfers_exists` rojo → verde; `check_hygiene.py` → OK 939 ficheros.

## 8. [e2e] Acceptance on real AWS

- [x] 8.1 [AWS, ≈ $0.04] Publish the M9 `rayito-base` image with the new `rayd` (`image_zip.py` + publish with `BASE_IMAGE_VERSION=1`); version, build time and `snapshotBuild` sizes in the task note
- [x] 8.2 `clients/python/tests/e2e/test_m9_transfer.py` with the 13 tests of design D24 (fixtures for `RAYITO_E2E_TRANSFER_BUCKET`/`RAYITO_E2E_TRANSFER_PREFIX`, `RAYITO_EXECUTION_ROLE_ARN` for test 13, S3 cleanup and multipart abort in teardown, `report(...)` lines); `clients/typescript/tests/e2e/m9-transfer.e2e.test.ts` per D24
  - Hecho (2026-09-24): `test_m9_transfer.py` con 14 tests (los 13 de D24 más la variante async de `upload_url`) y `m9-transfer.e2e.test.ts` con 6; los dos corren contra AWS en 8.3.
- [x] 8.3 [AWS, ≈ $0.10] `RAYITO_E2E=1 … uv run pytest tests/e2e -m e2e -k m9_transfer -v` green and the whole existing Python e2e suite green on the new image (regression); `pnpm test:e2e tests/e2e/m9-transfer.e2e.test.ts` green; zero MicroVMs alive, zero objects and zero multipart uploads left under the prefix afterwards
  - Hecho (2026-09-24, aceptación): `test_m9_transfer.py` **14/14** dentro de la regresión e2e de Python de 2026-09-24 sobre `rayito-base` 23.0, `rayito-base-caps` 12.0 y `rayito-base-poly` 7.0: **62 passed de 62** (server-timeout 12, observability 4, deno 8, egress 14, transfer 14, M4 1, M5 2, M6 2, M7 poly 5); la suite e2e de Python completa quedó verde entre dos pasadas (73 passed en `rayito-base` 22.0 y los 11 rojos de esa pasada, todos de ficheros que se corrigieron, verdes en la de 23.0; 2 skipped opt-in: conector de egress propio y la rotación lenta de credenciales). TS `m9-transfer.e2e.test.ts` **6/6** en la regresión completa de TS sobre `rayito-base` 22.0 (los fallos de esa pasada eran de `m9-timeout` y `m6`, verdes después). Informe: `upload_url` 10 MiB PUT 4,14 MB/s (portátil → S3) y `wait()` 3,73 s; PUT → visible 1,32 s por `files.read` y 0,23 s por `commands.run`; `download_url` 50 MB exportado en 1,02 s; 200 MB por S3 4,30/4,03 MB/s de escritura/lectura frente a 0,52/3,41 MB/s por gRPC (20 MB), enlace portátil ↔ S3 2,37/3,48 MB/s; multiparte de 100 MB en 3,59 s; `expires_in=5` → `TimeoutException` a los 4,68 s; 1 socket en las llamadas SSRF; gzip 0,80 → 52,34 MB/s. Limpieza: el teardown borra los objetos y aborta las multipartes, y el sweeper de sesión termina las VMs; recomprobar el prefijo con `list-objects-v2`/`list-multipart-uploads` en el cierre.
- [x] 8.4 Fill Q70–Q75 (with the real numbers of 0.3 and 8.3: import/export MB/s, PUT-to-visible p50/p95, 403 vs 404, JS CRC32, gzip through the proxy, binary delta, xattrs), ADR-010 and `files.md` measured paragraphs, `MILESTONES.md` M9 row for this change — placeholders only
  - Hecho (2026-09-24): Q70–Q75 medidas (Q74, gzip por el proxy, rellenada con la aceptación: 0,80 → 52,34 MB/s con 20 MB de texto); ADR-010 ya citaba Q71–Q75 con los números de `rayito-base` 21.0; `files.md` cita Q74 en lugar de *pendiente*; línea de `m9-file-transfer` en el bloque M9 de `MILESTONES.md`.

## 9. [infra] Gates, in one pass

- [x] 9.1 `buf lint`; `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test --workspace --locked`; `cargo deny check`; the auditable zigbuild + `python scripts/check_auditable.py`; `python scripts/gen_limits.py --check`; `python scripts/check_pins.py`; `python scripts/check_license.py`; `python scripts/check_hygiene.py`; scripts tests
  - Pendiente, paso de cierre de M9 (no se difiere, 2026-09-24): se corre sobre el árbol final cuando terminen los refactors sin cambio de comportamiento que están en curso en `crates/` y `clients/*/src`. Última pasada local completa (2026-09-23): Python 2090 passed, TypeScript 831 passed, sidecar 85 passed, `scripts/tests` 184, `check_hygiene`/`check_pins`/`check_license`/`buf lint` OK; faltan los gates de Rust (`cargo fmt`, `clippy -D warnings`, `cargo test --workspace --locked` en la VM Lima como uid 1500) sobre ese árbol.
  - Hecho (2026-09-24, cierre de M9, árbol final): Rust en la VM Lima (Ubuntu aarch64, `CARGO_TARGET_DIR` propio, `-j 2`): `cargo fmt --all --check` exit 0; `cargo clippy --workspace --all-targets --locked -- -D warnings` limpio; `cargo test --workspace --locked` con los binarios como uid 1500 (`setpriv --reuid=1500 --regid=1500 --clear-groups`): rayd lib 172, bin 5, m1 14, m2 25, m3 30, m4 19 (+1 ignored), m5_pty 15, m5_suspend_resume 7, m6_hooks 7, m6_imds 3, m6_limits 5, m7_poly 9, m9_deno 4, m9_egress 1, m9_metrics_history 6, m9_network 4, m9_timeout 11, m9_transfer 21, s3_store 0 (+1 ignored), rayd-core 498: 856 passed, 0 failed, 2 ignored; `m9_egress` como root en `unshare --net` con `RAYITO_REQUIRE_EGRESS_NETNS=1` → 1 passed; `cargo deny check` → advisories, bans, licenses, sources ok; `cargo auditable zigbuild --release --locked --target aarch64-unknown-linux-musl -p rayd` → `rayd` estático de 13 764 024 B y `check_auditable.py` → `.dep-v0: 257 packages, root rayd 0.2.0`; Python (macOS arm64): `uv run --with pytest-timeout pytest tests/unit -q --timeout 60` 2115 passed; `uvx ruff==0.16.7 check` limpio y `format --check` 219 ficheros; `mypy src tests` sin errores en 217 ficheros; sidecar: `pytest` 85 passed (14 deselected), ruff limpio, `mypy src` sin errores; TypeScript: `pnpm lint` (biome, 139 ficheros), `pnpm typecheck` exit 0, `pnpm test` 40 ficheros / 863 passed, `pnpm build` y `pnpm pack:check` exit 0; `buf lint` exit 0; `check_hygiene.py` OK (939 ficheros); `check_pins.py` OK (8 ficheros); `check_license.py` OK; `gen_limits.py --check` exit 0; `scripts/tests` 186 passed; `mkdocs build --strict` (target `docs` del `Makefile`) sin avisos.
- [x] 9.2 `cd clients/python && uv run pytest tests/unit && uv run ruff check . && uv run ruff format --check . && uv run mypy src tests`; `cd clients/typescript && pnpm lint && pnpm typecheck && pnpm test && pnpm build && pnpm pack:check`; `uvx cfn-lint==1.56.3 spike/m0/iam.yaml infra/*.yaml`; `mkdocs build --strict`
  Verificado (2026-09-23, macOS arm64, reintento de specs-docs): Python `pytest tests/unit --timeout 60` 2090 passed (321 s), `ruff check .` limpio, `ruff format --check .` 198 ficheros, `mypy src tests` sin errores en 196 ficheros; TypeScript `pnpm lint` (123 ficheros), `pnpm typecheck` exit 0, `pnpm test` 38 ficheros / 831 passed, `pnpm build` y `pnpm pack:check` exit 0; `uvx cfn-lint==1.56.3 spike/m0/iam.yaml infra/*.yaml` exit 0 sin salida; `uv run --group docs mkdocs build -f ../../docs/site/mkdocs.yml --strict` sin avisos.
  - Hecho (2026-09-24, cierre de M9, árbol final): Python (macOS arm64): `uv run --with pytest-timeout pytest tests/unit -q --timeout 60` 2115 passed; `uvx ruff==0.16.7 check` limpio y `format --check` 219 ficheros; `mypy src tests` sin errores en 217 ficheros; TypeScript: `pnpm lint` (biome, 139 ficheros), `pnpm typecheck` exit 0, `pnpm test` 40 ficheros / 863 passed, `pnpm build` y `pnpm pack:check` exit 0; `mkdocs build --strict` sin avisos.
- [x] 9.3 `openspec validate m9-file-transfer --strict --no-interactive` passes with every task ticked and the notes appended; the acceptance agent archives with `openspec archive m9-file-transfer --yes` only after 8.3 is green
  - Parcial (2026-09-24): `openspec validate m9-file-transfer --strict --no-interactive` → valid; el archivo secuencial simulado sobre una copia de `openspec/` (deno-kernels → file-transfer → server-timeout → sandbox-observability → egress-policy → e2b-v2-surface) archiva los seis sin error (2026-09-24). Sin marcar hasta que 9.1 esté verde; entonces archiva el cierre de M9.
  - Hecho (2026-09-24, cierre de M9, árbol final): `openspec validate m9-file-transfer --strict --no-interactive` → valid con todas las tareas marcadas; el archivo real lo hace el cierre de M9 tras 9.1, en el orden de la simulación.

## Notes

Infra implementer (2026-09-22, Windows 11 box, botocore/boto3 1.43.94 from `clients/python`'s venv, `uvx cfn-lint==1.56.3`):

- **Numbering (0.0)**: live check on 2026-09-22. `AWS_API_NOTES.md` §16 ends at row 62 (the Q63–Q65 rows of `m9-server-timeout` and the Deno row are not written yet), so **Q70–Q75** are free and used as designed. §18 is free (the last section is §17), `SECURITY.md` ends at T15 so **T16** is free, `ARCHITECTURE.md` ends at ADR-009 so **ADR-010** is free. The `StreamError` comment in `common.proto` already lists the four codes of this change plus `sandbox_timeout` (merged by the Contract step). No renumbering anywhere.
- **0.1** §18 written before any code. Python names checked against the botocore 1.43.94 model: operation input members, their wire names (`partNumber`, `uploadId`, `response-content-disposition`) and the boto3 method signatures. An offline presign with fake credentials confirmed D5: the default `Config` gives the global host plus SigV2, and the D5 `Config` gives `amzn-s3-demo-bucket.s3.us-east-1.amazonaws.com` plus SigV4, also for `generate_presigned_post`. JavaScript names checked against the published `.d.ts` of 3.1138.0, with the tarballs unpacked in the session scratchpad and never in the repo:
  - `getSignedUrl(client, command, options?: RequestPresigningArguments)`, whose `expiresIn` lives in `@smithy/types` 4.19.0.
  - `createPresignedPost(client, { Bucket, Key, Conditions, Fields, Expires })` returns `{ url, fields }`.
  - `Upload` takes `Options { client, params, queueSize, partSize, leavePartsOnError, tags, abortController }` and exposes `done()`.
  - `S3ClientConfigType` includes `FlexibleChecksumsInputConfig.requestChecksumCalculation` (`"WHEN_SUPPORTED" | "WHEN_REQUIRED"`, from `@aws-sdk/checksums` 3.1001.1).
  - The five command classes exist and their input members match.

  The `AWSJavaScriptSDK/v3/latest/Package/...` API Reference pages render client-side and returned no text, so §18 cites the `.d.ts` files plus the code-library, JS developer guide and SDK reference pages. `s3:signatureversion` is verified in `bucket-policy-s3-sigv4-conditions.html` (values `AWS` and `AWS4-HMAC-SHA256`), so `infra/README.md` can use it.
- Findings the `rayd` url policy (D6) must respect, all recorded in §18:
  - URLs signed with temporary credentials carry `X-Amz-Security-Token`.
  - `@aws-sdk/client-s3` 3.1138.0 declares `x-id=<operation>` in the URI of `PutObject`, `GetObject`, `UploadPart` and `DeleteObject`.

  Rule 5 must therefore accept extra query parameters and refuse only the SigV2 names, which is how D6 is written. The S3 error-code anchor `ErrorResponses.html#ErrorCodeList` now redirects to the API Reference home page, so the `ExpiredToken` text is cited from the S3 exception reference and Q70 measures it.
- **0.2** Rows Q70–Q75 are in, with "Desde docs" filled in. The "Medida" cell says `*Sin medir*` and names the task that fills it: 0.3 for Q70–Q73, the acceptance run for Q74–Q75.
- **0.3 and 0.5 are not done** ([AWS]). The infra step runs no real AWS, so they are left to the Accept step. Q73 (xattrs) gates the metadata adapter: apply the D17 stop rule before accepting 3.3.
- **0.4** `spike/m0/iam.yaml` now has:
  - Parameters `TransferBucket` (default empty) and `TransferPrefix` (default `rayito-transfer`, the `PersistencePrefix` pattern).
  - `Rules.TransferPrefixIsDisjoint`, which applies only when the bucket is set: the prefix is neither `rayito` nor `PersistencePrefix`.
  - `Conditions.HasTransferBucket`.
  - In `CallerPolicy`, `TransferObjects` (`s3:PutObject`, `s3:GetObject`, `s3:DeleteObject`, `s3:AbortMultipartUpload` on `<bucket>/<prefix>/*`) and `TransferMissingKeyIs404` (`s3:ListBucket` with `s3:prefix` = `<prefix>/*`). Both sit inside `!If` with `AWS::NoValue` as the false branch.

  `ExecutionRole` and `BuildRole` are untouched. `scripts/tests/test_iam_template.py` gains six tests: the three of D20, plus the default prefix equals `limits.json` `transferDefaultPrefix`, the `AllowedPattern` equals `PersistencePrefix`'s, and the `Rules` assertions. Before the template change: 5 failed, 5 passed (the execution-role test is a guard and passes by design). After: 10 passed. A mutation run on scratch copies caught all three mutations: a transfer statement under another condition, the execution role granted the prefix, and the default prefix equal to the persistence default. `uvx cfn-lint==1.56.3 -- infra/egress-connector.yaml infra/ci-oidc-role.yaml spike/m0/iam.yaml` exits 0 with no output. `aws cloudformation validate-template` was not run (no AWS in this step).
- **0.6** `limits.json` gains the 27 keys of D22 with D22's values. `scripts/gen_limits.py` gets three new `GROUPS`, appended after the egress and lifecycle groups of the sibling changes: transfer configuration and limits, agent internals, and metadata plus the compression header. `python scripts/gen_limits.py` regenerated `_limits.py` and `limits.ts` (for example `TRANSFER_DEFAULT_PREFIX`, `TRANSFER_MAX_PARTS`, `METADATA_XATTR_PREFIX`, `COMPRESSION_OPT_IN_HEADER`). `--check` exits 0, and ruff and biome are clean on both files. The generated header still says the values come from §2/§3/§6/§11 and are API facts, which is no longer accurate for the transfer, egress or lifecycle keys. Three changes share that header, so the Parity step rewords it.
- Section 7 (documentation) belongs to the Parity step. The section 9 gates are ticked once the whole change is implemented.
- **Resumed run (2026-09-22)**: the interrupted edit had left `spike/m0/iam.yaml` with CRLF line endings, against `.gitattributes` (`eol=lf`). It is back to LF, and the content is unchanged (`git diff --stat`: 47 insertions). Re-verified on the working tree:
  - The IAM tests fail 5 and pass 5 against the committed template in a scratch mirror, and pass 10 of 10 against the new one.
  - The three scratch mutations each turn exactly the expected test red.
  - `uvx cfn-lint==1.56.3 -- spike/m0/iam.yaml infra/*.yaml` exits 0.
  - `python scripts/gen_limits.py --check`, `check_hygiene.py`, `check_pins.py`, `check_license.py` and `uvx ruff==0.16.7 check scripts` all pass.
  - `cd clients/python && uv run pytest ../../scripts/tests -p no:cacheprovider` passes 160 of 160.
  - `infra/ci-oidc-role.yaml` stays without S3 permissions, because D20 does not change it. As a result the CI `e2e.yml` role cannot run `test_m9_transfer.py` unless it gets the transfer prefix; running that suite stays a maintainer run.

Python implementer (2026-09-22, Windows 11 box, `clients/python` venv: boto3/botocore 1.43.94, s3transfer 0.19.2, grpcio 1.84.0):

- **Resumed run.** The previous attempt died while rewriting `sandbox_sync/filesystem.py`, which was left as 26 381 NUL bytes; its session transcript lost the last step the same way. I rebuilt it from `HEAD` by replaying the recorded patch script (26 365 bytes against the lost 26 381; the gap is one line ruff wraps) and re-verified it with the tests below. `clients/typescript/tests/e2e/m9-egress.e2e.test.ts` is zeroed the same way; it belongs to the egress TS surface and I did not touch it.
- **5.1** Done in my scope: `S3Staging` (+ `from_env`), `UploadTicket`/`AsyncUploadTicket`/`DownloadLink` (`str` subclasses on a shared `SignedUrl`: redacted `repr`, `__reduce__` raises `TypeError`), `TransferStatus`, `EntryInfo.metadata` (read-only `MappingProxyType`), `TransferException(code, reason)`, `FileUploadException`, native `UnimplementedError(feature, reason, doc=None)`, all exported from `rayito`. Model and exception cases live in `test_transfer_base.py` (the e2b-v2 surface added its own case in `test_exceptions.py`). **Left open:** the `rayito.e2b.exceptions.UnimplementedError` re-export with `doc=COMPAT_DOC_PATH` is under `rayito/e2b/`, which the Parity step owns.
- **5.2** `_transfer_base.py` holds the pure helpers of D14/D15 (no I/O). `_s3.py` is the only module that calls boto3 S3, and uses only the §18 operations and parameters. The sync and async trees share both.
- **5.3** `_filesystem_base.py`:
  - metadata validation that mirrors D17, with messages that never echo a key or value;
  - `read_call_options`/`write_call_options`, so the gzip kwargs are only passed when `gzip=True` and the 0.2.0 call shape is unchanged otherwise;
  - `stream_idle_timeout` validation, with a sync watchdog thread (`guarded_messages`) and an async `wait_for` (`read_guarded`).

  In `_transport.py`, `resource_exhausted` maps to `DiskFullException` (`disk_reserve`/`disk_full`) or `RateLimitException`, and `failed_precondition` to `InvalidArgumentException`. `unavailable` and `cancelled` fall through to `SandboxException("<code>: …")`, the proto's unknown-code rule. The D13 table is `failure_from_state`.
- **5.4** Covers `sandbox_{sync,async}/transfer.py`, `filesystem.py` and `main.py`:
  - `create(transfer=)`, `connect(transfer=)` and `sbx.transfer`. `None` reads `RAYITO_TRANSFER_*`, and `create(pool=)` accepts it too.
  - The persist-overlap check runs before any AWS call.
  - Native `Sandbox.upload_url/download_url(path, user, use_signature_expiration)`.
  - The `GetTransfer("")` probe is cached per sandbox.
  - Routing follows D15: nothing is probed or routed without staging, and an older agent falls back to gRPC.
  - The sandbox stores the boto3 `session` given to `create`/`connect` for presigning (the default chain otherwise).
- **5.5 not done**: the Parity step owns `rayito/e2b/` (`upload_url`/`download_url` in the shim still raise `UnimplementedError`). The native surface they delegate to is in place.
- **5.6** Done in my scope:
  - `fake_s3.py`: a real boto3 client with a `before-send.s3` handler, so botocore still validates every parameter against the model, plus presigned-URL `PUT`/`GET`/`DELETE`/`POST` for the rayd side.
  - `fake_transfer.py`: the five RPCs on top of `FakeFilesystemService`.
  - `transfer_support.py`, `test_transfer_base.py` (86), `test_transfer_sync.py` (33) and `test_transfer_async.py` (16, including the sync/async parity test).
  - One older-agent case each in `test_files_sync.py` and `test_files_async.py`.
  - `fake_filesystem.py` now stores and returns metadata.
  - `conftest.py` gets `start_fake_rayd(filesystem=)` and an autouse fixture that clears `RAYITO_TRANSFER_*`.

  The metadata round trip runs on the transfer fake, because it needs the M9 probe. **Left open:** `test_e2b_compat_{sync,async,base}.py` (Parity).
- **Red/green**: 17 mutations were applied to the source one at a time, and each made its named test(s) fail; restoring the file made them pass again. The mutations covered:
  - the presign `Config`, the 604 800 clamp and the D13 table;
  - metadata on the first `WriteRequest`, metadata key validation and the gzip read header;
  - routing at `>= threshold_bytes`, the multipart abort, the probe cache and the routed-read sha256 check;
  - the sync `stream_idle_timeout` guard, the WatchTransfer reopen after `/suspend` and the persist-overlap check;
  - the older-agent refusal of metadata, async routing, the async idle guard and `create(pool=, transfer=)` (`test_pool_sync.py`).
- **8.2 (Python half)**: `tests/e2e/test_m9_transfer.py` holds the 13 D24 tests plus an async upload case. It uses stdlib `urllib`, env vars `RAYITO_E2E_TRANSFER_BUCKET`/`RAYITO_E2E_TRANSFER_PREFIX`, and a teardown that deletes `<prefix>/<sandbox_id>/` and aborts open uploads. It collects and deselects without `RAYITO_E2E=1`. It was not run: the Accept step runs it. Test 12 needs the Parity shim.
- **5.7 open**: the `CHANGELOG.md` `[Unreleased]` entry is written. ruff, ruff format and mypy are clean on every file of this surface. The full `pytest tests/unit` is not green, because sibling surfaces are still mid-edit (listing, metrics history, `test_sandbox_base`, the `_logger_or` roll-out). The failures are listed in my report to the orchestrator.

Python fixer (2026-09-23, macOS arm64, venv de `clients/python`), hallazgos de la revisión sobre Python:

- **La pata S3 ignoraba `request_timeout`/`stream_idle_timeout`** (`transfer.py` `write_routed`/`read_routed`, sync y async). Ahora:
  - La subida corre en un hilo acotado por el plazo de la operación (`run_bounded` en `sandbox_sync/transfer.py`, `asyncio.wait_for` + `shield` en `sandbox_async/transfer.py`). Al vencer, `HashingReader.abort()` hace fallar la siguiente lectura de s3transfer, se borra el objeto (best effort) y se levanta `TimeoutException`. `OperationDeadline.budget(size, now)` no acota una subida de tamaño desconocido sin `request_timeout` (no hay presupuesto por MB que calcular), como antes.
  - `format="bytes"`/`"text"` ya no usa `download_fileobj`: `_s3.ObjectFetch` lee `get_object` por trozos con `guarded_messages` (así rige `stream_idle_timeout`) dentro del mismo hilo acotado, y `cancel()` cierra el cuerpo. `S3Gateway.download` se eliminó y el docstring de `_s3.py` ya no nombra `download_fileobj`.
  - Rojo → verde: `test_request_timeout_bounds_a_stalled_s3_upload`, `test_request_timeout_bounds_a_stalled_s3_download` y `test_stream_idle_timeout_bounds_a_stalled_s3_download` (y sus gemelos `test_async_*`) con una pata S3 colgada por monkeypatch: 3 failed en cada árbol antes del cambio (`DID NOT RAISE`), verdes después. Casos puros nuevos en `test_transfer_base.py`: `test_an_aborted_hashing_reader_fails_its_next_read` y `test_the_s3_leg_budget_follows_the_operation_deadline`.
- **`Sandbox.create(pool=..., transfer=...)` enlazaba la sesión `None`**. `SandboxPool`/`AsyncSandboxPool` guardan la sesión y la exponen como `pool.session`; ambos `main.py` hacen `taken._bind_transfer(staging, pool.session)`. `test_create_with_pool_signs_transfers_with_the_pool_session` (sync y async) espía `S3Gateway.from_session`: con `None` falla (`assert [None] == [Session(...)]`), con el cambio pasa.
- Gates de esta ronda: `test_transfer_{base,sync,async}.py`, `test_files_{sync,async}.py`, `test_pool_{sync,async}.py` verdes; suite completa 1679 passed; `mypy src tests` y `ruff format --check .` limpios; `ruff check` limpio en todos los ficheros tocados.
- Los fallos previos citados por la revisión ya estaban resueltos en el árbol: `test_code_base.py::test_result_from_proto_maps_every_mime_field`, `cli/test_sandbox.py::test_info_prints_metadata_and_no_metadata_skips_the_probe` y el error de mypy en `tests/unit/test_transport.py:143` pasan hoy sin cambios míos.

TS implementer (2026-09-22, Windows 11 box, Node 22.21.1, pnpm 9.15.4):

- **6.1**: `pnpm add` resolved `@aws-sdk/client-s3`, `@aws-sdk/s3-request-presigner`, `@aws-sdk/s3-presigned-post` and `@aws-sdk/lib-storage` at 3.1138.0, the same minor. `package.json` has caret floors `^3.1138.0` and `pnpm-lock.yaml` pins them exactly. `src/sandbox/transfer.ts` loads them only with `import()`. The built `dist/index.mjs` and `dist/index.cjs` reference them only as `import("@aws-sdk/…")`, and a unit test fails if any `src` file imports them statically. The TS `S3Client` receives only `region`, `credentials` and `requestChecksumCalculation: "WHEN_REQUIRED"` (presign client), the names in §18. The unit tests point both clients at a local fake S3 through an opaque `Partial<S3ClientConfig>` set on `SandboxCore.s3ClientOverrides`; production code never sets that field.
- **6.2**: code lives in `src/sandbox/transfer.ts` (pure helpers, `S3Access`, `TransferClient`, `UploadTicket`, `DownloadLink`). `Filesystem` owns one `TransferClient` per sandbox, which caches the `GetTransfer("")` probe. `SandboxCore` gains `transfer`, `s3ClientOverrides` and `gzipFilesystemClient()`, which builds `openGzipTransport` on the unary `Http2SessionManager`. `Sandbox` gains the `transfer` option on `create`/`connect` (also with `pool`), the getter, the persist-overlap check before any AWS call, `uploadUrl`/`downloadUrl` returning strings, and `reincarnate()` forwarding `transfer`. `launch.ts` needed no change. `translateStreamError` maps the new codes like Python: `failed_precondition` becomes `InvalidArgumentError`, and `resource_exhausted` becomes `DiskFullError` with a disk detail and `RateLimitError` otherwise. The idle guard (`streamIdleTimeoutMs`) covers the gRPC `Read`, the S3 body and the export's `WatchTransfer`.
- **6.3**: `tests/unit/fake/transfer.ts` (the five RPCs on the fake `rayd`) and `tests/unit/fake/s3.ts` (path-style S3 subset with presign expiry, POST `content-length-range`, multipart). `transfer.test.ts` has 39 cases. `filesystem.test.ts` gains 8 cases (blob, gzip, metadata, idle, `useOctetStream`, validators). `sandbox.test.ts` gains "gzip transport reuses the unary session". `errors.test.ts` gains 2 cases, `transport-errors.test.ts` 1 and `pool.test.ts` 1 (`create({ pool, transfer })`). The pre-existing 3 MB test in `filesystem.test.ts` now compares with `Buffer.equals`: vitest's element-wise `toEqual` on MB-sized arrays was hitting the 20 s timeout under load, and at 20 MiB it ran the worker out of heap.
- **Red/green**: `transfer.test.ts` was red before `transfer.ts` existed (`Cannot find module '../../src/sandbox/transfer.js'`). The other new cases were checked by mutation, one at a time, each run red and then green after restoring the source. The mutations: the gzip read header, the gzip write transport, metadata on the first `WriteRequest`, `EntryInfo.metadata`, the blob type, the gRPC idle guard, the `resource_exhausted` stream mapping, the static S3 import and the export-watch idle guard.
- **8.2 (TS half)**: `tests/e2e/m9-transfer.e2e.test.ts` covers `files.uploadUrl` + `fetch` PUT + `wait()` (sha256, uid 1000, staging object 404), the barrier through `sandbox.uploadUrl` + `files.read`/`commands.run`, `downloadUrl` of 50 MB (plain `fetch`, `Range` 206, snapshot after overwrite), 200 MB `writeFiles`/`read` asserted at 10× or more the gRPC baseline of a `transfer: null` connection, gzip at 3× or more, and metadata. Env: `RAYITO_E2E_TRANSFER_BUCKET`/`_PREFIX`/`_REGION`. Cleanup goes through `@aws-sdk/client-s3`: `DeleteObject` of download keys, plus `ListMultipartUploads` + `AbortMultipartUpload` under `<prefix>/<sandboxId>/`. Not run: the Accept step runs it.
- **6.4 open**: the `CHANGELOG.md` `[Unreleased]` entry is written. `pnpm test` (29 files, 696 passed), `pnpm build` and `pnpm pack:check` are green, and biome and `tsc` are clean on every file of this change. Package-wide `pnpm lint`/`pnpm typecheck` are red only on sibling files that are still mid-edit (`Git` in `sandbox.ts`, `git.ts`, `git-args.ts`, `proxy-tunnel.ts`, `commands.ts`, `control-plane.ts`, and the new `callOptions` block in `core.ts`).

rayd implementer (2026-09-23, resumed run). Every build and test ran on a Linux ARM64 build host (Amazon Linux 2023 on Graviton, rust 1.98.1, cargo-zigbuild 0.23.4). Nothing was compiled on the Windows box, because of the disk incident.

- **Resumed state.** The backup commit on the branch already held the domain, adapters, managers, gRPC wiring and 20 of the 21 integration cases. This run added the last D23 case, `a_write_stores_metadata_that_stat_and_list_dir_read_back_until_an_overwrite`, and a `metadata_keys` count on the `Write` log line. The count never carries a key or a value.
  - Red without the log field: `panicked at crates/rayd/tests/m9_transfer.rs:1500:5: the write line counts keys`.
  - Green with it: `m9_transfer` 21 passed.
- **1.1** The proto matches D1 verbatim, including `StartImportRequest.metadata = 11`. It was checked field by field against the pre-M9 commit.
- **1.2** `buf lint` OK. `buf breaking --against '.git#ref=<pre-M9 commit>'` (FILE) OK. `crates/rayito-proto/build.rs` regenerates the Rust code on every build.
- **2.6** `cargo test -p rayd-core`: 482 passed on Linux. `crates/rayd-core/Cargo.toml` is unchanged since the pre-M9 commit. Windows was not run (local builds are forbidden).
- **3.1** Dependencies:
  - `cargo tree -d -e normal,build` gives the same 31 duplicate lines before and after the change; `comm` finds no new one.
  - `cargo deny check` (0.20.2, the Makefile pin) prints `advisories ok, bans ok, licenses ok, sources ok`, and `deny.toml` is unchanged.
  - `zlib-rs 0.6.8` (Zlib, already allowed) now appears in `Cargo.lock` and in the `.dep-v0` graph, but it is never compiled. The cause: tonic's `gzip` feature turns on flate2's default features, and flate2's `runtime_detection` names the weak feature `zlib-rs?/std`, which cargo still records.
  - Proof that it is not linked: `cargo tree -i zlib-rs --target all` prints nothing, and the release `deps/` holds only `flate2` and `miniz_oxide`.
  - So D7's "no new crate" is true only for the linked graph. The SBOM lists this one unlinked crate.
- **3.2** The committed fixtures are P-256 with 36 500-day validity: `CN=rayito test-only CA` (`CA:TRUE`), plus a leaf with `CN`/SAN `amzn-s3-demo-bucket.s3.us-east-1.amazonaws.com` (`CA:FALSE`). The interrupted run did not record its command. These commands produce an equivalent pair; the CA key, CSR and extension file are thrown away:
  ```
  openssl req -x509 -newkey ec -pkeyopt ec_paramgen_curve:P-256 -nodes -days 36500 -subj "/CN=rayito test-only CA" -addext "basicConstraints=critical,CA:TRUE" -keyout ca.key -out ca.pem
  openssl req -newkey ec -pkeyopt ec_paramgen_curve:P-256 -nodes -subj "/CN=amzn-s3-demo-bucket.s3.us-east-1.amazonaws.com" -keyout leaf.key -out leaf.csr
  printf 'basicConstraints=CA:FALSE\nsubjectAltName=DNS:amzn-s3-demo-bucket.s3.us-east-1.amazonaws.com\n' > leaf.ext
  openssl x509 -req -in leaf.csr -CA ca.pem -CAkey ca.key -CAcreateserial -days 36500 -extfile leaf.ext -out leaf.pem
  ```
- **3.7** Size from `cargo auditable zigbuild --release --locked --target aarch64-unknown-linux-musl -p rayd` (cargo-auditable 0.7.6):
  - pre-M9 commit: 12 532 320 bytes;
  - working tree: 13 741 176 bytes, +1 208 856 bytes (+9.6 %).

  The delta covers every M9 change in the tree (egress proxy, server timeout, metrics history, Deno and transfers), not this change alone. Q75 can quote it until a build with only this change exists. `scripts/check_auditable.py` reports `.dep-v0: 256 packages, root rayd 0.2.0` before and `257 packages, root rayd 0.2.0` after. The only added package is `zlib-rs` (see 3.1).
- **3.8** Linux results:
  - `cargo fmt --all --check` exits 0.
  - `cargo clippy --workspace --all-targets --locked -- -D warnings` exits 0.
  - `cargo test --workspace --locked --no-fail-fast` passes every target except three tests that fail because of the host:
    - `tar_archiver::restore_merges_directories_and_overwrites_files` gets permission denied unpacking as a non-root user, and fails the same way on the pre-M9 commit on this host.
    - `m5_pty::echo_round_trip_size_tty_uid_and_env` fails because the host's `/etc/profile.d/lang.sh` unsets `LC_ALL` in interactive bash. It also fails the same way on the pre-M9 commit.
    - `m5_suspend_resume` lost two timing tests under the load of the full parallel run, and passes 7 of 7 when run alone.
  - Windows was not run (local builds are forbidden after the disk incident).
- **4.1** The `kernel-sidecar/` changes in the tree belong to m9-deno-kernels (Deno kernelspecs and languages). None come from this change.

Accept-prep (2026-09-23, macOS arm64 + VM Lima ARM64, `<tu-perfil>`, us-east-1, boto3/botocore 1.43.94):

- **0.5** `aws cloudformation create-change-set` + `execute-change-set` sobre `rayito-m0-iam` con `spike/m0/iam.yaml` del árbol, `UsePreviousValue=true` en `ArtifactBucket`, `LogGroupPrefix`, `PersistenceBucket`, `PersistencePrefix` y `NetworkConnectorArns`, y `TransferBucket=amzn-s3-demo-bucket TransferPrefix=rayito-transfer`. El change set sólo modifica `CallerPolicy` y `ExecutionRole` (sin reemplazos; el segundo por el `Deny` `NeverTheImageArtifacts` de M8 que aún no estaba desplegado); `UPDATE_COMPLETE` en 63 s. `aws iam simulate-principal-policy` sobre el rol que lleva `CallerPolicy` (`rayito-devbox`): `s3:PutObject`, `s3:GetObject`, `s3:DeleteObject` y `s3:AbortMultipartUpload` sobre `arn:aws:s3:::amzn-s3-demo-bucket/rayito-transfer/x` → `allowed` (sentencia `TransferObjects`); `s3:PutObject`/`s3:GetObject` sobre `…/rayito/x` → `allowed` sólo por la sentencia previa `UploadArtifacts` (otra posición de la política; `TransferObjects` no casa) y `s3:DeleteObject` sobre `…/rayito/x` → `implicitDeny`, así que las sentencias nuevas no conceden nada fuera del prefijo; `s3:ListBucket` con `s3:prefix=rayito-transfer/x` → `allowed` (`TransferMissingKeyIs404`, más `ProbeArtifactBucket`). Sin regla de ciclo de vida en el bucket compartido de pruebas (los tests borran sus objetos).
- **8.1** `rayd` 13 756 408 B (`cargo auditable zigbuild --release --locked`, `.dep-v0` 257 paquetes) → `image/rayito-image.zip` 13 874 844 B → `rayito-base` **21.0** `UPDATED`/`SUCCESSFUL`/`ACTIVE`, build **216,6 s**, `snapshotBuild` memoria **927 043 584** / code install **1 360 146 432** / disco **39 071 744** B (Graviton 3; la build de Graviton 4: 935 993 344 / 1 360 146 432 / 39 071 744). Delta del binario frente a 20.0 (12 525 664 B por `stat` en un sandbox): **+1 230 744 B** (Q75).
- **0.3** Q70-Q73 medidos y escritos en §16 (placeholders sólo). Q70: portátil, `AssumeRole` de 900 s con política de sesión sin `s3:ListBucket` como segundo llamante (el rol que lleva `CallerPolicy` sólo confía en EC2): 404 `NoSuchKey` con `ListBucket`, 403 `AccessDenied` sin él, 403 `Request has expired` para una URL caducada y 400 `ExpiredToken` al caducar las credenciales temporales (el cuerpo repite el token de sesión: el clasificador de D9 lee sólo `<Code>`). Q71/Q73 en un sandbox de `rayito-base` 20.0 sin execution role (`microvm-<id>`, `kill()` al acabar, objetos borrados): Q71 p50 0,34 s / p95 0,83 s (20 × 1 MiB, sondeo de 1 s); Q73 `ext2/ext3`, `user.*` funciona como uid 1000, las xattrs viajan con el inodo en un `os.replace`, symlink `EPERM`, 64 KiB `ENOSPC` → la regla de parada de D17 **no** se dispara. Q72 con un script de Node en el scratchpad (`@aws-sdk/*` 3.1138.0): por defecto la URL lleva `x-amz-checksum-crc32=AAAAAA==` + `x-amz-sdk-checksum-algorithm=CRC32` y S3 aceptó igualmente un `PUT` de 1 MiB (200); con `WHEN_REQUIRED` sólo `x-id=PutObject` (200). Cero MicroVMs vivos después (`list-microvms`).
- **Cierre de M9 (2026-09-24)**: aceptación final contra AWS real sobre `rayito-base` 25.0, `rayito-base-caps` 14.0 y `rayito-base-poly` 9.0 (republicadas desde el `rayd` final): Python **52/52** de las suites afectadas (timeout, egress, observability, corpus E2B y M4), además de los **62/62** de la regresión completa anterior sobre 23.0/12.0/7.0; TypeScript **24/24** de las suites afectadas (timeout, egress, corpus, observability), además de los **26/26** anteriores sobre 24.0/13.0/8.0. Aceptación de paquete con instalación limpia (Python y TS, fuera del repo): subida y descarga de 50 MB por S3 con sha256 coincidente, y `set_timeout` → el sandbox acaba `TERMINATED`. Sólo marcadores de posición (`microvm-<id>`, sin cuenta, bucket ni ARN).
