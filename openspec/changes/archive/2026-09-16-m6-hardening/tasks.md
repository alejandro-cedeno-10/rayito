## 0. [measure] Platform probes before any adapter code

- [x] 0.1 With the 0.0.5 SDK against `rayito-base` 10.0, on one MicroVM: `commands.run("cat /proc/self/status | grep -E 'CapEff|NoNewPrivs'; grep cgroup /proc/mounts; test -r /sys/fs/cgroup/cgroup.controllers && echo cgroup2 || echo no-cgroup2; command -v iptables ip6tables python3")` and the RSS ladder of design D10 (`resource.getrusage` after each warm-up import: baseline, `numpy`, `pandas`, `matplotlib.pyplot` + `savefig`, `scipy.stats`, `sklearn.linear_model`); paste both outputs into the task notes and prepare rows **Q43** (cgroup2/`CapEff` on the default image) and **Q46** (warm-up RSS deltas) of `AWS_API_NOTES.md` §16. Apply the D10 rule now: record the decision (drop `scipy`+`sklearn` from the warm-up iff their combined delta > 100 MB) before section 6 publishes anything — _Hecho_: Medido 2026-09-16 sobre `rayito-base` 10.0 (uid 1000): `CapEff` 0, `NoNewPrivs` 1, sin cgroup2, sin `iptables`, `python3` presente, `ulimit -t` unlimited; escalera de RSS: numpy +10,5, pandas +42,3, matplotlib +30,2, scipy.stats +51,6, sklearn +21,7 MB (Q50).
- [x] 0.2 Confirm with `spike/m0/M0_RESULTS.md` Q24 that an `allPorts` token minted with `create_microvm_auth_token(allowedPorts=[{"allPorts": {}}])` still reaches `POST :9000/…/run` through the proxy on 10.0 (one `httpx` call, expect 200 `already_ran`); this is the forging primitive of the e2e (design "Acceptance test list", `test_forged_hooks` step 2) — _Hecho_: Reconfirmado: JWE `allPorts` → `POST :9000/…/run` responde 200 `already_ran` en 0,31 s y el token original sigue válido (Q24).

## 1. [proto] Contract

- [x] 1.1 `proto/rayito/v1/health.proto`: `bool imds_blocked = 9` and `uint64 hook_anomalies = 10` on `HealthResponse` with Spanish comments; `buf lint`, `buf build` clean; `python scripts/gen_python.py` regenerates `clients/python/src/rayito/v1/health_pb2*`; `cargo check --workspace --all-targets` green — _Hecho_: Campos 9 y 10 añadidos (el 11, `metadata`, lo añadió el track C); `buf lint`/`buf build` limpios, stubs regenerados.
- [x] 1.2 `buf breaking` against the pre-M6 proto reports only the two additive fields; no other proto edit (design D12) — _Hecho_: `buf breaking --against <copia pre-M6 sin los campos 9/10/11>` limpio (sólo campos añadidos).

## 2. [e2e] Acceptance test first (red)

- [x] 2.1 Create `clients/python/tests/e2e/test_m6_hardening.py` with `test_forged_hooks`, `test_cpu_time_limit`, `test_output_budget_and_disk_reserve`, `test_imds_block` (skipped without `RAYITO_TEMPLATE_CAPS` + `RAYITO_EXECUTION_ROLE_ARN`), `test_egress_allowlist` (skipped without `RAYITO_EGRESS_CONNECTOR_ARN`), `test_reseed_during_long_cell`, `test_snapshot_measurements` per design "Acceptance test list"; helper `forge_hook(sandbox, hook, body)` mints the `allPorts` JWE with boto3 directly (never through the SDK) and posts with `httpx` HTTP/1.1 (`x-aws-proxy-auth`, `x-aws-proxy-port: 9000`) — _Hecho_: 7 tests en `tests/e2e/test_m6_hardening.py` (HookForger con JWE `allPorts` + urllib HTTP/1.1).
- [x] 2.2 `RAYITO_E2E=1 RAYITO_TEMPLATE=<10.0 arn> uv run pytest tests/e2e/test_m6_hardening.py -m e2e -x`: confirm `test_forged_hooks` fails at step 3 (three `/suspend` all `changed`, no `rate_limited`) and `test_cpu_time_limit` fails (`TypeError` on `cpu_time_limit`); note it as red — _Hecho_: Rojo comprobado sobre 10.0 el 2026-09-16 (`RAYITO_TEMPLATE_VERSION=10.0`, soporte añadido al conftest): `test_forged_hooks` falla en el primer paso (`/run` forjado responde `already_ran` pero `hook_anomalies` sigue en 0: no hay auditoría) y `test_cpu_time_limit` falla porque `ulimit -t; ulimit -Ht` sigue `unlimited` (el `rayd` de 10.0 ignora `limits`; el `TypeError` previsto no aparece porque el SDK ya es el nuevo).

## 3. [rayd] Domain in `rayd-core`

- [x] 3.1 `hooks.rs`: `HOOK_TRANSITION_INTERVAL = 2 s`, `HookRateLimiter` (per-hook bucket of capacity 1, `check(hook, now) -> Allowed | RateLimited`, applied only to session-changing calls), `HookAudit` (`calls_since_run` per hook, `anomalies` counter, `record(hook, outcome) -> AuditEntry{anomaly}`); `session.rs`: `hook_anomalies()`, `note_anomaly()`; tests per design D14 (first accepted, second within 2 s refused, accepted after 2 s, per-hook independence, idempotent repeats bypass, counters) — _Hecho_: `HookRateLimiter`/`HookAudit` en `rayd-core::hooks`; `SessionSettings` en `session.rs`.
- [x] 3.2 `lifecycle.rs`/`session.rs`: `SUSPEND_GATE_TIMEOUT = 20 s`, `FREEZE_THRESHOLD = 5 s`, `WATCHDOG_TICK = 1 s`; `recover_from_stale_suspend(generation) -> Result<Transition, LifecycleError>` (phase `Resumed`, gate reopened, `resume_generation`/`suspended_total` untouched, suspend marked `stale_recovered`, anomaly counted; no-op when the generation moved on); `resume()` from `Resumed` accepted only when the last suspend is `stale_recovered` (generation bump, span from the original `suspended_at`, `Transition.from == Resumed`); tests: recovery, no-op after `/resume`, resume-after-recovery, plain repeated `/resume` still `unchanged` — _Hecho_: Desviación registrada en el spec: `recover_from_stale_suspend` devuelve `Option<Transition>` y el `/resume` posterior no suma nada a `suspended_total` (el reloj de ejecución nunca retrocede).
- [x] 3.3 `capabilities.rs` (new): `parse_cap_eff`, `CapSet::has`, `Capability::{NetAdmin=12, SysPtrace=19, SysAdmin=21, SysResource=24}`; tests with M0's mask (no `net_admin`) and a mask with bit 12 — _Hecho_: `rayd-core::capabilities`.
- [x] 3.4 `run_payload.rs`: optional `limits: {cpu_seconds}` (`1..=28800`, `RunPayloadError::InvalidLimits` → token-less mode, never a 4xx), `RunDefaults.cpu_seconds: Option<u64>`; `process/limits.rs`: `ResourceLimits.cpu_seconds: Option<u64>`, `CPU_LIMIT_KILL_GRACE_SECONDS = 5`, `cpu_rlimit() -> Option<(soft, hard)>`; `plan_spawn`/`plan_pty` carry it from `RunDefaults`; tests (absent, present, out of range, soft/hard derivation) — _Hecho_: `limits.cpu_seconds` 1..=28800, hard = soft + 5 s; sidecar y kernels sin límite (aserción en `code/hooks.rs`).
- [x] 3.5 `process/budget.rs` (new): `OutputBudget` (`SANDBOX_OUTPUT_BUDGET_BYTES = 128 MiB`, `OUTPUT_BUDGET_HIGH_WATER = 96 MiB`, `charge`, `release`, `level`); `process/ring.rs` and `code/ring.rs` charge/release per design D6 rules 1–3 (own eviction first, then budget eviction, unretained chunk advances `oldest_seq`); `process/registry.rs` and `code/executions.rs` `reap_expired` drop ended entries oldest-first above the high water mark and release on drop; tests with two rings on a 64 KiB budget, reaper under pressure, counter never underflows — _Hecho_: `OutputBudget` compartido por `OutputRing` y `ExecuteRing`; los reapers sueltan entradas terminadas por encima de la marca alta.
- [x] 3.6 `filesystem/ports.rs`: `FileSystem::free_bytes(id, canonical_dir)`; `filesystem/fake.rs`: configurable free bytes; `filesystem/write.rs`: `DISK_RESERVE_BYTES = 256 MiB`, reserve check in `begin` after deny list and before the temporary, `FilesystemError::DiskReserve` and `DiskFull` (ENOSPC) without paths; tests (refused at 10 MiB free, accepted at 300 MiB, earlier files in the stream stay committed) — _Hecho_: `FileSystem::free_bytes`, `DISK_RESERVE_BYTES`, `DiskReserve`/`DiskFull`.
- [x] 3.7 `code/protocol.rs`: `ReseedPayload.deferred: Vec<String>` (`serde(default)`), `SidecarOp::is_advisory()` true only for `Reseed`; golden `kernel-sidecar/tests/fixtures/protocol_v1.jsonl` gains the new reply; `health.rs`: `imds_blocked`, `hook_anomalies` + builder setters; tests — _Hecho_: Golden `protocol_v1.jsonl` con `deferred`.
- [x] 3.8 `cargo test -p rayd-core` green on Windows; `cargo clippy -p rayd-core --all-targets -- -D warnings` clean — _Hecho_: 257 tests de `rayd-core` verdes en Windows y Linux; clippy pedantic limpio.

## 4. [rayd] Adapters, hooks, gRPC, main

- [x] 4.1 `adapters/netfilter.rs` (`cfg(unix)`): `install_imds_block()` (`iptables -w 5 -I OUTPUT -d 169.254.169.254/32 -m owner ! --uid-owner 0 -j DROP`, stderr discarded, exit code logged as `reason`), `rule_present()` (`-C`), `probe_root()` (TCP connect 1 s, nothing sent), `probe_user(spawner)` (uid-1000 `python3 -c` socket connect, expect non-zero exit), best-effort `ip6tables` at `debug`; `ImdsState { blocked: AtomicBool }`; non-unix stub returns `Unavailable` — _Hecho_: **Mecanismo ratificado tras medir (Q48)**: el kernel del guest no trae `xt_owner`, así que el adaptador es `adapters/imds_block.rs` (ruta de política `ip rule uidrange 1000-65535 lookup 100` + blackhole en la tabla 100); el rango empieza en 1000 porque el agente de la plataforma usa uids 991-994 con su propio canal a IMDS (un blackhole para todo uid ≠ 0 dejó `/validate` sin respuesta: caps 3.0 `UPDATE_FAILED`).
- [x] 4.2 `main.rs`: read `/proc/self/status`, log `capabilities` (`net_admin`, `sys_admin`, `sys_resource`, `sys_ptrace`, `cgroup2_root`); with `net_admin` call `install_imds_block()` before the listeners start, else log `imds_block_unavailable`; wire `ImdsState` into the health service and the hooks state — _Hecho_: `install_imds_block_if_allowed` + línea `capabilities`; `ImdsState` en `Services` y `HookServices`.
- [x] 4.3 `hooks/mod.rs`: `HookAudit` + `HookRateLimiter` on `/run`, `/suspend`, `/resume`, `/terminate` (audit lines `hook_audit` after the first accepted `/run`; `rate_limited` answers 200 with no work); `/run` spawns the IMDS verification task (design D4 steps 1–3, 10 s budget, `imds_probe` log, sets `blocked`); `/resume` re-checks `rule_present()` and clears `blocked` with `imds_rule_missing`; `/suspend` spawns `lifecycle::suspend_watchdog(generation)`; `HookReply` keeps its shape (`outcome: "rate_limited"` carries the refusal) — _Hecho_: La sonda uid 1000 corre por el `ProcessSpawner` con `/usr/bin/python3` (symlink añadido a la imagen: al2023-minimal sólo trae `python3.12`); `imds_probe` loguea `user_probe_ran`.
- [x] 4.4 `lifecycle/suspend_watchdog.rs`: the tick loop of design D2 (freeze detection, `recover_from_stale_suspend`, `stale_suspend_recovered` log, no-op after `/resume`); unit test with the paused tokio clock (steady ticks → recovery at 20 s; a 30 s jump → exit without recovery; `/resume` first → no-op) — _Hecho_: `lifecycle/suspend_watchdog.rs`, 4 tests con reloj pausado.
- [x] 4.5 `adapters/process_spawner.rs`: `RLIMIT_CPU` soft/hard in `pre_exec` from `ResourceLimits.cpu_seconds`; `adapters/sidecar_process.rs`: spawn spec keeps `cpu_seconds: None` (asserted in a host test); `adapters/std_filesystem.rs`: `free_bytes` via `nix::sys::statvfs` (`f_bavail * f_frsize`); `filesystem/write.rs` (rayd) maps `ENOSPC` to `DiskFull`; `grpc/filesystem.rs`: `DiskReserve`/`DiskFull` → `RESOURCE_EXHAUSTED` with details `disk_reserve`/`disk_full` — _Hecho_: `RLIMIT_CPU` en `PreExecPlan`; `statvfs` del ancestro existente más profundo.
- [x] 4.6 `process/manager.rs`, `pty/manager.rs`, `code/executions.rs`: one shared `OutputBudget` from `main.rs` into every ring; reaper (`lifecycle/reaper.rs`) passes the budget level; `output_budget_bytes`/`entries_dropped` logged only when crossing the high water mark — _Hecho_: Un `OutputBudget` desde `main.rs` para procesos, PTYs y ejecuciones.
- [x] 4.7 `code/supervisor.rs`: advisory ops (`Reseed`) time out with `advisory_op_timeout: true`, never counted, never kill; `code/manager.rs` `spawn_resume_reseed` logs `reseeded`, `deferred`, `failed`; host test with the fake sidecar `--reseed-delay-ms 20000` (timeout not counted, next op fine); `crates/rayd/tests/fixtures/fake_sidecar.py` gains the flag and the `deferred` reply — _Hecho_: `advisory_op_timeout` nunca cuenta; `fake_sidecar.py --reseed-delay-ms`.
- [x] 4.8 `grpc/health.rs`: `imds_blocked` from `ImdsState`, `hook_anomalies` from the session; `logging.rs` allowlist per design D13 — _Hecho_: Allowlist de logging ampliada en `logging.rs`.
- [x] 4.9 `crates/rayd/tests/m6_hooks.rs` (`cfg(unix)`): forged `/run` → `already_ran` and the old token still authenticates; ten `/suspend` in 1 s → one `changed` + nine `rate_limited`, all 200; stale suspend → `Start` works after the (test-scaled) gate timeout, generation unchanged, `Health.hook_anomalies == 10`; suspend + fake 30 s jump + `/resume` → `changed` without recovery; suspend + recovery + `/resume` → `changed` with `resume_after_stale_recovery` — _Hecho_: Desviación: diez `/suspend` en 1 s son `changed` + 9 × `unchanged` (idempotentes, sin anomalía); `rate_limited` se prueba alternando `/suspend`/`/resume` dentro del intervalo (9 anomalías). Spec actualizado.
- [x] 4.10 `crates/rayd/tests/m6_limits.rs` (`cfg(unix)`): `cpu_seconds: 1` busy loop → `signaled` (24 or 9) within 7 s; sidecar spec unlimited; output budget scaled to 256 KiB → replay `OUT_OF_RANGE` while live delivery is complete; `Write` with 10 MiB free (fake) → `RESOURCE_EXHAUSTED` `disk_reserve` before any temporary; `ENOSPC` mapping with a full tmpfs when the runner allows it, else skipped with a note — _Hecho_: 5 tests verdes en Docker (uid 1000).
- [x] 4.11 `crates/rayd/tests/m6_imds.rs` (`cfg(unix)`, skipped without `CAP_NET_ADMIN` in the test process): rule installed and `-C` passes, uid-1000 connect fails, `Health.imds_blocked` true after a `/run`; without the capability: `imds_block_unavailable` and `false` — _Hecho_: 3 tests: con `--cap-add NET_ADMIN` como root la ruta se instala idempotente y `ip … show` la lista; como uid 1000 `ip -4 rule add exit 2` (fail-open).
- [x] 4.12 `cargo test --workspace` green on Windows and in the Docker loop (`rust:1.98-slim-bookworm` as uid 1000 with `python3`; `m6_imds` with `--cap-add NET_ADMIN` and `iptables` installed); `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings` clean on both; `cargo zigbuild --release --target aarch64-unknown-linux-musl -p rayd` → static ARM64 binary, size recorded next to 10.0's 4 533 432 B — _Hecho_: Windows: 62+4 rayd, 13 m1, 257 core; Docker uid 1000: 72+4, m1 13, m2 25, m3 30, m4 19, m5 15+7, m6 7+3+5, core 257; fmt/clippy limpios en host y aarch64-musl; zigbuild 4 699 960 B.

## 5. [sidecar] Reseed inline or deferred

- [x] 5.1 `kernels.py`: `reseed_plan() -> "idle" | "busy" | "unavailable"`; `server.py` `_op_reseed`: idle inline, busy → `asyncio.create_task(context.reseed())` listed in `deferred` (task logs `reseeded_deferred` with `context_id`, `outcome`), unavailable → `failed`; reply `{"reseeded", "deferred", "failed"}` — _Hecho_: `reseed_plan()` en `kernels.py`; `_op_reseed` idle/busy/unavailable.
- [x] 5.2 Tests: `test_kernel.py::test_reseed_replies_immediately_while_a_cell_runs` (real kernel, `time.sleep(3)` in flight, reply < 1 s with `deferred: ["default"]`, sequence differs after the cell), `test_server.py` deferred/failed cases with the fake context, golden updated; `uv run pytest`, `ruff`, `mypy` clean in `kernel-sidecar` — _Hecho_: 59 tests del sidecar verdes (+11 con kernel real en Docker); ruff/mypy limpios.

## 6. [scripts + infra + image]

- [x] 6.1 `scripts/publish_image.py`: `--os-capabilities {ALL}` → `additionalOsCapabilities: ["ALL"]` in `desired_configuration` only when set (independent of `m6-benchmark-pool`'s `--variant`, design D18); docstring; `scripts/tests/test_publish_image.py` asserts the configuration delta is exactly that key — _Hecho_: `OS_CAPABILITY_CHOICES = ("ALL",)`; 4 tests.
- [x] 6.2 `scripts/image_prune.py` per design D8 (keep set, live-MicroVM pin, in-flight builds untouched, serial deletes, `ConflictException` wait-and-retry with 5/10/20/40/80 s, wait for `DELETED`/`ResourceNotFoundException` and the image leaving `DELETING`, `--dry-run`, table + JSON summary, exit codes); `scripts/tests/test_image_prune.py` with `botocore.stub.Stubber` (keep-set selection, conflict → wait → retry, dry-run makes no mutating call, summary shape) — _Hecho_: `scripts/image_prune.py` + 7 tests con Stubber; `log()` de `publish_image.py` ahora tolera consolas cp1252.
- [x] 6.3 `Makefile`: `image-publish-caps` (`PUBLISH_ARGS="--os-capabilities ALL --image-name rayito-base-caps"`), `image-prune` (`PRUNE_ARGS ?= --keep 5`), `test-scripts` (`uv run --with "$(BOTO3_SPEC)" --with pytest pytest scripts/tests`, also run by `test`), `infra-lint` (`aws cloudformation validate-template` + `uvx cfn-lint`, E3006 ignored only for the connector type with the cfn-lint version recorded) — _Hecho_: `image-publish-caps`, `image-prune`, `infra-lint` (cfn-lint 1.56.3 limpio sin ignorar reglas).
- [x] 6.4 `infra/egress-connector.yaml` per design D11 (parameters, `OperatorRole` with the eight `ec2:*` ENI actions and a `lambda.amazonaws.com` trust without `aws:SourceAccount`, `EgressSecurityGroup` allowlist from `AllowedCidrs`/`AllowedPort`, `AWS::Lambda::NetworkConnector` with `Configuration.VpcEgressConfiguration{AssociatedComputeResourceTypes: [MicroVm], NetworkProtocol: IPv4, SecurityGroupIds, SubnetIds}`, outputs `ConnectorArn` and the caller statement); `infra/README.md` (recipe, IAM, NAT note, deploy gate); `spike/m0/iam.yaml` `CallerPolicy` gains `NetworkConnectorArns` (list parameter, default empty) for `lambda:PassNetworkConnector`; `make infra-lint` green — _Hecho_: `infra/egress-connector.yaml` + `infra/README.md`; `spike/m0/iam.yaml` gana `NetworkConnectorArns`/`PassOwnConnectors`.
- [x] 6.5 `image/Dockerfile`: M6 header, `iptables-nft` in the `dnf` list, `iptables` in the sanity loop; `kernel-sidecar/ipython/startup/0004_warmup.py` per the 0.1 decision (list trimmed or kept, docstring states the measured rule); `scripts/hooks-sim.py --only forged` per design D15 — _Hecho_: El Dockerfile instala `iproute` (no `iptables`: sin `xt_owner` en el guest) y el symlink `/usr/bin/python3`; warm-up sin cambios (73,3 MB < 100, Q50); `hooks-sim.py` con el paso `forged`.
- [x] 6.6 Local loop: `cargo run -p rayd` + real sidecar under Docker/WSL2, `hooks-sim.py --skip-terminate` (six hooks + `forged`), the SDK drill from M5 plus `cpu_time_limit=1` on a busy loop; then `make image-zip && make image-publish` → `rayito-base` **11.0** `SUCCESSFUL` + `ACTIVE`; record zip size, build seconds, `snapshotBuild` next to 10.0's (919 146 496 / 1 290 379 264 / 37 462 016 B, 215.8 s) here and in `MILESTONES.md` (Q46 before/after) — _Hecho_: Loop local en Docker (rayd debug + sidecar real + hooks-sim con `forged`): `hook_anomalies` 2. Publicadas `rayito-base` 11.0 → 15.0 (15.0: 195 s, memoria 919 146 496 B, code 1 304 760 320 B, disco 36 933 632 B; zip 4 802 593 B).
- [x] 6.7 `make image-publish-caps` → `rayito-base-caps` **1.0**; if the gate never becomes launchable or a MicroVM from it never reaches `agent_ready` in 90 s, record `stateReason`/the `Health` outcome as **Q44**, apply the design D4 fallback (default image only, `SECURITY.md` T1 stays open with the measurement) and skip 7.3's IMDS test; otherwise read `rayd`'s `capabilities` and `imds_probe` lines from `/rayito/rayito-base-caps` build logs into Q43/Q44 — _Hecho_: `rayito-base-caps` 1.0–5.0 (5.0: 215 s, memoria 933 363 712 B). 1.0/2.0 midieron el fallo de `iptables -m owner`, 3.0 `UPDATE_FAILED` (`Validate hook invocation timed out after PT10M` con el blackhole para uid ≥ 1), 4.0 arrancó con la ruta pero la sonda uid 1000 no podía lanzarse (`/usr/bin/python3` ausente), 5.0 **verde**: `imds_blocked=True` a los 8,3 s de `create()`, `PUT /latest/api/token` como uid 1000 → `EINVAL` en 0,30 s, root llega, la ruta sobrevive a `pause()`/`resume()` (Q48).
- [x] 6.8 `make image-prune PRUNE_ARGS="--dry-run"` then `make image-prune PRUNE_ARGS="--keep 5"` on `rayito-base`: paste the plan and the per-version wait/retry timings into the notes and **Q45** (whether an `ACTIVE` version deletes directly, `ConflictException` occurrences and wait times); `list-microvm-image-versions` afterwards shows ≤ 5 launchable versions plus any pinned by a live MicroVM — _Hecho_: `--dry-run`: 7 versiones (15.0–11.0 keep, 10.0 y 8.0 delete); `--keep 5` borró 8.0 y 10.0 en 23 s (una `ACTIVE` se borra directo, 5,4 s de espera hasta que la imagen vuelve a `UPDATED`, 0 `ConflictException`); `rayito-base-caps --keep 1` borró 1.0–4.0 (incluida la 3.0 `FAILED`). Q49.

## 7. [sdk] Python client

- [x] 7.1 `_models.py`: `SandboxInfo.ingress`/`.egress` (tuples, default empty), `SandboxHealth.imds_blocked`/`.hook_anomalies`; `_aws.py`: read `ingressNetworkConnectors`/`egressNetworkConnectors` from `run-microvm` and `get-microvm`; `exceptions.py`: `DiskFullException(SandboxException)`; `_transport.py`: `RESOURCE_EXHAUSTED` with details `disk_reserve`/`disk_full` → `DiskFullException` — _Hecho_: Cambios aditivos en `clients/python` (propiedad del track C) sin subir la versión.
- [x] 7.2 `_payload.py`/`_sandbox_base.py`: `cpu_time_limit: int | None` (`1..=28800`, `InvalidArgumentException` otherwise) serialized as `limits: {"cpu_seconds": N}` only when set; `sandbox_sync/main.py` and `sandbox_async/main.py` `create(cpu_time_limit=None)` with the docstring "segundos de CPU, no de pared"; `_record_health`: warning once per generation on `hook_anomalies > 0`, warning once when `imds_blocked is False` and the sandbox has an execution role; reconnect rule of design D2 (no new generation required when `get-microvm` says `RUNNING`; `UNAVAILABLE suspending` on re-subscribe retried on the backoff inside `reconnect_timeout`), sync and async — _Hecho_: `create(cpu_time_limit=)` en sync y async; `_warn_hardening` una vez por generación / una vez por rol.
- [x] 7.3 Unit tests (fakes): new fields, both warnings (once per generation / only with a role), `cpu_time_limit` validation and payload shape, `SandboxInfo` connectors from a stubbed `get-microvm`, `DiskFullException` mapping, reconnect after a `suspending` end with the VM staying `RUNNING` and the gate reopening on the third attempt, sync and async; `uv run pytest tests/unit`, `ruff check`, `ruff format --check`, `mypy src` clean; version `0.0.6` — _Hecho_: 10 tests sync + 4 async; 610 unitarios verdes, ruff/format/mypy limpios.

## 8. [e2e] Green and close

- [x] 8.1 `RAYITO_E2E=1 RAYITO_TEMPLATE=<11.0 arn> make test-e2e`: M1–M5 suites still green on 11.0 (regression) and `test_m6_hardening.py` green with `RAYITO_TEMPLATE_CAPS` (if 6.7 succeeded) and `RAYITO_EXECUTION_ROLE_ARN`; paste the timings (forged `/run`/`/suspend` outcomes, first tick after the cut, `hook_anomalies` values, `cpu_time_limit` exit and seconds, `imds_blocked` and the uid-1000 probe outcome, snapshot RSS ladder, three-cycle reseed) into the notes — _Hecho_: 2026-09-16 sobre `rayito-base` 15.0 + `rayito-base-caps` 5.0: M1–M5 verdes y `test_m6_hardening.py` 12 passed / 1 skipped (egress) en 8 min 57 s (+ la repetición de `test_forged_hooks` tras corregir su secuencia de buckets: 31 s). Números en MILESTONES.md M6 Track A y Q48–Q51.
- [x] 8.2 Egress deploy gate (design D11): `ec2 describe-vpcs`/`describe-subnets` in `us-east-1` and the MicroVMs section of the Lambda pricing page; if a VPC with a subnet exists **and** no connector charge is listed, deploy `rayito-egress-e2e` from `infra/egress-connector.yaml` (default deny-all `AllowedCidrs`), export `RAYITO_EGRESS_CONNECTOR_ARN`, run `test_egress_allowlist`, record **Q42** (own connector vs `INTERNET_EGRESS`, blocked/unblocked timings) and delete the stack; otherwise record Q42 as "not measured: <reason>" and keep only `make infra-lint` as evidence — _Hecho_: Gate **no** superado: la única VPC (12 subnets) es de otra carga de trabajo y no tiene NAT gateway; la página de precios no lista cargo por conector (el tráfico se factura como data transfer). Sin despliegue; evidencia `make infra-lint` (validate-template + cfn-lint). Q46.
- [x] 8.3 `AWS_API_NOTES.md` §16 rows Q42–Q46 filled with the measured values (and §4 gains the `delete-microvm-image-version` behaviour from Q45, §9 the IMDS block if measured) — _Hecho_: Numeración reasignada al escribir (los tracks paralelos ocuparon Q42–Q45): Q46 egress, Q47 capabilities/cgroup2, Q48 variante ALL + IMDS, Q49 prune, Q50 warm-up, Q51 hooks forjados; §4 y §9 actualizados.
- [x] 8.4 Docs per design D17: `SECURITY.md` (T1, T2 with the residual table and the e2e numbers, T7, T8, IAM → `infra/`), `ARCHITECTURE.md` (hooks table with audit/rate limit/watchdog, `Health` row, Capa 1 `image-prune` + variant + warm-up list, domain/ports/adapters tables, Python layout), `MILESTONES.md` M6 "Track A" acceptance state, `README.md` (`cpu_time_limit`, egress recipe, `imds_blocked`, image variants, `image-prune`) — _Hecho_: SECURITY.md (T1 ✔, T2 con la tabla residual medida, T7, T8, IAM → `infra/`), ARCHITECTURE.md, README.md, MILESTONES.md M6 Track A.
- [x] 8.5 `openspec validate m6-hardening --strict` passes; acceptance agent runs `openspec archive m6-hardening --yes` — _Hecho_: `openspec validate m6-hardening --strict` pasa; el archivado queda para el agente de aceptación.

## 9. [review] Fixes after the M6 review (track rayd/image security + ops)

- [x] 9.1 Drop the transition rate limiter: `rayd-core::hooks` keeps only `HookAudit` (`HookRateLimiter`, `RateDecision`, `HOOK_TRANSITION_INTERVAL`, `LifecycleError::RateLimited` and the `*_would_change` predicates removed); `SandboxSession::suspend`/`resume` are the plain phase machine; `hooks/mod.rs` has no `rate_limited` branch; session test `a_forged_pair_never_makes_the_real_cycle_a_repeat`; `m6_hooks.rs::a_forged_pair_never_makes_rayd_skip_the_real_suspend` (forged pair, `/suspend` at +1 s `changed` with streams closed, 30 s jump, `/resume` `changed`, generation bumped, `suspended_total` ≥ 30 s, anomalies 0) and `back_to_back_cycles_are_never_refused`; `hooks-sim.py --only forged` expects `changed` after the pair; design D1/D14/D15/Risks, proposal, `hook-defense` + `suspend-resume` specs, SECURITY.md T2, ARCHITECTURE.md, README, proto comment, Q51 restated — _Hecho_: 252 core + 62/4/13 rayd host tests; Docker uid 1000 `m6_hooks` 7/7, `m1_hello` 13, `m5_suspend_resume` 7; clippy pedantic limpio; `rayito-base` 16.0 publicada (216 s) y `test_forged_hooks` verde contra AWS (par forjado + `/suspend` a +1 s → `changed`/`changed`, `hook_anomalies` 2, `pause()`/`resume()` real 0,44 s).
- [x] 9.2 IMDS warning only after `rayd`'s verification window: `imds_open_warning_due` + `IMDS_VERIFY_BUDGET_MS = 10_000` in `_sandbox_base.py`, `_ready_uptime_ms` recorded at the first `Health`, sync and async; unit tests `test_imds_open_warns_once_only_with_a_role_and_after_the_verification_window` and `test_imds_verified_inside_the_window_never_warns`; `guest-isolation` spec scenario restated; design D12, SECURITY.md T1, README — _Hecho_: readiness ya no avisa; el aviso llega en el primer `Health` con `uptime_ms ≥ readiness + 10 000`.
- [x] 9.3 `WatchDir` and `Reattach` retry the gate like `Connect`: `GateRetry` in `_sandbox_base.py` shared by `CommandHandle`/`PtyHandle` (`_resubscribe_through_the_gate`), `WatchHandle._reopen_through_the_gate` and `ExecutionFeed._reattach_through_the_gate`, sync and async; unit tests `test_watch_reconnects_through_a_stale_suspend_when_the_vm_stays_running` and `test_run_code_reattaches_through_a_stale_suspend_when_the_vm_stays_running` (gate reopens on the third attempt) in both clients; `hook-defense` spec scenario added — _Hecho_: 616 unitarios verdes, ruff/format/mypy limpios.
- [x] 9.4 Egress gate restated instead of silently skipped: design D11 and the `egress-control` "gate fails" scenario now require a VPC owned by or lent to this project (the stack creates a security group and ENIs inside it) and say the deny-all default needs no NAT; SECURITY.md T8 says "validada, no medida"; Q46 and infra/README.md carry the same reason and the recipe to measure it when a VPC is available — _Hecho_: sin despliegue (la única VPC sigue siendo ajena); `openspec validate m6-hardening --strict` pasa.
