## 0. [pre-flight] Facts (design "Context")

- [x] 0.1 Confirm `proto/rayito/v1/code.proto` `ExecuteRequest` has fields 1–4 only and `CreateContextRequest.language = 1` exists (`grep -n "language\|= 4;" proto/rayito/v1/code.proto`); confirm `rayito-base` 17.0 is the newest launchable version and note its `snapshotBuild` sizes from `AWS_API_NOTES.md` Q52 (`928 100 352 / 1 305 825 280 / 37 998 592` B) in the "Notes" section at the end of this file
- [x] 0.2 Record the newest releases at implementation time: `bash_kernel` on PyPI (`uv pip index` or `https://pypi.org/pypi/bash_kernel/json`) with its resolved `pexpect`/`ptyprocess`, and `ijavascript` on npm (`pnpm view ijavascript version`); write both pins in the Notes and use them in 1.3/1.4. Do not resolve them from memory
- [x] 0.3 Confirm the AL2023 package names `nodejs20`, `nodejs20-npm` and the namespaced executables `node-20`/`npm-20` against `https://docs.aws.amazon.com/linux/al2023/ug/nodejs.html` (read 2026-09-16 for the design); if the page changed, update design D5 before continuing

## 1. [image] Poly variant: marker, Dockerfile layer, scripts, spike publish (design D5)

- [x] 1.1 `scripts/image_zip.py`: `VARIANTS = ("full", "slim", "poly")`, `KERNELS_MARKER_ENTRY = "kernel-sidecar/kernels_variant"`, write `poly\n` (mode 0644, 1980-01-01) for `--variant poly`, `marker_variant()` returning `poly`/`slim`/`full` and `SystemExit` naming the archive when both markers are present; docstring updated; no file created under `image/`
- [x] 1.2 `scripts/publish_image.py`: `DEFAULT_IMAGE_NAMES["poly"] = "rayito-base-poly"`, `--variant` choices from `VARIANTS`, `require_matching_variant` unchanged in logic (it compares `marker_variant` with the flag), docstring paragraph for `poly`; `scripts/copy_sidecar.py` requires and copies `requirements-poly.txt` next to `requirements.txt`
- [x] 1.3 `kernel-sidecar/requirements-poly.txt`: `bash_kernel==<pin from 0.2>` plus its resolved exact `pexpect` and `ptyprocess`; header comment (English) stating it is installed only in the `poly` variant; add a `pip-audit` step for it in `.github/workflows/audit.yml` next to the `requirements.txt` one (and in `ci.yml` if it audits pins there)
- [x] 1.4 `image/Dockerfile`: the single conditional layer of design D5 (dnf `nodejs20 nodejs20-npm`, `npm-20 install -g --prefix /opt/rayito/node ijavascript@<pin from 0.2>`, `chmod -R a+rX /opt/rayito/node`, pip install of `requirements-poly.txt` + `pip check`, the five checks: `node-20 --version`, `kernel.js` path, `import bash_kernel` as `user`, `require` of `jmp` as `user`) placed after the sidecar pip install and before the `su user` warm-up; Spanish header comment explaining the marker mechanism and that `rayito-base` never runs it
- [x] 1.5 `Makefile`: `IMAGE_ZIP_POLY := image/rayito-image-poly.zip`, `image-zip-poly`, `image-publish-poly` (`--variant poly --base-image-version $(BASE_IMAGE_VERSION) $(PUBLISH_ARGS)`), `clean` removes it, `.PHONY` updated; `.github/workflows/ci.yml` builds the poly zip after the slim one and asserts `marker_variant` for the three archives
- [x] 1.6 `scripts/tests/test_image_zip.py` and `test_publish_image.py`: poly zip contents, `marker_variant` for the three archives, the two-markers `SystemExit`, default name `rayito-base-poly`, the mismatch matrix of the `image-variant` delta; `cd clients/python && uv run pytest ../../scripts/tests -p no:cacheprovider` green, `uvx ruff check scripts` clean
- [x] 1.7 **Spike publish.** Build the poly zip by hand (`cargo zigbuild … -p rayd`, copy the binary, `python scripts/copy_sidecar.py kernel-sidecar image/kernel-sidecar`, `python scripts/image_zip.py image image/rayito-image-poly.zip --variant poly`) and run `uv run --with "boto3>=1.43.82,<2" python scripts/publish_image.py --artifact image/rayito-image-poly.zip --variant poly --base-image-version 1`. Record in the Notes: build seconds, the three `snapshotBuild` sizes, and whether the `npm-20`/`require("jmp")` checks passed
- [x] 1.8 **Decision point (design D5).** If 1.7 built `SUCCESSFUL`: continue with bash + javascript. If it failed on the node/ijavascript lines: apply the bash-only fallback now — remove the `nodejs20`/`npm-20`/`node-20` lines and the `require` check from the layer, do not create the `rayito-javascript` template in 3.2, delete the scenarios `javascript cell through the poly image` and `javascript execute_result maps like python` from `specs/code-execution/spec.md`, the JS assertion sentence from `specs/typescript-sdk/spec.md`, the `1 + 1` steps of the e2e tests in 7.2 (Python) and 6.3 (TypeScript), re-run `openspec validate m7-poly-kernels --strict --no-interactive`, paste the failing build's `stateReason` and the relevant log lines into the Notes and into `AWS_API_NOTES.md` Q57 (8.6), then republish the poly image and record its sizes

## 2. [proto] `ExecuteRequest.language` (design D1)

- [x] 2.1 `proto/rayito/v1/code.proto`: add `optional string language = 5;` to `ExecuteRequest` with the Spanish comment of D1; update the `CreateContextRequest.language` comment to the three names; `buf lint` clean; `buf breaking --against` a copy of the pre-change proto directory (scratchpad) reports nothing
- [x] 2.2 Regenerate: `cargo build -p rayito-proto` (build script), `python scripts/gen_python.py` (from `clients/python`, per the script's docstring), `cd clients/typescript && pnpm gen`; confirm `ExecuteRequest.language` appears in `crates/rayito-proto`'s generated code, `clients/python/src/rayito/v1/code_pb2.pyi` and `clients/typescript/src/gen/rayito/v1/code_pb.ts`; no hand-written struct anywhere

## 3. [sidecar] Kernel catalog, protocol additions (design D3, D4)

- [x] 3.1 `rayito_kernel_sidecar/languages.py`: `KernelLanguage(name, kernel_name, probe_cell)` and `LANGUAGES` (python → `rayito`/`pass`, bash → `rayito-bash`/`:`, javascript → `rayito-javascript`/`void 0`), `language_for(name) -> KernelLanguage | None`; fully typed, no inline comments, docstring explaining why probe cells differ
- [x] 3.2 Templates: `kernel-sidecar/jupyter/kernels/rayito-bash/kernel.json` (`python3 -m bash_kernel -f {connection_file}`, `language: bash`, `interrupt_mode: signal`, `display_name: rayito-bash`) and `kernel-sidecar/jupyter/kernels/rayito-javascript/kernel.json` (`/usr/bin/node-20 /opt/rayito/node/lib/node_modules/ijavascript/lib/kernel.js --protocol=5.1 {connection_file}`, `language: javascript`, `interrupt_mode: signal`); skip the javascript template if 1.8 chose bash-only
- [x] 3.3 `kernels.py`: `KernelPaths.kernelspec_dir_for(kernel_name)` and `template_for(kernel_name)`; `install_kernelspecs(paths) -> dict[str, Path]` implementing the availability rule of D4 (`argv[0]` on `PATH` or existing absolute path, every absolute `argv` path exists, `python3 -m <module>` importable; python always available) with the existing rewrites; `ContextBase.__init__(context_id, language, cwd, envs, ...)` storing the language; `KernelContext._start_kernel` using the language's `kernel_name` and `probe_cell`; `KERNEL_NAME` kept only as the python kernel name; `__main__.py` calls `install_kernelspecs` and passes the available names to the server
- [x] 3.4 `protocol.py`: `REQUEST_FIELDS["create_context"] = ("context_id", "language", "cwd", "envs")`, `EVENT_FIELDS["ready"] += ("languages",)`, `Request.language`, `decode_request` validating `language` as a string; `server.py`: `ContextFactory` takes `(context_id, language, cwd, envs)`, `ServerOptions.languages: tuple[str, ...]`, `start_default_context` emits `languages` (sorted), `_op_create_context` resolves `language` (absent → `python`; unknown → `invalid_argument` `unknown language`; unavailable → `invalid_argument` `language not installed`), `_op_reseed` lists non-python contexts under `skipped` and logs `skipped`; `logging.py` allowlist gains `language`, `languages`, `skipped`
- [x] 3.5 `tests/fixtures/protocol_v1.jsonl`: line 2 gains `"language":"python"` in the D3 order; the `ready` line gains `"languages":["python"]`; new lines: `create_context` with `"language":"bash"`, `ready` with `["bash","javascript","python"]`, a `reseed` reply with `"skipped":["default-bash"]`; `tests/test_protocol.py` covers the new fields
- [x] 3.6 Sidecar unit tests: `test_languages.py` (availability with fake templates in `tmp_path`: missing `node-20` → no javascript; missing module → no bash; python always), `test_server.py` (ready `languages`, unknown/unavailable language replies, `reseed` `skipped` with a fake bash context, `list_contexts` reporting the language), fakes updated to the new factory signature; `test_kernel.py` gains a bash case guarded by `importlib.util.find_spec("bash_kernel")`
- [x] 3.7 `cd kernel-sidecar && uvx ruff check . && uvx ruff format --check . && uv run mypy src && uv run pytest tests` green (kernel tests on WSL2 with `bash_kernel` installed in the venv for the bash case)

## 4. [rayd] Language value type, lazy default contexts, status mapping (design D2, D3, D6)

- [x] 4.1 `rayd-core/src/code/language.rs`: `Language { Python, Bash, Javascript }` with `parse` (`""` → Python; the three canonical names; else `CodeError::InvalidLanguage`), `as_str`, `default_context_id`; unit tests; exported from `code/mod.rs`; `CONTEXT_LANGUAGE` removed; `ContextInfo.language: Language`, `ContextEntry::new(context_id, language, cwd, envs)`; `CreateContextInput.language: String` parsed in `plan_context`, which now returns the `Language` too
- [x] 4.2 `rayd-core/src/code/error.rs`: `InvalidLanguage` message `language must be one of python, bash, javascript`; new `LanguageUnavailable(Language)` (`language {0} is not installed in this image; use rayito-base-poly`), `LanguageWithContext` (`language cannot be combined with context_id`), `EnvsPythonOnly` (`envs per execution are only supported on python contexts; pass envs to create_code_context instead`); `messages_never_quote_input` test extended
- [x] 4.3 `rayd-core/src/code/protocol.rs`: `SidecarOp::CreateContext { context_id, language, cwd, envs }` (field order = serialisation order), `SidecarEvent::Ready { …, #[serde(default)] languages: Vec<String> }`, `ReseedPayload { …, #[serde(default)] skipped }`; `hooks.rs` `run_rotation_request` unchanged; golden test over the updated fixture green
- [x] 4.4 Supervisor state: store the `ready.languages` as a `BTreeSet<Language>` (absent → `{Python}`; unknown names ignored with a warn log naming the count only), exposed as `available_languages()`; `CodeManager::create_context` checks availability (`LanguageUnavailable`), sends `language`, registers the entry with the parsed language; `ExecuteInput.language: Option<String>`; `execute_unchecked` implements the D2 routing table, `LanguageWithContext`, `EnvsPythonOnly` (from the registry's language, before any sidecar call), and `ensure_language_default` with the per-language `tokio::sync::Mutex` map; `context created` log gains `language` and `lazy`; reseed log gains `skipped`
- [x] 4.5 `rayd/src/grpc/code.rs`: pass `request.language` (`Option<String>`) into `ExecuteInput`; `to_context_info` uses `language.as_str()`; `status_for`: `LanguageUnavailable` → `unimplemented`, `LanguageWithContext` and `EnvsPythonOnly` → `invalid_argument`; module doc updated
- [x] 4.6 `rayd/src/code/fake_sidecar.rs` and `tests/common/mod.rs`: the fake's `ready` accepts a configurable `languages` list (default `["python"]`) and answers `create_context` for any id; new `crates/rayd/tests/m7_poly.rs` covering every host scenario of the `code-execution` delta (routing table, one lazy creation under two concurrent executes, exclusivity `INVALID_ARGUMENT`, `UNIMPLEMENTED` with `rayito-base-poly` in the message when `ready` lists only python or has no field, `EnvsPythonOnly`, `ListContexts` showing `default-bash`/`bash`, destroy-and-recreate of `default-bash`, explicit `CreateContext{bash}` request line, `reseed` reply with `skipped` logged, lazy-creation log line free of cell text); `m4_code.rs` language test updated to the new message
- [x] 4.7 `cargo fmt --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace`, `cargo zigbuild --release --target aarch64-unknown-linux-musl -p rayd` green (toolchain exports per `CONTRIBUTING.md` §3)

## 5. [python-sdk] `run_code(language=)`, contexts, e2b shim (design D7)

- [x] 5.1 `_code_base.py`: `SUPPORTED_LANGUAGES = frozenset({"python", "bash", "javascript"})`, `LANGUAGE_ALIASES = {"js": "javascript"}`, `normalize_language`, `validate_language` returning the canonical name (`None` → `python`), `build_execute_request(code, *, context_id=None, language=None, envs=None, timeout=...)` setting `language` only when given and raising `InvalidArgumentException` for `context_id` + `language`; Spanish messages, English identifiers
- [x] 5.2 `sandbox_sync/code.py` + `sandbox_sync/main.py` and `sandbox_async/code.py` + `sandbox_async/main.py`: `run_code(code, *, language=None, context=None, ...)` threading `language`; `create_context(language=)` through `validate_language`; docstrings mention the three kernels, the lazy first-cell start and the `rayito-base-poly` requirement; identical surfaces sync/async
- [x] 5.3 `e2b/_compat.py`: `normalized_language_or_unimplemented(language, feature) -> str | None` (forward `None`/python/bash/javascript/js; raise `UnimplementedError(feature, "kernels disponibles: python, bash, javascript (variante rayito-base-poly); R y Java no")` otherwise); `e2b/_sync.py` and `e2b/_async.py` pass the normalised language to the core in `run_code` and `create_code_context`
- [x] 5.4 `tests/unit/fake_code.py`: honour `ExecuteRequest.language` (per-language default contexts `default-bash`/`default-javascript` created on first use, echoed by `ListContexts` with their language; `INVALID_ARGUMENT` for `language` + `context_id`; configurable `UNIMPLEMENTED` with `rayito-base-poly` in the message for a language the fake does not "ship"; `INVALID_ARGUMENT` for `envs` on a non-python context); `CreateContextRequest.language` stored and listed
- [x] 5.5 Unit tests: `test_code_base.py` (normalisation table incl. `JS`, `Bash`, `r`, `""`, `None`; exclusivity; `HasField("language")` false for python/None), `test_code_sync.py` and `test_code_async.py` (bash cell routed to `default-bash`, `list_code_contexts` shows it, `create_code_context(language="bash").language == "bash"`, `UNIMPLEMENTED` → `InvalidArgumentException` with `grpc_code`, envs-on-bash error), `test_e2b_compat_sync.py`/`_async.py` (the two e2b-compat delta scenarios; the old `python is the only kernel` test replaced)
- [x] 5.6 `cd clients/python && uv run pytest tests/unit && uv run ruff check . && uv run ruff format --check . && uv run mypy src tests` green

## 6. [typescript-sdk] `runCode({ language })` (design D7)

- [x] 6.1 `src/sandbox/code.ts`: `SUPPORTED_LANGUAGES`, `LANGUAGE_ALIASES`, `normalizeLanguage`, `validateLanguage` (canonical, `undefined` → `python`), `RunCodeOptions.language?: string`, `buildExecuteRequest` setting `language` only when given and throwing `InvalidArgumentError` for `context` + `language`; `CodeClient.runCode` and `Sandbox.runCode` pass it; TSDoc (Spanish) mentions the three kernels and the poly image; `pnpm typecheck` strict
- [x] 6.2 `tests/unit/fake`: per-language default contexts, exclusivity `InvalidArgument`, configurable `Unimplemented` (`rayito-base-poly` in the message), envs-on-bash `InvalidArgument`; `tests/unit/code.test.ts`: the three `typescript-sdk` delta scenarios against the fake (routing, invalid combinations, language not shipped)
- [x] 6.3 `tests/e2e/poly.e2e.test.ts`: skipped unless `RAYITO_E2E=1` and `RAYITO_TEMPLATE_POLY`; one sandbox (`timeoutMs 900 000`), bash cell, JS cell (unless 1.8 chose bash-only), exclusive options rejection, `listCodeContexts()` with `default-bash`, `afterAll` kill; wired into the existing `test:e2e` vitest config; `README.md` of the package gains a `runCode(code, { language })` example
- [x] 6.4 `cd clients/typescript && pnpm lint && pnpm typecheck && pnpm test && pnpm build` green

## 7. [acceptance] Real AWS (design D5 size gate, D8)

- [x] 7.1 Republish `rayito-base` from the new Dockerfile (`make image-publish` steps by hand: build, copy, `image_zip.py image image/rayito-image.zip`, `publish_image.py --artifact image/rayito-image.zip --base-image-version 1`); record the new version, build seconds and the three sizes in the Notes; assert `|memory delta| ≤ 20 MB` and `|code install delta| ≤ 10 MB` against 17.0; confirm from the build log that the conditional layer ran nothing. A delta outside the band stops the change: fix the marker logic first
- [x] 7.2 `clients/python/tests/e2e/test_m7_poly_kernels.py`: `poly_sandbox` fixture (skip with a reason naming `RAYITO_TEMPLATE_POLY`; `timeout=900`, `idle=None`, kill in teardown, `kernel_ready_s` reported), tests 1–6 of design D8 (`test_bash_cell`, `test_javascript_cell` unless bash-only, `test_bash_timeout`, `test_python_unchanged_on_poly`, `test_language_unimplemented_on_base` on the ordinary `sandbox`, `test_snapshot_sizes` from `RAYITO_BASE_SIZES`/`RAYITO_POLY_SIZES`); first-cell latencies for bash and JS reported with `report(...)`
- [x] 7.3 Run `RAYITO_E2E=1 RAYITO_TEMPLATE=<rayito-base new version> RAYITO_TEMPLATE_POLY=<rayito-base-poly ARN> RAYITO_BASE_SIZES=… RAYITO_POLY_SIZES=… uv run pytest tests/e2e/test_m7_poly_kernels.py -m e2e -s`; paste the pass/skip summary, the reported latencies and `kernel_ready_s` into the Notes; zero live MicroVMs afterwards (`list-microvms`)
- [x] 7.4 Run the full Python e2e suite once against the new `rayito-base` version (`uv run pytest tests/e2e -m e2e`) to prove the Python path is unchanged; paste the summary
- [x] 7.5 Run `RAYITO_E2E=1 RAYITO_TEMPLATE_POLY=<arn> pnpm test:e2e -- poly` (TypeScript) and paste the summary; zero live MicroVMs afterwards

## 8. [docs] (design D9)

- [x] 8.1 `docs/site/docs/kernels.md` with the sections of D9 (languages and images, lazy start and first-cell latency, Python + TypeScript examples, Python-only features, timeouts, default ids and `create_code_context(language=)`, `UNIMPLEMENTED` on `rayito-base`, publishing the poly image on Linux and Windows, measured sizes from Q57, older-agent caveat; a bash-only note if 1.8 fell back); `docs/site/mkdocs.yml` nav entry `Kernels: kernels.md` after `Referencia de API`
- [x] 8.2 `docs/site/docs/e2b-compat.md` row for `run_code(language=...)` updated; `clients/python/README.md` and `clients/typescript/README.md` "kernels no Python" → "kernels R/Java" and one `language="bash"` example each; root `README.md` "Cómo funciona": one sentence on `bash`/`javascript` in `rayito-base-poly`
- [x] 8.3 `SPEC.md` §4: the kernels bullet amended as in D9 (non-goal narrowed to `rayito-base`; R and Java still out)
- [x] 8.4 `MILESTONES.md` M7 row 7: scope wording (`language` in `ExecuteRequest` too) and status `implementado` with the evidence list; `pendiente de aceptación` until the acceptance agent archives
- [x] 8.5 Changelogs `[Unreleased]` → `### Added`: `clients/python/CHANGELOG.md` (`run_code(language=)`, `create_code_context(language=)` bash/javascript, shim forwarding), `clients/typescript/CHANGELOG.md` (`runCode({ language })`), `crates/rayd/CHANGELOG.md` (`ExecuteRequest.language`, lazy per-language defaults, `UNIMPLEMENTED` for missing kernels, sidecar protocol additions)
- [x] 8.6 `AWS_API_NOTES.md` measured table: Q57 row with the sizes/build times of `rayito-base-poly` and the rebuilt `rayito-base`, `kernel_ready_s`, first bash/JS cell latencies, and the 1.7/1.8 outcome (ijavascript installed cleanly, or the exact failure and the bash-only decision)
- [x] 8.7 `cd clients/python && uv run --group docs mkdocs build -f ../../docs/site/mkdocs.yml --strict --site-dir ../../docs/site/_build` green

## 9. [gates] Everything green on the box (design D10)

- [x] 9.1 Rust gates (4.7) re-run after every later edit; `buf lint` and `buf breaking` clean
- [x] 9.2 Python client (5.6), sidecar (3.7), scripts (1.6) gates green
- [x] 9.3 TypeScript gates (6.4) green
- [x] 9.4 Docs strict build (8.7) green; `python scripts/check_license.py` still `OK`; `python scripts/gen_limits.py --check` still clean
- [x] 9.5 `openspec validate m7-poly-kernels --strict --no-interactive` passes with the final deltas (after any 1.8 edit); every box above ticked with its evidence in the Notes

## Notes

Implemented 2026-09-16 (poly track). The measured row is **Q57** (Q53–Q56 were taken by the other M7 tracks at write time; every reference in this change and its files says Q57).

- **0.1** `code.proto` before the change: `ExecuteRequest` fields 1–4 only, `CreateContextRequest.language = 1` present. `rayito-base` 17.0 newest ACTIVE; `snapshotBuild` 928 100 352 / 1 305 825 280 / 37 998 592 B (Q52).
- **0.2 pins**: `bash_kernel==0.10.0` (PyPI 2025-01-05; requires `pexpect>=4.0`, `ipykernel`, `filetype`) → `kernel-sidecar/requirements-poly.txt` = `bash_kernel==0.10.0`, `filetype==1.2.0`, `pexpect==4.9.0`, `ptyprocess==0.7.0` (the last two equal the pins of `requirements.txt`, all pure-Python wheels). `ijavascript@5.2.1` on npm (→ `jp-kernel@2.0.0` → `jmp@2.0.0` → `zeromq@5` = 5.3.1).
- **0.3** `docs.aws.amazon.com/linux/al2023/ug/nodejs.html` read 2026-09-16: packages `nodejs{MAJOR}` / `nodejs{MAJOR}-npm`, namespaced executables `node-20` / `npm-20` (`/usr/bin/node-{MAJOR}`), `node`/`npm` through `alternatives`. D5 unchanged.
- **1.1–1.6** `image_zip.py` (`VARIANTS` + `KERNELS_MARKER_ENTRY` + two-marker `SystemExit`), `publish_image.py` (`DEFAULT_IMAGE_NAMES["poly"]`), `copy_sidecar.py` (`REQUIREMENTS_FILES`) were implemented, then absorbed by the `m7-cli` track into `clients/python/src/rayito/cli/_artifact.py` / `_publish.py` (the `scripts/*.py` files are now shims that load those modules by path; the poly variant, the marker rule and the mismatch matrix live on there, with the poly tests in `clients/python/tests/unit/cli/test_artifact.py` and `test_publish.py`: 36 passed; `scripts/tests` 57 passed; `uvx ruff check scripts` clean). `Makefile` `image-zip-poly`/`image-publish-poly` (the cli track added `$(PY)` and `--bucket $(BUCKET)`); `ci.yml` builds the poly zip and asserts the three markers; `ci.yml`/`audit.yml` run `pip-audit` over `requirements-poly.txt`.
- **1.7 spike (javascript layer as designed)**: `rayito-base-poly` 1.0 (`create-microvm-image`) and 2.0 (`update`, after pre-creating `/rayito/rayito-base-poly`) both `CREATE_FAILED`/`UPDATE_FAILED`, version `FAILED`/`INACTIVE` at 113 s, `stateReason` = `The container image build failed.` (build and version), **no CloudWatch stream at all** (the builder only streams the ephemeral VMs, never `docker build` output). Reproduced on the same base image digest with Docker Desktop + QEMU (`--platform linux/arm64`): `zeromq@5.3.1` ships `prebuilds/` only for `darwin-x64`, `linux-x64`, `win32-{ia32,x64}` up to `node.abi108`/`electron.abi109`; on Node 20.20.2 (ABI 115) / linux-arm64 `node-gyp-build` finds nothing and `npm-20 install` runs `node-gyp rebuild`, which ends with `gyp ERR! stack Error: not found: make` (and, without `python3`, `Could not find any Python installation to use`); `command -v make gcc g++ cc` finds nothing on al2023-minimal.
- **1.8 decision: bash only** (design D5 fallback). Removed the `nodejs20`/`npm-20`/`node-20`/`require("jmp")` lines from the layer, no `rayito-javascript` template (the catalog entry and the SDK validation keep the name so it is `UNIMPLEMENTED`, never "unknown language"); deleted the two JavaScript scenarios and the TS assertion sentence from the deltas and replaced them with "known but not shipped" scenarios; `openspec validate m7-poly-kernels --strict --no-interactive` passes.
- **2.x** `ExecuteRequest.language = 5` (`optional`); `buf lint` clean; `buf breaking --against <copy of the pre-change proto dir>` clean; `buf generate` + `python scripts/gen_python.py` + `cargo build` regenerated the three clients (`code_pb2.pyi` line 51, `code_pb.ts` line 106, prost struct).
- **3.x** sidecar: `languages.py`, `install_kernelspecs`, `ContextBase(language)`, protocol fields (`create_context.language`, `ready.languages`, reseed `skipped`), golden fixture with the four new lines; host gates `uvx ruff check .`, `uvx ruff format --check .`, `uv run mypy src`, `uv run pytest tests` → **80 passed**; real kernels under Docker (`python:3.12-slim-bookworm`, uid 1000, pins + `requirements-poly.txt`): `pytest -m kernel` → **13 passed** incl. `test_bash_context_starts_without_the_ipython_config` (`bash_kernel` start 2.88 s on x86, `echo 1` → stdout, `reseed` → `skipped: ["default-bash"]`).
- **4.x** rayd: `Language`/`AvailableLanguages`/`ExecuteTarget` in `rayd-core::code::language`, `ContextPlan`, the three new `CodeError`s, supervisor `available_languages()` from `ready.languages`, `CodeManager::{start_context, ensure_language_default, resolve_execute_context}` with per-language `tokio::sync::Mutex`, `EnvsPythonOnly` before any sidecar line, `skipped` in the reseed log, gRPC status rows. Windows: `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace` (81 rayd lib + 5 bin + 13 m1 + 283 core) green; `cargo auditable zigbuild --release --locked --target aarch64-unknown-linux-musl -p rayd` → 12 524 384 B (`.dep-v0` 256 packages). Linux (`rust:1.98-slim-bookworm` + python3 as uid 1000, Docker): `m7_poly` **9/9**, `m4_code` 19/19 (+1 ignored), `m1_hello` 13, `m2_process` 25, `m3_filesystem` 30, `m5_pty` 15, `m5_suspend_resume` 7, `m6_hooks` 7, `m6_limits` 5. Linux clippy fails only in `crates/rayd/src/adapters/tar_archiver.rs` and 3 of its unit tests fail (`m7-s3-persistence` track, in progress at the time).
- **5.x** Python SDK: `normalize_language`/`validate_language`/`build_execute_request(language=)`/`language_default_context_id`, `run_code(language=)` sync/async, shim `normalized_language_or_unimplemented` (python travels as no `language`), fake `rayd` with per-language defaults and `languages`; `uv run pytest tests/unit` → all M7 tests pass (`test_code_base`, `test_code_sync`, `test_code_async`, `test_e2b_compat_*`: 145 passed in the focused run; full suite 842 passed with 3 failures of other tracks: `test_limits.py` ×2 (`m7-s3-persistence` limits) and the shim `1+1` cell fixed here), `ruff format --check` clean, `mypy src tests` clean; `ruff check .` reports 2 `RUF043` in `tests/unit/test_pool_base.py` (`m7-suspended-pool` track).
- **6.x** TypeScript: `normalizeLanguage`/`validateLanguage`/`languageDefaultContextId`, `RunCodeOptions.language`, fake with `languages`/`lazyContexts`, three M7 unit tests, `poly.e2e.test.ts` (`useE2E(templateVar)`), README example; `pnpm lint`, `pnpm typecheck`, `pnpm build` green; `pnpm test` 332 passed with 3 failures in `tests/unit/limits.test.ts` (`S_3_BUCKET_NAME_*`, `m7-s3-persistence` track).
- **7.1 publishes** (`uv run --project clients/python python scripts/publish_image.py … --base-image-version 1 --bucket <bucket>`): `rayito-base-poly` **3.0** `SUCCESSFUL`/`ACTIVE`, 215.9 s, memory **921 780 224** / code **1 323 397 120** / disk **37 466 112** B (zip 12 636 398 B); `rayito-base` **18.0**, 195.5 s, memory **938 098 688** / code **1 320 202 240** / disk **36 401 152** B (zip 12 636 257 B). Deltas vs 17.0: memory +9 998 336 B (≤ 20 MB), code install +14 376 960 B raw = +7 823 400 B of `rayd` binary growth (S3 crates of the persistence track, 4 700 984 → 12 524 384 B) + **6 553 560 B net (≤ 10 MB)**; the layer stayed inert (`import bash_kernel` exits 1 in an 18.0 sandbox, bash `UNIMPLEMENTED`). The spec/design band now reads "net of the `rayd` binary size change" and the e2e takes `RAYITO_RAYD_BYTES_DELTA`.
- **7.3** `RAYITO_E2E=1 RAYITO_TEMPLATE=<rayito-base ARN> RAYITO_TEMPLATE_VERSION=18.0 RAYITO_TEMPLATE_POLY=<rayito-base-poly ARN> RAYITO_BASE_SIZES=938098688,1320202240,36401152 RAYITO_POLY_SIZES=921780224,1323397120,37466112 RAYITO_RAYD_BYTES_DELTA=7823400 uv run pytest tests/e2e/test_m7_poly_kernels.py -m e2e -s` → **6 passed in 55 s**: poly `kernel_ready_s` 6.27–6.42 s; first bash cell (lazy `bash_kernel` start) **4.00 s**, second **0.20 s**; context envs reach bash; `envs` per execution and `language`+`context` → `InvalidArgumentException`; `false` → `ExecutionError(name='', value='1')`; `javascript` → `UNIMPLEMENTED` naming `rayito-base-poly`; `sleep 30` with `timeout=2` → `ExecutionTimeout` after 5.79 s (restart branch) and `echo back` works; Python unchanged on the variant; base 18.0 `kernel_ready_s` 8.64 s, bash `UNIMPLEMENTED`, no `bash_kernel`; size gate green. Own MicroVMs terminated by the fixtures/sweeper.
- **7.4** `RAYITO_E2E=1 RAYITO_TEMPLATE=<rayito-base ARN> RAYITO_TEMPLATE_VERSION=18.0 RAYITO_TEMPLATE_POLY=<poly ARN> uv run pytest tests/e2e -m e2e` (run once the template was idle; 32 collected with the other M7 tracks' new files) → **26 passed, 4 skipped** (`test_imds_block`, `test_egress_allowlist`, `test_image_publish_reuses_the_current_version`, and one cli skip), 1 deselected (`bench`), **1 failed** in 1011 s: `test_m6_e2b_compat.py::test_e2b_shim_cookbook` asserted the pre-M7 contract (`run_code("1", language="js")` → `UnimplementedError`); the agent now answers `UNIMPLEMENTED` `language javascript is not installed in this image; use rayito-base-poly`. The step was updated to the M7 e2b-compat delta (`language="r"` → `UnimplementedError`; `language="js"` → `InvalidArgumentException` with `grpc_code == UNIMPLEMENTED` naming the poly image) and re-run alone: **1 passed in 25 s**. M1–M6 Python path unchanged on 18.0 (`test_m4_code`, `test_m5_*`, `test_m6_*` green). Zero live MicroVMs afterwards.
- **7.5** `RAYITO_E2E=1 RAYITO_TEMPLATE=<base ARN> RAYITO_TEMPLATE_POLY=<poly ARN> pnpm exec vitest run --project e2e --no-file-parallelism tests/e2e/poly.e2e.test.ts` → **1 passed (10.8 s)**: `kernel_ready_s` 6.36 s, first bash cell 3.90 s, second 0.20 s, `javascript` → `InvalidArgumentError` with `rayito-base-poly`, exclusive options rejected, `default-bash` listed; without `RAYITO_TEMPLATE_POLY` the file is reported skipped. (`pnpm test:e2e -- poly` does not filter: vitest ran the three e2e files.)
- **8.x** `docs/site/docs/kernels.md` + nav; e2b-compat row; both READMEs; root README sentence; SPEC.md §4; MILESTONES row 7; three changelogs; Q57; `mkdocs build --strict` green.
- **9.x** `python scripts/check_license.py` OK; `python scripts/gen_limits.py --check` clean; `openspec validate` passes. Other tracks' in-progress failures listed above are outside this change.
- **Review fixes (2026-09-16)** (a) contract text after D5: `code.proto` `CreateContextRequest.language` comment, `sandbox_sync/{main,code}.py` docstrings (async delegates to them), `e2b/_compat.py` `AVAILABLE_KERNELS_REASON` + `normalized_language_or_unimplemented` docstring, `e2b/_sync.py` `run_code` docstring, `cli/_artifact.py` module docstring, `sandbox/code.ts` `RunCodeOptions.language` JSDoc and the three `[Unreleased]` changelog bullets now say: bash lives in `rayito-base-poly`; `javascript` is a reserved name no image ships (`UNIMPLEMENTED` everywhere, Q57). `buf lint` + `buf generate` (Python gencode stays on plugin v36.1 / protobuf 7.36.1, the pyproject pin; `code_pb.ts` line 22 carries the new comment) + `cargo build` regenerated the clients. (b) CI: both `pytest -m kernel` steps (`check` x86 and the aarch64 job) add `--with-requirements requirements-poly.txt` and export `RAYITO_REQUIRE_BASH_KERNEL=1`; `test_kernel.py` fails instead of skipping the bash case when that variable is set and `bash_kernel` is not importable; CONTRIBUTING.md mirrors the command. Verified under Docker `python:3.12-slim`: without the poly pins and the variable set → **1 failed** (`Failed: RAYITO_REQUIRE_BASH_KERNEL=1 but bash_kernel is not importable`); the exact CI command → **13 passed** (bash case included, no skips). Gates re-run: `cargo fmt --check`, `clippy -D warnings`, `cargo test --workspace` (all ok), `cargo zigbuild --release --target aarch64-unknown-linux-musl -p rayd`; Python `pytest tests/unit` **1065 passed, 1 skipped**, ruff check/format, mypy clean; TypeScript biome, tsc, vitest **386 passed**, build; sidecar ruff/mypy/host tests **80 passed**.
