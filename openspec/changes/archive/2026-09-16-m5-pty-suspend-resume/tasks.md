## 0. [e2e] Platform probe before any adapter code

- [x] 0.1 With the M4 SDK (0.0.4) against the current `rayito-base` 8.0 image, run `commands.run("test -c /dev/ptmx && grep -c devpts /proc/mounts")` and `commands.run("python3 -c 'import pty, os; m, s = pty.openpty(); print(os.ttyname(s))'")` on one MicroVM; record the outputs as `AWS_API_NOTES.md` §16 **Q37** (devpts in the guest). If either fails: **stop**, do not start section 4, and propose an ADR (mounting devpts needs `CAP_SYS_ADMIN` → `additionalOsCapabilities`, M6)

## 1. [proto] Contract (done in this change; verify)

- [x] 1.1 `proto/rayito/v1/pty.proto`: `PtyServerMessage.seq` (`uint64`, field 5, outside the `oneof`, comment on the seq rule) added; `buf lint` and `buf build` clean; `python scripts/gen_python.py` regenerated `clients/python/src/rayito/v1/pty_pb2*`; `cargo check --workspace --all-targets` green (Rust regenerates in `rayito-proto/build.rs`)
- [x] 1.2 Confirm no other proto edit is needed against design D1 (fields used: `PtyStart{size?, envs, cwd?, user?, shell?, timeout_ms}`, `PtyServerMessage{started|data|exited|keepalive, seq}`, `PtyExited{exit_code, exited, status, error?, signal?}`, `ResizeRequest{pid, size}`, `KillPtyRequest{pid}`, shared `ConnectRequest{pid, from_seq}` / `SendInputRequest{pid, data}`, `ReattachRequest{context_id, execution_id, from_seq}`, `HealthResponse{resume_generation, clock_offset_ms, kernel_state_lost}`); `buf breaking` against the pre-M5 proto in CI reports only the additive field

## 2. [e2e] Acceptance test first (red)

- [x] 2.1 Create `clients/python/tests/e2e/test_m5_pty_suspend_resume.py` with the 15 blocks of `test_pty_suspend_resume` and `test_auto_resume` from design.md "Acceptance test list" (marker `e2e`, `m5_sandbox` fixture with `timeout=1800` and `IdlePolicy(max_idle_seconds=600, auto_resume=True)`, helpers `read_until`, `collect_in_thread`, timing prints, `wait_for_terminal_state` from M1)
- [x] 2.2 `tests/e2e/conftest.py`: factor `create_test_sandbox` so the M5 fixture can pass its own `timeout`/`idle`; keep the M1 guardrails (sweeper, pre-flight, `kernel_ready_s` print)
- [x] 2.3 Run `RAYITO_E2E=1 RAYITO_TEMPLATE=<arn> uv run pytest tests/e2e/test_m5_pty_suspend_resume.py -m e2e -x` against the 8.0 image once the SDK side exists and confirm it fails at block 4 (`pty.create` → `InvalidArgumentException` from `UNIMPLEMENTED`); if the M5 image is already published by then, note it as superseded like M4 2.3

## 3. [rayd] Domain in `rayd-core`

- [x] 3.1 `process/identity.rs`: `ProcessIdentity.shell: String`; update every constructor site (`spec.rs` tests, `rayd` fakes, `NixUserLookup` from `pw_shell` with `/bin/sh` fallback, `CurrentUserLookup`, `UnsupportedLookup`); `cargo test -p rayd-core` green
- [x] 3.2 `process/registry.rs`: `kind(pid) -> Result<ProcessKind, ProcessError>`; `process/error.rs`: `WrongKind{pid, expected: ProcessKind}`, `TooManySubscribers` already present or added; tests
- [x] 3.3 `pty/{mod,error,ports}.rs` per design D2: constants (`DEFAULT_PTY_COLS/ROWS`, `MAX_PTY_DIMENSION`, `PTY_CHUNK_BYTES`, `PTY_TERM`, `PTY_LOCALE`, `PTY_SHELL_ARGS`, `PTY_DRAIN_GRACE`, `FALLBACK_SHELL`), `PtySize` (validated newtype, `new`, `Default`, `from_request`), `resolve_shell`, `pty_base_env`, `PtySpawnInput`, `PtyPlan`, `plan_pty` (identity → shell → env order → cwd → `SpawnSpec` with `["-i","-l"]`, `StdinMode::Pipe`, registry `config`), `PtyError` (thiserror, `Display` without envs/cwd/bytes), `PtyBackend`/`PtyChild` port; `lib.rs` exports `pty`; tests for every rule of design D16 "rayd-core" (sizes, shell, env order, `SHELL`, root refused, unknown user)
- [x] 3.4 `lifecycle.rs`: `suspended_total: Duration` accumulated in `resume(now)` (not on a repeated `/resume`), `suspended_total()`; `session.rs`: `running_now()`, `suspended_total()`; tests with the fake clock (advance / suspend / advance 300 / resume → running advanced by the non-suspended part only; three cycles)
- [x] 3.5 `clock.rs`: `Deadline{at}` with `Deadline::after(running_now, timeout)` and `remaining(running_now) -> Option<Duration>`; `process/timeout.rs`: `TimeoutPlan{term_at: Deadline, kill_at: Deadline}` built from `running_now`; tests (before/at/after, remaining after a simulated pause)
- [x] 3.6 `code/ring.rs`: `ExecuteRing` (`EXECUTE_RING_CAPACITY_BYTES = 4 MiB`, `push`, `replay_from`, `oldest_seq`, `next_seq`, event cost rules, oversized event evicts all and is not stored); `code/executions.rs`: `ExecutionLimits{max_subscribers: 8, retention: 30 s, max_retained: 32}`, `ExecutionRegistry<S: SubscriberSlot>` (`register`, `push`, `attach` with `Attachment{replay, ended, subscriber}`, `detach`, `mark_ended`, `reap_expired`, `len`, `retained_count`); `code/execution.rs`: `ExecutionId::parse`; `code/error.rs`: `ExecutionNotFound`, `InvalidExecutionId`, `ReplayOutOfRange{oldest, next}`, `TooManySubscribers{max}`; tests per design D16 (replay bounds, eviction, 9th subscriber, context mismatch, ended replay, retention and the 32 cap)
- [x] 3.7 `hooks.rs` (new or extended): `suspend_actions(&Transition) -> SuspendActions{close_streams}`; `code/hooks.rs`: `probe_outcome(&[(ContextId, bool)]) -> ProbeOutcome{alive, lost}`, `restart_after_resume(lost, &ContextRegistry) -> Vec<SidecarOp>`; `code/ports.rs`: `KernelStatus::kernel_state_lost()`; constants `STREAM_CLOSE_GRACE = 2 s`, `QUIESCE_TIMEOUT = 2 s`, `RESUME_PROBE_BUDGET = 12 s`; `OpTimeouts.resume` default 15 s; tests
- [x] 3.8 `cargo test -p rayd-core` green on the Windows host; `cargo clippy -p rayd-core --all-targets -- -D warnings` clean

## 4. [rayd] Adapters, application layer, gRPC and hooks

- [x] 4.1 `adapters/pty_backend.rs` (`cfg(unix)`): `NixPtyBackend{identity_switch}` implementing `PtyBackend` per design D3 (`openpty` with the initial `Winsize`, `fchown`/`fchmod 0o620` of the slave under `Enforce`, `Command` with three slave dups and **no** `process_group(0)`, `pre_exec` = `setsid` → `ioctl(0, TIOCSCTTY, 0)` → `PreExecPlan::apply`, master `O_NONBLOCK` in `AsyncFd`, `PtyMaster` `AsyncRead`/`AsyncWrite` with `EIO` → EOF, `PtyResizer` over `TIOCSWINSZ`, `signal_group` shared with M2); `UnsupportedPtyBackend` otherwise; `adapters/mod.rs` exports `PlatformPtyBackend`; `main.rs` logs `pty_devices` at boot
- [x] 4.2 `pty/child.rs`: `PtyIo`, `PtyResizer`, `PtySpawner`; `process/fanout.rs`: `fan_out` and `mark_ended` extracted from `ProcessManager` (`pub(crate)`), `SharedRegistry` type; `ProcessManager::new` takes the shared registry; `connect`/`send_input`/`close_stdin` add the `Process` kind check, `send_signal` accepts both kinds; existing `m2_process.rs` still green
- [x] 4.3 `pty/manager.rs`: `PtyManager<B: PtySpawner>` with `PtySettings{stall_timeout, drain_grace}`, `create` (gate, `plan_pty`, `ensure_directory`, `NoPtyDevices` check, capacity + open + register + first subscriber under one lock, supervise task with the 16 KiB pump, `select!` on wait vs EOF with `PTY_DRAIN_GRACE`, master closed, `mark_ended`), `connect` (kind check, attach), `send_input`, `resize`, `kill` (SIGKILL, `EndReason::Signal`), timeout task via `running_sleep` (4.5); logs per design D19
- [x] 4.4 `grpc/pty.rs`: `PtyGrpc<B>` implementing the five RPCs with the design D5 conversions, `KeepAliveStream` (30 s) and the suspend wrapper with the `exited{suspending}` terminal; `status_for` table + unit test; `grpc/pending.rs` deleted; `grpc/mod.rs` registers `PtyServiceServer::new(PtyGrpc…)` and `router_with_settings` takes `pty: Arc<PlatformPtyManager>` and `suspend: Arc<SuspendSignal>`
- [x] 4.5 `lifecycle/suspend.rs`: `SuspendSignal`, `SuspendWatch` (open-stream guard), `SuspendClose`, `SuspendAware`, `SuspendableStream` per design D7; `lifecycle/running_sleep.rs`: `running_sleep(session, deadline)`; unit tests with the paused tokio clock (terminal close, status close, `on_suspend` before drop, counter, immediate close when already changed, early wake + re-sleep)
- [x] 4.6 Wrap every server-stream: `grpc/process.rs` (`EndEvent{suspending}` terminal), `grpc/pty.rs` (4.4), `grpc/filesystem.rs` (`WatchDir` and `Read` → `SuspendClose::Status`; `Write` handler `select!`s on the watch, aborts the session, drains ≤ 200 ms, `UNAVAILABLE suspending`), `grpc/code.rs` (`Execute` and `Reattach` → `SuspendClose::Status`); `SuspendAware` impls (`SubscriberStream`, `WatchStream`, `ReadStream` no-op; `ExecutionSubscriberStream` sets `detached`)
- [x] 4.7 Process and PTY timeout tasks and the registry reaper on the running clock: `TimeoutPlan` from `session.running_now()`, `running_sleep` for `term_at`/`kill_at`, `mark_ended(.., running_now)`, `reap_expired(running_now)`; `m2_process.rs` timeout cases still green
- [x] 4.8 `code/executions.rs`: `ExecuteSink`/`ExecuteReceiver` (256, `send_timeout` 30 s, per-subscriber `OutputTruncated` synthetic pair), `ExecutionRecorder` task (tracker → ring → fan-out, `running_sleep` timeout schedule, queued events drained before timers, `mark_ended`); `code/execute.rs`: `ExecutionSubscriberStream` replacing `ExecuteStream` (`origin`, `detached`, drop → `interrupt` only for a non-detached origin); `code/manager.rs`: `execute` registers + spawns the recorder + attaches the origin, `reattach(context_id, execution_id, from_seq)`, `detach_for_suspend`, `reap_expired`, `quiesce(timeout)`, `probe_after_resume(budget) -> ProbeOutcome` (latches `kernel_state_lost`, spawns `restart_context` for lost contexts), `kernel_state_lost()` (`impl KernelStatus`); `code/supervisor.rs`: `OpTimeouts.resume` 15 s, the `op_timeout_across_resume` rule (issued-at generation compared to the current one; logged, not counted); unit tests over `code/fake_sidecar.rs` per design D16 "rayd unit"
- [x] 4.9 `grpc/code.rs`: `Reattach` served (5 s keepalive + suspend wrapper), status additions (`ExecutionNotFound` → `NOT_FOUND`, `InvalidExecutionId` → `INVALID_ARGUMENT`, `ReplayOutOfRange` → `OUT_OF_RANGE`, `TooManySubscribers` → `RESOURCE_EXHAUSTED`); `grpc/health.rs`: `kernel_state_lost` from `KernelStatus`
- [x] 4.10 `hooks/mod.rs`: `/suspend` per design D8 (transition → `suspend_actions` → `detach_for_suspend` + `broadcast` → `wait_streams_closed(2 s)` → `quiesce(2 s)` → `sync` → 200 with `streams_closed`; `suspend_ms` logged; idempotent) and `/resume` per design D10 (transition → `probe_after_resume(12 s)` → `spawn_resume_reseed` → log `resume recorded` with `resume_generation`, `clock_offset_ms`, `suspended_ms`, `probe_ms`, `kernels_alive`, `kernels_lost`; `warn!` above 5000 ms → 200 with `kernel_state_lost`); `HookReply` optional fields (`skip_serializing_if`); `hooks::router` takes `Arc<SuspendSignal>`
- [x] 4.11 `main.rs`: shared registry, `PtyManager` with `NixPtyBackend`, `SuspendSignal` into both routers, `pty_devices` log, reaper covering executions; `logging.rs` allowlist per design D19; `crates/rayd/tests/fixtures/fake_sidecar.py`: `--resume-lost <ctx>` and `--resume-delay-ms <n>` flags, `quiesce`/`resume` requests logged
- [x] 4.12 Adapt `crates/rayd/tests/m1_hello.rs`, `m2_process.rs`, `m3_filesystem.rs`, `m4_code.rs` to the shared registry, `SuspendSignal` and router signatures; M1 `PtyService.Resize` probe → `NOT_FOUND`; M4 `Reattach` case → `NOT_FOUND` for an unknown id; M4 `big 2000` stall case → truncation at the subscriber with the sidecar never parked
- [x] 4.13 `crates/rayd/tests/m5_pty.rs` (`#![cfg(unix)]`): every case of design D16 "rayd integration" for PTYs (started first, `echo hola`, `stty size` before/after `Resize`, `tty`, `id -u`, env vars and override, `List` kind, cross-kind `FAILED_PRECONDITION` both ways, `SendSignal` on a PTY, 16 KiB chunks, `Connect(from_seq)` replay and `OUT_OF_RANGE`, second subscriber truncated alone, `Kill` → 137 then `NOT_FOUND`, `exit 3` retained, `timeout_ms` with SIGTERM first, size and shell validation, `/bin/false`, live cap, phase gate, keepalive at 200 ms, background child holding the slave)
- [x] 4.14 `crates/rayd/tests/m5_suspend_resume.rs` (`#![cfg(unix)]`): every case of design D16 "m5_suspend_resume" (eight streams closed with the right forms, `streams_closed == 8`, `quiesce` seen and no `interrupt`, `Write` temporary gone, processes alive, gate, idempotent `/suspend`, `/resume` generation and probe, `Connect`/`Pty.Connect`/`Reattach` replay, retention `NOT_FOUND`, stalled `Execute` vs `Reattach` subscriber, timeout re-arm for a process and an execution with a 2 s simulated pause, `--resume-lost` → restart + `kernel_state_lost` latch and clear, `--resume-delay-ms 13000` → 200 within 13 s with `kernel_state_lost`, op timeouts across resume not counted, `/suspend` before `/run` → `illegal`)
- [x] 4.15 `cargo test --workspace` green on Windows (unix suites compiled out) and inside the Docker `rust:1.98-slim-bookworm` loop as uid 1000 with `python3` (`m5_pty`, `m5_suspend_resume`, the adapted M1–M4 suites); `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings` clean on both
- [x] 4.16 `cargo zigbuild --release --target aarch64-unknown-linux-musl -p rayd` produces a statically linked `rayd` (record the size next to M4's 4 329 272 B)

## 5. [sdk] Python client

- [x] 5.1 `_models.py`: `PtySize` (validated) and `SandboxHealth`; `_pty_base.py`: constants, `validate_pty_size`, `validate_shell`, `build_pty_start_request`, `build_resize_request`, `build_kill_request`, `pid_from_pty_started`, `PtyMessages` adapter; `_process_base.py`: `StreamAdapter` protocol, `Consumed` (`Chunk`/`Nothing`/`Ended`/`Suspending`), `ProcessEvents`, `CommandProgress(pid, accumulator, adapter)` with `suspended`; `_code_base.py`: `ExecutionBuilder.context_id`/`execution_id`/`last_seq`/`reattached`, `build_reattach_request`; `_sandbox_base.py`: `DEFAULT_RECONNECT_TIMEOUT_SECONDS`, `CLOCK_OFFSET_WARN_MS`, `ReconnectPoll`, `ReconnectOutcome`, `reconnect_failure`, `health_from_proto`; `_transport.py`: `is_reconnectable`, the three reconnect-backoff `CHANNEL_OPTIONS`; unit tests `test_pty_base.py`, additions to `test_process_base.py`, `test_code_base.py`, `test_sandbox_base` cases, `test_transport.py`
- [x] 5.2 `sandbox_sync/main.py`: `pty` property → `Pty`; `_resume_generation`, `_reconnect_lock`, `_reconnect_timeout` (`reconnect_timeout` kwarg on `create()`/`connect()`), `_record_health`, `_reconnect(reason, seen_generation)` per design D14, `_is_reconnectable`, `_call_unary` and `_open_stream` reconnect + one retry, `get_health()`, `resume()` re-mint + `_record_health`, `close()` aborting a reconnect; the `NotImplementedError` stub for `pty` removed
- [x] 5.3 `sandbox_sync/commands.py`: `CommandHandle` with the adapter, `reconnects`, `_resubscribe(from_seq)` (`commands.connect`), the `Suspending`/reconnectable-error path in `_events()` with the `OUT_OF_RANGE` → `from_seq=0` fallback and warning, `NOT_FOUND` stored; `sandbox_sync/pty.py`: `Pty` (`create`, `connect`, `send_input` + `send_stdin` alias, `resize`, `kill`) and `PtyHandle(CommandHandle)` (`resize`, `send_input`, `kill` via `PtyService.Kill`, `on_data`, `(None, None, bytes)` iteration, `_resubscribe` via `pty.connect`) on the stream channel
- [x] 5.4 `sandbox_sync/filesystem.py`: `WatchHandle` re-issues `WatchDir` after a successful reconnect (same request, remaining deadline, `WatchStarted` awaited, `on_exit` not called); `sandbox_sync/code.py`: `_feed_events` reattaches after `started` (`Reattach` on the unary channel, remaining deadline, same builder), never before `started`; `OUT_OF_RANGE`/`NOT_FOUND` → `SandboxException`
- [x] 5.5 `sandbox_async/{main,commands,pty,filesystem,code}.py`: the same surface as coroutines (`AsyncPty`, `AsyncPtyHandle`, `asyncio.Lock`, `asyncio.sleep`, `to_thread` for boto3); `__init__.py` exports `PtySize`, `PtyHandle`, `SandboxHealth`; `IdlePolicy` docstring on the 300 s cost note
- [x] 5.6 `tests/unit/fake_pty.py` (`FakePtyService` per design D16: echo shell, `stty size`, `exit`, `Resize`, `Kill`, `Connect(from_seq)` ring, token check, `suspend()`); `fake_process.py` `suspend()` + `connect_calls`; `fake_filesystem.py` `WatchDir` `suspend()` + `watch_calls`; `fake_code.py` ring + `Reattach` + `slow <s>` + `suspend()`; `conftest.py` `FakeRayd.suspend_resume(unavailable_calls)` and `clock_offset_ms`/`kernel_state_lost` scripting; register the PTY servicer in `fake_rayd`
- [x] 5.7 `tests/unit/test_pty_sync.py`, `test_pty_async.py`, `test_reconnect_sync.py`, `test_reconnect_async.py` and the additions listed in design D16 "Python SDK unit" (PTY surface, reconnect schedule, handle/PTY/watch/run_code reconnection, unary retry, terminal-state and no-auto-resume stops, deadline, dedup, `resume()` re-mint + warning, `pause()` False, refresher alive across `pause()`, `get_health()`, channel options, `disconnect()` never reconnects, async parity)
- [x] 5.8 `uv run pytest tests/unit`, `uv run ruff check .`, `uv run ruff format --check .`, `uv run mypy src` all clean; `pyproject.toml` and `_version.py` bumped to `0.0.5`

## 6. [image] Dockerfile, hooks-sim and publish

- [x] 6.1 `image/Dockerfile`: M5 header; sanity list adds `tty`, `stty` and `test -c /dev/ptmx`; `CMD` unchanged; `scripts/publish_image.py` `IMAGE_HOOKS` unchanged (assert in a unit test that `suspend`/`resume` stay 30 s, the value `hooks::declared_timeout` derives its budget from)
- [x] 6.2 `scripts/hooks-sim.py`: `--only suspend,resume` opens a `Start` stream and a PTY, asserts the `suspending` ends, posts `/resume`, asserts the generation and the `Connect(from_seq)`/`Pty.Connect` replays, prints `streams_closed` and `kernel_state_lost`
- [x] 6.3 Local loop before publishing: `cargo run -p rayd -- --sidecar-root kernel-sidecar` under WSL2/Docker, `python scripts/hooks-sim.py --skip-terminate` (all six hooks incl. the new suspend/resume checks) and the SDK's `pty.create` + `echo hola` + `pause`-less reconnect drill (`fake`-free: kill the local `rayd` connection and watch the handle reconnect once `Health` answers) against `127.0.0.1:8080`
- [x] 6.4 `make image-zip` + `image-publish` → new `rayito-base` version `SUCCESSFUL` + `ACTIVE`; record zip size, build seconds and `snapshotBuild` sizes next to 8.0's (925 466 624 / 1 289 846 784 / 36 929 536 B, 195.9 s) here and in `MILESTONES.md`

## 7. [e2e] Green and close

- [x] 7.1 `RAYITO_E2E=1 RAYITO_TEMPLATE=<arn> make test-e2e`: `test_m1_hello.py`, `test_m2_processes.py`, `test_m3_filesystem.py`, `test_m4_code.py`, `test_m5_pty_suspend_resume.py` pass; paste the design D18 timings (`kernel_ready_s`, PTY create/echo, `pause_s`, `resume_s`, re-subscribe latency, kernel-alive cell, re-arm timeout latency, `Reattach` completion, `auto_resume_s`, idle → SUSPENDED delay, `clock_offset_ms`) and `suspend_ms`/`probe_ms`/`streams_closed` from the `rayd` log into `MILESTONES.md` M5 "Estado de aceptación"
- [x] 7.2 `AWS_API_NOTES.md` §16: rows Q37 (devpts, from 0.1), Q38 (client view of an explicit `pause()` with streams open), Q39 (resume → `Health` generation, `Connect(from_seq)`/`Pty.Connect`/`Reattach` after resume, kernel alive), Q40 (auto-resume through gRPC via the SDK), Q41 (timeout re-arm and `clock_offset_ms`); §15 clock paragraph points at the running-clock decision
- [x] 7.3 Docs: `ARCHITECTURE.md` (`PtyService` row, "Suspend / resume" section with the close-form table and the running clock, reconnection contract, domain/ports/adapters tables, Python layout) per design D20; `MILESTONES.md` M5 snippet aligned with the e2e; `SECURITY.md` T2 updated and new row T13 (PTY posture); `README.md` `pty`/`pause`/`resume` examples, `reconnect_timeout`, the 0.0.5 ↔ M5-image note
- [x] 7.4 `openspec validate m5-pty-suspend-resume --strict` passes; acceptance agent runs `openspec archive m5-pty-suspend-resume --yes`

### Notes (rayd + image implementer, 2026-09-16)

- Windows host (`x86_64-pc-windows-gnu`, zig as host cc): `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings` (pedantic) and `cargo test --workspace` green: **287** tests (51 + 4 `rayd`, 12 `m1_hello`, 220 `rayd-core`; the `cfg(unix)` suites compile out). `cargo-zigbuild clippy --workspace --all-targets --target aarch64-unknown-linux-musl -- -D warnings` clean, which lints the unix adapters (`pty_backend`, `sidecar_process`) and `m5_pty.rs` / `m5_suspend_resume.rs` for Linux. `buf lint` clean; no proto change beyond 1.1.
- Linux (`rust:1.98-slim-bookworm`, Docker Desktop, x86_64, as uid 1000 with `/bin/bash` as login shell and `python3` for the fake sidecar; cargo home, rustup and target in named volumes): `cargo fmt --all --check` and `cargo clippy --workspace --all-targets -- -D warnings` clean; `cargo test --workspace` **390** tests green (60 + 4 `rayd`, 12 `m1_hello`, 25 `m2_process`, 30 `m3_filesystem`, 19 + 1 ignored `m4_code`, **13 `m5_pty`** in 12.7 s, **7 `m5_suspend_resume`** in 12.1 s, 220 `rayd-core`); both M5 suites repeated three times in a row without a flake (the 13 s `--resume-delay-ms 13000` case and the 2 s simulated pauses run in parallel with the rest).
- `cargo zigbuild --release --target aarch64-unknown-linux-musl -p rayd` → **4 536 248 B** (M4: 4 329 272 B), `ELF 64-bit LSB executable, ARM aarch64, statically linked, stripped`.
- 6.3 local loop (Linux container, debug `rayd` as root + real sidecar from `kernel-sidecar/requirements.txt` in a uv venv with a managed CPython 3.12, `--sidecar-root /work/kernel-sidecar` — the root must be absolute because the sidecar's cwd is the user's home and `PYTHONPATH` derives from it): `hooks-sim.py --skip-terminate` → `/ready` 200 after 3 × 503 (`warmup_ms` 3 693), `/validate` `validated` after 4 × 503 (2 results), `/run` `installed` → `restart_ms` 3 624; gRPC drill: `Start sleep 60` + `Pty.Create` with `echo hola`, then `/suspend` 200 in **245 ms** (`streams_closed` 2, `streams_pending` 0, `sidecar quiesced`; `suspend_ms` is mostly `sync(2)` on the bind mount), both streams ended with status `suspending`; `/resume` 200 in **3 ms** (`probe_ms` 2, `kernels_alive` 1, `kernels_lost` 0, `kernel_state_lost` false, `reseeded` 1); `Health.resume_generation` 1; `Connect(pid, from_seq)` and `Pty.Connect` replayed and the re-attached PTY answered `echo back`; no python process left after `rayd` stopped. Observation worth a note for Q39/M6: posting `/suspend` inside the `/run` rotation window (the drill did so once, ≈ 450 ms after `/run`) makes the sidecar report the restarting default kernel as not alive (`probe` answers `false` for any context whose state is not `ready`), so `/resume` logged `kernels_lost` 1, tried `restart_context` (refused: "context is being restarted; retry", the in-flight restart completes on its own) and latched `kernel_state_lost` true until the next `/resume`; the SDK never hits this because `create()` returns only when `kernel_ready` is true, and the drill now waits for `kernel_ready` the same way (`scripts/hooks-sim.py` `_wait_kernel_ready`).
- 6.3 SDK drill (same container, fake-free: `Sandbox.connect` over `TransportSettings(local_channel_credentials, port 8080)` with a stub control plane, `/run` posted by the script): `connect` + `kernel_ready` 3.78 s (x86 debug), `pty.create` 0.01 s, `echo hola` round trip < 10 ms in 4 `data` chunks, `stty size` → `30 100`, `id -u` → `1000`, `tty` → `/dev/pts/N`, `$TERM $LANG` → `xterm-256color C.UTF-8`, `commands.list()` shows the pid with `kind == "pty"`; with a background `for … echo tick … sleep 1` handle iterated in a thread, `POST /suspend` (`streams_closed` 2) + `POST /resume` (`kernel_state_lost` false) on the hooks port: the PTY handle reconnected lazily on its next read (`reconnects` 1) and answered `echo back-42` **0.40 s** after the resume, the background handle kept ticking (2 → 4, `reconnects` 1, no exception), `get_health().resume_generation` 1, `run_code("6*7")` → `42`, `pty.kill` True then False.
- 6.4 publish (test account, us-east-1, `--stack-name rayito-m0-iam`): artifact `rayd-e7fce213dd14.zip` **4 632 912 B** (8.0: 4 425 299 B) → `update-microvm-image` version **9.0** `UPDATED` + `SUCCESSFUL` + `ACTIVE` in **195.4 s** (8.0: 195.9 s); `snapshotBuild` memory **935 469 056 B** (8.0: 925 466 624), code install **1 290 379 264 B** (8.0: 1 289 846 784), disk **35 864 576 B** (8.0: 36 929 536); `chipsetGeneration` 4.
- Real-AWS probe on 9.0 through the 0.0.5 SDK (one MicroVM, `microvm-<id>`, killed; `timeout=900`, `IdlePolicy(600, auto_resume=True)`, CloudWatch logging): `run-microvm → kernel_ready` 8.17 s; **Q37 measured on the guest: `test -c /dev/ptmx && grep -c devpts /proc/mounts` → `1`, `python3 -c 'import pty, os; m, s = pty.openpty(); print(os.ttyname(s))'` → `/dev/pts/0`** (devpts is mounted; no ADR needed; task 0.1's question answered on 9.0 rather than 8.0, the e2e agent records it as Q37); `pty.create(100×30)` **0.35 s**, `echo hola` **0.14 s**, `stty size` `30 100`, `id -u` `1000`, `tty` `/dev/pts/0`, `$TERM $LANG` `xterm-256color C.UTF-8`, `resize(120×40)` → `40 120`; `pause()` → `SUSPENDED` in **0.88 s**; after 5 s `resume()` → `Health` with `resume_generation` 1 in **0.40 s**, `clock_offset_ms` 0, `kernel_state_lost` false (`uptime_ms` 28 358 → 35 628, i.e. suspended time included as documented); the PTY handle reconnected (`reconnects` 1) and answered `echo back-42` **0.18 s** after the resume; `run_code("6*7")` → `42` (kernel alive); `pty.kill` → exit 137; `kill()` True.
- M5 review fixes (2026-09-16, SDK): D14 amended — the reconnect poll is two-phase (dormant outside the lock without `Health` or `reconnect_timeout` while `get-microvm` says `SUSPENDING|SUSPENDED`; `Health` poll under the lock with a fresh budget once it leaves); `pause()` marks a pending pause that keeps in-flight foreground `run`/`run_code` dormant until `resume()`; `resume()`/a new generation wake every dormant handle at once; new unit tests in `test_reconnect_sync.py`/`test_reconnect_async.py` (foreground stop without auto-resume, handle sleeps through a suspension for both idle policies, resume wakes at once, dormant handle does not block a foreground call, explicit pause keeps `run`/`run_code` dormant, `rayito.code` logs the `Reattach`); e2e block 13 back to `pause(wait=True)` asserting `SUSPENDED` before `resume()`, the `Reattach` log record and the exact generation delta; block 6 asserts `SUSPENDED` after its 30 s sleep. Q40 rows are still the acceptance agent's.
- Left to the e2e/acceptance agent: 0.1's row (Q37, values above), 2.3, 7.1–7.4 (`make test-e2e` against 9.0, Q38–Q41, docs, `openspec archive`).

### Notes (acceptance agent, 2026-09-16)

- 0.1 / 2.3: superseded like M4 2.3. The `rayd` review fixes landed after 9.0 (`code/{execute,executions,manager}.rs`, `adapters/pty_backend.rs`, `tests/m5_pty.rs`), so the acceptance rebuilt (`cargo zigbuild` → `rayd` **4 533 432 B**, `ELF 64-bit LSB executable, ARM aarch64, statically linked, stripped`) and published `rayito-base` **10.0** (`rayd-fc0df3c41a28.zip`, 4 630 096 B; `update-microvm-image` → `UPDATED` + `SUCCESSFUL` + `ACTIVE` in **215.8 s**; `snapshotBuild` memory **919 146 496 B**, code install 1 290 379 264 B, disk 37 462 016 B; `chipsetGeneration` 3; hooks `port` 9000, six hooks `ENABLED`). Q37 measured on 10.0 by the e2e itself (`devpts mounts = 1`, `/dev/pts/0`).
- Host before the e2e: Windows `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace` **288** tests (52 + 4 `rayd`, 12 `m1_hello`, 220 `rayd-core`); Linux (`rust:1.98-slim-bookworm` as uid 1000 with `python3`, named volumes) `cargo test --workspace` **393** tests (61 + 4 `rayd`, 12 `m1_hello`, 25 `m2_process`, 30 `m3_filesystem`, 19 + 1 ignored `m4_code`, **15 `m5_pty`**, **7 `m5_suspend_resume`**, 220 `rayd-core`); `clients/python` `pytest tests/unit` **429**, `ruff check`, `ruff format --check`, `mypy src` clean.
- 7.1: `uv run pytest tests/e2e -v -s -m e2e` against 10.0 → **6 passed in 333.18 s** (M1–M5 in one session, seven MicroVMs) after two red passes that changed only tests and one SDK rule:
  - Pass 1 hung in block 4: `read_until(pty, b"

hola

")` never matched because AL2023's `bash` 5.2.15 with `-i -l` enables readline's bracketed paste and the terminal sends `echo hola

[?2004l
hola

]0;user@:~[?2004h[user@localhost ~]$ ` (probed on a fresh VM), and the iterator blocked forever on `next()` (no deadline on a `timeout=None` handle). Fix in `test_m5_pty_suspend_resume.py`: `output_line()` is now the regex `[

]<text>

`, `read_until`/`async_read_until` read each message with a bounded wait (daemon thread + queue / `asyncio.wait_for`). `rayd` and the SDK were right.
  - Pass 1 also failed `test_m3_filesystem.py` `check_watch_recursive`: `mkdir sub && echo x > sub/n.txt` delivered `("sub", CREATE)` and `("sub/n.txt", WRITE)` but no `CREATE` for the file (2/2 on 10.0, passed 2/2 in M3/M4): inotify installs the watch on the new subdirectory when `notify` sees its `CREATE`, so a file created in that gap at 1 vCPU only reports its later `MODIFY`. The test now waits for `("sub", CREATE)` before creating the file; `rayd` unchanged.
  - Pass 2 failed block 6: the second `pause()` returned `True`. Measured with a dedicated probe: `suspend-microvm` on a `SUSPENDED` VM answers **HTTP 200** in 0.15–0.17 s (three calls), `resume-microvm` on a `RUNNING` VM 200 too (generation unchanged); `ConflictException` never fires. D14 amended (`design.md`, delta spec): `pause()` reads `get-microvm` first and returns `False` on `SUSPENDING|SUSPENDED` without calling the API (`_sandbox_base.already_suspended`, sync + async + classmethod); unit tests in `test_sandbox_{sync,async}.py` and `test_reconnect_{sync,async}.py` stub the extra `get_microvm` (the explicit-pause tests start `RUNNING` and flip the fake state after `pause()`).
  - Green M5 numbers (passes 3 and 4) are in `MILESTONES.md` M5 "Estado de aceptación" and `AWS_API_NOTES.md` Q37–Q41: `kernel_ready_s` 3.96 / 9.46, PTY create 0.38, echo 0.47 / 0.09, `pause_s` 1.49, `resume_s` 0.37 / 0.73, re-subscribe 0.09, kernel-alive 0.10, re-arm 22.56 / 22.33, `Reattach` 15.96 / 15.95, watch 0.20 / 0.00, `auto_resume_s` 0.67, idle → `SUSPENDED` 66.71 / 61.65, `clock_offset_ms` 0; `rayd`: `suspend_ms` 10–11 / 7 / 1 with `streams_closed` 3 / 1 / 0, `probe_ms` 1–3.
  - Observation for M6: the reseed `/resume` queues behind the in-flight 25 s cell hits `OpTimeouts` 15 s (`sidecar op timed out`, `consecutive_timeouts` 1, `reseed failed`) and the sidecar still executes it when the cell ends (`reseeded 1`); three such resumes in a row would trip the sidecar kill-switch.
- 7.2: rows Q37–Q41 added, §5 gained the measured idempotence of `suspend-microvm`/`resume-microvm`, §15's clock paragraph points at the running-clock decision.
- 7.3: `ARCHITECTURE.md` (`ProcessService`/`PtyService`/`CodeService` rows, "Reloj de ejecución", both checklists with the close-form table, the reconnection contract incl. who may wake a VM, domain/ports/adapters rows, Python layout and error table), `SECURITY.md` (T2 updated, T13 added), `README.md` (state, `pty`/`pause`/`resume` example, `reconnect_timeout`, 0.0.5 ↔ 10.0 note) aligned; `MILESTONES.md` M5 status written.
- 7.4: `openspec validate m5-pty-suspend-resume --strict` valid; archived with `openspec archive m5-pty-suspend-resume --yes`.
- Cost: 1 image version + 26 MicroVMs of 10.0 (four e2e passes, one extra `test_auto_resume` with `log_cli`, three probes), all `TERMINATED`; a paginated `list-microvms` shows none live at the end.

