## 0. [prepare] Tools, pins and the sibling's state

- [x] 0.1 Confirm `m7-oss-hygiene`'s edits to `.github/workflows/ci.yml` (`check_license.py` step), `SECURITY.md` (reporting section), `README.md` (badges), `MILESTONES.md` (M7 section), `docs/RELEASING.md` and `crates/rayd/CHANGELOG.md` are on disk; if any is missing, create only `crates/rayd/CHANGELOG.md` (Keep a Changelog 1.1.0 header, `[Unreleased]`) and note the rest as "extended when the sibling lands" (design D14)
- [x] 0.2 Install the local tools outside `C:` and record versions in the task note: `actionlint` 1.7.12 and `cargo-deny` 0.20.2 on the `PATH` (release zips, checksum compared with the `.sha256` asset), `cargo install --locked cargo-auditable@0.7.6 cargo-cyclonedx@0.5.9`, `uvx pip-audit==2.10.1 --version`, `uvx cfn-lint==1.56.3 --version`
- [x] 0.3 Re-resolve every action SHA with the recipe of design D1 (`gh api`) and write the table (action, tag, commit) into the task note; use those values in every `uses:` below

## 1. [ci] Actions hardening and Scorecard

- [x] 1.1 `.github/workflows/ci.yml`: top-level `permissions: contents: read`; every `uses:` pinned `owner/repo@<sha> # vX.Y.Z` (no tag or branch left); `persist-credentials: false` on every checkout; `step-security/harden-runner` (`egress-policy: audit`) as the first step of every job; `--locked` on `cargo clippy`, `cargo test` and `cargo zigbuild`; the sibling's `check_license.py` step kept verbatim
- [x] 1.2 `ci.yml` `check` job: step "actionlint" that downloads the 1.7.12 release binary for linux_amd64, verifies its checksum, and runs `./actionlint -no-color`; step "no unpinned actions" that fails on `grep -nE 'uses: [^@]+@(v[0-9]|main|master|release/)' .github/workflows/*.yml`
- [x] 1.3 `.github/workflows/release.yml`: same pinning, `persist-credentials: false`, harden-runner on every job (the rewrite of section 7 builds on this)
- [x] 1.4 `.github/workflows/scorecard.yml` per design D3 (triggers, permissions, `results_file`/`results_format: sarif`/`publish_results: true`, artifact `SARIF file` 5 days, `github/codeql-action/upload-sarif`)
- [x] 1.5 `Makefile` `lint`: run `actionlint -no-color` when the binary is on `PATH`, otherwise print the install hint and continue; `README.md` badge for Scorecard next to the sibling's badges

## 2. [audit] cargo-deny, pip-audit, pnpm audit

- [x] 2.1 `deny.toml` at the repository root exactly as in design D4; `cargo deny check` → `advisories ok, bans ok, licenses ok, sources ok` (paste the summary and the duplicate-version warnings in the task note)
- [x] 2.2 `ci.yml` job `deny`: harden-runner, checkout, `EmbarkStudios/cargo-deny-action` (`command: check`, `rust-version: "1.98"`); `Makefile` `lint` runs `cargo deny check` when `cargo-deny` is on `PATH`
- [x] 2.3 `ci.yml` job `audit` per design D5: `uvx pip-audit==2.10.1 -r kernel-sidecar/requirements.txt --no-deps --strict`; `uv export --frozen --no-dev --no-emit-project` of `clients/python` and `kernel-sidecar` audited the same way; `cd clients/typescript && pnpm install --frozen-lockfile && pnpm audit --prod --audit-level moderate`
- [x] 2.4 `.github/workflows/audit.yml`: `schedule: '41 6 * * 1'` + `workflow_dispatch`, `permissions: contents: read`, jobs `rust` (cargo-deny-action `command: check advisories`), `python` (the two pip-audit runs), `node` (`pnpm audit --prod --audit-level moderate` and `pnpm audit --audit-level high`)
- [x] 2.5 `clients/typescript`: bump `vitest` to the latest 4.x (`pnpm add -D vitest@^4`), `pnpm install --frozen-lockfile` after the lockfile update, `pnpm lint && pnpm typecheck && pnpm test && pnpm build && pnpm pack:check`; if the 292 tests do not pass unchanged, revert to 3.2.7 and record the failing tests in the task note (design D5)
- [x] 2.6 Local: `pip-audit` over the three requirement sets and `pnpm audit --prod` green; outputs in the task note

## 3. [sbom] cargo auditable and CycloneDX

- [x] 3.1 `scripts/check_auditable.py <elf>`: standard-library ELF64 section-header parser, decompresses `.dep-v0` (zlib), asserts root package `rayd` with the version of the root `Cargo.toml` and the presence of `tonic`, `tokio`, `axum`, `nix`; prints the package count; exit 1 with the missing item otherwise; unit test in `scripts/tests/test_check_auditable.py` on a tiny synthetic ELF built in the test (no binary fixture committed); `ruff` clean
- [x] 3.2 `Makefile`: `build` uses `cargo auditable zigbuild --release --locked --target $(TARGET) -p rayd` when `cargo-auditable` is on `PATH` (else plain `cargo zigbuild`, printing which) and then `python scripts/check_auditable.py $(RAYD_BIN)` when the auditable path was used; new target `sbom` running `cargo cyclonedx --manifest-path crates/rayd/Cargo.toml --target $(TARGET) --format json --no-build-deps --spec-version 1.5` and printing `crates/rayd/rayd.cdx.json`; `.gitignore` gains `crates/rayd/rayd.cdx.json`
- [x] 3.3 `ci.yml` `build` job: `cargo install --locked cargo-zigbuild@0.23.4 cargo-auditable@0.7.6 cargo-cyclonedx@0.5.9`, the auditable build, `check_auditable.py`, the cyclonedx command, and `crates/rayd/rayd.cdx.json` added to the `rayd-aarch64-musl` artifact paths
- [x] 3.4 Local: `make build` equivalent by hand → `check_auditable.py` prints the count (145 today) and root `rayd 0.1.0`; `make sbom` equivalent → `rayd.cdx.json` with `specVersion` `1.5` and metadata component `rayd`; `python scripts/image_zip.py image image/rayito-image.zip` listing unchanged (no `.json` at the root)

## 4. [arm] ubuntu-24.04-arm job

- [x] 4.1 `ci.yml` job `arm` per design D6: `runs-on: ubuntu-24.04-arm`, harden-runner, checkout, `rustup show active-toolchain`, `Swatinem/rust-cache`, `cargo test --workspace --locked`, `astral-sh/setup-uv` (3.12), sidecar host tests and `-m kernel` tests with `--with-requirements requirements.txt`
- [ ] 4.2 Record in the task note which Rust suites skip on the runner user (non-root) and confirm the sidecar kernel tests installed the `manylinux_2_28_aarch64` wheels (`uv` resolution log); if harden-runner fails to start on ARM, remove it from that job only and say so (design D2)

## 5. [e2e] e2e workflow and the OIDC role template

- [x] 5.1 `infra/ci-oidc-role.yaml` per design D8b (parameters, conditional `AWS::IAM::OIDCProvider`, role trust with `aud` and exact `sub`, inline policy with only the listed actions, conditional `iam:PassRole`, output `RoleArn`); `uvx cfn-lint==1.56.3 infra/ci-oidc-role.yaml` clean and `aws cloudformation validate-template` OK (not deployed)
- [x] 5.2 `infra/README.md`: table row, deploy command with `CAPABILITY_NAMED_IAM`, the three repository variables, the `e2e` environment with required reviewers, the AWS Budget step; `Makefile` `infra-lint` lints both templates
- [x] 5.3 `.github/workflows/e2e.yml` per design D8: `workflow_dispatch` inputs (`suite`, `template_version`, `expression`), nightly `17 5 * * *`, `concurrency: e2e`, `permissions: contents: read`, jobs `python` (75 min) and `typescript` (30 min, `both` only) in environment `e2e` with `id-token: write`, `aws-actions/configure-aws-credentials` with `role-to-assume`/`role-session-name`/`role-duration-seconds`, the CLI pre-flight (`> 10` → exit 1), the suites, the `if: always()` sweeper; Spanish header with the cost note and the manual steps
- [x] 5.4 `MILESTONES.md`: the M1 CI paragraph's `e2e.yml` sentence gets "(M7: sin `make image-publish`, ver `m7-supply-chain` D8)"; `actionlint` clean on `e2e.yml`

## 6. [release-please] Manifest mode with linked versions

- [x] 6.1 `release-please-config.json` and `.release-please-manifest.json` exactly as in design D9; `uvx check-jsonschema --schemafile https://raw.githubusercontent.com/googleapis/release-please/main/schemas/config.json release-please-config.json` passes; `python3 -m json.tool` on both
- [x] 6.2 `clients/python/src/rayito/_version.py`: `__version__ = "0.1.0"  # x-release-please-version`; `clients/typescript/src/version.ts`: `export const VERSION = "0.0.5"; // x-release-please-version`; `uv run pytest tests/unit/test_packaging.py` and `pnpm test -- version` still green; `grep -c x-release-please-version` is 1 in each file
- [x] 6.3 Scratchpad script (design D13): root `Cargo.toml` has one `workspace.package.version`, `Cargo.lock` has exactly one `[[package]]` for each of `rayd`, `rayd-core`, `rayito-proto`; output in the task note
- [x] 6.4 `.github/workflows/release-please.yml`: `on: push: branches: [main]`, `permissions: contents: read` top-level, job with `contents: write` + `pull-requests: write`, harden-runner, `googleapis/release-please-action` with `config-file`, `manifest-file`, `token: ${{ secrets.RELEASE_PLEASE_TOKEN || github.token }}`; `actionlint` clean

## 7. [release] release.yml for the three components

- [x] 7.1 Rewrite `.github/workflows/release.yml` per design D10: triggers (`python-v*`, `typescript-v*`, `rayd-v*`, `workflow_dispatch` with `tag` and `dry_run`), job `resolve` (component, version, publish) with a shell `case` that fails on any other tag shape, Spanish header with the manual steps and the npm-CLI exception
- [x] 7.2 Job `python`: the existing build/check/tag-parity steps, artifact upload, `pypa/gh-action-pypi-publish@<sha>` in environment `pypi` with `id-token: write`, skipped unless `publish`
- [x] 7.3 Job `typescript`: `pnpm/action-setup` 9.15.4, `actions/setup-node` 24 with `registry-url`, npm ≥ 11.5.1 assertion (`sort -V`), tag-equals-`package.json` check, `pnpm install --frozen-lockfile && pnpm build && pnpm pack:check && pnpm pack`, artifact upload, `npm publish rayito-<version>.tgz --access public` in environment `npm` with `id-token: write`, skipped unless `publish`
- [x] 7.4 Job `rayd`: setup-zig 0.16.0, rust-cache, the three `cargo install --locked`, tag-equals-root-`Cargo.toml` check, `cargo auditable zigbuild`, `check_auditable.py`, `cargo cyclonedx`, `copy_sidecar.py` + `image_zip.py`, `sigstore/cosign-installer`, `cosign sign-blob --yes --bundle` for `rayd` and `rayito-image.zip`, the two `cosign verify-blob` self-checks with the identity regexp of D10, `SHA256SUMS`, artifact upload, `gh release upload … --clobber` with `contents: write` skipped unless `publish`
- [x] 7.5 `docs/site/docs/verify.md` (the user recipe of D10), `docs/site/mkdocs.yml` nav entry, `README.md` "Verificar una release" snippet; `mkdocs build --strict` green
- [x] 7.6 `docs/RELEASING.md`: automated flow paragraph (release PR → merge → tags → `release.yml`), npm trusted publisher workflow filename corrected to `release.yml`, `RELEASE_PLEASE_TOKEN` and the `workflow_dispatch` fallback with `tag`; `actionlint` clean on `release.yml`

## 8. [image] Base image digest and explicit version

- [x] 8.1 `image/Dockerfile`: `FROM public.ecr.aws/lambda/microvms:al2023-minimal@sha256:05cb9b38d841e7ff1b693dc9e894909612f340bf99ec97d426e8000a5bbe96c3`; the comment above it rewritten per design D11 (how recorded, date, arm64 manifest `sha256:63831a97…`, refresh recipe with and without Docker)
- [x] 8.2 `scripts/publish_image.py`: `--base-image-version` required (`required=True`), `PublishSettings.base_image_version: str`, `configuration["baseImageVersion"]` always set; `scripts/tests/test_publish_image.py`: parser refuses a call without the flag (SystemExit 2), the request carries `baseImageVersion`, existing tests pass the flag; `uvx ruff check scripts` and `cd clients/python && uv run pytest ../../scripts/tests -p no:cacheprovider` green
- [x] 8.3 `Makefile`: `BASE_IMAGE_VERSION ?= 1`; `image-publish`, `image-publish-slim`, `image-publish-caps` pass `--base-image-version $(BASE_IMAGE_VERSION)`; header comment says where the value comes from (`list-managed-microvm-image-versions`) and the Q52 rule
- [x] 8.4 [AWS, ≈ $0.04] Publish `rayito-base` from the digest-pinned `Dockerfile` with `--base-image-version 1`: if `update-microvm-image` answers `ValidationException`, retry with `1.0`, set the Makefile default to the accepted spelling; record in `AWS_API_NOTES.md` §16 **Q52** the accepted spelling, the echoed `baseImageVersion`, the version number, build time and `snapshotBuild` sizes next to 16.0's, and that the digest-pinned `FROM` built `SUCCESSFUL` (or the `63831a97…` fallback of design Risks if it did not); run `RAYITO_E2E=1 RAYITO_TEMPLATE=<arn> RAYITO_TEMPLATE_VERSION=<new> uv run pytest tests/e2e -m e2e` green

## 9. [docs] SECURITY.md and bookkeeping

- [x] 9.1 `SECURITY.md` T10 mitigation and hito columns per design D12; "Cadena de suministro" table: `CI` row rewritten, rows `Dependencias`, `SBOM y firma`, `Publicación` added, `Imagen base` row with the digest and the refresh recipe; `docs/site/docs/security.md` mirrors the digest refresh note
- [x] 9.2 `MILESTONES.md` M7 section: this change's row (scope, acceptance, size M) with the measured numbers filled at acceptance (binary size delta, SBOM component count, e2e run cost from Cost Explorer)
- [x] 9.3 `grep -rn "pinneadas por versión mayor\|se fija cuando exista" SECURITY.md image/Dockerfile MILESTONES.md` returns nothing

## 10. [gates] Everything local, in one pass

- [x] 10.1 `actionlint -no-color .github/workflows/*.yml` exit 0; the "no unpinned actions" grep returns nothing; `cargo deny check` four `ok`; `pip-audit` ×3 clean; `pnpm audit --prod` clean; `uvx cfn-lint==1.56.3 infra/*.yaml` clean; `check-jsonschema` on the release-please config passes
- [x] 10.2 `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace`, `cargo auditable zigbuild --release --target aarch64-unknown-linux-musl -p rayd` + `check_auditable.py`; `cd clients/python && uv run pytest tests/unit && uv run ruff check . && uv run ruff format --check . && uv run mypy src tests`; `cd clients/typescript && pnpm lint && pnpm typecheck && pnpm test && pnpm build && pnpm pack:check`; `uvx ruff check scripts`; scripts tests; `mkdocs build --strict`
- [x] 10.3 `openspec validate m7-supply-chain --strict --no-interactive` passes with every task above ticked and the notes appended

## 11. [acceptance] On GitHub and AWS (acceptance agent)

- [ ] 11.1 Migration Plan steps 1–3 done by the maintainer (repository, branch protection, role deployed, environments, variables, trusted publishers, budget); `ci.yml` green on a PR with `deny`, `audit`, `arm` and `actionlint`; `scorecard.yml` ran on `main`
- [ ] 11.2 [AWS, ≈ $0.03] `e2e.yml` dispatched: role assumed (`rayito-e2e-<run_id>` in the log), pre-flight passed, Python suite green, zero live MicroVMs afterwards; Cost Explorer figure for the run recorded in the M7 row the next day
- [ ] 11.3 release-please PR merged: three manifests, `_version.py`, `version.ts`, root `Cargo.toml` and the three `Cargo.lock` entries at the same version, CI green with `--locked`; `release.yml` ran on the three tags; PyPI attestations present, `npm view rayito@<v> dist.attestations` lists provenance, GitHub release `rayd-v<v>` has the six assets and `cosign verify-blob` passes from a clean machine with the documented identity regexp
- [x] 11.4 `openspec archive m7-supply-chain --yes`
  - Acceptance 2026-09-17: archived by the M7 acceptance agent with the local half green on the 0.2.0 tree (`actionlint`, `cargo deny check`, `pip-audit` ×4 over the sidecar pins, the poly pins and both `uv export`s, `pnpm audit`, `cfn-lint` 1.56.3 over `spike/m0/iam.yaml` + `infra/*.yaml`, `cargo auditable zigbuild` → `.dep-v0` 256 crates root `rayd 0.2.0`, SBOM CycloneDX 1.5 regenerated with 217 components, release-please manifest bumped to 0.2.0 for the three components, `rayito-base` 20.0 / `rayito-base-caps` 8.0 / `rayito-base-poly` 4.0 published from the digest-pinned `Dockerfile` with `--base-image-version 1` and the full e2e green on them). **4.2, 11.1, 11.2 and 11.3 stay open on purpose**: they need the repository on GitHub (ARM runner, branch protection, OIDC role deployment, release-please PR, `cosign verify-blob` from a clean machine, Cost Explorer of the CI run); tracked in `MILESTONES.md` M7 row 2 and `docs/RELEASE_NOTES_0.2.0.md` "Diferido".

## Task notes (implementation, 2026-09-16, Windows host, tools on the `PATH`)

- **0.1** Sibling `m7-oss-hygiene` edits confirmed on disk before starting: `ci.yml` `check_license.py` step (kept verbatim), `SECURITY.md` reporting section, `README.md` badges, `MILESTONES.md` M7 table, `docs/RELEASING.md`, `crates/rayd/CHANGELOG.md`, `.github/dependabot.yml`.
- **0.2** Tools: `actionlint` 1.7.12 (release asset `actionlint_1.7.12_linux_amd64.tar.gz` sha256 `8aca8db96f1b94770f1b0d72b6dddcb1ebb8123cb3712530b08cc387b349a3d8`, the value pinned in `ci.yml`), `cargo-deny` 0.20.2, `cargo-auditable` v0.7.6 and `cargo-cyclonedx` 0.5.9 (`cargo install --locked`), `cargo-zigbuild` 0.23.4, zig 0.16.0, `uvx pip-audit==2.10.1` → `pip-audit 2.10.1`, `uvx cfn-lint==1.56.3` → `cfn-lint 1.56.3`, `uvx check-jsonschema`, shellcheck 0.11.0 via `uvx --from shellcheck-py` (fed to `actionlint -shellcheck=`), `gh` 2.92.0, Docker 29.1.3, AWS CLI 2.36.23.
- **0.3** SHAs re-resolved with the D1 recipe (`gh api`, annotated tags dereferenced): all 17 identical to the design table (`actions/checkout` v7.0.1 `3d3c42e5…`, `bufbuild/buf-action` v1.5.0 `8c6a16e1…`, `Swatinem/rust-cache` v2.9.2 `6323deb1…`, `astral-sh/setup-uv` v10.1.0 `bec219d2…`, `pnpm/action-setup` v6.1.0 `ea17c68d…`, `actions/setup-node` v7.0.0 `82076278…`, `mlugg/setup-zig` v2.2.1 `d1434d08…`, `actions/upload-artifact` v7.0.1 `043fb46d…`, `actions/download-artifact` v8.0.1 `3e5f45b2…` (not used: no job downloads an artifact), `pypa/gh-action-pypi-publish` v1.14.2 `dc37677b…`, `step-security/harden-runner` v2.21.1 `e14015d5…`, `ossf/scorecard-action` v2.4.4 `2d114668…`, `github/codeql-action` v4.38.0 `b96794f0…` (`gh release view` returns the `codeql-bundle-*` release for this repo; the `v4.38.0` tag was resolved explicitly), `EmbarkStudios/cargo-deny-action` v2.1.1 `3c634983…`, `aws-actions/configure-aws-credentials` v6.3.0 `e1253824…`, `googleapis/release-please-action` v5.0.0 `45996ed1…`, `sigstore/cosign-installer` v4.1.2 `6f9f1778…` with `cosign-release: v3.1.3`).
- **1.x** `actionlint -no-color` (with shellcheck) over the five workflows: exit 0; `grep -nE 'uses: [^@]+@(v[0-9]|main|master|release/)' .github/workflows/*.yml`: no match. `ci.yml` jobs: `check` (+ actionlint step, no-unpinned step, `--locked`), `typescript`, `sidecar`, `deny`, `audit`, `arm`, `build` (auditable + SBOM), `docs`; every job starts with harden-runner (audit) and every checkout has `persist-credentials: false`. `scorecard.yml` per D3; README carries the Scorecard badge.
- **2.1** `cargo deny check` (cargo-deny 0.20.2, `deny.toml` of D4): `advisories ok, bans ok, licenses ok, sources ok`; warnings: `found 2 duplicate entries` for `base64`, `hashbrown`, `logos`, `logos-codegen`, `logos-derive`, `syn`, `windows-sys` (the seven known ones).
- **2.5** `pnpm add -D vitest@^4` → vitest 3.2.7 → **4.1.11** (`package.json` `^4.1.11`, `pnpm-lock.yaml` updated, `pnpm install --frozen-lockfile` clean); `pnpm lint`, `pnpm typecheck`, `pnpm test` (**20 files, 292 tests passed unchanged**), `pnpm build`, `pnpm pack:check` green. Kept.
- **2.6** `uvx pip-audit==2.10.1 -r kernel-sidecar/requirements.txt --no-deps --strict` → `No known vulnerabilities found`; `uv export --frozen --no-dev --no-emit-project` of `clients/python` (101 lines) → `No known vulnerabilities found`; the same export of `kernel-sidecar` (header only: the sidecar declares no runtime dependencies, its pins live in `requirements.txt`) → `No known vulnerabilities found`. `pnpm audit --prod` → `No known vulnerabilities found`; `pnpm audit` (all scopes) → `No known vulnerabilities found` after the vitest bump (was 2 moderate, GHSA-82fw-gwwq-j7x9).
- **3.x** `scripts/check_auditable.py` (stdlib ELF64 parser, 8 unit tests on a synthetic ELF); `cargo auditable zigbuild --release --locked --target aarch64-unknown-linux-musl -p rayd` → 4 700 984 B (plain zigbuild 4 698 744 B, +2 240 B); `check_auditable.py` → `.dep-v0: 145 packages, root rayd 0.1.0`. `cargo cyclonedx … --spec-version 1.5` → `crates/rayd/rayd.cdx.json` (`specVersion` 1.5, metadata component `rayd 0.1.0`, **113 components**); the tool also writes `rayd-core.cdx.json` and `rayito-proto.cdx.json` next to their manifests (no package filter exists), so `.gitignore` carries `/crates/*/*.cdx.json`. `image_zip.py` listing unchanged: root entries `Dockerfile`, `rayd`, 30 files, no JSON. Note: another agent running plain `cargo zigbuild` in the shared `target/` re-links `release/rayd` to the plain artefact, so the release recipe always rebuilds with `cargo auditable` right before zipping (as `make build` does).
- **4.2** Rust suites that self-skip when not root (`running_as_root()` guards): `m2_process` (root refusal / setuid cases), `m3_filesystem` (fsuid cases), `m4_code` (`allow_root`), `m5_pty`, `m5_suspend_resume`; the same self-skips as on the x86 `check` job. The aarch64 wheel resolution log and whether harden-runner starts on `ubuntu-24.04-arm` can only be read on the first GitHub run (acceptance 11.1); harden-runner is kept in the job per D2.
- **5.x** `uvx cfn-lint==1.56.3 infra/ci-oidc-role.yaml` → clean (also `infra/egress-connector.yaml`); `aws cloudformation validate-template` → OK (6 parameters). Not deployed. `e2e.yml`: `actionlint` clean; the `typescript` job runs only from `workflow_dispatch` with `suite` `typescript`/`both`, after `python` when both (`!cancelled() && needs.python.result != 'failure'`).
- **6.x** `uvx check-jsonschema --schemafile https://raw.githubusercontent.com/googleapis/release-please/main/schemas/config.json release-please-config.json` → `ok -- validation done`; `python -m json.tool` on both files OK. Scratchpad check: `Cargo.toml` has one `workspace.package.version` (0.1.0); `Cargo.lock` has exactly one `[[package]]` for each of `rayd`, `rayd-core`, `rayito-proto` (0.1.0). `grep -c x-release-please-version` → 1 in `_version.py` and in `version.ts`; `test_packaging.py` (65 passed) and `pnpm test -- version` (1 passed) green.
- **7.x** `release.yml`: `resolve` job refuses an empty `tag` on dispatch, any other tag shape, and a non-dry-run dispatch whose run does not start from the tag ref (the cosign certificate identity carries the run ref, so the documented `refs/tags/rayd-v` regexp only holds for tag runs; dry runs from a branch verify with `release\.yml@refs/`). `mkdocs build --strict` green with `verify.md` in the nav.
- **8.x** Digest re-verified 2026-09-16 with `docker buildx imagetools inspect` (manifest list `05cb9b38…`, single `linux/arm64` manifest `63831a97…`) and with the registry API (`Docker-Content-Digest: sha256:05cb9b38…`). `publish_image.py` without `--base-image-version` → argparse exit 2 (usage line, no AWS call); scripts tests 71 passed; `uvx ruff check scripts` clean.
- **8.4** [AWS] `update-microvm-image` with `baseImageVersion: "1"` accepted → `rayito-base` **17.0**, build `SUCCESSFUL` in **205.7 s** from the digest-pinned `FROM` (no fallback needed), `get-microvm-image-version` echoes `baseImageVersion: "1.0"` (Makefile default stays `1`); `snapshotBuild` 928 100 352 / 1 305 825 280 / 37 998 592 B vs 16.0 931 258 368 / 1 299 968 000 / 36 933 632 B. Recorded as `AWS_API_NOTES.md` Q52. E2E on 17.0: 13 passed, 2 skipped (`test_imds_block`, `test_egress_allowlist`: need the caps image + execution role and the egress connector), 1 deselected (bench), 536.9 s; `list-microvms` shows zero live MicroVMs afterwards.
- **8.5 (review fix)** `list-microvm-image-versions` items echo `baseImageVersion: "1.0"` for a version created with `1` (re-read 2026-09-16 on `rayito-base` 17.0/16.0/15.0), so the literal per-key comparison in `configuration_matches` never matched and the version-reuse path of `publish_image.py` was dead. Fix: `base_image_version_matches` compares that key numerically (`Decimal`, exact match for non-numeric spellings), every other key stays literal; tests `test_configuration_matches_reuses_a_version_echoing_normalised_base` and `test_base_image_version_matches_only_numerically_equal_spellings` added (scripts tests 73 passed; ruff check/format clean; mypy strict clean on the script and its test). Proven read-only on AWS: `published_version(desired with base_image_version="1")` → `17.0`, with `"2"` → `None`, the old literal comparison → no match. Q52 records the echoed list spelling.
- **9.x** `grep -rn "pinneadas por versión mayor\|se fija cuando exista" SECURITY.md image/Dockerfile MILESTONES.md` → nothing.
- **10.x** Local gates: `actionlint` exit 0; no unpinned actions; `cargo deny check` four `ok`; `pip-audit` ×3 clean; `pnpm audit --prod` clean; `cfn-lint` clean on both templates; `check-jsonschema` OK; `cargo fmt --all --check` OK; `cargo clippy --workspace --all-targets --locked -- -D warnings` OK; `cargo test --workspace --locked` 331 passed; `cargo auditable zigbuild` + `check_auditable.py` OK; Python client: 617 unit tests passed, ruff check/format clean, mypy clean (81 files); TypeScript: lint, typecheck, 292 tests, build, pack:check green; `uvx ruff check scripts` clean; scripts tests 71 passed; `mkdocs build --strict` OK; `openspec validate m7-supply-chain --strict --no-interactive` → valid (`--all`: 18 passed). Not run locally: `make` (not installed; every Makefile recipe was run by hand), `act` (unavailable).
- Naming note: the design calls the settings dataclass `PublishSettings`; the existing class in `scripts/publish_image.py` is `Settings` and was kept (only the field type changed to `str`).
