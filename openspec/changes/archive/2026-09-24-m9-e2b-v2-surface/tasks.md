## 0. [infra] Pre-flight

- [x] 0.1 The five sibling M9 changes are implemented and green. Confirm the native surfaces listed in `design.md` D1 exist. The command below prints nothing new; only the grep hits matter. Record in "Notes" the real names each sibling shipped, and adapt only `rayito/e2b/_compat.py::info_from_native` and `src/e2b/compat.ts` if a name differs:
  ```
  grep -n "def set_timeout\|def connect\|def get_metrics_history\|def upload_url\|def download_url\|ALL_TRAFFIC\|cpu_count\|agent_version\|lifecycle\|network" clients/python/src/rayito/sandbox_sync/main.py clients/python/src/rayito/_models.py clients/python/src/rayito/__init__.py
  ```
- [x] 0.2 Confirm that `rayito/exceptions.py` holds `UnimplementedError(feature, reason, doc=None)`, `TransferException` and `FileUploadException(TransferException)` (from `m9-file-transfer`), and that TS `src/errors.ts` holds `UnimplementedError`, `DiskFullError`, `TransferError` and `FileUploadError`. Record in "Notes" whether `rayito.e2b.UnimplementedError` is a re-export or a subclass (D1).
- [x] 0.3 Run `openspec validate m9-e2b-v2-surface --strict --no-interactive`. It must pass before any code. Verificado (2026-09-23): valid tras el re-base de 10.3.

## 1. [rayd] [sidecar] No change

- [x] 1.1 [rayd] `git diff --stat crates/ proto/` stays empty for this change. `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings` and `cargo test --workspace --locked` stay green (D2).
  - verificado aquí (2026-09-23): este cambio no toca `crates/` ni `proto/`; `git diff --stat -- proto/` vacío, y lo único pendiente en `crates/` en el árbol de trabajo son arreglos de `m9-egress-policy` (3.9: tabla de rutas inexistente, `RAYITO_REQUIRE_EGRESS_NETNS`), el test `tar_archiver` que solo pasaba como root y la nota de `zlib-rs` en `crates/rayd/CHANGELOG.md` (hallazgo de `m9-file-transfer`), ninguno de este cambio. En la VM Lima aarch64: `cargo fmt --all --check` exit 0, `cargo clippy --workspace --all-targets --locked -- -D warnings` limpio y `cargo test --workspace --locked` verde con los binarios como uid 1500 (rayd-core 483, rayd lib 166, todas las suites de integración; detalle en `m9-egress-policy` 3.10).
- [x] 1.2 [sidecar] `git diff --stat kernel-sidecar/` stays empty for this change.
  - verificado aquí (2026-09-23): `git diff --stat -- kernel-sidecar/` vacío. Lo que cambió en `kernel-sidecar/` desde 0.2.0 (`git diff dde681c --stat`, 11 ficheros: kernels `rayito-javascript`/`rayito-typescript`, `languages.py`, `kernels.py`, `executions.py`, `protocol.py` y sus tests) es de `m9-deno-kernels`, no de este cambio.

- [x] 1.3 [rayd] D26: the atomic-write landing surfaces as one `WRITE` (with `entry` under `include_entry`), found by the corpus `watch` programs on `rayito-base` 23.0 (`FilesystemEvent(name='note.txt', type=RENAME, entry=None)`). Supersedes the "`crates/` untouched" line of 1.1 for `rayd-core/src/filesystem/events.rs`, `rayd/src/adapters/notify_watcher.rs`, `rayd/src/filesystem/watch.rs` (test helper) and `rayd/tests/m3_filesystem.rs`; MODIFIED `filesystem` requirement in `specs/filesystem/spec.md`.
  - verificado (2026-09-23), VM Lima aarch64: `cargo fmt --all --check` exit 0; `cargo clippy --workspace --all-targets --locked -- -D warnings` limpio; todos los binarios de test del workspace como uid 1500 en verde (rayd-core 490, rayd lib 169, m3 30, resto sin cambios). Rojos sin el arreglo: los tres tests nuevos de `filesystem::events` (sin `landing_kind`), `a_rename_reaches_the_sink_with_one_cookie_on_both_halves` y `write_rpc_into_a_watched_directory_surfaces_as_one_write_with_its_entry` (sin la cookie en el adaptador: llega `RENAME`). Pendiente: republicar `rayito-base` con este `rayd` y volver a correr `sync/watch` y `async/watch` (11.2).

## 2. [infra] Image, pin gate, NOTICE (D15, D16)

- [x] 2.1 `image/Dockerfile`:
  - Add `git-core-2.50.1-1.amzn2023.0.1` to the first `dnf install` line, with a comment above the `RUN` giving the reason and the measured 7 packages / 31,452,146 B.
  - Add `git` to the build-time tool check loop.
- [x] 2.2 `scripts/check_pins.py`: add the dnf gate (`PINNED_DNF_PACKAGES = frozenset({"git-core"})`, `image/Dockerfile` in `DEFAULT_PATHS`, finding text `el paquete dnf no lleva -<versión>-<release>`) and update the module docstring to describe three gates.
- [x] 2.3 `scripts/tests/test_check_pins.py`: a bare `git-core` gives one finding; the pinned NEVRA gives none; an unrelated unpinned package gives none. `python scripts/check_pins.py` prints OK.
- [x] 2.4 Add the E2B git attribution line from D15 to `NOTICE`, `clients/python/NOTICE` and `clients/typescript/NOTICE`, byte-identical. `python scripts/check_license.py` prints OK.
  - Evidencia: la línea D15 va en `NOTICE`, `clients/python/NOTICE` y `clients/typescript/NOTICE`, byte a byte iguales (`cmp` sin diferencias). `uv run --no-project python scripts/check_license.py` imprime OK; `scripts/tests/test_check_license.py` 17 passed.

## 3. [python] Native SDK additions (D6, D9, D11, D12, D13)

- [x] 3.1 `rayito/exceptions.py`:
  - `GitAuthException(AuthenticationException)` and `GitUpstreamException(SandboxException)`.
  - `UnimplementedError`, `TransferException` and `FileUploadException(TransferException)` come from `m9-file-transfer`. If `m9-egress-policy` landed a shim subclass of `UnimplementedError`, it collapses into the re-export in 5.3.
  - Export them from `rayito/__init__.py`.
  - Tests in `tests/unit/test_exceptions.py`: the MRO and the `code` attribute.
  - Verificado aquí (2026-09-23, macOS arm64): `GitAuthException(AuthenticationException)`, `GitUpstreamException(SandboxException)`, `UnimplementedError(NotImplementedError)` con `feature`/`reason`/`doc`, `TransferException`/`FileUploadException` exportadas desde `rayito`; `test_exceptions.py` verde (se añadió `test_unimplemented_error_names_feature_reason_and_optional_doc`). Mutación en una copia aislada: `GitAuthException(SandboxException)` → 1 fallo. `rayito.e2b.exceptions.UnimplementedError is rayito.UnimplementedError` ya es `True`.
- [x] 3.2 `rayito/_models.py`: add `Logs.to_json`, `ExecutionError.to_json` and `CodeContext.from_json` (a missing key raises `InvalidArgumentException` naming it). Tests in `tests/unit/test_models.py`: round-trips, plus `json.loads(execution.to_json())["logs"] == logs.to_json()`.
  - Verificado aquí: `test_models.py` (round-trips, anidado de `Execution.to_json`, `CodeContext.from_json` con clave ausente) verde; `Logs.to_json` sin `stderr` → 1 fallo.
- [x] 3.3 `rayito/_transport.py`:
  - `TransportSettings.extra_metadata` and `TransportSettings.http_proxy`.
  - `open_channel`/`open_aio_channel` append `("grpc.http_proxy", url)`.
  - `ProxyAuthPlugin(extra=...)` appends the pairs after the reserved keys.
  - Every construction site passes `extra=settings.extra_metadata`: the authenticated plugin, the stream channel, and the anonymous `Health` plugin in `probe_health`/`probe_metadata`, in both trees.
  - Tests in `tests/unit/test_transport.py`.
  - Verificado aquí: `test_transport.py` 29/29 (`extra_metadata` tras las claves reservadas en el plugin autenticado y en el anónimo de `Health`, `http_proxy` como opción del canal). Mutaciones: sin `metadata.extend(extra)` → 3 fallos; sin `grpc.http_proxy` → 1. `mypy tests/unit/test_transport.py` limpio (el error de la línea 143 ya no existe).
- [x] 3.4 `rayito/_aws.py`:
  - `ClientSettings` (frozen: `retries`, `proxy`, `integration`).
  - `client_config(settings=None)` with the D6 fields.
  - `LambdaMicrovmsControlPlane.from_session(..., settings=None)`.
  - `shared_control_plane(..., settings=None)` with the cache key `(session, region, settings)`.
  - Tests in `tests/unit/test_aws.py`: `total_max_attempts == retries + 1`, the `proxies` dict, the `user_agent_extra` suffix, and a distinct plane per settings.
  - Verificado aquí: `test_aws.py` verde (`total_max_attempts == retries + 1`, `proxies`, `user_agent_extra`, un plano por settings, `repr` sin la URL del proxy); `total_max_attempts` fijo en 5 → 2 fallos.
- [x] 3.5 `logger=`:
  - Native `Sandbox.create`/`connect` (sync and async) accept `logger: logging.Logger | None`. `self._logger` is handed to `Commands`, `Filesystem`, `Pty`, `CodeClient`, `PersistenceClient`, `Git` and `TokenRefresher(logger=)`.
  - The module-level helpers that log take `log: logging.Logger`.
  - `create` logs `run-microvm aceptado` through `logger or module logger`.
  - Tests in `tests/unit/test_sandbox_sync.py`/`_async.py`: the given logger gets the records, `rayito.sandbox` gets none for that sandbox, and the record texts are unchanged.
  - Verificado aquí: `test_sandbox_sync.py`/`_async.py` (`test_logger_receives_the_create_and_readiness_records`, `test_without_logger_the_records_keep_their_module_loggers`, `test_failed_boot_cleanup_is_logged_on_the_given_logger` y su gemelo async) verdes; `sandbox_logger` ignorando el logger dado → 3 fallos.
- [x] 3.6 `CommandHandle.wait(on_pty=None, on_stdout=None, on_stderr=None)` (sync) and `AsyncCommandHandle.wait(...)`, the latter awaiting awaitable results. Chunks go to the callbacks after the `run()`-time ones, with no replay (D11). Tests in `tests/unit/test_commands_sync.py`/`_async.py`/`test_pty_sync.py`, per the process-lifecycle ADDED scenarios.
  - Verificado aquí: `test_wait_callbacks_receive_the_chunks_after_the_run_callbacks`, `test_wait_callbacks_never_replay_consumed_chunks`, `test_wait_delivers_pty_output_to_on_pty` y `test_async_wait_awaits_async_callbacks` verdes. Mutaciones: `wait()` sin entregar → 3 fallos; sin `await` del resultado awaitable → 1.
- [x] 3.7 Native `is_running(*, request_timeout=None)` (sync and async) bounds the Health probe. Tests.
  - Verificado aquí: `test_is_running_request_timeout_bounds_the_health_deadline` (sync) y su gemelo async verdes; sonda fija en 5 s → 1 fallo en cada árbol.
  - Fix de revisión (2026-09-23): los dos tests eran intermitentes también aislados (10 corridas: 4 con fallo, `0.501 <= 0.5`), porque gRPC redondea hacia arriba el `grpc-timeout` codificado. La cota admite ahora ese redondeo (`GRPC_TIMEOUT_ROUNDING_SLACK = 0.01` en `conftest.py`, y `default` debe superarla); 10 de 10 corridas verdes y la mutación «sonda fija en 5 s» sigue en rojo.
  - Causa raíz y fix definitivo (2026-09-23): el único fallo observado fue ese `0.501 <= 0.5`; con la holgura 0 se reproduce en 30 de 100 corridas. gRPC redondea el deadline al milisegundo hacia arriba (2000 `Health` de 0.5 s: 877 por encima, máximo +1 ms). No era otra sonda de fondo: con `idle=None` no queda ningún hilo que llame a `Health`, y el recuento de hilos antes del test lo confirma. Como aun con holgura el test dependía del reloj (un `Health` real de 0.5 s puede caducar con el GIL ocupado, y entonces `is_running` devuelve `False`), los dos gemelos espían ahora `sandbox._health.Health` y comprueban que los plazos pedidos son `[0.5, ReadinessPoll.MAX_RPC_TIMEOUT]`. `GRPC_TIMEOUT_ROUNDING_SLACK` sale de `conftest.py`. Resultado: 100 de 100 corridas verdes, y las mutaciones «`request_timeout` ignorado» y «sonda fija en 5 s» fallan en los dos árboles. El shim (`test_e2b_v2_{sync,async}.py`) usa el mismo patrón: el `is_running` async sale de `test_async_accessors_and_running` a su propio test, y la mutación «el shim no reenvía `request_timeout`» falla en los dos árboles.
- [x] 3.8 Confirm that the native instance `connect(*, timeout=None, request_timeout=None)` of `m9-server-timeout` exists in both trees and in TS (`connect({ timeoutMs, requestTimeoutMs })`). The shim instance `connect` delegates to it (D5). Record its exact signature in "Notes".
  - Verificado aquí: existe en ambos árboles y en TS; la firma exacta está en "Notes" (3.8).

## 4. [python] Native git module (D15)
- [x] 4.1 `rayito/_git_base.py` (pure): `shell_quote`, `git_command`, `GIT_ENV`, the argv builders (the D15 table), `resolve_config_scope`, `build_clone_plan`, the parsers (`parse_git_status`, `parse_git_branches`, `derive_repo_dir_from_url`), the models (`GitFileStatus`, `GitStatus` with its properties, `GitBranches`, `GitResetMode`), `with_credentials` (percent-encoding), `strip_credentials`, `redact`, `is_auth_failure`, `is_missing_upstream`, and the Spanish messages for auth and upstream errors. There are no inline comments in function bodies.
  - Evidencia: `_git_base.py` completo (698 líneas), sin comentarios dentro de funciones (`grep` de `^\s+#` vacío) ni logging. `ruff check`, `ruff format --check` y `mypy` limpios sobre los ficheros git y CLI (35 ficheros).
- [x] 4.2 `tests/unit/test_git_base.py`: the argv table element by element, quoting with `'`, the porcelain fixtures (clean, ahead/behind, detached, unborn, rename, conflict, untracked), branches, the snippet tables, encoding and redaction, and the clone plan without a derivable path.
  - Evidencia: `uv run --with pytest-timeout pytest tests/unit/test_git_base.py --timeout 60`: 76 passed. En `dde681c` el módulo no existía, así que el fichero entero fallaba al importar.
- [x] 4.3 `rayito/sandbox_sync/git.py` `Git(commands)`: the full method set with E2B signatures; the credential flows (clone strip, push/pull set-url with restore in `finally`, `dangerously_authenticate`); the error mapping with redaction and `from None`; no logging.
  - Evidencia: `sandbox_sync/git.py` `Git(commands)`: clone con strip de credenciales, push/pull con `set-url` restaurado en `finally`, `dangerously_authenticate`, errores redactados con `from None`, sin logging. Cubierto por `test_git_sync.py` (4.6).
- [x] 4.4 `rayito/sandbox_async/git.py` `AsyncGit(commands)`: the identical surface as coroutines over the same pure helpers.
  - Evidencia: `sandbox_async/git.py` `AsyncGit(commands)` sobre los mismos helpers puros; `test_async_surface_matches_sync` compara los conjuntos de métodos y las firmas.
- [x] 4.5 Add the lazy `git` property to native `Sandbox`/`AsyncSandbox`. Export `Git`, `AsyncGit`, `GitStatus`, `GitBranches`, `GitFileStatus` and `GitResetMode` from `rayito`.
  - Evidencia: `Sandbox.git`/`AsyncSandbox.git` perezosos (`self._git` en ambos `main.py`); `rayito.__init__` exporta `Git`, `AsyncGit`, `GitStatus`, `GitBranches`, `GitFileStatus` y `GitResetMode`. `test_git_is_a_lazy_property` en verde.
- [x] 4.6 `tests/unit/test_git_sync.py`/`test_git_async.py` against the fake `ProcessService`: the exact commands and `GIT_TERMINAL_PROMPT=0`; the push-with-credentials order even on exit 128; `GitAuthException` and `GitUpstreamException` classification; a credentialed clone failure with no password in the message, `stderr` or cause chain; the two `dangerously_authenticate` commands; zero log records.
  - Evidencia: `test_git_sync.py` + `test_git_async.py`: 20 passed (comandos exactos con `GIT_TERMINAL_PROMPT=0`, orden `get-url`/`set-url`/`push`/`set-url` aunque el push salga 128, `GitAuthException`/`GitUpstreamException`, clone con credenciales sin el password en mensaje, `stderr` ni cadena de causas, los dos comandos de `dangerously_authenticate`, cero registros de log). Total git: 96 passed.

## 5. [python] `rayito.e2b` shim to 2.x (D4-D14)

- [x] 5.1 `rayito/e2b/_types.py`: the D4 aliases.
  Verificado aquí (2026-09-23, macOS arm64, `clients/python`, reintento): `_types.py` con los alias D4 literales; `test_e2b_v2_exports.py::test_type_aliases_follow_e2b` (`OutputHandler[str]` subscribible).
- [x] 5.2 `rayito/e2b/_unimplemented.py`: `UNIMPLEMENTED_REASONS` (the D14 table, verbatim) and `unimplemented(feature)`. `unimplemented_resource(name, feature, methods)` generates `Template`, `AsyncTemplate`, `Volume`, `AsyncVolume`, `Secret` and `AsyncSecret`. `get_signature` raises.
  Verificado aquí (2026-09-23, macOS arm64, `clients/python`, reintento): `UNIMPLEMENTED_REASONS` literal de D14, `unimplemented(feature)` con `doc=COMPAT_DOC_PATH`, `unimplemented_resource` para las seis clases y `get_signature`; `test_e2b_v2_base.py::test_every_table_key_builds_its_error` (parametrizado por clave), `test_the_table_covers_d14`, `test_resources_raise_unimplemented_never_attribute_error`.
- [x] 5.3 `rayito/e2b/exceptions.py` (D13):
  - Make `UnimplementedError` a re-export of the native class, collapsing a subclass if one landed (D1). Every shim raise passes `doc=COMPAT_DOC_PATH`.
  - Re-export `FileNotFoundException`, `SandboxNotFoundException`, `FileUploadException`, `GitAuthException` and `GitUpstreamException`.
  - `NotEnoughSpaceException = DiskFullException` and `ServiceBusyException = CapacityException`.
  - Add `BuildException(Exception)` with its "never raised" docstring.
  - Update `__all__`.
  Verificado aquí (2026-09-23, macOS arm64, `clients/python`, reintento): re-exportaciones nativas (incluida `UnimplementedError`), alias `NotEnoughSpaceException = DiskFullException` y `ServiceBusyException = CapacityException`, `BuildException(Exception)` y `__all__`; `test_e2b_v2_exports.py::test_exception_hierarchy_follows_d13` y `test_the_shim_unimplemented_error_is_the_native_class`. Mutación: `UnimplementedError` como subclase → 2 fallos.
- [x] 5.4 `rayito/e2b/_connection.py` (pure): `ApiParams` (TypedDict), `IGNORED_API_PARAMS`, `split_api_params`, `validate_extra_headers`, `validate_proxy_url`, `resolve_retries`, `ConnectionSettings`, `connection_overrides`, `merge_bound_params`, `ConnectionConfig` with `set_integration` (D6, D7).
  Verificado aquí (2026-09-23, macOS arm64, `clients/python`, reintento): `_connection.py` puro con todo lo listado; `test_e2b_v2_base.py` (cabeceras reservadas, proxy sin eco de la contraseña, `resolve_retries`, `merge_bound_params`, `split_api_params` con ocho avisos sin valores, `ConnectionConfig`/`set_integration`): 74 tests.
- [x] 5.5 `rayito/e2b/_compat.py`:
  - `map_create_kwargs` for the 2.x positional order, including the pre-mapping `UnimplementedError` checks for `mcp`, `iam` and `volume_mounts`.
  - `m9-server-timeout`'s lifecycle validation builds its `keep_memory: False` error through `unimplemented("lifecycle.on_timeout.keep_memory=False")`, so it carries the D14 reason.
  - `m9-egress-policy`'s `map_network` builds its three `UnimplementedError`s through `unimplemented(...)` so they carry the D14 reasons; its `TypeError` for unknown keys stays.
  - `reject_callable_in_user_slot`, `native_stdin`.
  - `info_from_native` for 2.x (D10).
  - `native_call_kwargs(cls, call_kwargs)` for the class variants.
  Verificado aquí (2026-09-23, macOS arm64, `clients/python`, reintento): `map_create_kwargs` en el orden posicional 2.x con `mcp`/`iam`/`volume_mounts` antes de mapear, `keep_memory: False` y las tres claves de red vía `unimplemented(...)` con el motivo D14 (el `TypeError` de clave desconocida sigue), `reject_callable_in_user_slot`, `native_stdin`, `info_from_native` 2.x y `native_call_kwargs`.
  Mutaciones: `rules` aceptado → 6 fallos; `_default_max_lifetime` fijo → 14 fallos.
- [x] 5.6 `rayito/e2b/_models.py`: `SandboxInfo` with the D10 fields and order (`raw_state` last, `volume_mounts` defaulting to `[]`).
  Verificado aquí (2026-09-23, macOS arm64, `clients/python`, reintento): `SandboxInfo` con los campos y el orden D10 (`raw_state` último, `volume_mounts` a `[]`); `test_e2b_compat_base.py::test_info_from_native_sandbox_info_field_order`.
- [x] 5.7 `rayito/e2b/_sync.py`:
  - `create` with the explicit 2.x signature; the constructor keeps its 1.x keywords plus `logger`, `retries` and `headers`.
  - `connect` as a `class_method_variant` (class and instance forms, D5).
  - `pause(keep_memory=None) -> bool` and the class `pause`/`beta_pause`.
  - `is_running(request_timeout=None)`.
  - The properties `envd_api_url`, `envd_direct_url`, `traffic_access_token` and `connection_config`.
  - `git`.
  - `fork`, `create_snapshot`, `list_snapshots`, `delete_snapshot`, `get_mcp_url` and `get_mcp_token` raising.
  - The code-context trio.
  - The wrappers `Commands` (positional), `Filesystem` (`write_files`, 2.x sync `watch_dir`) and `Pty` (2.x sync `create`/`connect`).
  - Every classmethod merges `_bound_params` and resolves the connection settings.
  Verificado aquí (2026-09-23, macOS arm64, `clients/python`, reintento): todo lo listado en `e2b/_sync.py`; `test_e2b_v2_sync.py` (64 tests): triggers D14 sin peticiones, wrappers posicionales, `pause()` booleano, `connect()` de instancia, `is_running(request_timeout=)`, accesores, `git`, trío de contextos, `E2B` ligado. Mutación: `pause()` que devuelve el id → 2 fallos.
- [x] 5.8 `rayito/e2b/_async.py`: the identical surface as coroutines, with E2B's async orders for `watch_dir`, `pty.create`/`pty.connect` and `commands.connect(on_stdout, on_stderr)`.
  Verificado aquí (2026-09-23, macOS arm64, `clients/python`, reintento): misma superficie en `e2b/_async.py` con los órdenes async de E2B; `test_e2b_v2_async.py` 33 tests (incluye transferencias y paso de `gzip`/`metadata`/`use_octet_stream`/`stream_idle_timeout`, añadidos en este reintento).
  Fix de revisión (2026-09-23): mapeos D11 sin test que fallase: `test_async_commands_forward_callbacks_and_the_connect_arguments` (`run(on_stdout=, on_stderr=)` y `connect(pid, 5, 2, on_stdout, on_stderr)` posicional llegan al nativo), `test_commands_forward_callbacks_and_the_connect_timeout` (sync: `connect(pid, 7, 3)` pasa `timeout`) y `test_async_connection_config_keeps_the_logger_of_create_and_connect`. Rojo en una copia con `on_stdout=None` en `run`/`connect` async, sin `timeout` en `Commands.connect` y `logger=None` en los dos `snapshot_config` async (3 fallos), verde en el árbol.
- [x] 5.9 `rayito/e2b/_client.py`: `E2B` and `bind_class` (D8).
  Verificado aquí (2026-09-23, macOS arm64, `clients/python`, reintento): `E2B` y `bind_class`; `test_bound_params_are_used`, `test_two_clients_are_isolated`, `test_client_warns_once_for_ignored_params`.
  Fix de revisión: `test_e2b_v2_base.py::test_bind_class_copies_the_params_it_binds` (mutar el dict del llamador no cambia `_bound_params`); rojo con `MappingProxyType(params)` sin copia, verde en el árbol.
- [x] 5.10 `rayito/e2b/__init__.py`: the 2.x docstring and the D4 `__all__`.
  Verificado aquí (2026-09-23, macOS arm64, `clients/python`, reintento): docstring 2.x y `__all__` D4 más los nombres hermanos (`UploadTicket`, `DownloadLink`). `test_packaging.py::test_e2b_all_matches_the_proposal_list` se actualizó en este reintento (lista M6 + `E2B_V2_NAMES` + hermanos); quitar `get_signature` de `__all__` → 1 fallo.
- [x] 5.11 Tests:
  - `tests/unit/test_e2b_v2_exports.py`, `test_e2b_v2_base.py`, `test_e2b_v2_sync.py` and `test_e2b_v2_async.py`, as listed in D22.
  - Update the existing `test_e2b_compat_sync.py`/`_async.py`/`_base.py` cases that asserted 1.x behaviour: `pause` returning the id, `proxy` warning, `connection_config` unimplemented, `NotEnoughSpaceException` never raised. Each of these is covered by a new assertion of the 2.x behaviour.
  - The log-hygiene test (D21).
  Verificado aquí (2026-09-23, macOS arm64, `clients/python`, reintento): los cuatro ficheros `test_e2b_v2_*.py` de D22, los casos 1.x reescritos (`pause` booleano, `NotEnoughSpaceException` que sí se lanza, `connection_config`, proxy) y `test_e2b_v2_sync.py::test_secrets_never_reach_the_logs` (D21). Ficheros del shim: 530 passed.
- [x] 5.12 Run `cd clients/python && uv run pytest tests/unit && uv run ruff check . && uv run ruff format --check . && uv run mypy src tests`: green.
  Gate de Python (2026-09-23, macOS arm64, reintento de specs-docs): `uv run --with pytest-timeout pytest tests/unit --timeout 60` da 2090 passed en 321 s, exit 0; `ruff check .` limpio; `ruff format --check .` 198 ficheros (el `README.md` ya formateado); `mypy src tests` sin errores en 196 ficheros.
  Sin marcar. Última corrida (2026-09-23, macOS arm64, reintento): `uv run --with pytest-timeout pytest tests/unit --timeout 60` 2058 passed, 2 failed; los dos fallos son nativos y ajenos al shim: `test_sandbox_sync.py::test_is_running_request_timeout_bounds_the_health_deadline` y su gemelo async (el plazo de `Health` llega como `0.501 <= 0.5` con la máquina cargada; la revisión los vio fallar también aislados; los corrigió después el fixer de python y la corrida completa de hoy da 2090 passed). `ruff check .` limpio, `ruff format --check .` 198 ficheros, `mypy src tests` sin errores en 196 ficheros. Los ficheros del shim (`test_e2b_compat_{base,sync,async}.py`, `test_e2b_v2_{base,sync,async,exports}.py`, `test_packaging.py`) dan 530 passed.
  Corrida del fixer python (2026-09-23, macOS arm64, tras los fixes de revisión): `uv run --with pytest-timeout pytest tests/unit --timeout 60` 2090 passed, 0 failed (332 s; los `is_running` ya no fallan); `ruff check .` limpio; `mypy src tests` sin errores en 196 ficheros; `ruff format --check .` sólo marca `clients/python/README.md` (bloques de código de la doc, área de docs, en edición por otro agente). Queda sin marcar hasta que ese fichero pase el formato.

## 6. [python] CLI (D19)

- [x] 6.1 `rayito/cli/_tokens.py` (stdlib only): `resolve_token(token_file, environ) -> str | None`, `write_token_file(path, token)` (`O_WRONLY | O_CREAT | O_EXCL`, `0o600`), `parse_pairs(values) -> dict[str, str]`.
  - Evidencia: `cli/_tokens.py` sólo con la biblioteca estándar (`os`, `pathlib`, `typing`): `resolve_token`, `write_token_file` (`O_EXCL`, `0o600`) y `parse_pairs`. Cubierto por `test_resolve_token_prefers_the_file`, `test_write_token_file_refuses_to_overwrite` y `test_parse_pairs`.
- [x] 6.2 `rayito/cli/_terminal.py` (stdlib + SDK): `run_terminal(sandbox, *, user, cwd, envs, stdin, stdout) -> int` with the POSIX raw-mode, Windows console and pipe modes of D19, a daemon reader thread, `SIGWINCH`/polled resize, and exit code 124 on a server timeout.
  - Evidencia: `cli/_terminal.py`: modos POSIX raw, consola Windows y tubería, hilo daemon, `SIGWINCH`/sondeo y 124 por timeout. Corrección de esta pasada: el lector POSIX espera con `select` en tramos de 50 ms y se para (`stop` + `join`) antes de restaurar la terminal, así al volver `run_terminal` ningún hilo sigue consumiendo la entrada ni bloquea el `close` de la PTY en macOS. Rojo→verde: `test_terminal_reader_stops_without_consuming_later_input` falla con el `_terminal.py` anterior (1 failed) y pasa con el nuevo.
- [x] 6.3 `rayito/cli/sandbox.py`: the commands `create`, `connect`, `exec` and `metrics` with the D19 options and exit codes. Update the group help to `MicroVMs: list, info, kill, logs, create, connect, exec, metrics.`
  - Evidencia: `rayito sandbox create|connect|exec|metrics` con las opciones y códigos de D19; la ayuda del grupo es `MicroVMs: list, info, kill, logs, create, connect, exec, metrics.`.
- [x] 6.4 `tests/unit/cli/test_sandbox_interactive.py`: the cli spec scenarios (detach without a token destination, token file written before `run-microvm` and never printed, an existing token file, exec exit code passthrough and the `shlex.join` command, metrics JSON) plus pipe-mode `run_terminal`. Extend `test_app.py::test_library_modules_import_without_typer` with `_terminal` and `_tokens`. On POSIX, add the terminal-restore test using `pty.openpty()`.
  - Evidencia: `tests/unit/cli/test_sandbox_interactive.py` + `test_app.py`: 38 passed con `--timeout 60` en macOS (antes: 2 failed y un cuelgue). Arreglos: (a) `test_detached_create_writes_the_token_file_first_and_never_prints_it` fija `RAYITO_TEMPLATE` en su entorno; dependía del entorno del host y fallaba con `falta el template`. (b) `test_terminal_attributes_are_restored_on_posix` se colgaba en `tty.setraw` (TCSAFLUSH espera a que se lea el eco y nadie leía el lado controlador; además descartaba el `exit 0` tecleado antes). Ahora un hilo emula la terminal: teclea cuando la PTY ya está en raw y lee la salida con `select`; la comparación ignora PENDIN, que el kernel BSD/macOS enciende solo al volver a modo canónico; y comprueba que una entrada posterior sigue sin leer. (c) `test_library_modules_import_without_typer` incluye ahora `_terminal` y `_tokens`.

## 7. [ts] Native TypeScript additions (D18)

- [x] 7.1 `src/errors.ts`: add `GitAuthError` and `GitUpstreamError`. `DiskFullError`, `UnimplementedError`, `TransferError` and `FileUploadError` come from `m9-file-transfer`. `src/transport/errors.ts` maps `ResourceExhausted` + `disk_reserve`/`disk_full` to `DiskFullError`. Export them from `src/index.ts`. Tests in `errors.test.ts`.
  - verificado aquí: `GitAuthError extends AuthenticationError` y `GitUpstreamError extends SandboxError` en `src/errors.ts`; `translateRpcError`/`translateStreamError` dan `DiskFullError` para `ResourceExhausted` con `disk_reserve`/`disk_full` (la misma regla exacta que Python `DISK_FULL_DETAILS`) y `RateLimitError` para el resto. `GitAuthError`/`GitUpstreamError` (y `Git` con sus tipos) se exportan ahora desde `src/index.ts`. `errors.test.ts` +3 tests (jerarquía y exports; la tabla `ResourceExhausted`; `files.write` rechazado por el `rayd` falso con `disk_reserve` → `DiskFullError` y con `rate` → `RateLimitError`, con `writeRejections` nuevo en `fake/filesystem.ts`). Rojo→verde: quitando la regla de disco de `translateRpcError` fallan 2 de 10; restaurada, 10/10.
- [x] 7.2 `src/transport/headers.ts` `validateExtraHeaders` (D6 rules). `TransportSettings.extraHeaders` is set by the interceptor after the reserved headers. Tests in `headers.test.ts`.
  - verificado aquí: `validateExtraHeaders` (claves reservadas y prefijos `x-aws-proxy-`/`grpc-`/`:`, `-bin`, token RFC 9110, valor ASCII imprimible; mensajes con la clave y nunca el valor); `resolveTransportSettings` valida antes de tocar AWS y el interceptor pone los pares tras las cuatro cabeceras reservadas. `tests/unit/headers.test.ts` (5 tests): `x-trace: 1` llega a `Health`, a un unario (`commands.list`) y a un stream (`commands.run` en background); `x-access-token` y `X-AWS-Proxy-Port` rechazan el `create` con cero llamadas al plano. Rojo→verde: sin el bucle del interceptor y sin la regla `-bin` fallan 2 de 5; restaurado, 5/5.
- [x] 7.3 `src/transport/proxy-tunnel.ts` `ProxyTunnelSocket` and `ProxyTunnelAgent`. `TransportSettings.proxy` feeds `openTransport`'s `createConnection`. Tests in `proxy-tunnel.test.ts` against a local `net` CONNECT server.
  - verificado aquí: `ProxyTunnelSocket`/`ProxyTunnelAgent` en `src/transport/proxy-tunnel.ts` y `sessionOptions` da `createConnection` al `Http2SessionManager` sólo con `proxy`. `tests/unit/proxy-tunnel.test.ts` (8 tests) contra un servidor `net` local: `CONNECT sandbox.example.com:443 HTTP/1.1` con `Proxy-Authorization: Basic`, bytes intactos tras el 200, un 407 destruye el socket con `el proxy rechazó CONNECT con HTTP 407` sin la contraseña ni la URL, un `Sandbox` completo llega al `rayd` falso por el túnel, y el plano usa `ProxyTunnelAgent`. Rojo→verde: sin `Proxy-Authorization` y con `sessionOptions` ignorando `proxy` fallan 4 de 8; restaurado, 8/8.
- [x] 7.4 `ControlPlaneOptions.retries`/`integration`: `maxAttempts`, `customUserAgent`, and the `NodeHttpHandler` with `ProxyTunnelAgent` when `proxy` is set; a dedicated plane when any is set. Tests in `aws.test.ts`.
  - verificado aquí: `clientConfig` fija `maxAttempts = retries + 1` (5 por defecto), `customUserAgent` con `["rayito-integration", integration]` y el `NodeHttpHandler` con `ProxyTunnelAgent`; `sharedControlPlane` da un plano por combinación y `resolveControlPlane` rechaza los ajustes junto a `controlPlane`/`client`. `aws.test.ts` +4 tests (también `maxAttempts()` resuelto del cliente real del SDK = 3). Rojo→verde: forzando `DEFAULT_MAX_ATTEMPTS`, sin la integración y sin el agente fallan 2 de 19; restaurado, 19/19.
- [x] 7.5 `RequestOptions.signal`: unary `CallOptions.signal`, stream controller linking in `SandboxCore.openStream`, and `SandboxCreateOptions.signal` for the readiness poll and control-plane `abortSignal`. Rejection uses `signal.reason`. Tests in `abort.test.ts`.
  - verificado aquí: unarios con `CallOptions.signal`, streams enlazados con `linkAbortSignal`, rechazo con `signal.reason` sin envolver. Añadido en esta pasada: `ControlPlaneCallOptions.signal` en `resolveTemplateArn`, `getMicrovm`, `resumeMicrovm` y `createAuthToken` del puerto (el adaptador pasa `{ abortSignal }` a `send`, opción del SDK, y rechaza con el `reason`; `LaunchObserver` lo reenvía); `create` lo usa en `resolveTemplateArn`, `connect` en `get-microvm`/`resume-microvm`, el primer `mint` y el sondeo de readiness (Health y la espera) se cortan con `raceAbort`. Divergencia deliberada documentada en el puerto: `run-microvm` y `terminate-microvm` no llevan `signal` (cortar `run-microvm` dejaría un MicroVM sin id; la limpieza tiene que correr tras el aborto, y el aborto se mira justo después de `run-microvm`). `tests/unit/abort.test.ts` (8 tests): `runCode` abortado rechaza con el `reason` y el fake marca la celda interrumpida; un `signal` ya abortado no envía nada; el `reason` por defecto es `DOMException` `AbortError`; aborto en readiness rechaza sin esperar `readyTimeoutMs` y termina el VM salvo `keepOnFailure`; un plano colgado recibe el `signal` en `create` y `connect`; el adaptador AWS entrega `abortSignal`. Rojo→verde: los 3 tests del plano fallaban antes de añadir `signal` al puerto; mutando `linkAbortSignal` fallan 2, y quitando la carrera de la readiness fallan otros 2; restaurado, 8/8.
  - hallazgo de la review (D17/D18, `signal` ignorado en lifecycle, métricas y red): nuevo `SandboxCore.signalledUnary(call, signal, translate)` (ya abortado → rechaza sin enviar; abortado en vuelo → `signal.reason` sin traducir) y `probeHealth(timeoutMs, signal)`; `CallOptions.signal` en `setTimeout` (instancia y estático, que además pasa el `signal` a `get-microvm` y al `mint`), `connect` de instancia (`get-microvm`, `resume-microvm`, readiness y `SetTimeout(AT_LEAST)`) y estático (la extensión), `isRunning`, `getHealth`, `getMetrics`, `getMetricsHistory`, `getNetwork` y `updateNetwork` (instancia y estático; `UpdateNetworkOptions.signal`). El shim reenvía `opts.signal` en `isRunning`, `setTimeout`, `connect`, `getMetrics`, `getInfo` y `updateNetwork`, y `kill`/`pause` rechazan un `signal` ya abortado sin tocar el plano. Fake `rayd` con `held` (rutas que nunca responden). Tests: `abort.test.ts` +17 (8 en vuelo, 8 ya abortados, estático) y `e2b-shim.test.ts` +7; rojo antes (17 y 7 fallos, los de vuelo por timeout), verde después. Gate: `pnpm lint` (biome, 123 ficheros, 0 errores), `pnpm typecheck` exit 0, `pnpm test` 38 ficheros / 831 passed, `pnpm build` exit 0 y `pnpm pack:check` exit 0.
- [x] 7.6 `Sandbox.currentProxyToken(port = 8080)` and `isRunning({ requestTimeoutMs })`. Tests.
  - verificado aquí: `currentProxyToken(port = 8080)` lee el `TokenStore` sin acuñar y `isRunning({ requestTimeoutMs })` acota el `Health` a `min(5000, requestTimeoutMs)`. `tests/unit/accessors.test.ts` (2 tests, `grpc-timeout` leído en el fake). Rojo→verde: con `isRunning` ignorando la opción y el token leído de otro puerto fallan 2 de 2; restaurado, 2/2.

## 8. [ts] Native TypeScript git (D15)

- [x] 8.1 `src/sandbox/git-args.ts` (pure): the camelCase port of `_git_base.py`, with the same argv, parsers, models, credential helpers, redaction and classification.
  - verificado aquí contra `_git_base.py`: mismos argv, parsers, modelos, snippets, `percentEncode` = `quote(safe="")`, `redact` de la forma cruda y codificada. Añadido `remoteListArgs`/`resolveRemoteName` (el remoto para credenciales es el dado, el único o `origin`, como Python) y la acción `remote add` en la clasificación.
- [x] 8.2 `src/sandbox/git.ts` `Git` with E2B JS signatures and credential and error behaviour. Add `readonly git: Git` to the native `Sandbox`. Export the types.
  - verificado aquí: `Git` con las firmas JS de E2B, `readonly git: Git` en el `Sandbox` nativo y exportados `Git`, los `Git*Opts` y los modelos desde `src/index.ts`. Alineado con Python en esta pasada: push/pull con credenciales y sin `remote` resuelven el remoto con `git remote`; `remoteAdd({ fetch: true })` clasifica un fallo de autenticación como `GitAuthError`. Sin logs.
- [x] 8.3 `git.test.ts`: the command strings, the credential restore order, classification and redaction, mirroring 4.2 and 4.6.
  - verificado aquí: `tests/unit/git.test.ts` (28 tests): la tabla de argv elemento a elemento, `shellQuote`, porcelain (limpio, ahead/behind, detached, sin commits, rename, conflicto, untracked), ramas, snippets, codificación y redacción, el plan de clone sin ruta derivable, los comandos exactos con `GIT_TERMINAL_PROMPT=0`, el orden get-url → set-url(creds) → push → set-url(original) también con exit 128, `GitAuthError`/`GitUpstreamError`, un clone fallido sin la contraseña en mensaje, salida ni `cause`, los dos comandos de `dangerouslyAuthenticate`, y `sandbox.git` real contra el `ProcessService` falso sin registrar la contraseña. Rojo→verde: antes de `resolveRemoteName` fallaban 3 de 27; después 28/28.

## 9. [ts] `rayito/e2b` entry point (D17)

- [x] 9.1 Add `exports["./e2b"]` to `package.json`; set the `tsdown.config.ts` `entry` to `{ index, e2b }`; add the four `dist/e2b.*` entries to `scripts/pack-check.mjs` `REQUIRED_ENTRIES`. Hecho (2026-09-23): `exports["./e2b"]` con `types`/`import`/`require`, `entry: { index, e2b }` y los cuatro `package/dist/e2b.*` en `REQUIRED_ENTRIES`; `e2b-exports.test.ts` lo comprueba. `node -e 'require("rayito/e2b")'` y el `import` ESM por auto-referencia resuelven `Sandbox.{create,connect,kill,getInfo,list}` y `NotEnoughSpaceError === DiskFullError`.
- [x] 9.2 `src/e2b/unimplemented.ts` (the D14 table with Python-identical reasons), `src/e2b/errors.ts` (`TemplateError`, `BuildError`), and `src/e2b/resources.ts` (`Template`, `Volume`, `Secret`, `getSignature`). Hecho (2026-09-23): las 19 claves TS de D14 con los motivos comparados carácter a carácter contra la tabla D14 de `design.md` (script puntual: 19/19 iguales); `e2b-compat.test.ts` compara además cada motivo con `rayito/e2b/_unimplemented.py` (ya presente): los 11 motivos distintos aparecen byte a byte en el `.py`. `TemplateError extends SandboxError`, `BuildError extends Error`; `Template`/`Volume`/`Secret` (todos los estáticos de E2B) y `getSignature` lanzan `UnimplementedError`.
- [x] 9.3 `src/e2b/compat.ts` (pure: `mapCreateOptions`, `splitConnectionOpts`, `rejectUnsupportedNetworkKeys`, `infoFromNative`, `emitCompatWarning`) and `src/e2b/connection.ts` (`ConnectionConfig.setIntegration`, `ConnectionOpts`). Hecho (2026-09-23): `compat.ts` puro con `mapCreateOptions` (300 000 ms, `maxLifetimeMs = max(3 600 000, min(t + 60 000, 28 800 000))`, bloque `lifecycle` siempre, `idle: null` en kill y `{300, autoResume}` en pause, `ingress` `ALL_INGRESS`, `egress` `INTERNET_EGRESS`), `splitConnectionOpts` (`headers` → `transport.extraHeaders`, `proxy` → transporte y plano, `setIntegration` → `integration`), `mapLifecycle`, `rejectUnsupportedNetworkKeys`, `mapNetworkUpdate`, `mapListOptions`, `infoFromNative` (D10), `metricsFromNative`, `emitCompatWarning`. `httpsPorts` se valida (1–65535) y se ignora: QE2 de `m9-egress-policy` sigue pendiente de AWS. `connection.ts` ya estaba.
  - hallazgos de la review: (1) `httpsPorts` no vacío ya no se ignora: `HTTPS_PORTS_SUPPORTED = false` y `HTTPS_PORTS_REASON` (el texto QE2 de Python) → `UnimplementedError("network.httpsPorts")`; `[]` pasa. (2) `infoFromNative` sigue D10 como `internet_access_from`/`network_dict` de Python: `network` es la política leída sea cual sea su `enforcement`, `InfoExtras.networkRead` y, sin política leída, `allowInternetAccess` es `true` si los conectores de egress incluyen `INTERNET_EGRESS`; el `SandboxInfo` nativo gana `ingress`/`egress` (`ingressNetworkConnectors`/`egressNetworkConnectors` de `GetMicrovmResponse`, campos del SDK). (3) `#guestNetwork` sólo absorbe `UnimplementedError`. Tests: `e2b-compat.test.ts` (regla QE2, tabla de verdad con `NONE`, respaldo por conectores), `aws.test.ts` (mapeo de conectores) y `e2b-shim.test.ts` (`getInfo` con política sin aplicar, imagen sin `GetNetwork` y un `Internal` que se propaga): rojo antes (1 + 3 + 1 fallos), verde después.
- [x] 9.4 `src/e2b/sandbox.ts`, `filesystem.ts` and `pty.ts`: the D17 surface. The statics and instance members map to the native and sibling TS methods: `setTimeout`, the instance `connect({ timeoutMs })`, `getMetricsHistory`, static `getMetrics`, the `list` paginator, `updateNetwork`, `uploadUrl` and `downloadUrl`. Hecho (2026-09-23): `Sandbox` por composición (`readonly native`), constructor protegido; estáticos `create` (dos sobrecargas), `connect`, `kill`, `getInfo`/`getFullInfo`, `isRunning` (sólo `get-microvm`), `pause`/`betaPause`, `setTimeout`, `getMetrics` (→ `getMetricsHistory`), `list` (→ `SandboxPaginator` sobre `paginate()`), `updateNetwork`, `fork`/`deleteSnapshot`/`listSnapshots`; instancia con `getHost` síncrono, `getHostHeaders`, `trafficAccessToken` (`currentProxyToken(8080)`), `connect({ timeoutMs, onResume })` → `this`, `getMetrics` con la instantánea como respaldo, `uploadUrl()` sin ruta = `InvalidArgumentError`. Un `LifecycleUnsupportedError` del nativo sale como `UnimplementedError("lifecycle")`. `Filesystem` extiende el nativo con `watchDir(path, onEvent, opts)` (60 000 ms por defecto); `Pty` compone el nativo con `create({ cols, rows, onData })`.
  - hallazgos de la review: (1) `Sandbox.getInfo(id)`/`getFullInfo` del shim usan el nuevo `NativeSandbox.probedInfo` (interno; el espejo de `Sandbox.get_info(sandbox_id)` de Python): sobre un sandbox `RUNNING`, un JWE y un `Health` anónimo por un transporte dedicado rellenan `lifecycle` (`endAt` = plazo lógico), `metadata` y la vista del guest; otro estado no se sondea. El `Sandbox.getInfo(id)` nativo sigue sin sondear (divergencia documentada de `m9-sandbox-observability` D-TS). Test `the static getInfo reads the logical deadline…`: rojo antes (`endAt` = tope de plataforma), verde después. (2) Tests de los wrappers D17 que faltaban: `connect({ timeoutMs })` → `SetTimeout{120000, AT_LEAST}`, `watchDir` con `includeEntry`, el deadline de 60 000 ms y `signal`, `pty.create` con `signal` y `pty.connect` con `fromSeq 0` y `onData`; mutando cada mapeo (7 mutaciones: `timeoutMs`, `includeEntry`, el default, el `signal` de `watchDir`, `fromSeq: 5`, `onData` y el `signal` de `pty.create`) la suite del shim pasa a rojo; restaurado, 24/24. (3) El docstring de `Pty.connect` ya dice que `fromSeq: 0` entrega sólo salida nueva.
- [x] 9.5 `src/e2b/client.ts` `E2B`, and `src/e2b/index.ts` with the exports, default export and polyfill. Hecho (2026-09-23): `E2B` avisa una vez al construirse, liga el resto con `bindSandbox` (subclase cuyos estáticos fusionan con la regla de E2B) y sus getters `Template`/`Volume`/`Secret` lanzan; `index.ts` con los exports de D17 (alias como bindings nativos), `export default Sandbox` y el polyfill de `Symbol.asyncDispose`.
- [x] 9.6 Tests: `e2b-exports.test.ts`, `e2b-compat.test.ts` and `e2b-shim.test.ts`, as listed in D22, including the TS log-hygiene assertion over the `Logger` spy. Hecho (2026-09-23): `e2b-exports.test.ts` (3), `e2b-compat.test.ts` (19, el de paridad con `_unimplemented.py` ya activo) y `e2b-shim.test.ts` (13), incluida la higiene: `headers` con `valor-secreto`, `proxy` con `clave` y un `git.clone` con password no aparecen en el `RecordingLogger` ni en los avisos. Rojo → verde: sin `src/e2b/{index,sandbox,compat,client,filesystem,pty}.ts` los 3 ficheros fallan al importar; con ellos, 35/35 (`npx vitest run --project unit tests/unit/e2b-*.test.ts`).
- [x] 9.7 Run `cd clients/typescript && pnpm lint && pnpm typecheck && pnpm test && pnpm build && pnpm pack:check`: green, and the listing shows `package/dist/e2b.{mjs,cjs,d.mts,d.cts}`. Verde (2026-09-23): `pnpm lint` (123 ficheros), `pnpm typecheck`, `pnpm test` (38 ficheros, 797 tests), `pnpm build` y `pnpm pack:check`; el listado incluye `package/dist/e2b.mjs`, `e2b.cjs`, `e2b.d.mts` y `e2b.d.cts`.

## 10. [infra] Docs, bookkeeping, archive preparation (D24)

- [x] 10.1 Rewrite `docs/site/docs/e2b-compat.md` against E2B 2.51. It gets the import tables for Python and JS, the three behaviour tables with the D14 reasons, the full parity ledger (110 rows), per-change divergence sections and the 1.x → 2.x migration table. Add the new `docs/site/docs/git.md` plus its entry in the `docs/site/mkdocs.yml` nav. Update `cli.md` and add `api.md` entries for `rayito.Git`/`rayito.AsyncGit`.
  Verificado (2026-09-23, reintento de specs-docs): `e2b-compat.md` reescrita contra E2B 2.51: tablas de import Python y JS, «Funciona sin cambios», «Se mapea, con una nota» y «Lanza `UnimplementedError`» con los motivos de D14, la tabla de paridad completa (las 113 filas del alcance de `m9-handoff/state.json`, en español, columnas `API de E2B`, `Estado tras M9`, `Cambio`, `Nota`), una sección de diferencias por cambio M9 y la tabla de migración 1.x → 2.x. `git.md` nueva (uso, credenciales en negrita «visibles para el código del sandbox», errores, aviso de obsolescencia) con su entrada en la nav; `cli.md` con `create`/`connect`/`exec`/`metrics`, el fichero de token, los códigos de salida y Windows; `api.md` con `rayito.Git`, `rayito.AsyncGit` y `SandboxListPaginator`. `mkdocs build --strict` verde; `test_m9_docs.py::test_e2b_compat_no_longer_lists_mapped_features_as_unimplemented` rojo → verde.
- [x] 10.2 Update `clients/python/README.md`, `clients/typescript/README.md` and both `CHANGELOG.md` files (Unreleased, with the breaking changes). Update `SPEC.md` §3 rows and the §4 CLI bullet; `SECURITY.md` T3 and T9 per D21/D24; and add one sentence to `ARCHITECTURE.md` naming the two shims.
  Verificado (2026-09-23, reintento de specs-docs): READMEs con la sección E2B 2.x (`rayito/e2b` en TS) y `[Unreleased]` de los dos CHANGELOG con los cambios incompatibles («Changed (shim)» en Python, `DiskFullError` en TS); `SPEC.md` §3 filas del shim 2.x (Python y TS), Git y CLI, y el bullet de CLI de §4; `SECURITY.md` T3 (`traffic_access_token`) y T9 (credenciales git en `StartRequest.cmd`, `dangerously_authenticate`, `headers=` sin claves reservadas); `ARCHITECTURE.md` nombra los dos shims como capas de composición en Capa 3. `ruff format --check .` de `clients/python` limpio tras formatear los bloques de código del README (198 ficheros).
- [x] 10.3 Re-diff every MODIFIED requirement under `specs/e2b-compat/spec.md` against the then-current `openspec/specs/e2b-compat/spec.md`, now carrying the five archived siblings. Merge any sibling wording that is missing, so the archived text of the other changes is not lost. Run `openspec validate m9-e2b-v2-surface --strict --no-interactive` again.
  Verificado (2026-09-23): se restauraron los nombres de escenario que el spec principal conserva (`E2B defaults on the wire`, `bash and javascript are forwarded, other kernels are not`, `info and metrics shapes`, `PTY size order`, `write overloads`) con el contenido 2.x, y se añadió `set_timeout` reescrito al comportamiento M9 (mapeado, no lanza). Los bloques solapados de los cinco hermanos se hicieron acumulativos en el orden de archivo (cada MODIFIED lleva los escenarios y el texto de los anteriores; egress ya no dice que `auto_pause` lanza ni que `UnimplementedError` es subclase, sino el re-export nativo). `openspec validate m9-e2b-v2-surface --strict --no-interactive` → valid; una simulación del archivo secuencial (copia de `openspec/` en un scratchpad, `openspec archive --yes` en el orden deno-kernels → file-transfer → server-timeout → sandbox-observability → egress-policy → e2b-v2-surface) archiva las seis sin error y `openspec validate --all --strict` sobre el resultado solo falla en `m9-handoff` (sin deltas, esperado). Re-verificado (2026-09-23): las seis validaciones `openspec validate <cambio> --strict --no-interactive` → valid; el archivo secuencial simulado no pierde ningún escenario del spec principal (comparación de nombres por capability: 0 perdidos; `e2b-compat` 20 → 59) y `openspec validate --specs --strict` sobre el resultado → 36/36.
- [x] 10.4 Run `python scripts/check_hygiene.py`: it exits 0 with no account, bucket, profile, ARN, microvm id or local path in any tracked file this change touched.
  Verificado (2026-09-23, reintento de specs-docs): `uv run --no-project python scripts/check_hygiene.py` da `OK 939 fichero(s) de texto sin identificadores del entorno`, exit 0; los ejemplos nuevos usan `amzn-s3-demo-bucket`, `123456789012` y `<tu-perfil>`.

## 11. [e2e] Real-AWS acceptance (D23)

- [x] 11.1 Publish the M9 `rayito-base` from the updated `Dockerfile` (`rayito image publish ... --base-image-version <pinned>`). Record the `codeInstallSizeInBytes`/`memorySnapshotSizeInBytes` delta against the previous version in a new `AWS_API_NOTES.md` §16 row "git-core en rayito-base", with the git-core RPM share of 31,452,146 B and placeholders only.
  - Hecho (2026-09-23, accept-prep): `rayito-base` **21.0** (Dockerfile con `git-core-2.50.1-1.amzn2023.0.1`, `publish_image.py --base-image-version 1`, 216,6 s) frente a 20.0: `codeInstallSizeInBytes` 1 360 146 432 − 1 321 267 200 = **+38 879 232 B** (git-core 31 452 146 B de RPM + `rayd` +1 230 744 B + ≈ 6,2 MB de resto de la instalación); `memorySnapshotSizeInBytes` 927 043 584 frente a 922 832 896 (`items[0]` de cada versión) = +4 210 688 B, dentro del ruido de ±13 MB de Q50 (nada se precalienta). Fila Q76 de `AWS_API_NOTES.md` §16.
- [x] 11.2 `clients/python/tests/e2e/e2b_corpus/{sync,async}/`: the nine programs of D23, each citing its docs.e2b.dev page and importing only `rayito.e2b`. `tests/e2e/test_m9_e2b_v2.py` runs them as subprocesses with `RAYITO_TEMPLATE`, `RAYITO_ACCESS_TOKEN`, `AWS_REGION` and `AWS_PROFILE` from the environment, asserts exit 0 and the live `fork()` `UnimplementedError`, and prints `corpus_ok` and the timings.
  Escrito, pendiente de ejecución en AWS (2026-09-23): `clients/python/tests/e2e/e2b_corpus/{sync,async}/` (9 + 9 programas, primera línea con su página de docs.e2b.dev, sólo `rayito.e2b` más stdlib) y `clients/python/tests/e2e/test_m9_e2b_v2.py` (18 subprocesos con un bootstrap que acota `max_lifetime` por defecto a 900 s, sweep por programa, `fork()` vivo, `corpus_ok`). Gates estáticos verdes; `--collect-only` sin `RAYITO_E2E` recoge y salta.
  - Hecho (2026-09-24, aceptación): tras el arreglo de `watch` (la primera pasada en 23.0 dio 23/26 con `sync/watch`, `async/watch` y el resumen en rojo), `test_m9_e2b_v2.py` + `test_m3_filesystem.py` **22/22** en `rayito-base` 24.0: `corpus_ok=18/18` (9 sync + 9 async, 6,6–12,9 s por programa, sweep ≤ 1 VM), `fork()` vivo → `UnimplementedError`, el corpus sólo importa `rayito.e2b`; M3 sigue verde (watch 0,44 s, REMOVE en 0,92 s). Re-corrida en 25.0 en curso.
- [x] 11.3 `tests/e2e/test_m9_git.py`: the D23 git sequence (public clone, local operations, bare push and pull, upstream error, auth error, `dangerously_authenticate` visibility, log hygiene), printing `git_version` and per-operation seconds.
  Escrito, pendiente de ejecución en AWS (2026-09-23): `clients/python/tests/e2e/test_m9_git.py` (fixture `sandbox`; `RAYITO_E2E_GIT_REPO` opcional; token falso y secreto aleatorios comprobados sin imprimirse contra la excepción, la URL del remoto, `git config` y `caplog`).
  - Hecho (2026-09-24): `test_m9_git.py` **1/1** en `rayito-base` 23.0: `git version 2.50.1`, clone depth=1 2,45 s, operaciones locales ≈ 0,11 s, push/pull a un bare 0,11/0,24 s, pull sin upstream y push sin credenciales con su error, token falso 0,47 s, `dangerously_authenticate` 0,21 s, sin el token ni el secreto en excepción, URL del remoto, `git config` ni logs.
- [x] 11.4 `tests/e2e/test_m9_cli_sandbox.py`: `create --detach --token-file`, `exec` (hi / exit 3), `connect` in pipe mode (every OS) and through `pty.fork()` (POSIX; skipped on win32 with the reason), `metrics --json`, and a `kill` teardown. Run once on Linux (the e2e workflow on `ubuntu-24.04-arm`, or WSL) so the pty scenario executes.
  Escrito, pendiente de ejecución en AWS (2026-09-23): `clients/python/tests/e2e/test_m9_cli_sandbox.py` (`sys.executable -m rayito.cli`, sin `RAYITO_ACCESS_TOKEN` en el entorno, `--timeout 900`, `kill` por CLI y `terminate-microvm` en teardown). Falta la corrida en Linux para el escenario `pty.fork()`.
  - Hecho (2026-09-24): `test_m9_cli_sandbox.py` **4/4** en `rayito-base` 23.0 sobre macOS arm64 (POSIX, así que el escenario `pty.fork()` se ejecutó, no se saltó): `create --detach` 7,90 s, `exec` hi exit 0 y `exit 3` → 3, `connect` en tubería 2,65 s y por PTY (prompt 2,29 s), `metrics --json`, `kill` 1,38 s. La corrida en Linux (`e2e.yml` en `ubuntu-24.04-arm`) llega con el PR.
- [x] 11.5 `clients/typescript/tests/e2e/e2b-corpus/*.mjs` and `m9-e2b.e2e.test.ts`: build, then run each program with `node` (self-reference `rayito/e2b`), including the `AbortController` case: rejects within 10 s, and the next `runCode("1+1")` answers `2`.
  Escrito, pendiente de ejecución en AWS (2026-09-23): `clients/typescript/tests/e2e/e2b-corpus/*.mjs` (8 programas) y `m9-e2b.e2e.test.ts` (`pnpm build` en `beforeAll`, `node` por programa, sweep por programa). Los programas no pueden bajar `maxLifetimeMs` sin una opción de Rayito: el límite efectivo es el plazo lógico de 300 s más el sweep. `pnpm lint`, `pnpm typecheck` y `pnpm build` verdes.
  - Hecho (2026-09-24): `m9-e2b.e2e.test.ts` **11/11** (8 programas `.mjs` con `node`, incluido el `AbortController` que rechaza en < 10 s y el siguiente `runCode("1+1")` da `2`) dentro de los 26/26 de TS sobre `rayito-base` 24.0.
- [x] 11.6 Run the M6 e2e `test_m6_e2b_compat.py::test_e2b_shim_cookbook` again against the M9 image, adjusted only where 2.x semantics changed (`pause` returns a bool). It stays green.
  Escrito, pendiente de ejecución en AWS (2026-09-23): `test_e2b_shim_cookbook` ajustado a 2.x (timeout 840 bajo `max_lifetime=900`, `end_at` con tolerancia de 60 s, `pause()` → `True`, `get_metrics()` serie, `set_timeout(300)` mapeado, `create_snapshot` en lugar de `upload_url`, `language="js"` → `UnimplementedError`).
  - Hecho (2026-09-24): `test_m6_e2b_compat.py` **2/2** (`test_e2b_shim_cookbook` ajustado a 2.x) dentro de la regresión e2e de Python de 2026-09-24 sobre `rayito-base` 23.0, `rayito-base-caps` 12.0 y `rayito-base-poly` 7.0: **62 passed de 62** (server-timeout 12, observability 4, deno 8, egress 14, transfer 14, M4 1, M5 2, M6 2, M7 poly 5).
- [x] 11.7 Add the `MILESTONES.md` M9 status line for this change with the corpus counts and the git-core row reference, using placeholders only.
  - Hecho (2026-09-24): línea de `m9-e2b-v2-surface` en el bloque M9 de `MILESTONES.md` con el corpus 18/18 + 8 de TS, git, CLI y la fila Q76.

## 12. [infra] Gates

- [x] 12.1 `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace --locked`.
  - Pendiente, paso de cierre de M9 (no se difiere, 2026-09-24): se corre sobre el árbol final cuando terminen los refactors sin cambio de comportamiento que están en curso en `crates/` y `clients/*/src`. Última pasada local completa (2026-09-23): Python 2090 passed, TypeScript 831 passed, sidecar 85 passed, `scripts/tests` 184, `check_hygiene`/`check_pins`/`check_license`/`buf lint` OK; faltan los gates de Rust (`cargo fmt`, `clippy -D warnings`, `cargo test --workspace --locked` en la VM Lima como uid 1500) sobre ese árbol.
  - Hecho (2026-09-24, cierre de M9, árbol final): Rust en la VM Lima (Ubuntu aarch64, `CARGO_TARGET_DIR` propio, `-j 2`): `cargo fmt --all --check` exit 0; `cargo clippy --workspace --all-targets --locked -- -D warnings` limpio; `cargo test --workspace --locked` con los binarios como uid 1500 (`setpriv --reuid=1500 --regid=1500 --clear-groups`): rayd lib 172, bin 5, m1 14, m2 25, m3 30, m4 19 (+1 ignored), m5_pty 15, m5_suspend_resume 7, m6_hooks 7, m6_imds 3, m6_limits 5, m7_poly 9, m9_deno 4, m9_egress 1, m9_metrics_history 6, m9_network 4, m9_timeout 11, m9_transfer 21, s3_store 0 (+1 ignored), rayd-core 498: 856 passed, 0 failed, 2 ignored; `m9_egress` como root en `unshare --net` con `RAYITO_REQUIRE_EGRESS_NETNS=1` → 1 passed; `cargo deny check` → advisories, bans, licenses, sources ok; `cargo auditable zigbuild --release --locked --target aarch64-unknown-linux-musl -p rayd` → `rayd` estático de 13 764 024 B y `check_auditable.py` → `.dep-v0: 257 packages, root rayd 0.2.0`.
- [x] 12.2 `cd clients/python && uv run pytest tests/unit && uv run ruff check . && uv run ruff format --check . && uv run mypy src tests`.
  Gate de Python (2026-09-23, macOS arm64, reintento de specs-docs): `uv run --with pytest-timeout pytest tests/unit --timeout 60` da 2090 passed en 321 s, exit 0; `ruff check .` limpio; `ruff format --check .` 198 ficheros (el `README.md` ya formateado); `mypy src tests` sin errores en 196 ficheros.
  - Hecho (2026-09-24, cierre de M9, árbol final): Python (macOS arm64): `uv run --with pytest-timeout pytest tests/unit -q --timeout 60` 2115 passed; `uvx ruff==0.16.7 check` limpio y `format --check` 219 ficheros; `mypy src tests` sin errores en 217 ficheros.
- [x] 12.3 `cd clients/typescript && pnpm lint && pnpm typecheck && pnpm test && pnpm build && pnpm pack:check`.
  Gate de TypeScript (2026-09-23, reintento de specs-docs): `pnpm lint` (biome, 123 ficheros, sin cambios), `pnpm typecheck` exit 0, `pnpm test` 38 ficheros / 831 passed, `pnpm build` exit 0 y `pnpm pack:check` exit 0.
  - Hecho (2026-09-24, cierre de M9, árbol final): TypeScript: `pnpm lint` (biome, 139 ficheros), `pnpm typecheck` exit 0, `pnpm test` 40 ficheros / 863 passed, `pnpm build` y `pnpm pack:check` exit 0.
- [x] 12.4 `buf lint`, `python scripts/check_pins.py`, `python scripts/check_license.py`, `python scripts/check_hygiene.py`.
  Verificado (2026-09-23, reintento de specs-docs): `buf lint` (1.73.0) exit 0; `check_pins.py` da OK sobre 8 ficheros (con la puerta de descargas endurecida y la de dnf); `check_license.py` da OK; `check_hygiene.py` da OK sobre 939 ficheros; `scripts/tests` 184 passed.
  - Hecho (2026-09-24, cierre de M9, árbol final): `buf lint` exit 0; `check_hygiene.py` OK (939 ficheros); `check_pins.py` OK (8 ficheros); `check_license.py` OK; `gen_limits.py --check` exit 0; `scripts/tests` 186 passed; `mkdocs build --strict` (target `docs` del `Makefile`) sin avisos.
- [x] 12.5 `openspec validate m9-e2b-v2-surface --strict --no-interactive`.
  Verificado (2026-09-23, reintento de specs-docs, openspec 1.10.0): `Change 'm9-e2b-v2-surface' is valid`; los otros cinco cambios M9 también.
  - Hecho (2026-09-24, cierre de M9, árbol final): `openspec validate <cambio> --strict --no-interactive` → valid para los seis cambios M9 (openspec 1.10.0).

## Notes

- (0.1) Native names shipped by the siblings (verificado 2026-09-23, reintento de specs-docs; el `grep` de 0.1 da 88 líneas sobre `sandbox_sync/main.py`, `_models.py` y `__init__.py`): Python `Sandbox.set_timeout(timeout, *, request_timeout=None)` y la forma de clase `Sandbox.set_timeout(sandbox_id, timeout, *, access_token=...)`; `connect(*, timeout=None, request_timeout=None)` (instancia) y `Sandbox.connect(sandbox_id, *, timeout=None, ...)`; `get_metrics_history(*, start, end, max_points, request_timeout)` y su forma de clase con `access_token`; `Sandbox.upload_url/download_url(path, user=None, use_signature_expiration=None)` sobre `files.upload_url/download_url`; `update_network`/`get_network`; `ALL_TRAFFIC`, `SandboxHealth.cpu_count`/`memory_total_bytes`, `SandboxInfo.agent_version`/`cpu_count`/`memory_mb`/`lifecycle`. TS: `setTimeout(timeoutMs)` y estático, `connect({ timeoutMs })`, `getMetricsHistory`, `uploadUrl`/`downloadUrl`, `updateNetwork`/`getNetwork`, `ALL_TRAFFIC`. Coinciden con D1: `info_from_native` y `src/e2b/compat.ts` no necesitaron adaptar ningún nombre.
- (0.2) Form of `rayito.e2b.UnimplementedError` (re-export or subclass) before this change: antes de este cambio era una **subclase** del nativo (`m9-egress-policy` 5.1); D13 la sustituye por la **re-exportación** (`rayito.e2b.UnimplementedError is rayito.UnimplementedError` da `True`, 2026-09-23). `rayito/exceptions.py` tiene `UnimplementedError(NotImplementedError)`, `TransferException(SandboxException)` y `FileUploadException(TransferException)`; `src/errors.ts` tiene `UnimplementedError`, `DiskFullError`, `TransferError` y `FileUploadError`.
- (3.8) Native instance `connect` signature (Python and TS):
  - Python sync: `Sandbox.connect(self, *, timeout: int | None = None, request_timeout: float | None = None) -> Sandbox` (`class_method_variant("_class_connect")`; la forma de clase es `Sandbox.connect(sandbox_id, *, timeout=None, ...)`). Orden: `get-microvm`, `resume-microvm` si está `SUSPENDED` sin auto-resume, sondeo de `Health`, `SetTimeout(AT_LEAST)`; devuelve `self`.
  - Python async: `AsyncSandbox.connect(self, *, timeout: int | None = None, request_timeout: float | None = None) -> AsyncSandbox`, misma semántica.
  - TS: `connect(options: InstanceConnectOptions = {}): Promise<Sandbox>` con `InstanceConnectOptions extends RequestOptions { timeoutMs?: number }` (`requestTimeoutMs` viene de `RequestOptions`), y `static connect(sandboxId, options: SandboxConnectOptions = {})`.
- (1.1) [rayd] This change leaves `crates/` and `proto/` untouched: every diff there belongs to the Contract step and to the sibling changes. On 2026-09-22 the box stayed open because the workspace gates were red while the siblings were still editing (superado: 1.1 se marcó el 2026-09-23 con los gates verdes en la VM Lima, ver su nota). The 2026-09-22 run gave:
  - `cargo fmt --check` red on `m9-server-timeout` files: `rayd/src/lifecycle/{mod,suspend,timeout_watcher}.rs`, `rayd/src/main.rs`, `rayd/tests/{common/mod,m9_timeout}.rs`.
  - `clippy -D warnings` red on `rayd-core/src/transfer/import.rs`.
  - `cargo test --locked` did not compile `rayd`: the `m9-file-transfer` metadata additions are not wired yet into `rayd/src/filesystem/{write,watch}.rs` and `rayd/src/grpc/filesystem.rs`.
  Re-run at 12.1 once the siblings are green.
- (2.1-2.3) `git-core-2.50.1-1.amzn2023.0.1` is in the first `dnf install` layer, and `git` is in the build-time tool loop. `m9-deno-kernels` had already landed the "descargas" gate as the third gate, so `check_pins.py` now documents four gates, "dnf" being the fourth. The findings count from the first line of the instruction, like the download gate, and quote the package token. The tests were written first and ran red (4 failed, 11 passed), then green (15 passed; 168 passed for the whole `scripts/tests`). `python scripts/check_pins.py` prints OK over 8 files including `image/Dockerfile`. A copy of the real Dockerfile with a bare `git-core` yields exactly one `KO ...:63: el paquete dnf no lleva -<versión>-<release>`. `test_the_image_installs_the_pinned_git_core_and_checks_git` pins both requirement lines of the real Dockerfile. 2.4 (NOTICE) quedó abierta ese día porque los ficheros git de Python y TS aún no existían; se cerró después (ver la evidencia de 2.4).
- **Cierre de M9 (2026-09-24)**: aceptación final contra AWS real sobre `rayito-base` 25.0, `rayito-base-caps` 14.0 y `rayito-base-poly` 9.0 (republicadas desde el `rayd` final): Python **52/52** de las suites afectadas (timeout, egress, observability, corpus E2B y M4), además de los **62/62** de la regresión completa anterior sobre 23.0/12.0/7.0; TypeScript **24/24** de las suites afectadas (timeout, egress, corpus, observability), además de los **26/26** anteriores sobre 24.0/13.0/8.0. Aceptación de paquete con instalación limpia (Python y TS, fuera del repo): subida y descarga de 50 MB por S3 con sha256 coincidente, y `set_timeout` → el sandbox acaba `TERMINATED`. Sólo marcadores de posición (`microvm-<id>`, sin cuenta, bucket ni ARN). Arreglo tardío del SDK de TypeScript (en `clients/typescript/CHANGELOG.md`, «Fixed»): al abortar el controller de un server-stream ya terminado o abandonado, el SDK pide una lectura más para que `@connectrpc/connect` 2.x suelte el timer (con ref) de su `timeoutMs`; antes el proceso de Node quedaba vivo hasta el deadline del stream (~315 s tras `runCode`, ~65 s tras `commands.run`). El deadline se sigue imponiendo mientras el stream vive; cubierto por los 24/24 de TS de arriba.
