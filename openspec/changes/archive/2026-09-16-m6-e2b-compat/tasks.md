## 0. [proto + rayd] Metadata echo in Health (design D1)

- [x] 0.1 `proto/rayito/v1/health.proto`: add `map<string, string> metadata = 11;` to `HealthResponse` (9 and 10 belong to `m6-hardening`) with the comment from design D1; `buf lint` and `buf build` clean; `buf breaking --against <pre-change proto>` reports nothing (additive); `python scripts/gen_python.py` regenerates `clients/python/src/rayito/v1/health_pb2.py` and `health_pb2.pyi`
- [x] 0.2 `crates/rayd-core/src/run_payload.rs`: `RunPayloadWire.metadata: BTreeMap<String, String>` with `#[serde(default)]`, `RunPayload.metadata`; tests: present, absent, empty, non-string value → `MalformedJson`, unknown extra keys still ignored *(implementado por el track de endurecimiento en el mismo `crates/`, que lo integró con su propio `limits`; verificado aquí: `metadata_is_optional_and_ignores_unknown_keys` verde)*
- [x] 0.3 `crates/rayd-core/src/session.rs`: store the map on the accepted `/run` (`metadata: Mutex<BTreeMap<String, String>>`), `SandboxSession::metadata()`, `health()` fills `HealthSnapshot.metadata`; `crates/rayd-core/src/health.rs`: `HealthSnapshot.metadata` + builder method; tests: empty before `/run`, echoed after, unchanged by a repeated `/run` *(idem, `session.rs::metadata_is_echoed_from_the_accepted_run_only` verde)*
- [x] 0.4 `crates/rayd/src/grpc/health.rs`: `to_response` copies the map into `HealthResponse.metadata`; `crates/rayd/src/hooks`: the `/run` log line adds `metadata_keys` (count only; `m1_hello.rs::health_echoes_the_run_payload_metadata_and_logs_only_the_count` captures the JSON log and proves the `/run` line carries `"metadata_keys":2` and no key or value is written); `crates/rayd/tests/m1_hello.rs`: `Health` before `/run` → empty map, after a `/run` payload with `"metadata":{"a":"1"}` → `{"a":"1"}` *(`grpc/health.rs` y `hooks` `metadata_keys` los llevó endurecimiento; `m1_hello.rs::health_echoes_the_run_payload_metadata_and_logs_only_the_count` añadido aquí, con `tests/common/log_capture.rs` compartido con `m6_hooks.rs`: 13 passed)*
- [x] 0.5 `cargo test --workspace` green on the Windows host, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings` clean; `cargo zigbuild --release --target aarch64-unknown-linux-musl -p rayd` still produces a static ARM64 binary (record the size next to M5's 4 536 248 B) *(2026-09-16: `cargo test --workspace` 335 passed; `cargo fmt --all --check` limpio; `cargo clippy --workspace --all-targets -- -D warnings` limpio; `cargo zigbuild` → `rayd` ARM64 estático (`e_machine` 183) **4 679 032 B**, M5: 4 536 248 B)*

## 1. [e2e] Acceptance test first (red)

- [x] 1.1 Create `clients/python/tests/e2e/test_m6_e2b_compat.py` with `test_e2b_shim_cookbook` (design D13 steps 1–12) and `test_no_egress_connector` (design D11/D13), marker `e2e`, the M1 guardrails (`create_test_sandbox`-style timing prints, sweeper, `timeout <= 1800`), timings printed as `kernel_ready_s`, `get_info_metadata_s`, `list_metadata_s`, `list_metadata_n`
- [x] 1.2 Confirm it fails at the import line (`rayito.e2b` does not exist yet) with the current tree; note the failure in the task *(el shim se implementó antes de escribir el e2e, así que el rojo en el import no se observó como tal; sin `RAYITO_E2E` el fichero se deselecciona limpiamente y `mypy` lo cubre)*

## 2. [sdk] Metadata surface (design D2)

- [x] 2.1 `_payload.py`: `build_run_hook_payload(metadata=)`, `validated_metadata`, `metadata` omitted when empty, budget error naming `envs` and `metadata`; `tests/unit/test_payload.py` cases from the design "Acceptance test list"
- [x] 2.2 `_models.py`: `SandboxHealth.metadata: dict[str, str]`, `SandboxInfo.metadata: dict[str, str] | None = None`, `SandboxListItem.metadata: dict[str, str] | None = None` (docstrings: `None` = not read from the agent); `_sandbox_base.py`: `health_from_proto` copies the map, `metadata_matches(candidate, wanted)`, `list_states_for_metadata(states)`, `metadata_from_health(response)`, `build_launch_plan(metadata=)`; `tests/unit/test_sandbox_base.py` cases
- [x] 2.3 `sandbox_sync/main.py`: `create(metadata=)`, `metadata` property fed by `_record_health` (every `Health`, not only generation changes), instance `get_info()` returning `metadata=self.metadata`, class `_class_get_info(sandbox_id, *, read_metadata=True, request_timeout=5.0, ...)` with the `RUNNING`-only JWE mint + `Health` probe over a dedicated channel closed afterwards, `_read_metadata(...)` helper, `list(metadata=)` per design D2 (sequential probes, `agent_ready=False` skipped with an info log naming only the sandbox id, state changes skipped, `Health` failure → `SandboxException` naming the id, `states` validation)
- [x] 2.4 `sandbox_async/main.py`: the same surface with `asyncio.to_thread` for boto3 and `grpc.aio` for the probe channel; `__init__.py` exports unchanged names (models already exported)
- [x] 2.5 `tests/unit/conftest.py`: `FakeRayd.metadata: dict[str, str]` echoed by `Health`; a helper to stand up a second fake `rayd` endpoint with its own metadata, and Stubber helpers for the `list → get-microvm → create-auth-token` sequence; `tests/unit/test_metadata_sync.py` and `test_metadata_async.py` covering every case in the design "Acceptance test list" (including "probe channel closed after each VM")
- [x] 2.6 `uv run pytest tests/unit` green; `uv run ruff check .`; `uv run ruff format --check .`; `uv run mypy src` clean

## 3. [sdk] `rayito.e2b` shim (design D3–D7)

- [x] 3.1 `src/rayito/e2b/_compat.py`: `RayitoCompatWarning`, `unimplemented(feature, reason) -> UnimplementedError`, `CreateMapping`, `map_create_kwargs(...)` implementing the design D4 table (`timeout` default 300, `idle=None`, `allow_internet_access` → `egress`, `ingress` default `["ALL_INGRESS"]`, one warning per ignored kwarg, `secure=False` warning), `sandbox_state_from_aws`, `info_from_native(info | list_item)`, `metrics_from_native`, `pty_size_to_native`, `states_for(state, query)`, `language_or_unimplemented(language, feature)`; no I/O; `tests/unit/test_e2b_compat_base.py`
- [x] 3.2 `src/rayito/e2b/exceptions.py`: re-exports + `NotEnoughSpaceException`, `TemplateException` (never raised; docstring), `UnimplementedError(NotImplementedError)` with `feature`/`reason` and the Spanish message from design D7; `src/rayito/e2b/_models.py`: `SandboxState`, `SandboxQuery`, `SandboxInfo` (+ `ListedSandbox` alias, `raw_state`), `SandboxMetrics` (E2B names, bytes), `PtySize(rows=24, cols=80)`, `SandboxPaginator`, `AsyncSandboxPaginator`
- [x] 3.3 `src/rayito/e2b/_sync.py`: `Sandbox` (constructor with E2B v1 kwargs + native pass-through, `create`, `connect`, `beta_create`, `list` → `SandboxPaginator`, `kill`/`get_info`/`set_timeout`/`get_metrics` instance + class variants via `class_method_variant`, `beta_pause`/`pause`, `upload_url`/`download_url`, `connection_config`, `sandbox_domain`, `get_host`, `run_code(language=)`, `create_code_context(language=)`, `native`, context manager), `Filesystem` (write overload, `watch_dir` positional `on_event` and `timeout=60`), `Pty` (E2B `PtySize` conversion on `create`/`resize`); every `UnimplementedError` site from design D5/D6
- [x] 3.4 `src/rayito/e2b/_async.py`: `AsyncSandbox`, `AsyncFilesystem`, `AsyncPty`, `AsyncSandboxPaginator` with the identical surface as coroutines / `async with`
- [x] 3.5 `src/rayito/e2b/__init__.py`: `__all__` with every name in the proposal's re-export list plus `Sandbox`, `AsyncSandbox`, `UnimplementedError`, `RayitoCompatWarning`, `Context`, `WriteInfo`, `ListedSandbox`, the chart classes; module docstring with the two import swaps
- [x] 3.6 `tests/unit/test_e2b_compat_sync.py` and `test_e2b_compat_async.py`: design D8 items 1–10 as cookbook snippets against `fake_rayd` + `control_plane` (Stubber assertions on the `run-microvm` request for `timeout`, `idlePolicy` absent, `ingressNetworkConnectors`, `egressNetworkConnectors` present/absent); `tests/unit/test_packaging.py::test_e2b_names_importable`
- [x] 3.7 `uv run pytest tests/unit` green; `uv run ruff check .`; `uv run ruff format --check .`; `uv run mypy src` clean

## 4. [gates] `mypy` over tests, lint and CI (design D9)

- [x] 4.1 `pyproject.toml`: `[tool.mypy] files = ["src", "tests"]`; the single scoped override `disallow_subclassing_any = false` for `tests.unit.conftest`, `tests.unit.fake_process`, `tests.unit.fake_filesystem`, `tests.unit.fake_code`, `tests.unit.fake_pty`; `_process_base.encode_stdin` accepts `str | bytes | bytearray`
- [x] 4.2 Fix the 34 errors per the design D9 table: 8 stale ignores removed, 9 unreachable statements restructured (`yield from (); raise` generators, reachable asserts), 6 `arg-type` (typed `**fields: Any`, `FilesystemEventType.ValueType`, `encode_stdin`), 2 `attr-defined` (isinstance narrowing), 2 `call-overload` (`# type: ignore[call-overload]` on the deliberate invalid literal), 2 `comparison-overlap` (`.value`); no new blanket ignores; test behaviour unchanged (429 + new tests still pass)
- [x] 4.3 `uv run mypy src tests` reports 0 errors; `Makefile` `lint` adds `cd clients/python && uv run mypy src tests` and `uv run ruff format --check .`; `.github/workflows/ci.yml` Python step runs `uv run pytest tests/unit`, `uv run ruff check .`, `uv run ruff format --check .`, `uv run mypy src tests`

## 5. [release] Packaging, changelog, README, wheel, workflow (design D10)

- [x] 5.1 `pyproject.toml`: `version = "0.1.0"`, classifiers, `keywords`, `[project.urls]` (repository URL confirmed against the root README or left as the documented placeholder with a note in CHANGELOG), `[dependency-groups] docs`; `_version.py` 0.1.0; `tests/unit/test_packaging.py::test_version_matches_pyproject` (reads `pyproject.toml` with `tomllib`)
- [x] 5.2 `clients/python/CHANGELOG.md` (Keep a Changelog, Spanish): `0.1.0` entry per design D10, `0.0.1–0.0.5` collapsed as unpublished milestone builds
- [x] 5.3 `clients/python/README.md` rewritten per design D10 (install, credentials, one example per surface, metadata + O(n) note, the E2B shim before/after, `AsyncSandbox`, limits/costs table, docs links); root `README.md` install line + shim snippet
- [x] 5.4 `scripts/check_wheel.py` (ruff-clean, `uvx ruff check scripts`): asserts `rayito/py.typed`, `rayito/e2b/__init__.py`, `rayito/v1/health_pb2.py`, `rayito/v1/health_pb2.pyi` present, nothing under `tests/`, `Requires-Python: >=3.11` and the three runtime deps in `METADATA`; `cd clients/python && uv build` then `python scripts/check_wheel.py clients/python/dist/*.whl` and `uvx twine check clients/python/dist/*` pass locally; `clients/python/.gitignore` already ignores `dist/`
- [x] 5.5 `.github/workflows/release.yml` per design D10 (`python-v*` tags + `workflow_dispatch`; `build` job with `uv build`, wheel check, `twine check`, tag/version assertion, artifact upload; `publish` job with `environment: pypi`, `id-token: write`, `pypa/gh-action-pypi-publish@release/v1`, `packages-dir: clients/python/dist`, only on tag push); `ci.yml` `check` job also runs `uv build` + `scripts/check_wheel.py`; nothing is published and no tag is pushed in this change
- [x] 5.6 Document the manual steps in `CHANGELOG.md` "Publicación" and the root README: register the PyPI Trusted Publisher (project `rayito`, workflow `release.yml`, environment `pypi`), create the `pypi` environment in GitHub, push `python-v0.1.0`

## 6. [docs] Site skeleton (design D12)

- [x] 6.1 `docs/site/mkdocs.yml` (material theme, `mkdocstrings` python handler with `paths: ["../../clients/python/src"]`, nav with the eight pages, `strict` build) and `docs/site/docs/{index,quickstart,concepts,api,security,limits,e2b-compat,cost}.md` with the content listed in design D12; every number on `cost.md` and `limits.md` cites its source file; `docs/site/_build` git-ignored
- [x] 6.2 `Makefile` target `docs` (`cd clients/python && uv run --group docs mkdocs build -f ../../docs/site/mkdocs.yml --strict --site-dir ../../docs/site/_build`); `ci.yml` job `docs` running it on `ubuntu-24.04`; the build passes locally with `--strict` (no broken links, mkdocstrings resolves `rayito.Sandbox`, `rayito.AsyncSandbox`, `rayito.e2b`)

## 7. [e2e] Acceptance against real AWS and docs alignment (design D11, D13, D15)

- [x] 7.1 Image: confirm the `rayito-base` version used for acceptance carries the section 0 `rayd` (publish a new version with `make image-publish` steps by hand if no other M6 track has); record the version *(ninguna versión de `rayito-base` con el echo existía; `rayito-base-slim` 1.0 lo devolvía vacío (sondeado). Publicada **`rayito-base-e2b` 1.0** desde el árbol actual con `--image-name` para no tocar la numeración de `rayito-base` del track de endurecimiento: build 195.5 s, memoria 930 734 080 B, artefacto `rayd-ffa32c613313.zip`)*
- [x] 7.2 `RAYITO_E2E=1 RAYITO_TEMPLATE=<arn> uv run pytest tests/e2e/test_m6_e2b_compat.py -m e2e -v -s` green: `test_e2b_shim_cookbook` steps 1–12 and `test_no_egress_connector`; record `kernel_ready_s`, `get_info_metadata_s`, `list_metadata_s`/`list_metadata_n`, the pause/resume timings and the Q42 outcome *(2 passed en 27.65 s, dos pasadas; `kernel_ready_s` 11.29 / 10.54, `get_info_metadata_s` 0.66 / 0.61, `list_metadata_s` 0.77 / 0.76 con `list_metadata_n` = 1, pause 1.01 / 1.04, connect tras pausa 0.79 / 0.78; Q44: internet reachable sin conector)*
- [x] 7.3 If Q42 shows outbound access without a connector: switch `map_create_kwargs` to raise `UnimplementedError` for `allow_internet_access=False` (design D11), flip its unit test, rerun 7.2 *(aplicado: la medición dio salida a internet sin conector (hereda el de la versión de imagen) → `map_create_kwargs` lanza `UnimplementedError("allow_internet_access=False")`, tests unitarios volteados, e2e reescrito para afirmar el `UnimplementedError` y repetir la medición con el SDK nativo)*
- [x] 7.4 `AWS_API_NOTES.md` §16: Q42 row and the `list(metadata=)`/`get_info` probe timings; `MILESTONES.md` M6: Track C state paragraph (numbers, cost, image version); `SPEC.md` §3/§4/§5, `ARCHITECTURE.md` (payload example, Python layout, Health row), `SECURITY.md` (T4/T9 metadata, shim default connectors) updated per design D15 *(filas **Q44** y **Q45** de §16: Q42/Q43 ya los había tomado el benchmark; `MILESTONES.md` M6 Track C; `SPEC.md` §3/§4/§5; `ARCHITECTURE.md`; `SECURITY.md` T4/T9 + nota del shim)*
- [x] 7.5 Full gate run: `buf lint`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace`, `cd clients/python && uv run pytest tests/unit && uv run ruff check . && uv run ruff format --check . && uv run mypy src tests`, `uvx ruff check scripts`, `uv build` + `scripts/check_wheel.py` + `uvx twine check`, `make docs` equivalent; every MicroVM launched by 7.2 is `TERMINATED` (`Sandbox.list(states=[...])` sweep) *(`buf lint`, `cargo fmt/clippy/test`, `uv run pytest tests/unit` 610 passed, `ruff check`, `ruff format --check`, `mypy src tests` 0 errores (`Success: no issues found in 81 source files`, tras tipar `payload_dict` en `test_m6_hardening_sync.py`), `uvx ruff check scripts`, `uv build` + `check_wheel.py` OK + `twine check` PASSED, `mkdocs build --strict` OK; `list-microvms`: los 6 MicroVMs lanzados `TERMINATED`)*
- [x] 7.6 `openspec validate m6-e2b-compat --strict` passes; hand over to the acceptance agent for `openspec archive m6-e2b-compat --yes` *(`openspec validate m6-e2b-compat --strict` → valid; el archivado queda para el agente de aceptación)*
