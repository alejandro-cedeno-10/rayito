## 1. [proto] Contract (no edits)

- [x] 1.1 Confirm `filesystem.proto`/`common.proto` need no change against design D1 (fields used: `ReadRequest/ReadResponse`, `WriteRequest{path?, user?, mode?, chunk}`, `WriteResponse.entries`, `StatRequest/StatResponse`, `ListDirRequest.depth`, `MakeDirRequest`, `MoveRequest`, `RemoveRequest.recursive`, `WatchDirRequest{recursive, include_entry}`, `WatchDirResponse{started|filesystem|keepalive}`, `EntryInfo`, `FileType`, `FilesystemEventType`); `buf lint` and `buf build` clean; no regeneration

## 2. [e2e] Acceptance test first (red)

- [x] 2.1 Create `clients/python/tests/e2e/test_m3_filesystem.py` with the 18 blocks of `design.md` "Acceptance test list" (marker `e2e`, `sandbox` fixture, `BASE = "/home/user/m3"`, timing helpers reused from M2, `wait_for_terminal_state` from M1)
- [x] 2.2 Create `clients/python/tests/e2e/test_m3_bench.py` (marker `bench` only): 50 MB write + read, sha256 equal, total ≤ 120 s, MB/s per direction logged
- [x] 2.3 `tests/e2e/conftest.py`: `pytest_collection_modifyitems` also skips `bench` items when `RAYITO_E2E` is unset; `Makefile` gains `test-bench` (`uv run pytest tests/e2e -m bench -v -s`)
- [x] 2.4 Run `RAYITO_E2E=1 RAYITO_TEMPLATE=<arn> uv run pytest tests/e2e/test_m3_filesystem.py -m e2e -x` against the M2 image (3.0) and confirm it fails at block 1 with `InvalidArgumentException` (`UNIMPLEMENTED`) — the SDK side must already exist for this, so run it after section 6 if the client is not ready yet (superseded as in M2 2.3: the SDK cannot pin `imageVersion` and 4.0 was published before the acceptance run; the `UNIMPLEMENTED` contract of the pending service is covered by `crates/rayd/tests/m1_hello.rs` (`PtyService.Resize`) and the unit suites)

## 3. [rayd] Domain in `rayd-core`

- [x] 3.1 `rayd-core/src/filesystem/{mod,path,entry,identity,listing,write,events,ops,ports,error}.rs` per design D4; `lib.rs` exports `filesystem`; constants `READ_CHUNK_BYTES`, `MAX_WRITE_CHUNK_BYTES`, `DEFAULT_FILE_MODE`, `DEFAULT_DIR_MODE`, `MODE_MASK`, `TEMP_PREFIX`, `MAX_LIST_ENTRIES`
- [x] 3.2 `path.rs`: `RequestPath::parse(raw, home)` (empty/NUL/`..` refused, relative → home, `//` and `.` normalised, trailing slash), `parent()`, `name()`, `join()`; `DenyList` with `DEFAULT_DENIED_PREFIXES` + `RAYD_BINARY` + extra entries, component-wise `check()`; unit tests incl. `/etcetera`
- [x] 3.3 `entry.rs`: `EntryKind`, `RawEntry`, `Entry`, `permissions_string` (10-char `ls -l` form), `NameCache` over `NameResolver`, `build_entry` with numeric fallback; tests
- [x] 3.4 `error.rs`: `FilesystemError` (thiserror) with every variant of D4 and a test that no `Display` string quotes input; `ports.rs`: `FileSystem`, `WriteSink`, `Watcher`, `WatchSubscription`, `NameResolver`, `FsIoError`, `WatchError`
- [x] 3.5 `listing.rs`: `effective_depth`, `ListingLimits`, `walk_listing` (DFS pre-order, byte-sorted names, depth rule `d <= depth`, no descent into symlinks or denied directories, `lstat` races skipped, `TooManyEntries`); tests against `FakeFileSystem`
- [x] 3.6 `write.rs`: `WriteMessage`, `WriteSession::{accept, finish}`, `WriteStep`; tests for missing path, path switch → `[Commit, Begin]`, empty first chunk, `ChunkTooLarge`, mode default/`InvalidMode`, `UserWithoutPath`, `NoFiles`
- [x] 3.7 `events.rs`: `WatchEventKind`, `WatchEvent`, `RawWatchKind`, `RawWatchEvent`, `WatchTranslator::translate` (relative names, outside-root dropped, `TEMP_PREFIX` dropped, kind mapping incl. `RenameBoth` → two events, `RootGone`/`QueueOverflow` → `End`, `Other` → `Ignore`); tests
- [x] 3.8 `ops.rs`: `FilesystemOps` (`stat`, `list_dir`, `make_dir`, `rename`, `remove`, `prepare_read`, `write_target`, `prepare_watch`) applying parse → canonical → deny → port per D2/D7; `FakeFileSystem` test double (tree, symlink resolution in `canonicalize`, permission flag); end-to-end host tests for every rule in design D12 "rayd-core"
- [x] 3.9 `identity.rs`: `FsIdentity{uid, gid, home}` from `ProcessIdentity`; `FilesystemOps`/manager identity rules reuse `resolve_username`, `UserPolicy::authorize`, `authorize_identity`, `UserLookup` (no duplication of M2 logic); tests
- [x] 3.10 `cargo test -p rayd-core` green on the Windows host; `cargo clippy -p rayd-core --all-targets -- -D warnings` clean

## 4. [rayd] Adapters, application layer and gRPC in `rayd`

- [x] 4.1 `Cargo.toml`: workspace `notify = { version = "=8.<latest>", default-features = false }`; `rayd` `[target.'cfg(unix)'.dependencies]` gains `notify` and `tempfile`; `cargo tree -p rayd --target aarch64-unknown-linux-musl` shows no macOS backend or `crossbeam-channel`
- [x] 4.2 `adapters/fs_identity.rs`: `FsIdentityGuard::enter(IdentitySwitch, &FsIdentity)` (`cfg(unix)`: `setfsgid` then `setfsuid`, read-back check, restore in `Drop`; `KeepCurrent` and non-unix: no-op); unit test that the guard restores on drop
- [x] 4.3 `adapters/std_filesystem.rs` (`cfg(unix)`): `StdFileSystem` implementing `FileSystem` over `std::fs` with the guard in every method; `open_read` with `O_NOFOLLOW | O_CLOEXEC` + `fstat` regular-file check (`ELOOP` → `IsSymlink`, `EISDIR` → `IsADirectory`, FIFO/device → `NotARegularFile`); `begin_write` creating missing parents (0o755, from the deepest existing ancestor) + `tempfile::Builder::prefix(".rayito-tmp-").tempfile_in(parent)`; `TempWriteSink` (`write_chunk`, `commit` = `sync_all` → `fchmod` → `fchown` (`Enforce`) → `persist` → parent `fsync` → `lstat`; `Drop` unlinks); `make_dir` (`mkdir -p`, `AlreadyExists` only for the final component), `rename`, `remove` (`unlink`/`rmdir`/`remove_dir_all`); errno → `FsIoError` mapping incl. `EXDEV`, `ENOSPC`; `UnsupportedFileSystem` for `cfg(not(unix))`
- [x] 4.4 `adapters/notify_watcher.rs` (`cfg(unix)`): `NotifyWatcher` implementing `Watcher` with `RecommendedWatcher`, `Config::default().with_follow_symlinks(false)`, the watcher created inside the identity guard (notify's thread inherits the fsuid) and one `RecursiveMode::NonRecursive` watch per directory of the domain's walk, `raw_kind(notify::EventKind)` per design D8 (`Access` ignored, `Rescan` → `QueueOverflow`, `Create(Folder)` → `DirectoryCreated`, untracked `Name(From)` → `MovedSelf`, root path → `RootGone`), `NotifySubscription` (`Mutex<RecommendedWatcher>`, `add_directory`) dropping the watcher; `ENOSPC` → `LimitReached`; `UnsupportedWatcher` otherwise
- [x] 4.5 `adapters/name_resolver.rs`: `NixNameResolver` (`User::from_uid`, `Group::from_gid`) and `NumericNameResolver`; `adapters/mod.rs` exports `PlatformFileSystem`, `PlatformWatcher`, `PlatformNameResolver`
- [x] 4.6 `filesystem/manager.rs`: `FilesystemManager`, `FilesystemSettings` (list limits, `watch_queue_capacity` 1024, `max_watches` 64, drain 1 MiB / 2 s), `identity()`, `stat`/`list_dir`/`make_dir`/`rename`/`remove` via `spawn_blocking(FilesystemOps)`, `live_watches()`
- [x] 4.7 `filesystem/read.rs`: `read()` = `prepare_read` then a blocking reader task filling 256 KiB buffers into `mpsc(4)`; EOF → end, read error → trailing `INTERNAL`; receiver drop stops the reader
- [x] 4.8 `filesystem/write.rs`: `write()` driving `WriteSession` over the tonic `Streaming` (`Begin` → `write_target`, first chunk written, `Append`, `Commit` → `Entry`), `finish()` at end of stream, per-file atomicity, `drain_after_error` (≤ 1 MiB / ≤ 2 s) before returning any error, temp unlinked on cancel (sink `Drop`)
- [x] 4.9 `filesystem/watch.rs`: `watch_dir()` = phase gate → cap → `prepare_watch` (recursive walk under the identity) → channel + overflow flag → `Watcher::watch` → `WatchStream` (`WatchStarted` first; field order receiver → translator → resolver → subscription → counter guard; `try_send` sink with the overflow flag; `include_entry` `lstat` under identity; the pump follows a new directory of a recursive watch through `is_watchable_directory` + `WatchSubscription::add_directory`; `RootGone` → trailing `NOT_FOUND`; overflow → buffered events then trailing `RESOURCE_EXHAUSTED`)
- [x] 4.10 `grpc/filesystem.rs`: `FilesystemGrpc` implementing all eight RPCs, proto ↔ domain conversion (`EntryInfo`, `FileType`, `FilesystemEvent`, `FilesystemEventType`), status table of design D10, `KeepAliveStream` on `WatchDir` with `StreamSettings.watch_keepalive_interval` (default 50 s); error messages never echo a path
- [x] 4.11 `grpc/mod.rs`: `router(session, processes, files, metrics)` / `router_with_settings`, `StreamSettings.watch_keepalive_interval`; remove `PendingFilesystemService` from `pending.rs`; `main.rs`: `DenyList` (defaults + canonical `current_exe`), `StdFileSystem`, `NotifyWatcher`, `NixNameResolver`, `FilesystemManager` with the same `UserPolicy` and `IdentitySwitch` as processes; `logging.rs` allowlist doc extended (`rpc`, `files`, `bytes`, `chunks`, `entries`, `depth`, `recursive`, `events`, `live_watches`, `duration_ms`, `errno`, `outcome`)
- [x] 4.12 Adapt `crates/rayd/tests/m1_hello.rs` and `m2_process.rs` to the new `router` signature; move the "pending service answers `UNIMPLEMENTED`" probe from `FilesystemService.Stat` to `PtyService.Resize`
- [x] 4.13 `crates/rayd/tests/m3_filesystem.rs` (`#![cfg(unix)]`): every case of design D12 "rayd integration" (read chunking/empty/missing/dir/symlink/FIFO/denied/denied-through-symlinked-parent, recursive watch never follows symlinks, root-only: recursive watch as `user` skips root-only directories and never reports `/etc` through a symlink, write single/multi/oversized chunk/no path/empty stream/symlink destination/dir destination/parents/denied/cancel-cleans-temp, stat, listdir depth+order+cap+symlink, makedir, move, remove, watch started-first/early-event/kinds/recursive/include_entry/rename-on-atomic-write/missing/file/drop-frees-watch/cap/keepalive/overflow/root-gone, phase gate, root refused); identity cases `#[ignore]` unless root; settings injected via `FilesystemSettings`/`StreamSettings`
- [x] 4.14 `cargo test --workspace` green on Windows (unix suite compiled out) and inside the Docker `rust:1.98-slim-bookworm` loop as root (`m3_filesystem` incl. the ignored identity cases); `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings` clean on both
- [x] 4.15 `cargo zigbuild --release --target aarch64-unknown-linux-musl -p rayd` produces a statically linked `rayd` with `notify` (record the binary size next to M2's 3 429 831 B)

## 5. [sdk] Python client

- [x] 5.1 `_models.py`: `FileType`, `EntryInfo`, `FilesystemEventType`, `FilesystemEvent`, `WriteEntry`; export from `rayito/__init__.py` together with `WatchHandle`/`AsyncWatchHandle`
- [x] 5.2 `_filesystem_base.py`: constants, `file_request_deadline`, `coerce_data`, `validate_path`/`validate_mode`/`validate_depth`, `build_write_requests` (re-creatable generator, `path`/`user`/`mode` only on a file's first message, 1 MiB chunks, empty chunk for empty data), `entry_info_from_proto`, `file_type_from_proto`, `filesystem_event_from_proto`, `decode_text` (strict), `WatchState`, `watch_failure`
- [x] 5.3 `sandbox_sync/main.py`: `files` property, `_files` stub on the unary channel, `_files_stream` on the lazy stream channel, `_stub(service, *, stream)` generalisation used by `_open_stream`, `close()` stops live watch handles; remove the `files` `NotImplementedError` stub
- [x] 5.4 `sandbox_sync/filesystem.py`: `Filesystem` (`read` overloads text/bytes/stream with `Stat` first and the size-based deadline, `write`, `write_files`, `list`, `exists`, `get_info`, `remove(recursive=True)`, `rename`, `make_dir -> bool` via `ALREADY_EXISTS`, `watch_dir`) and `WatchHandle` (`path`, `is_running`, `get_new_events`, `stop`, context manager; daemon consumer thread; `on_event`/`on_exit`; keepalive ignored; `timeout` → gRPC deadline → `TimeoutException`)
- [x] 5.5 `sandbox_async/main.py` and `sandbox_async/filesystem.py`: `AsyncFilesystem`/`AsyncWatchHandle` with the same surface over `grpc.aio` (`AsyncIterator[bytes]` for `format="stream"`, `asyncio.Task` consumer, `await stop()`, `async with`)
- [x] 5.6 `tests/unit/fake_filesystem.py`: `FakeFilesystemService` (in-memory tree seeded with `/home/user`, `/tmp`, `/etc/passwd`; deny `/etc`, `/usr`; 256 KiB `Read`; `Write` state machine with the 1 MiB rule; `ListDir` depth walk; `MakeDir`/`Move`/`Remove` statuses; `WatchDir` with `WatchStarted`, one `keepalive`, `push_event`, `end_watch`; token check and client-port recording on every RPC); register it in the `fake_rayd` fixture as `RaydEndpoint.filesystem`
- [x] 5.7 `tests/unit/test_filesystem_base.py`: deadline math, `coerce_data`, chunking, proto conversions, `decode_text`, `WatchState`, `watch_failure`
- [x] 5.8 `tests/unit/test_files_sync.py` and `test_files_async.py`: every case of design D12 "Python unit" (read formats, directory refused before `Read`, write/write_files, list depth, exists, get_info, remove both modes, rename, make_dir True/False, denied path → `AuthenticationException(proxy_rejected=False)`, watch_dir blocking/events/callback/stop/terminal error/on_exit/timeout, 403 re-mint on `Write` with the Stubber expectation, channel usage, `close()` stops handles)
- [x] 5.9 `uv run pytest tests/unit`, `uv run ruff check .`, `uv run ruff format --check .`, `uv run mypy src` all clean; `pyproject.toml` version bumped to `0.0.3`

## 6. [image] Dockerfile and publish

- [x] 6.1 `image/Dockerfile`: sanity `RUN` extended with `sha256sum stat rm mv mkdir chmod touch ln`; header comment updated (M3); no `RAYITO_ALLOW_ROOT`; `rayd` still `CMD` as root
- [x] 6.2 `cargo zigbuild` + `python scripts/image_zip.py image image/rayito-image.zip` + `scripts/publish_image.py` → new `rayito-base` version `SUCCESSFUL` + `ACTIVE`; record `snapshotBuild` sizes and the zip size in the change notes (2026-09-15: `rayd` 3 868 664 B static ARM64 (M2: 3 429 831 B), zip `rayd-6c2ce20c53bf.zip` 3 870 613 B, `update-microvm-image` → version **4.0** `UPDATED` + `SUCCESSFUL` + `ACTIVE` in **123.9 s**; `snapshotBuild` memory 572 235 776 B, code install 436 166 656 B, disk 23 502 848 B; `chipsetGeneration` 3; hooks unchanged)
- [x] 6.3 Local loop before publishing: the `cfg(unix)` suite under Docker (4.14) plus a manual `Write`/`Read`/`WatchDir` round trip with the SDK against `rayd` on `127.0.0.1:8080` (`TransportSettings` local, `python scripts/hooks-sim.py --skip-terminate`) or, without a Linux host, the Docker run of the integration suite as the gate

## 7. [e2e] Green and close

- [x] 7.1 `RAYITO_E2E=1 RAYITO_TEMPLATE=<arn> make test-e2e`: `test_m1_hello.py`, `test_m2_processes.py`, `test_m3_filesystem.py` pass; `make test-bench` once; paste timings (8 MB write/read MB/s, 50-file batch, `WatchStarted` latency, event latency, `Stat` p95, 50 MB bench) into `MILESTONES.md` M3 "Estado de aceptación" (2026-09-15, image 4.0: `pytest tests/e2e -m e2e` 3 passed in 99.95 s; write 8 MB 12.37 s / 0.65 MB/s, read 8 MB 1.19 s / 6.71 MB/s, 50-file batch 0.11 s, `WatchStarted` 0.38 s, create → REMOVE 0.91 s, 30 `exists` p95 0.096 s; `-m bench` 1 passed in 90.40 s: write 50 MB 74.01 s / 0.68 MB/s, read 50 MB 11.71 s / 4.27 MB/s, total 85.72 s)
- [x] 7.2 `AWS_API_NOTES.md` §16: new rows for upload throughput through the proxy (client-stream) and for the D6 drain vs Q29 on an early `PERMISSION_DENIED`; §7 updated if the drain changes the picture (Q32 upload window; Q33 measured with 12 denied `Write`s: 1 B → `PERMISSION_DENIED` 6/6, 4 MB → `RST_STREAM(CANCEL)` 6/6 because the drain stops at 1 MiB / 2 s; follow-up noted for M6)
- [x] 7.3 Docs: `ARCHITECTURE.md` `FilesystemService` row and adapter names (`StdFileSystem`, `FsIdentityGuard`, `NotifyWatcher`, `NixNameResolver`), domain table rows `filesystem`/`watch`; `MILESTONES.md` M3 snippet; `SECURITY.md` row T11 (M3 ✔); `README.md` example if it shows `files.write`
- [x] 7.4 `openspec validate m3-filesystem --strict` passes; acceptance agent runs `openspec archive m3-filesystem --yes`
