## 0. [prepare] Ground truth, baseline and IAM

- [x] 0.0 Check the live numbering before writing anything: the next free `AWS_API_NOTES.md` §16 row and `SECURITY.md` threat row (design Context: `m7-suspended-pool` also claims Q53/T14); if taken, use the next free numbers everywhere this change says Q53/Q54/T14 and note the mapping in the task note; confirm whether `m7-poly-kernels` already archived its `e2b-compat`/`typescript-sdk` MODIFIED text and rebase this change's two MODIFIED deltas on the archived requirement if so
- [x] 0.1 `AWS_API_NOTES.md` §17 "S3 desde el MicroVM": the D2 table (operation, every parameter sent, purpose) with the Amazon S3 API Reference URL per operation and the sentence that these are the only S3 parameters `rayd` may send; §16 rows **Q53** (VM→S3 throughput) and **Q54** (Rust SDK IMDS provider against `execution_role`) added with the "Medida" column empty
- [x] 0.2 Baseline measurement (design D6 step 1): clean `cargo auditable zigbuild --release --locked --target aarch64-unknown-linux-musl -p rayd` from an empty `target-linux/` → wall time `T0` and `ls -l` size `S0` in the task note (expected 4 700 984 B)
- [x] 0.3 `spike/m0/iam.yaml` per design D11 (`PersistenceBucket`, `PersistencePrefix` with pattern, `HasPersistenceBucket`, conditional `persistence` policy on `ExecutionRole` with the four actions and the `s3:prefix` condition; caller policy untouched); `uvx cfn-lint==1.56.3 spike/m0/iam.yaml` and `aws cloudformation validate-template` clean; `Makefile` `infra-lint` lints it; `infra/README.md` gains the parameters, the lifecycle-rule and SSE/KMS notes and the VPC-endpoint note
- [x] 0.4 [AWS, maintainer] Update stack `rayito-m0-iam` with `PersistenceBucket=<bucket>` and `PersistencePrefix=rayito-e2e` (`CAPABILITY_NAMED_IAM`); `aws iam simulate-principal-policy` for `s3:PutObject` on `arn:aws:s3:::<bucket>/rayito-e2e/x/home.tar.gz` (allowed) and on `arn:aws:s3:::<bucket>/rayito/x` (denied) in the task note

## 1. [proto] FilesystemService.Checkpoint / Restore

- [x] 1.1 `proto/rayito/v1/filesystem.proto`: the two RPCs and the messages of design D1 exactly (names, field numbers, comments in Spanish); `buf lint` clean without touching `buf.yaml`; `buf breaking --against <copy of the pre-change proto dir>` clean (command and output in the task note)
- [x] 1.2 `make proto` equivalent (`buf generate`): regenerated `clients/python/src/rayito/v1/filesystem_pb2.py`, `filesystem_pb2.pyi`, `filesystem_pb2_grpc.py` and `clients/typescript/src/gen/rayito/v1/filesystem_pb.ts`; `cargo build -p rayito-proto` regenerates the Rust side; no hand edits to generated files
- [x] 1.3 `limits.json` gains `PERSIST_KEY_PREFIX_MAX_BYTES 900`, `PERSIST_EXCLUDE_MAX 64`, `S3_BUCKET_NAME_MIN 3`, `S3_BUCKET_NAME_MAX 63`, `DEFAULT_PERSIST_TIMEOUT_SECONDS 600`; `python scripts/gen_limits.py` regenerates `_limits.py` and `limits.ts`; `--check` passes

## 2. [rayd-core] Persistence domain

- [x] 2.1 `crates/rayd-core/src/persistence/keys.rs`: `BucketName` and `KeyPrefix` parsers with the D2 rules, `ObjectKeys{archive, manifest}`; unit tests for every rule (leading/trailing slash, `..`, `//`, charset, length, bucket IPv4 literal)
- [x] 2.2 `persistence/plan.rs`: `IgnoreRules` (the fixed D3 list: name components and relative paths), `ExcludeList::parse` (`PERSIST_EXCLUDE_MAX`, `RequestPath` rules, whole-component prefix match), `ArchivePlan{identity, excludes}`, `should_skip`; tests including `data/raw` vs `data/raw2` and `.rayito-tmp-` prefixes at depth
- [x] 2.3 `persistence/manifest.rs`: `Manifest` v1 (serde, unknown keys ignored, `validate()` rejects `version != 1` and an empty `sha256`); golden round trip test
- [x] 2.4 `persistence/progress.rs`: `Counters` (atomic `files_done`, `bytes_read`, `bytes_uploaded`, `bytes_downloaded`, `bytes_written`, `skipped`) with `snapshot()`; `persistence/error.rs`: `PersistenceError` → (`tonic`-free) `StatusKind` before `started` and `StreamError` code after, per the D8 table, messages fixed and path-free (test asserts no `/` in any message)
- [x] 2.5 Ports `HomeArchiver`, `ObjectStore`, `PartSource` (design D7 signatures) and `persistence/checkpoint.rs` / `restore.rs`: the event state machines (`Started → Progress* → Done | Error`), `PersistenceGate` lease with a `Drop` guard, progress sampling rule (≤ 1/s, only on change); tests with an in-memory `ObjectStore` and a fake `HomeArchiver` covering success, store failure after `started` (abort observed), cancellation, busy gate, manifest-missing → `not_found` before `started`, checksum mismatch → `internal`
- [x] 2.6 `cargo test -p rayd-core` green on Windows; `cargo clippy --workspace --all-targets -- -D warnings` clean; `rayd-core/Cargo.toml` unchanged (no new dependency)

## 3. [rayd] Adapters, gRPC and build (design D6 ladder)

- [x] 3.1 Workspace pins of design D6 (`aws-config`, `aws-sdk-s3`, `tar`, `flate2` with the listed features) in `Cargo.toml`, added to `crates/rayd/Cargo.toml`; `cargo deny check` → four `ok` (or a named exception with the crate, licence and reason in the task note, never a widened allowlist)
- [x] 3.2 [D6 steps 2–4] `cargo auditable zigbuild --release --locked --target aarch64-unknown-linux-musl -p rayd` with a compiled-in `S3ObjectStore`: record the rung that linked (`aws-lc-rs` | `ring` | fallback D6b), the knobs tried, the link error text if any, `T1` and `S1`, `cargo bloat -n 15` when `S1 > 3 × S0`; if D6b is reached, stop here, do sections 3b/3c of the fallback (below) instead of 3.3–3.5, and record the reason in the task note
- [x] 3.3 `crates/rayd/src/adapters/s3_store.rs`: `S3ObjectStore` per design D2/D5/D7 (IMDS-only provider, region resolution, `BehaviorVersion::latest()`, `get`, `put`, `put_multipart` with sequential 8 MiB parts, `ETag` bookkeeping, `CompleteMultipartUpload`, `AbortMultipartUpload` on every error/drop path within 5 s, `PutObject` for a single part, error classification into `StoreError{kind, s3_error_code, request_id}`); `#[ignore]` test `crates/rayd/tests/s3_store.rs` round-tripping 20 MiB against `RAYITO_PERSIST_BUCKET` with the `default` credential chain (run from WSL2, output in the task note)
- [x] 3.4 `crates/rayd/src/adapters/tar_archiver.rs`: `TarHomeArchiver` per design D3/D4 (`FsIdentityGuard`, sorted DFS, `st_dev` check, kinds, capped/padded reader, symlinks verbatim, gzip level 1, sha256; restore via `GzDecoder` + `tar::Archive`, accepted kinds only, `unpack_in` semantics, mode mask `0o777`, overwrite, mtime, `ENOSPC` → disk full); `cfg(unix)` tests on a `tempfile` tree: ignore list, exclude, unreadable file skipped, live shrink padded, FIFO skipped, symlink round trip, crafted archive with `..`, an absolute path and a device entry (refused/skipped), setuid mode masked, overwrite of an existing file and of a symlink at the path
- [x] 3.5 `crates/rayd/src/grpc/persistence.rs` + delegation in `grpc/filesystem.rs`: the two RPCs on the stream side (`SuspendableStream`, `KeepAlive`, `x-access-token`), the D7 pipelines (blocking thread ↔ bounded channels), cancellation and abort, the D8 status/`StreamError` mapping, the D13 log fields; `main.rs` wiring with `--persistence-credentials imds|default` (default `imds`); unit tests with the fake ports through tonic's in-process channel: event order, `FAILED_PRECONDITION` when busy, `PERMISSION_DENIED` without credentials, `NOT_FOUND` on missing manifest, client cancel aborts
- [x] 3.6 `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace` (WSL2 for the `cfg(unix)` suites), the auditable build + `scripts/check_auditable.py`; `T1`/`S1` final values in the task note

### 3b. [rayd, only if D6b] Fallback helper as uid 1000

- [x] 3b.1 (not needed: D6 rung 1 linked; closed as not applicable) `kernel-sidecar/src/rayito_kernel_sidecar/persist.py` (`checkpoint`/`restore` subcommands, D3/D4 rules with `tarfile` + `gzip` + `boto3`, JSON-lines events matching D1, fully typed), `boto3` pinned in `kernel-sidecar/requirements.txt` and `pyproject.toml`; host tests with a fake S3 client covering the same cases as 3.4; `ruff`/`mypy` clean
- [x] 3b.2 (not needed: D6 rung 1 linked; closed as not applicable) `rayd`: port `PersistenceHelper` and adapter over `ProcessSpawner` (uid 1000, scratch env of D6b, stdout JSON lines, stderr through the allowed-fields filter), `grpc/persistence.rs` translating helper events to D1 events with the D8 mapping; `SECURITY.md` T1 and `persistence.md` fallback paragraphs; `MILESTONES.md` row says "opción 1 (fallback)"

## 4. [image] Dockerfile and publication

- [x] 4.1 `image/Dockerfile`: `ca-certificates` in the `dnf install` list and `test -f /etc/pki/tls/certs/ca-bundle.crt` in the build checks; `python scripts/image_zip.py image image/rayito-image.zip` listing unchanged apart from `rayd`
- [x] 4.2 [AWS, ≈ $0.074] `make image-zip` equivalent, then `make image-publish` and `make image-publish-caps` with `BASE_IMAGE_VERSION=1`; both versions `SUCCESSFUL`/`ACTIVE`; version numbers, build times and `snapshotBuild` sizes next to 17.0's in the task note and `AWS_API_NOTES.md` Q52 addendum ("binary with the S3 adapter")

## 5. [python] SDK surface

- [x] 5.1 `_models.py`: `S3Prefix` (validation from `_limits.py`, `key_prefix`, `archive_key`, `manifest_key`, `uri`, `with_name`), `CheckpointProgress`, `CheckpointResult`, `RestoreProgress`, `RestoreResult`, `LaunchOptions`; `exceptions.py`: `PersistenceException(code)`; exported from `rayito/__init__.py`; `test_models.py` covers every validation rule
- [x] 5.2 `_persistence_base.py` (pure helpers shared by sync/async: request builders, exclude validation, event → result translation, status → exception mapping incl. `UNIMPLEMENTED`, deadline) with `test_persistence_base.py`
- [x] 5.3 `sandbox_sync/main.py` and `sandbox_async/main.py`: `create(persist=, persist_timeout=)` rules 1–4 of design D9 (role required, bind, auto-restore with `NotFound` swallowed, failure policy), `connect(persist=)`, `persist` and `last_restore` properties, `checkpoint_files`, `restore_files`, `reincarnate` (ordering, old sandbox left alive on failure, `connect()` handles refused), stream channel + 403 re-mint before the first message, no mid-stream reconnection, `info` logging of `uri` only
- [x] 5.4 `tests/unit/fake_persistence.py` (fake `Checkpoint`/`Restore` servicer with scripted events and failures) and `test_persistence_sync.py` / `test_persistence_async.py`: create without role rejected before any plane call, bind with and without `name`, auto-restore swallow vs raise, explicit restore `NotFoundException`, checkpoint result fields, progress callback, `permission_denied` → `PersistenceException`, `UNIMPLEMENTED` mapping, busy → `PersistenceException(code="failed_precondition")`, `reincarnate` ordering against the fake plane (checkpoint → run-microvm → restore → terminate of the old id), failure of `create` leaves the old sandbox untouched, `connect()` handle refused; sync/async parity test lists the new names in both trees
- [x] 5.5 `rayito/e2b`: `set_timeout` reason names `reincarnate()`; `test_e2b_compat_*` scenario updated (message mentions both `UpdateMicrovm` and `reincarnate`)
- [x] 5.6 `cd clients/python && uv run pytest tests/unit && uv run ruff check . && uv run ruff format --check . && uv run mypy src tests` green; `clients/python/CHANGELOG.md` `[Unreleased]` entry

## 6. [typescript] SDK surface

- [x] 6.1 `src/sandbox/persistence.ts`: `S3Prefix`, result/progress types, `PersistenceError` in `errors.ts`, request builders and mapping (design D10); exported from `index.ts`; `limits.ts` regenerated
- [x] 6.2 `src/sandbox/sandbox.ts` / `launch.ts`: `create({ persist, persistTimeoutMs })`, `connect(id, { persist })`, `persist`, `lastRestore`, `checkpointFiles`, `restoreFiles`, `reincarnate` with the launch-options record; stream transport + 403 re-mint before the first message
- [x] 6.3 `tests/unit/persistence.test.ts` mirroring 5.4 one-to-one on the fake `rayd` and fake plane; `pnpm lint && pnpm typecheck && pnpm test && pnpm build && pnpm pack:check` green; `clients/typescript/CHANGELOG.md` entry

## 7. [docs] Architecture, security, constitution, site

- [x] 7.1 `ARCHITECTURE.md`: `FilesystemService` row, hexagonal tables (`persistence` module, `HomeArchiver`/`ObjectStore`/`PartSource` ports, `S3ObjectStore`/`TarHomeArchiver` adapters), **ADR-009** per design D14 with the D6 outcome and numbers, ADR-007 consequence sentence; `openspec/project.md` tech-stack TLS sentence and hard rule 3 non-goal list; `SPEC.md` §4 bullet struck through with the M7 note
- [x] 7.2 `SECURITY.md`: T1 mitigation addendum, new T14 row, "Execution role: por defecto ninguno" paragraph on `persist=`; `docs/site/docs/security.md` mirrors T14
- [x] 7.3 `docs/site/docs/persistence.md` (design D14 outline), `mkdocs.yml` nav entry, `README.md` section "Persistencia más allá de las 8 h", `e2b-compat.md` and `concepts.md` `set_timeout` rows → `reincarnate()`; `mkdocs build --strict` green; `crates/rayd/CHANGELOG.md` entry
- [x] 7.4 `grep -rn "no cross-session filesystem persistence\|candidato M6" openspec/project.md SPEC.md` returns nothing

## 8. [e2e] Acceptance on real AWS (design D15)

- [x] 8.1 `clients/python/tests/e2e/test_m7_persistence.py`: fixtures (`RAYITO_TEMPLATE_CAPS`, `RAYITO_EXECUTION_ROLE_ARN`, `RAYITO_PERSIST_BUCKET`, `RAYITO_PERSIST_PREFIX`), S3 cleanup in teardown with the developer's boto3 client, tests 1–4 of D15 with the `report(...)` lines (bytes, seconds, MB/s, first vs second checkpoint), test 5 behind `RAYITO_E2E_SLOW=1` with the `slow` marker registered in `pyproject.toml`
- [x] 8.2 `clients/typescript/tests/e2e/m7.e2e.test.ts`: 20 MB round trip + `reincarnate()` with the same env vars and cleanup
- [x] 8.3 [AWS, ≈ $0.05] `RAYITO_E2E=1 RAYITO_TEMPLATE=<default arn> RAYITO_TEMPLATE_VERSION=<new> RAYITO_TEMPLATE_CAPS=<caps arn> RAYITO_EXECUTION_ROLE_ARN=<arn> RAYITO_PERSIST_BUCKET=<bucket> uv run pytest tests/e2e -m e2e -v` → `test_m7_persistence.py` green **and** the whole existing suite still green on the new image (regression of the binary); `pnpm test:e2e` green; zero MicroVMs alive and zero objects left under `rayito-e2e/` afterwards
- [x] 8.4 Numbers into `AWS_API_NOTES.md` Q53 (upload and download MB/s at 2 GB through `INTERNET_EGRESS`), Q54 (IMDS provider behaviour observed in the `rayd` log fields), Q1 note ("sin medir" unless 8.5 ran), `MILESTONES.md` M7 row 4 (status, `T0/T1`, `S0/S1`, provider rung, 50 MB timings, first vs second checkpoint, image versions), ADR-009 and `persistence.md` measured-numbers paragraph
- [x] 8.5 [AWS, optional, ≈ $0.13, 62 min] `RAYITO_E2E_SLOW=1 … -m "e2e and slow" -k credentials_after_55_minutes`; outcome recorded in Q1 either way

## 9. [gates] Everything local, in one pass

- [x] 9.1 `buf lint`; `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test --workspace`; `cargo deny check`; the auditable zigbuild + `check_auditable.py`; `python scripts/gen_limits.py --check`; `uvx ruff check scripts`; scripts tests; `python scripts/check_license.py`
- [x] 9.2 `cd clients/python && uv run pytest tests/unit && uv run ruff check . && uv run ruff format --check . && uv run mypy src tests`; `cd clients/typescript && pnpm lint && pnpm typecheck && pnpm test && pnpm build && pnpm pack:check`; `uvx cfn-lint==1.56.3 spike/m0/iam.yaml infra/*.yaml`; `mkdocs build --strict`
- [x] 9.3 `openspec validate m7-s3-persistence --strict --no-interactive` passes with every task above ticked and the notes appended; acceptance agent archives with `openspec archive m7-s3-persistence --yes` after 8.3 is green
  - Acceptance 2026-09-17 (`rayito-base` 20.0 + `rayito-base-caps` 8.0, `rayd` 0.2.0, prefix `rayito-e2e` as authorised by the deployed role): `test_m7_persistence.py` inside the full suite **3 passed, 1 skipped (slow)**: caps `kernel_ready_s` 13.60, `imds_blocked` `True` 0.10 s after readiness, checkpoint #1 21 files / 52 429 870 B in 1.65 s wall (1.55 s agent, 31.85 MB/s), #2 1.35 s (1.25 s, 38.88 MB/s), second life 11.57 s incl. restore 0.87 s agent (60.60 MB/s) → same sha256, `reincarnate()` 8.88 s with the old VM `TERMINATING`, missing name → `NotFoundException` 0.11 s, no role → `permission_denied` 1.45 s, 2 objects deleted in teardown, zero multipart uploads left; `m7.e2e.test.ts` green inside `pnpm test:e2e`. 8.5 stays deferred (Q1 "sin medir").
## Notes

Implementer (2026-09-16/17, box: Windows 11, Rust 1.98.1 via rustup, zig 0.16.0, `cargo-zigbuild` 0.23.4, `uv` 0.7.21, Python 3.12.10, pnpm, Docker `rayito-dev:m6` for the `cfg(unix)` suites, test account, us-east-1):

- **Numbering** (0.0): `m7-suspended-pool` had taken `SECURITY.md` **T14** (custodia de secretos del pool), so this change's threat row is **T15** everywhere (`SECURITY.md`, `docs/site/docs/security.md`, ADR-009, `persistence.md`); `AWS_API_NOTES.md` §16 rows **Q53/Q54** were free and are used as written (the poly track's Q57 already cites this change's binary size). `m7-poly-kernels` is `Complete` but not archived: the `e2b-compat` and `typescript-sdk` MODIFIED deltas here are rebased on the current `openspec/specs/*` text (verified by diff: only this change's sentences differ); `m7-poly-kernels` and `m7-suspended-pool` also modify the same two requirements, so whoever archives last merges the three texts.
- **0.1** §17 table with the exact parameters per operation (`CreateMultipartUpload`, `UploadPart`, `CompleteMultipartUpload`, `AbortMultipartUpload`, `PutObject`, `GetObject`; `HeadObject` listed as unused), API Reference URLs, error-code classification and the IAM minimum; Q53/Q54 added empty first, filled in 8.4.
- **0.2** Baseline from an empty `target-linux/`: `T0 = 106 s`, `S0 = 4 700 984 B` (`cargo auditable zigbuild --release --locked --target aarch64-unknown-linux-musl -p rayd`).
- **0.3/0.4** `spike/m0/iam.yaml`: `PersistenceBucket` (default empty), `PersistencePrefix` (default `rayito`, pattern `^[A-Za-z0-9!_.*'()-][A-Za-z0-9!_.*'()/-]*[A-Za-z0-9!_.*'()-]$|^[A-Za-z0-9!_.*'()-]$`), `HasPersistenceBucket`, conditional policy `persistence` on `ExecutionRole` (Sids `HomeArchiveObjects`: `s3:PutObject`/`GetObject`/`AbortMultipartUpload` on `arn:aws:s3:::${PersistenceBucket}/${PersistencePrefix}/*`; `MissingKeyIs404`: `s3:ListBucket` with `s3:prefix` = `${PersistencePrefix}/*`); `uvx cfn-lint==1.56.3` and `validate-template` clean; `Makefile` `infra-lint` gained `IAM_TEMPLATE := spike/m0/iam.yaml`; `infra/README.md` section "Persistencia en S3". Stack `rayito-m0-iam` → `UPDATE_COMPLETE` with `PersistenceBucket=<bucket> PersistencePrefix=rayito-e2e`; `simulate-principal-policy`: `s3:PutObject` on `…/rayito-e2e/x/home.tar.gz` → `allowed`, on `…/rayito/x` → `implicitDeny`, `s3:ListBucket` with `s3:prefix=rayito-e2e/*` → `allowed`.
- **1.1** `buf lint` clean; `buf breaking --against <scratchpad copy of proto/ before the change>` clean (no output, exit 0). Messages and field numbers exactly as D1 (`S3Location{bucket=1,key_prefix=2,region=3}`, `CheckpointRequest{target=1,user=2,exclude=3}`, events `started=1|progress=2|done=3|error=4|keepalive=5`, `duration_ms` `uint32`).
- **1.2** `buf generate` regenerated the Python `filesystem_pb2*` and TS `filesystem_pb.ts` (normalized to LF per `.gitattributes`); `rayito-proto` rebuilds from `build.rs`; no hand edits.
- **1.3** `limits.json` new group + `gen_limits.py` `CONSTANT_NAME_OVERRIDES` so `s3BucketNameMin/Max` become `S3_BUCKET_NAME_MIN/MAX` (the default splitter would produce `S3_BUCKET_NAME_MIN` only for the TS side); `--check` clean.
- **2.x** `rayd-core::persistence` = `keys.rs`, `plan.rs`, `manifest.rs`, `progress.rs`, `error.rs`, `ports.rs`, `checkpoint.rs`, `restore.rs`, `fake.rs` (test-only). Deviation from D7 recorded: `PartSource::next_part` returns `Result<Option<Bytes>, StoreError>` (not `Option<Bytes>`) so a dead archive thread can never complete a truncated multipart upload; the fakes return `impl Future` via `std::future::ready` (clippy `unused_async` / refining-impl-trait). `rayd-core/Cargo.toml` unchanged. `cargo test -p rayd-core` → 283 passed on Windows.
- **3.1** Pins: `aws-config =1.12.0` (`rt-tokio`, `default-https-client`), `aws-sdk-s3 =1.148.0` (`rt-tokio`, `default-https-client`, `http-1x`), `tar =0.4.46`, `flate2 =1.1.10` (`rust_backend`), `sha2` reused. `cargo deny check` → `advisories ok, bans ok, licenses ok, sources ok`; new duplicate warnings (warn level, no allowlist change): `base64`, `crypto-common`, `digest`, `foldhash`, `hashbrown`, `http`, `http-body` (smithy pulls `http` 0.2 + 1.x) next to the seven pre-existing ones.
- **3.2** D6 ladder resolved at **rung 1 (`aws-lc-rs`)** with zero knobs: `aws-lc-sys` 0.45.0 compiles its C sources with `zig cc` through the `cc` builder (no cmake, pregenerated bindings for `aarch64-unknown-linux-musl`). `T1 = 222 s`, `S1 = 12 524 384 B` (+7 823 400 B, ×2.66 < ×3: `cargo bloat` not required); `.dep-v0` 256 packages (was 145); static ARM64 ELF (`e_machine` 183, no `PT_INTERP`); `check_auditable.py` OK. No link error at any point; 3b not needed.
- **3.3** `S3ObjectStore` (`--persistence-credentials imds|default`, IMDS profile `execution_role`, one client per region, `BehaviorVersion::latest()`, `RetryConfig::standard()`, checksums `WhenRequired` so no checksum headers leave the contract of §17, `MultipartUpload` guard aborting in `Drop` with a 5 s budget, `classify()` → `StoreError{kind, s3_error_code, request_id}`). `#[ignore]` test `crates/rayd/tests/s3_store.rs` run from Docker with the developer's exported credentials (`aws configure export-credentials --format env`, the `default` chain has no `sso` feature): 20 MiB multipart (3 parts) upload 3.11 s (6.74 MB/s from the dev box), download 1.59 s (13.21 MB/s), `NoSuchKey` → `NotFound`, single-part `PutObject` path, cleanup OK.
- **3.4** `TarHomeArchiver` (unix) / `UnsupportedHomeArchiver` (other); 11 `cfg(unix)` tests incl. crafted archives with `..`, absolute path and a device entry written as raw GNU header bytes (the `tar` crate refuses `..` in `append_data`). Run under Docker (`rayito-dev:m6`): all green.
- **3.5** `grpc/persistence.rs` (`PersistenceGrpc`, `status_for`, `SuspendableStream` with `SuspendClose::Terminal`, `KeepAliveStream` 30 s), `FilesystemGrpc::with_persistence`, `Services.persistence: Arc<dyn PersistenceBackend>` (`UnavailablePersistence` → `UNIMPLEMENTED` in every existing test harness), `rayd::persistence::{PersistenceManager, TokioRunner, part_channel, chunk_channel}`, `main.rs` `--persistence-credentials` (default `imds`) + `persistence_manager()`; 9 loopback-tonic tests (round trip, statuses, busy, stream error, client cancel → abort observed, suspend → `suspending`, progress + keepalives, seeded restore, status table). `Health.imds_blocked` untouched.
- **3.6** Windows: `cargo fmt --all --check` OK, `cargo clippy --workspace --all-targets -- -D warnings` OK, `cargo test --workspace` OK (rayd lib 81, rayd-core 283, proto 13, 1 ignored). Docker (x86_64 Linux, `CARGO_TARGET_DIR=/work/target-docker`): `rayd` lib 102, `rayd-core` 283, `m1_hello`, `m2_process`, `s3_store` (ignored, run explicitly) green; `m3_filesystem` 29/30 (the pre-existing root-identity case needs a real second uid in the container); `m4_code`/`m5_*`/`m6_*`/`m7_*` fail with "kernel never became ready" in that ad-hoc container for every track (environmental: no sidecar kernel image in the bind mount), unchanged by this change. Auditable zigbuild + `check_auditable.py` OK; final `T1`/`S1` as in 3.2.
- **4.1** `ca-certificates` in the `dnf install` list and `test -f /etc/pki/tls/certs/ca-bundle.crt` in the tool check (rustls-native-certs reads that bundle); `image_zip.py` listing unchanged apart from `rayd` (33 files, 12 636 521 B).
- **4.2** [AWS] `rayito-base` **19.0** (`SUCCESSFUL`/`ACTIVE`, ≈ 201 s; `snapshotBuild` memory 922 832 896 / code install 1 321 267 200 / disk 37 998 592 B) and `rayito-base-caps` **7.0** (≈ 190 s; 925 462 528 / 1 320 734 720 / 33 738 752 B) with `BASE_IMAGE_VERSION=1`; vs 17.0 928 100 352 / 1 305 825 280 / 37 998 592 B (+15 MB of code install for +7.8 MB of binary). Q52 addendum written. (The poly track had published `rayito-base` 18.0 with the same `rayd` binary in between.)
- **5.x** Python: `S3Prefix`, `CheckpointProgress/Result`, `RestoreProgress/Result`, `LaunchOptions`, `PersistenceException(code)`, `_persistence_base.py`, `sandbox_sync/persistence.py` + `sandbox_async/persistence.py` (`_open_stream(translate=)` hook on both trees), `create(persist=, persist_timeout=)`, `connect(persist=)` (name check before `get-microvm`), `checkpoint_files`, `restore_files`, `reincarnate` (checkpoint → `create(**LaunchOptions)` → `kill()` swallowing `SandboxNotFoundException`; old sandbox untouched when `create` fails, note with the `uri`), `create(pool=)` rejects `persist=`; `e2b` `set_timeout` reason names `reincarnate()`. Tests: `fake_persistence.py`, `test_persistence_base.py` (38), `test_persistence_sync.py` (25), `test_persistence_async.py` (11), `test_models.py` S3Prefix cases, `test_e2b_compat_*`. Gates: `pytest tests/unit` **1043 passed, 1 skipped**; `ruff check .`/`ruff format --check .` clean on this change's files (one `E501` in the MCP track's `tests/unit/test_mcp_settings.py` is theirs); `mypy src tests` clean (140 files). `pyproject.toml`: marker `slow`, `addopts = "-m 'not e2e and not bench and not slow'"`.
- **6.x** TypeScript: `src/sandbox/persistence.ts` (`S3Prefix` with `equals`, `PersistenceClient` over `openStream({ service, stream: true, translate })`), `PersistenceError` in `errors.ts`, `OpenStreamOptions.translate` in `core.ts`, `Sandbox.create({ persist, persistTimeoutMs })`/`connect(id, { persist })`/`persist`/`lastRestore`/`checkpointFiles`/`restoreFiles`/`reincarnate`, `limits.ts` regenerated; `tests/unit/persistence.test.ts` (48) on the fake `rayd` + fake plane (`runMicrovmError`, `sandboxIds` queue). Gates: `pnpm lint` (77 files), `pnpm typecheck`, `pnpm test` **384 passed (22 files)**, `pnpm build`, `pnpm pack:check` green.
- **7.x** `ARCHITECTURE.md` (FilesystemService row, `persistence` domain row, three port rows, adapters bullet, ADR-007 consequence, **ADR-009** with the measured numbers), `openspec/project.md` (TLS sentence, hard rule 3), `SPEC.md` §4 bullet struck through, `SECURITY.md` (T1 addendum, **T15**, execution-role paragraph), `docs/site/docs/security.md` "Persistencia en S3 (T15)", `docs/site/docs/persistence.md`, `mkdocs.yml` nav, `README.md` "Persistencia más allá de las 8 h", `e2b-compat.md`, `concepts.md`, three `CHANGELOG.md` entries; `mkdocs build --strict` green; 7.4 grep returns nothing.
- **8.1/8.2** `tests/e2e/test_m7_persistence.py` (tests 1–4 + `slow` test 5; extra env `RAYITO_TEMPLATE_DEFAULT`/`RAYITO_TEMPLATE_DEFAULT_VERSION` for test 4 because `RAYITO_TEMPLATE` may point at the caps image) and `clients/typescript/tests/e2e/m7.e2e.test.ts` (S3 cleanup through the developer's `aws` CLI since the SDK has no `@aws-sdk/client-s3` dependency).
- **8.3** [AWS] `test_m7_persistence.py` on `rayito-base-caps` 7.0 + `rayito-base` 19.0: **3 passed, 1 skipped (slow) in 57.9 s** — caps `kernel_ready_s` 11.80, `imds_blocked` `True` 0.10 s after readiness and still `True` after the checkpoint, checkpoint #1 21 files / 52 429 870 B read / 52 479 326 B archive / 0 skipped in 1.67 s wall (1.50 s agent, 31.4 MB/s), checkpoint #2 1.50 s wall (1.40 s agent, 35.0 MB/s), `create(persist=)` second life 7.35 s incl. restore (0.67 s agent, 78.0 MB/s), same sha256 of the 50 MB blob, excluded dir absent, `reincarnate()` 8.85 s with the old VM `TERMINATING`, explicit missing-name restore → `NotFoundException` in 0.11 s, default image without role → `PersistenceException(code="permission_denied")` in 1.35 s (`no execution role credentials`), 2 S3 objects deleted in teardown. Full Python suite on 19.0/7.0 (M1–M6 + M7 persistence; the other M7 tracks' suites excluded: they need their own images/pool sessions): **17 passed, 2 skipped (`test_egress_allowlist`, slow), 1 deselected (bench) in 626.7 s**, persistence numbers repeated (32.9 / 36.0 MB/s up, 87.9 MB/s down, `reincarnate()` 9.47 s, `permission_denied` 1.00 s). `pnpm test:e2e tests/e2e/m7.e2e.test.ts tests/e2e/m6.e2e.test.ts`: **2 files, 3 tests passed in 223.5 s** (latest ACTIVE versions 19.0/7.0). Afterwards: `list-microvms` live = 0, zero objects and zero multipart uploads under `rayito-e2e/`.
- **8.4** Q53 (31.4 / 35.0 MB/s up, 78.0 MB/s down at 50 MB; 2 GB not measured), Q54 (observed log fields; no-role VM had no CloudWatch stream), Q1 ("sin medir"), Q52 addendum, `MILESTONES.md` M7 row 4, ADR-009 and `persistence.md` "Coste y números medidos" written.
- **8.5** Not run (optional, 62 min): Q1 says "sin medir".
- **9.x** All local gates green on 2026-09-17 after the last doc edit: `buf lint`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace`, `cargo deny check`, auditable zigbuild + `check_auditable.py`, `gen_limits.py --check`, `uvx ruff check scripts`, scripts tests (57 passed), `check_license.py` OK, Python and TypeScript client gates as in 5.x/6.x, `uvx cfn-lint==1.56.3 spike/m0/iam.yaml infra/*.yaml` clean, `mkdocs build --strict` OK, `openspec validate m7-s3-persistence --strict --no-interactive` valid. Not run: `make` (not installed; every recipe run by hand).

- **Review fixes (2026-09-16, post-implementation)** (a) `tar_archiver.rs`: an absolute entry name is now refused with `EntryRefused` by an explicit `entry.path().is_absolute()` check before `unpack_in` (which silently re-rooted `/tmp/x` under the home); the `crafted_parent_references_and_absolute_paths_are_refused` test asserts the refusal and that nothing is created; design D4 wording updated. (b) Python `LaunchOptions` is `repr=False` with a `__repr__` that redacts `access_token` and shows `envs` as a key count (`REDACTED` moved to `_models.py`, `SlotRecord` imports it); test `test_launch_options_repr_redacts_token_and_envs`. (c) TypeScript `reincarnate()` rethrows the SAME error instance from `create()` with the note appended to `message` (`withReincarnateNote`), so `PersistenceError.code`, `SandboxNotReadyError.state`, etc. survive (parity with Python `add_note`); fake rayd gained `failRestoreAfterStarted`; tests `rethrows the typed create error with its fields intact` and `wraps a non-Error rejection in SandboxError with cause`. (d) While at it, `cargo clippy -p rayd --all-targets -- -D warnings` under Linux (Docker `rayito-dev:m6`, the only place the `cfg(unix)` archiver is linted) flagged four pre-existing `needless_pass_by_value` in `tar_archiver.rs` (`Appender::append`, `archive_write_error`, `extract_read_error`, `unpack_error`): now by reference. Gates after the fixes: Windows `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace` (382 passed), `cargo zigbuild --release --target aarch64-unknown-linux-musl -p rayd`; Docker Linux `cargo clippy -p rayd --all-targets -- -D warnings` clean and `cargo test -p rayd --lib` 102 passed (11 `tar_archiver`); Python `pytest tests/unit` 1065 passed / 1 skipped, `ruff check`, `ruff format --check`, `mypy src tests` clean; TypeScript `pnpm lint`, `pnpm typecheck`, `pnpm test` (386 passed), `pnpm build`; `openspec validate m7-s3-persistence --strict --no-interactive` valid.
