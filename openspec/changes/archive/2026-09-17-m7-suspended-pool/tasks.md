## 0. [pre-flight] Baseline and budget

- [x] 0.1 Read `design.md` D1–D17 and ADR-008; confirm every AWS call named there is already listed in `AWS_API_NOTES.md` §2, §3, §5, §6 (`run-microvm`, `create-microvm-auth-token`, `get-microvm`, `list-microvms`, `suspend-microvm`, `resume-microvm`, `terminate-microvm`) and that no new parameter is needed
- [x] 0.2 `cd clients/python && uv run pytest tests/unit && uv run ruff check . && uv run ruff format --check . && uv run mypy src tests` and `cd clients/typescript && pnpm lint && pnpm typecheck && pnpm test && pnpm build` green on the current tree (the baseline every later task must keep)
- [x] 0.3 With `AWS_PROFILE`/`AWS_REGION` set: `aws lambda-microvms list-microvms --image-identifier <rayito-base arn>` shows no `PENDING|RUNNING|SUSPENDING|SUSPENDED` VM before any e2e of this change

## 1. [python] Pure core: `_pool_base.py`

- [x] 1.1 `clients/python/src/rayito/_pool_base.py`: `PoolConfig` (fields and defaults of design D1) with `__post_init__` validation (`1 <= size <= 64`; `validate_timeout`; `idle` is an `IdlePolicy` with `auto_resume=True` and `max_idle_seconds < timeout`; `60 <= min_remaining_seconds <= timeout - 60`; `1 <= fill_concurrency <= 8`; `sweep_interval_seconds >= 5`; `ready_timeout > 0`; `envs`/`metadata`/`cpu_time_limit` through the `_payload` validators), every `InvalidArgumentException` naming the field; `launch_kwargs(config)` → the exact `create()` kwargs of design D3
- [x] 1.2 `SlotRecord` (frozen; fields of design D2; `expires_at`, `remaining_seconds(now)`; `__repr__` redacts `access_token`), `record_to_dict`/`record_from_dict` (ISO-8601 UTC dates, `idle` as three fields, `schema` `rayito.pool/1`), `sandbox_info_from_record(record, state="SUSPENDED")`
- [x] 1.3 Pure rules: `pick_ready_slot(records, now, min_remaining_seconds)` (earliest `expires_at` among `ready` records with enough life; `None` otherwise), `slot_is_stale(record, now, min_remaining_seconds)`, `reconcile_action(record, listed_state)` → `keep | repark | check | drop` per design D6.2, `FillBackoff` (1 s doubling to 60 s, ±25 % jitter from an injected `random`, `reset()` on success), `PoolStats` and `PoolSlotInfo` dataclasses (design D10) with `stats_from_records(...)`, `TakePoll(ReadinessPoll)` with `INITIAL_DELAY = 0.1`, `MAX_DELAY = 0.5`, `JITTER = 0.0`; `PoolClosedException(SandboxException)` in `rayito/exceptions.py`
- [x] 1.4 `clients/python/tests/unit/test_pool_base.py`: every validation rule and message; `pick_ready_slot` ordering and staleness; `slot_is_stale` at the boundary; every `reconcile_action` branch; `FillBackoff` sequence with a fixed `random`; `PoolStats` arithmetic (`takes == hits + misses`); record round-trip and redaction (`"<redacted>"` in `repr`, the token absent from `str`); `TakePoll` delays 0.1, 0.2, 0.4, 0.5, 0.5 with a fake clock

## 2. [python] Backends: `_pool_backends.py`

- [x] 2.1 `PoolBackend` protocol (`persistent`, `load`, `save`, `delete`), `InMemoryPoolBackend` (`persistent = False`, dict keyed by `sandbox_id`), `JsonFilePoolBackend(path)` (`persistent = True`; `{"schema": "rayito.pool/1", "slots": [...]}`; `os.open(..., O_CREAT | O_WRONLY | O_TRUNC, 0o600)` on `<path>.tmp` then `os.replace`; `load()` → `()` on a missing file, `InvalidArgumentException` on another `schema`; `delete` of a missing id is a no-op); module docstring states single-process, file as sensitive as `RAYITO_ACCESS_TOKEN`
- [x] 2.2 `clients/python/tests/unit/test_pool_backends.py`: in-memory upsert/delete/load; JSON round-trip of two records; atomic write (`.tmp` absent after `save`); mode `0o600` on POSIX (`pytest.mark.skipif(os.name == "nt")`); missing file → `()`; foreign schema rejected; `delete` of an unknown id is silent; the file content contains the token (it is the secret) and nothing else unexpected

## 3. [python] Fake control plane for the pool tests

- [x] 3.1 `clients/python/tests/unit/fake_control_plane.py`: in-memory `ControlPlane` (design D16): per-VM state machine (`PENDING` → `RUNNING` after `pending_gets` `get`s, `suspend` → `SUSPENDED`, `resume` → `RUNNING`, `terminate` → `TERMINATED`, `list` with the SDK's default state filter, `resolve_template_arn` passthrough, `create_auth_token` → fake JWE), real `TokenBucket`s per operation on a `FakeClock` (call log with timestamps), programmable failures `fail(operation, call_index, exception)`, `run_microvm` returning an `endpoint` of a fake `rayd` started through `fake_rayd_factory` (one per VM, its `FakeRayd.sandbox_id` set to the VM id) and `TrackingTransport.for_loopback()` mapping endpoints to `host:port`
- [x] 3.2 Extend `FakeRayd` in `tests/unit/conftest.py` with a `sandbox_id` field (default `SANDBOX_ID`) used in `Health` so a fake per slot answers its own id; `test_sandbox_sync.py`/`test_sandbox_async.py` still green

## 4. [python] Sync pool: `sandbox_sync/pool.py` and the `create()` hooks

- [x] 4.1 `Sandbox`: `launch_info` property (the `SandboxInfo` held since `run-microvm`/`connect`, no network); keyword-only `readiness: type[ReadinessPoll] = ReadinessPoll` on `_open` and `_wait_until_ready` (default schedule unchanged; `create()`, `connect()`, `resume()` untouched)
- [x] 4.2 `SandboxPool.__init__` (design D1 signature), `start()` (idempotent; recovery per design D9: `warming` records terminated + deleted with `lost += 1`, `ready` records swept before being served; then the filler thread `rayito-pool-filler-<n>`), the filler loop (deficit → `ThreadPoolExecutor(max_workers=fill_concurrency)` warm-ups; `Condition` wake-ups on warm-up end, take, sweep interval, close; `FillBackoff` after any failure with one `WARNING` naming the exception class and the id when known)
- [x] 4.3 Warm-up (design D3): fresh `generate_access_token()` per slot → `Sandbox.create(**launch_kwargs, access_token=token, keep_on_failure=False, control_plane=..., transport=...)` → record saved as `warming` right after `run-microvm` returns (from `sandbox.launch_info`) → `pause(wait=True)` → `close()` → record saved as `ready` with `parked_at`; on the stop flag after `create()`, terminate instead of park; `launched += 1` per accepted `run-microvm`; `INFO` lines with id and ms
- [x] 4.4 `take(*, wait=0.0, ready_timeout, request_timeout, reconnect_timeout)` (design D4): `PoolClosedException` when not started or closed; `pick_ready_slot` under the lock, record deleted before any network call, `hits += 1`; `resume_microvm` (`False`/`SandboxNotFoundException`/`SandboxStateException` → `terminate_quietly`, `lost += 1`, fallback); `Sandbox._open(info=sandbox_info_from_record(...), access_token=record.access_token, proxy_ports=(PortSpec.single(DEFAULT_PORT),), terminate_on_failure=True, readiness=TakePoll, ...)` (an exception → `failed += 1`, fallback); `wait > 0` blocks on the condition until a slot is ready or the deadline; fallback = `Sandbox.create(**launch_kwargs, ...)` with a fresh token, `misses += 1`, `launched += 1`; `takes == hits + misses`; the filler is signalled after every take
- [x] 4.5 Sweep (design D6) inside the filler thread every `sweep_interval_seconds` and once at `start()`: recycle stale `ready` records (`terminate_microvm`, delete, `recycled += 1`); one `list_microvms(image_arn=..., image_version=...)`; `reconcile_action` per `ready` record → `repark` (`suspend_microvm` + `WARNING`), `check` (`get_microvm`; terminal/not found → delete + `lost += 1` + `WARNING` with `stateReason`), `keep`; a listing failure aborts the sweep with one `WARNING`
- [x] 4.6 `stats()` (lock-protected, no network, `slots` without tokens), `close(drain=True)` (stop flag, wake, join ≤ 30 s, drain terminates + deletes every `ready` record, executor shutdown; `drain=False` rejected with `InvalidArgumentException` on a non-persistent backend; idempotent), `__enter__`/`__exit__`
- [x] 4.7 `Sandbox.create(pool: SandboxPool | None = None, ...)`: with `pool`, reject with `InvalidArgumentException` naming the first launch/plane kwarg that differs from its default (`template`, `template_version`, `timeout`, `idle`, `envs`, `metadata`, `cpu_time_limit`, `execution_role_arn`, `allowed_ports`, `ingress`, `egress`, `logging`, `region`, `session`, `access_token`, `keep_on_failure`, `control_plane`, `transport`) and return `pool.take(ready_timeout=..., request_timeout=..., reconnect_timeout=...)`; docstring updated
- [x] 4.8 `clients/python/tests/unit/test_pool_sync.py` (design D16, every case): warm-up sequence and record contents, distinct tokens and matching `token_sha256` per launch payload; `fill_concurrency` bound (open fake channels ≤ N) and bucket pacing (`run` ≤ 5/s, `suspend` ≤ 2/s on the fake clock); `take()` pops the oldest, `resume` issued, no `get`, correct `x-access-token` on a `Metrics` call, record deleted, `run_code("1+1").text == "2"`; empty pool → fallback (`misses`, `launched`); `take(wait=5)` served during the wait; slot lost at take → terminate + fallback + `lost`; `_open` failure → `failed` + fallback; refill after take; recycle with the fake clock (`recycled`, new slot); reconcile drop / repark / check-terminal; backoff delays 1 s, 2 s and reset; `close(drain=True)` terminates all and joins; `close(drain=False)` rejected in memory, accepted on JSON with records left; recovery (orphan `warming` terminated, `ready` re-verified and taken); `create(pool=)` sugar, each rejected kwarg named, knobs passed through, unstarted pool → `PoolClosedException`; `caplog` contains no slot token and no JWE; `stats()` values after a scripted sequence

## 5. [python] Async pool: `sandbox_async/pool.py`

- [x] 5.1 `AsyncSandbox`: `launch_info` and the `readiness` parameter as in 4.1
- [x] 5.2 `AsyncSandboxPool` with the surface of design D1/D12 as coroutines (`start`, `take`, `close`, `__aenter__`/`__aexit__`; `stats()` sync), filler as an `asyncio.Task` with `asyncio.Semaphore(fill_concurrency)` and `asyncio.Condition`, warm-ups `AsyncSandbox.create()` + `await pause(wait=True)` + `await close()`, backend calls through `asyncio.to_thread` when `backend.persistent`, the same sweep, recovery, fallback, counters, logging and secret rules; `AsyncSandbox.create(pool: AsyncSandboxPool | None = None, ...)` with the same kwarg rejection
- [x] 5.3 `clients/python/tests/unit/test_pool_async.py`: the cases of 4.8 one by one over the async pool and the fake control plane (thread-specific assertions replaced by task-specific ones: no more than `fill_concurrency` warm-ups awaiting at once)
- [x] 5.4 Exports in `rayito/__init__.py` (`PoolConfig`, `SandboxPool`, `AsyncSandboxPool`, `PoolStats`, `PoolSlotInfo`, `PoolBackend`, `InMemoryPoolBackend`, `JsonFilePoolBackend`, `PoolClosedException`) and `__all__` sorted; `test_e2b_compat_base.py`: `rayito.e2b.Sandbox.create(pool=object())` raises `TypeError` and no `rayito.e2b.__all__` name contains `Pool`; `cd clients/python && uv run pytest tests/unit && uv run ruff check . && uv run ruff format --check . && uv run mypy src tests` clean

## 6. [typescript] Parity

- [x] 6.1 `clients/typescript/src/pool/core.ts` (pure: `validatePoolConfig`, `SlotRecord` + `recordToJson`/`recordFromJson`, `pickReadySlot`, `slotIsStale`, `reconcileAction`, `FillBackoff`, `statsFromRecords`, `TakePoll` schedule 100 → 500 ms) and `src/pool/config.ts` (`PoolConfig` with `timeoutMs`, `minRemainingMs`, `fillConcurrency`, `sweepIntervalMs`, `readyTimeoutMs`, same defaults and rules as Python)
- [x] 6.2 `src/pool/backend.ts`: `PoolBackend`, `InMemoryPoolBackend`, `JsonFilePoolBackend` (`node:fs/promises`, `writeFile(tmp, data, { mode: 0o600 })` + `rename`, same JSON schema as Python so a file is interchangeable between SDKs)
- [x] 6.3 `src/pool/pool.ts`: `SandboxPool` (`start()`, `take({ waitMs, readyTimeoutMs, requestTimeoutMs, reconnectTimeoutMs })`, `stats()`, `close({ drain })`, `Symbol.asyncDispose`), filler as a promise loop on an `AbortController`, `unref()`ed sweep and backoff timers, explicit `resumeMicrovm` + open with the take schedule and the slot's token, fallback, recycle, reconcile, recovery, counters, `PoolClosedError extends SandboxError`; `Sandbox.create({ pool, ... })` with the same option rejection (first offending option named); `launchInfo` on `Sandbox`; exports in `src/index.ts`; logging through the SDK `Logger` with the same secret rules
- [x] 6.4 `tests/unit/pool.test.ts` (fake control plane + fake `rayd` from `tests/unit/fake/`, `vi.useFakeTimers()` where a clock is needed): the case list of 4.8 minus thread cases, plus "an idle started pool holds no ref'd timer" and "`await using pool` drains"; `pnpm lint && pnpm typecheck && pnpm test && pnpm build` clean; `pnpm pack:check` still passes
- [x] 6.5 `tests/e2e/pool.e2e.test.ts` (guard of `tests/e2e/helpers.ts`): `size: 2`, 5 takes with the D16 protocol, each `take` → first cell printed, `hits === 5`, `1+1` answered, `close({ drain: true })` leaves no live VM of the template; `clients/typescript/CHANGELOG.md` `Unreleased / Added`

## 7. [e2e] Acceptance against real AWS (Python)

- [x] 7.1 `clients/python/tests/e2e/test_m7_pool.py` (`pytest.mark.e2e`, uses `e2e_settings`, `control_plane`, `template_arn`, the sweeper): `test_pool_take_latency` exactly as design D16 (size 3, `timeout=900`, `min_remaining_seconds=300`, `sweep_interval_seconds=10`, `fill_concurrency=3`; 20 takes each after `stats().ready >= 1`; 20 sequential creates; nearest-rank p95; prints `T_take`, `T_create`, p50/min/max, RTT of the client and `stats()`; asserts `T_take < 1.5`, `hits == 20`, `misses == 0`; drain; no live VM of the 43 ids)
- [x] 7.2 `test_pool_recycles_before_the_wall` (size 1, `timeout=720`, `min_remaining_seconds=600`, sweep 10 s; `recycled == 1` within 240 s, a different ready id within 60 s more, the first id `TERMINATING|TERMINATED`) and `test_pool_json_backend_recovery` (size 1 on `tmp_path / "pool.json"`, `close(drain=False)`, new pool recovers the same id with `launched == 0`, takes it, `1+1`)
- [x] 7.3 `RAYITO_E2E=1 RAYITO_TEMPLATE=<rayito-base arn> make test-e2e` (or `cd clients/python && uv run pytest tests/e2e -m e2e -v -s` on Windows): the three tests green in one run; paste the printed `T_take`/`T_create` lines, `stats()` and the run's wall time into this file next to 7.3; `list-microvms` afterwards shows no live VM; note the Cost Explorer estimate for the day
  - Run 2026-09-16 19:03–19:09 (local, UTC−5), `rayito-base` 17.0, `uv run pytest tests/e2e/test_m7_pool.py -m e2e -v -s`: **3 passed in 335.86 s**.
  - `[m7-pool] T_take (take() -> primera celda): p50 0.770 s / p95 0.897 s / min 0.739 s / max 0.922 s (n=20)`
  - `[m7-pool] T_create (create() -> primera celda): p50 6.151 s / p95 6.488 s / min 3.029 s / max 6.500 s (n=20)`
  - `[m7-pool] RTT del cliente al proxy (p50): 109 ms`
  - `stats tras 20 tomas: PoolStats(size=3, ready=0, warming=3, takes=20, hits=20, misses=0, launched=23, recycled=0, lost=0, failed=0, ...)`
  - recycle: `reciclado tras: 121.2 s desde ready`, replacement ready, first id terminal, `stats: size=1, ready=1, launched=2, recycled=1`; recovery: same id recovered, `launched == 0`, `take() recuperado -> primera celda: 0.726 s`.
  - `list-microvms` afterwards: 0 live (415 `TERMINATED` entries of the template, all runs of the day). Cost: ≈ 46 launches of seconds ≈ $0.25 for this run (two earlier diagnostic runs of `test_pool_take_latency`, see design A2, ≈ $0.3 more); Cost Explorer for the day to be read tomorrow (billing lag).
  - Earlier runs of the same test (before the settle cell, design A2): p95 6.16 s (2 outliers of 12.4 / 6.2 s) and p95 0.803 s (1 outlier of 11.7 s), both caused by `kernel_state_lost` on slots parked before the `/run` rotation (Q56).
- [x] 7.4 `RAYITO_E2E=1 RAYITO_TEMPLATE=<arn> make test-e2e-typescript`: `pool.e2e.test.ts` green, timings pasted here
  - `pnpm exec vitest run --project e2e tests/e2e/pool.e2e.test.ts --no-file-parallelism` (2026-09-16 19:11 local): **1 passed, 34.5 s**; `take -> primera celda: 0.662, 0.677, 0.687, 0.645, 0.660 s`; `stats: hits 5, misses 0, launched 7, lost 0, failed 0`; no live VM of the template after `close({ drain: true })`.
- [x] 7.5 If `T_take >= 1.5 s`: do not tick 7.3; record the per-phase timings, amend `design.md` (an "Amendments" section, as `m6-benchmark-pool` did) and re-run — the rule D17 itself is not changed
  - Happened on the first run (p95 6.16 s): per-phase timings added to the take log (`resume-microvm` 139 ms, open 11 485 ms on the outlier), root cause found in `rayd`'s `/run` → `Rotating` dispatch lag (design A2, Q56), mitigated with the settle cell, re-run green (7.3). Rule D17 untouched.

## 8. [docs] Numbers and text into the repo

- [x] 8.1 `docs/site/docs/pool.md` per design D15 (sections: por qué suspendidos, cuándo compensa, API sync/async/TypeScript, semántica de `take()` y fallback, relleno y cuotas, reciclado y `list()`, backends y sus límites, custodia del secreto, tabla de costes, regla de dimensionado, cuota regional) with the measured `T_take`/`T_create`; `docs/site/mkdocs.yml` nav entry `Pool de sandboxes: pool.md` after `Modelo de costes`; `docs/site/docs/cost.md` row; `cd clients/python && uv run --group docs mkdocs build -f ../../docs/site/mkdocs.yml --strict` clean and `api.md` renders `rayito.SandboxPool`
- [x] 8.2 `README.md` snippet (six lines: `PoolConfig`, `with SandboxPool(...) as pool:`, `sbx = pool.take()`, `run_code`, `kill`) under the Python example, linking `pool.md`; `clients/python/CHANGELOG.md` `Unreleased / Added`
- [x] 8.3 `SECURITY.md`: row **T14** "custodia de secretos del pool" (design D14: per-slot secret, deleted at take, no rotation, 0600 file, same trust level as the SDK operator, residuals) with `M7` in the milestone column
- [x] 8.4 `ARCHITECTURE.md`: Capa 3 paragraph "Pool de suspendidos (SDK)" (thread in the client process, shared buckets, the record, the take path, no AWS component) and the ADR-008 consequence (2) amendment sentence pointing at `m7-suspended-pool` D4/D14 and T14
- [x] 8.5 `SPEC.md` §4 pool bullet (suspended pool implemented in `m7-suspended-pool`; pre-warmed stays out); `MILESTONES.md` M7 row 3 status with `T_take`/`T_create` p50/p95, the run cost and date; `AWS_API_NOTES.md` §16 row **Q53** (`take()` → primera celda p50/p95 sobre 20 tomas frente a `create()`, RTT del cliente, fecha)
- [x] 8.6 `openspec validate m7-suspended-pool --strict --no-interactive` still passes after any amendment made during implementation (amend `design.md`, never deviate silently)

## 9. [acceptance] Close Track 3

- [x] 9.1 Acceptance agent re-runs every gate of 0.2 on the final tree, reads the e2e output pasted in 7.3/7.4 (rule D17: `T_take < 1.5 s`, `hits == 20`, `misses == 0`, recycle and recovery green, no live VM), checks the docs of section 8 quote the same numbers, checks `rayito.e2b` is untouched (5.4) and that no file under `crates/`, `proto/`, `image/` or `kernel-sidecar/` changed
  - Review fixes (2026-09-16, before acceptance): (1) `_record_launch` / `#recordLaunch` now save to the backend first and, if the save fails, terminate the just-launched VM before re-raising (it runs inside `run_microvm`, before `create()` can clean up); regression tests `test_backend_failure_at_launch_terminates_the_vm` (sync + async) and `backend failure at launch terminates the VM instead of leaking it` (TS) assert the VM is `TERMINATED`, `failed == 1`, `launched == 1`, backoff 1 s and no leaked VM; (2) `clients/python/CHANGELOG.md` pool entry re-added; (3) gate breakage from parallel tracks fixed: `constant_name` override `s3BucketName*` -> `S3_BUCKET_NAME_*` in `test_limits.py` / `limits.test.ts` (mirrors `scripts/gen_limits.py`), `test_mcp_settings.py` E501, `fake_persistence.py` generators typed `Generator` so `.close()` typechecks.
  - Gates re-run by the review-fix agent on the tree as of 2026-09-16 20:12 local (other tracks were still editing docs; last code edit seen: `crates/rayd/src/main.rs` 20:12): Python `uv run pytest tests/unit` **1064 passed, 1 skipped** (20:12-20:18), `ruff check` clean, `ruff format --check` 143 files, `mypy src tests` clean (141 files); TypeScript `pnpm lint` clean (77 files), `pnpm typecheck` clean, `pnpm test` **384 passed** (22 files), `pnpm build` OK; Rust `cargo fmt --check` OK, `cargo clippy --workspace --all-targets -- -D warnings` rc=0, `cargo test --workspace` 81+5+13+283 passed, `cargo zigbuild --release --target aarch64-unknown-linux-musl -p rayd` finished; `openspec validate m7-suspended-pool --strict --no-interactive` valid.
- [x] 9.2 `openspec archive m7-suspended-pool --yes`
  - Acceptance 2026-09-17 (tree 0.2.0, `rayito-base` 20.0 with `rayd` 0.2.0): every gate of 0.2 green again (Rust fmt/clippy/test `--locked`/deny, auditable zigbuild, Python unit 1066 passed + ruff + mypy, TypeScript lint/typecheck/386 tests/build/pack:check); `test_m7_pool.py` inside the full suite: `T_take` p50 0.773 / p95 0.805 s (min 0.716, max 0.807), `hits` 20, `misses` 0, `T_create` p50 6.196 / p95 6.639 s, RTT 111 ms, recycled 120.9 s after ready, recovery take 0.820 s; alone: **3 passed in 362.6 s**, `T_take` p50 0.768 / p95 0.810 s (min 0.715, max 0.831), `T_create` p50 6.200 / p95 6.621 s, RTT 110 ms, recycled 119.3 s, recovery 0.768 s; `pool.e2e.test.ts` inside `pnpm test:e2e` (4 files / 5 tests, 264 s) green; rule D17 holds (`T_take < 1.5 s`); `rayito.e2b` untouched; no live VM afterwards. Docs (`pool.md`, README, M7 row) quote the implementation run's p50 0.770 / p95 0.897 s, consistent with these (same band; the acceptance numbers live in the M7 acceptance block of `MILESTONES.md`).
