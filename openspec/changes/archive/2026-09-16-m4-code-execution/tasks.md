## 1. [proto] Contract (no edits)

- [x] 1.1 Confirm `code.proto`/`common.proto`/`health.proto` need no change against design D1 (fields used: `ExecuteRequest{context_id?, code, timeout_ms, envs}`, `ExecuteEvent{stdout|stderr|result|error|end|started|keepalive, seq}`, `ExecutionStarted{execution_id, execution_count}`, `OutputChunk{text, timestamp_unix_ns}`, `ExecutionResult{is_main_result, text…data, extra}`, `ExecutionError{name, value, traceback}`, `ExecutionEnd{execution_count}`, `CreateContextRequest{language, cwd?, envs}`, `ContextInfo`, `HealthResponse.kernel_ready`); `buf lint` and `buf build` clean; no regeneration

## 2. [e2e] Acceptance test first (red)

- [x] 2.1 Create `clients/python/tests/e2e/test_m4_code.py` with the 19 blocks of `design.md` "Acceptance test list" (marker `e2e`, `sandbox` fixture, timing helpers reused from M2, `wait_for_terminal_state` from M1, a second sandbox in block 18 killed in `finally`)
- [x] 2.2 `tests/e2e/conftest.py`: the `sandbox` fixture prints `run-microvm -> Health agent_ready and kernel_ready` seconds (the M4 `kernel_ready_s`)
- [x] 2.3 Run `RAYITO_E2E=1 RAYITO_TEMPLATE=<arn> uv run pytest tests/e2e/test_m4_code.py -m e2e -x` against the M3 image (4.0) once the SDK side exists and confirm it fails in `create()` with `SandboxNotReadyException` (4.0 never reports `kernel_ready`); if the M4 image is already published by then, note it as superseded like M2 2.3 / M3 2.4 — superseded: 6.0/7.0 were published by the implementer before the SDK-side red run; the readiness gate is covered by `test_code_sync.py` (`create()` waits for `kernel_ready`) and by 8.1 against 8.0

## 3. [sidecar] `kernel-sidecar/` package

- [x] 3.1 Scaffold `kernel-sidecar/pyproject.toml` (`rayito-kernel-sidecar` 0.0.4, `>=3.12`, `uv_build`, dev group `pytest`/`ruff`/`mypy`, `[tool.ruff]` and `[tool.mypy]` mirroring the client, `_vendor` excluded from lint/mypy) and `requirements.txt` with `ipykernel==6.31.0`, `ipython==9.15.0`, `jupyter_client==8.10.0`, `pyzmq==27.2.0`, `matplotlib==3.10.9`, `pandas==2.2.3`, `numpy==2.3.5` plus the exact `scipy`, `scikit-learn`, `matplotlib-inline`, `pillow` versions resolved at task time (`uv pip compile`, one `==` per line, aarch64 cp312 wheels verified on PyPI for each)
- [x] 3.2 Vendor `e2b_charts` from PyPI verbatim into `src/rayito_kernel_sidecar/_vendor/e2b_charts/` with its `LICENSE` (MIT) and `VENDORED.md` (version, sdist sha256, upstream URL); import path `rayito_kernel_sidecar._vendor.e2b_charts`
- [x] 3.3 `protocol.py`: `PROTOCOL_VERSION = 1`, request/event `TypedDict`s per design D2, `decode_request`, `encode_event`, `split_text_chunks(text, 65536)` on UTF-8 boundaries, `strip_ansi`, `serialise_mime(bundle, max_bytes=8 MiB)` (str verbatim, `json.dumps` with numpy-scalar `default`, `rayito/omitted`), reply error codes `not_found|invalid_argument|kernel_dead|busy|internal`; `tests/fixtures/protocol_v1.jsonl` golden lines (one per op and per event) and `tests/test_protocol.py`
- [x] 3.4 `logging.py`: JSON-lines logger on stderr with the D16 allowlist and fixed `msg` literals (`code_len`/`text_len` only, never content)
- [x] 3.5 `ipython/ipython_kernel_config.py` (`NoColor`, `max_seq_length = 0`, `HistoryManager.enabled = False`, `capture_fd_output = True`, `InteractiveShellApp.exec_files` from the config's own directory) and `ipython/startup/0001_charts.py` (`e2b/chart` formatter + `Figure._repr_e2b_chart_`, extractor errors swallowed), `0002_data.py` (`e2b/data` for `DataFrame`/`Series`), `0003_images.py` (PIL `_repr_png_`/`_repr_jpeg_` when missing), `0004_warmup.py` (imports `numpy`, `pandas`, `matplotlib.pyplot`, `scipy.stats`, `sklearn.linear_model`; one PNG via `savefig`; one `chart_figure_to_chart`; `describe()`; `linalg.inv`; namespace left clean); `jupyter/kernels/rayito/kernel.json` template (`argv` with `-m ipykernel_launcher -f {connection_file} --config=<root>/ipython/ipython_kernel_config.py`, `interrupt_mode: signal`)
- [x] 3.6 `kernels.py`: `KernelContext` (`context_id`, `cwd`, `envs`, `lock`, `state`, `kernel_pid`) with `start()` (socket dir `0700`, `AsyncKernelManager(kernel_name="rayito", transport="ipc", ip=<dir>/k, connection_file=<dir>/kernel.json)`, kernel env per D4 incl. `OPENBLAS_NUM_THREADS=1`, `MPLBACKEND` untouched, `wait_for_ready(120)` + silent `pass`, `warmup_ms` logged), `interrupt()`, `restart(envs)` (shutdown `now=True`, cleanup, rmtree, **new** `AsyncKernelManager`, same id), `destroy()`, `probe(timeout=5)`, `reseed()` (silent cell of D11), death watch task (`provisioner.wait()` → `kernel_died` + auto-restart); kernelspec generated into `<socket_root>/kernelspec/kernels/rayito/kernel.json` from `--sidecar-root` and prepended to the kernel's `JUPYTER_PATH`
- [x] 3.7 `executions.py`: `run_execution(context, request, emit)` implementing design D5 (lock, silent env set/restore, parent `msg_id` filter, `execute_input → started`, `stream` chunks, `display_data`/`execute_result → result`, `error` with ANSI strip, `execute_reply` abort → `ExecutionAborted`, `end` after reply + idle, `KernelDied`/`KernelRestarted`/`ContextDestroyed` synthetic ends, queued-execution cancellation on `interrupt{execution_id}`)
- [x] 3.8 `server.py` + `__main__.py`: argparse (`--socket-root`, `--sidecar-root`, `--default-cwd`, `--default-context-id`), stdin reader / stdout writer (one lock around write+drain so lines stay atomic), dispatcher for the ten ops with the D2 reply payloads and timeouts (`resume` probes ≤ 8 kernels concurrently, 5 s each; `quiesce` flushes), default context started at boot then `ready{v, default_context_id, kernel_pid, warmup_ms}` emitted once, SIGTERM → shutdown every kernel; `tests/test_server.py` against a `FakeKernelContext` (queueing, interrupt of queued, restart/destroy cancellation, unknown op → `invalid_argument`, `ready` once)
- [x] 3.9 `tests/test_kernel.py` (marker `kernel`, Linux only, real ipykernel with the pins): every case of design D13 "Sidecar" (startup mime types, `x = 42`/`x`/`print(x)`, `1/0` traceback without ANSI, execution counts, per-execution envs restored, interrupt of `time.sleep(10)` < 2 s with state intact, restart → new `kernel_pid` + new key + state lost, `reseed` changes `random.random()`, `resume` reports alive, `kernel_died` after `os._exit(0)` then a working execution, warm-up namespace clean, `warmup_ms` logged)
- [x] 3.10 `cd kernel-sidecar && uv run --with-requirements requirements.txt pytest`, `uv run ruff check .`, `uv run ruff format --check .`, `uv run mypy src` clean (kernel tests under WSL2/Docker/CI; `test_protocol.py` and `test_server.py` also on Windows)

## 4. [rayd] Domain in `rayd-core`

- [x] 4.1 `rayd-core/src/code/{mod,protocol,context,execution,timeout,readiness,hooks,error,ports}.rs` per design D2–D8; `lib.rs` exports `code`; constants `DEFAULT_CONTEXT_ID`, `MAX_CONTEXTS = 8`, `MAX_CODE_BYTES`, `MAX_EXECUTE_TIMEOUT_MS`, `EXECUTE_KEEPALIVE_INTERVAL = 5 s`, `INTERRUPT_GRACE = 5 s`, `EXECUTE_QUEUE_CAPACITY = 256`, `STALL_TIMEOUT = 30 s`, `CONTEXT_READY_TIMEOUT = 30 s`, `READY_ESCAPE = 300 s`, `SIDECAR_READY_TIMEOUT = 180 s`, `MAX_SIDECAR_LINE_BYTES = 16 MiB`, `SIDECAR_PROTOCOL_VERSION = 1`
- [x] 4.2 `protocol.rs`: `SidecarRequest`, `SidecarOp`, `SidecarEvent`, `ReplyPayload`, `SidecarErrorCode`, `ProtocolError`, `encode_request`, `decode_event` (unknown fields ignored, unknown events rejected); tests over `kernel-sidecar/tests/fixtures/protocol_v1.jsonl` (path relative to the workspace root via `env!("CARGO_MANIFEST_DIR")`)
- [x] 4.3 `context.rs`: `ContextId` (syntax `[A-Za-z0-9_-]{1,64}`, `ctx-<12 hex>` generator over a `RandomSource` byte slice), `ContextInfo`, `ContextState`, `ContextEntry`, `ContextRegistry` (`register` with the 8 cap, `get`, `remove` refusing default, `set_state`, `set_kernel_pid`, `in_flight` counter, `list` default-first, `kernel_pids`, `clear`); cwd via `process::cwd::{resolve_cwd, validate_cwd}`; tests
- [x] 4.4 `execution.rs`: `ExecutionId` (`exec-<16 hex>`), `ExecuteOutput` (`Started`, `Stdout`, `Stderr`, `Result(ResultBundle)`, `Error(ExecutionErrorInfo)`, `End`, each with `seq`), `ResultBundle::from_mime` (mapping of D5 + `extra`), `ExecutionTracker` (`seq` numbering, drop after `End`, `mark_timed_out`, kernel `Error` dropped after timeout, `ExecutionTimeout` prepended to `End`, `synthetic_end(name)`), the closed synthetic name set; tests
- [x] 4.5 `timeout.rs`: `plan_timeout(timeout_ms) -> Option<TimeoutSchedule{interrupt_at, restart_at}>` with the 8 h clamp; tests. `readiness.rs`: `SidecarState` machine (`Disabled`, `Starting`, `Warming`, `Ready`, `Rotating`, `Exited{attempt, backoff_until}`), `restart_delay(attempt)`, `kernel_ready()`, `ReadyDecision` + `ready_hook_decision(state, boot_elapsed)`; tests incl. the backoff sequence and the 300 s escape
- [x] 4.6 `hooks.rs`: `SidecarConfig{python, sidecar_root, socket_root}`, `sidecar_spawn_spec(identity, config) -> SpawnSpec` (args and env of D4 through `build_child_env`), `run_rotation_request(envs) -> SidecarOp::RestartContext` for the default, `RESEED_CELL`, `VALIDATE_CELL`, `ValidationState{Idle, Running, Done(outcome)}` with `validate_hook_decision`; tests (program, args, env keys, identity, cwd, no payload envs in the sidecar env)
- [x] 4.7 `error.rs`: `CodeError` (thiserror) with `InvalidContextId`, `CodeTooLarge{max}`, `InvalidLanguage`, `InvalidCwd`, `InvalidEnvs`, `ContextNotFound`, `DefaultContextProtected`, `TooManyContexts{max}`, `KernelNotReady{reason}`, `SidecarUnavailable`, `ContextBusy`, `NotAcceptingStreams{phase}`, `SidecarProtocol(ProtocolError)`, `Unsupported`, `Internal(String)`; a test that no `Display` string quotes code, envs or ids. `ports.rs`: `KernelSidecar`, `SidecarLink`, `SidecarEventSink`, `SidecarExitSink`, `SidecarIoError`, `KernelStatus`, `RandomSource` (`fn fill(&self, buf: &mut [u8]) -> Result<(), RandomError>`)
- [x] 4.8 `cargo test -p rayd-core` green on the Windows host; `cargo clippy -p rayd-core --all-targets -- -D warnings` clean

## 5. [rayd] Adapters, application layer, gRPC and hooks in `rayd`

- [x] 5.1 `Cargo.toml`: workspace `getrandom = "=0.3.<latest>"` (exact patch at task time, recorded in `Cargo.lock`) as a `rayd` dependency; `adapters/random.rs`: `OsRandomSource` implementing `RandomSource` over `getrandom`; `cargo tree -p rayd --target aarch64-unknown-linux-musl` still clean
- [x] 5.2 `adapters/sidecar_process.rs` (`cfg(unix)`): `TokioSidecarLauncher{identity_switch}` implementing `KernelSidecar` with `tokio::process::Command` + the M2 `PreExecPlan` (made `pub(crate)` in `process_spawner.rs`, no behaviour change), stdin writer task over `mpsc::channel(1024)` (`QueueFull`/`Closed`), stdout reader with `LinesCodec::new_with_max_length(16 MiB)` → `decode_event` → sink (protocol error → log line length only → `kill()`), stderr task re-emitting only allowlisted JSON fields and counting other lines, wait task → `exited(code)`; `TokioSidecarLink{pid, sender, kill}` (`kill` = `SIGKILL` to the process group); `UnsupportedSidecarLauncher` otherwise; `adapters/mod.rs` exports `PlatformSidecarLauncher`
- [x] 5.3 `code/supervisor.rs`: `SidecarSupervisor` (state `watch::Sender<SidecarState>`, request id counter, pending replies `HashMap<u64, oneshot::Sender<…>>`, execution channels `HashMap<u64, mpsc::Sender<SidecarEvent>>`, dispatch of `reply`/execution events/`kernel_died`/`ready`, `call(op, timeout)`, `open_execution(op) -> Receiver`, `interrupt`, `send_timeout` backpressure with `stall_timeout` → `OutputTruncated` synthetic pair, relaunch loop with `restart_delay`, `SIDECAR_READY_TIMEOUT`, kernel process-group kill via `nix::sys::signal::killpg` on exit, registry clear, three consecutive op timeouts → `kill()`, logs with `attempt`/`backoff_ms`/`exit_code`/`sidecar_restarts`)
- [x] 5.4 `code/manager.rs`: `CodeManager{session, supervisor, registry: Mutex<ContextRegistry>, random: Arc<dyn RandomSource>, settings: CodeSettings{execute_keepalive_interval, interrupt_grace, stall_timeout, queue_capacity, op_timeouts, sidecar: Option<SidecarConfig>}}` with `create_context`, `execute -> ExecuteStream`, `list_contexts`, `destroy_context` (default refused), `restart_context`, `kernel_ready()` (`impl KernelStatus`), `readiness()` watch, `spawn_run_rotation(envs)` (`Rotating` → `restart_context{default, envs}` → `Ready`, `restart_ms` logged), `spawn_resume_reseed()`, `validate()` runner (`ValidationState`), `CodeManager::disabled()` for `--no-sidecar` and tests; `platform_code_manager(session, platform, policy, lookup, settings)` resolving the sidecar identity through `NixUserLookup` + `UserPolicy` like processes
- [x] 5.5 `code/execute.rs`: `ExecuteStream` (hand-written `Stream`; field order receiver → tracker → timeout sleeps → supervisor handle → in-flight guard; interrupt at `interrupt_at`, `restart_context` at `restart_at`, `Drop` before `End` → `interrupt` + unregister); `code/validate.rs`: `VALIDATE_CELL` runner with the 60 s timeout and outcome recording
- [x] 5.6 `grpc/code.rs`: `CodeGrpc` implementing the six RPCs (`Reattach` → `UNIMPLEMENTED "Reattach arrives in M5"`), proto ↔ domain conversion (`ExecuteEvent` with `seq`, `keepalive` seq 0, `ExecutionResult` from `ResultBundle`, `ContextInfo`), status table of design D10 with the `kernel not ready: ` message prefix, `KeepAliveStream` with `StreamSettings.execute_keepalive_interval` (default 5 s); error messages never echo code, envs, cwd or output
- [x] 5.7 `grpc/mod.rs`: `router(session, processes, files, code, metrics)` / `router_with_settings` with `StreamSettings.execute_keepalive_interval`; `HealthGrpc::new(session, metrics, kernel_status)` filling `kernel_ready`; remove `PendingCodeService` from `pending.rs` (keep `PendingPtyService`)
- [x] 5.8 `hooks/mod.rs`: `router(session, code, shutdown)`; `/ready` → `ready_hook_decision` (503 `kernel_warming` without transition / 200 / `ready_escape` with `tracing::error!`); `/validate` → `validate_hook_decision` (503 `validating` while running, 200 `validated`/`validate_failed`); `/run` → on `RunOutcome::Installed` spawn `code.spawn_run_rotation(defaults.envs)` after building the 200; `/resume` → `code.spawn_resume_reseed()`; `HookReply.outcome` strings documented in the doc comment
- [x] 5.9 `main.rs`: flags `--sidecar-cmd`, `--sidecar-root`, `--socket-root`, `--no-sidecar` (parse tests), `/run/rayito/k` prepared at boot (`mkdir -p`, `chown` to the sidecar identity, `0700`; `cfg(unix)`), `OsRandomSource`, `TokioSidecarLauncher`, `CodeManager` wired into both routers; `logging.rs` allowlist doc extended per design D16
- [x] 5.10 `crates/rayd/tests/fixtures/fake_sidecar.py` (stdlib only, protocol v1): scripted cells `x = 42`, `x`, `print(x)`, `1/0`, `plot`, `df`, `sleep <s>` (obeys `interrupt`), `hang <s>` (ignores it), `big <n>`, `omit`, `die`, `kernel-die`; `--warmup-ms`; every received request appended to `--log <file>` as JSON (for assertions); `restart_context`/`destroy_context` cancellation semantics; plain-text stderr line on `--noisy-stderr`
- [x] 5.11 Adapt `crates/rayd/tests/m1_hello.rs`, `m2_process.rs`, `m3_filesystem.rs` to the new `router`/`hooks::router` signatures using `CodeManager::disabled()`; keep the `PtyService.Resize` → `UNIMPLEMENTED` probe
- [x] 5.12 `crates/rayd/tests/m4_code.rs` (`#![cfg(unix)]`): every case of design D13 "rayd integration" (ready 503→200, `kernel_ready` transitions, validate 503→200, `/run` rotation request with envs and the `kernel_ready` dip, event order and `seq`, default/unknown context, code cap, timeout interrupt vs restart branches, drop → interrupt, keepalive at 200 ms, `OutputTruncated` with `stall_timeout` 1 s, two contexts interleaved, 8-context cap, list order, destroy default/user, restart cancels, `die` relaunch with backoff and orphan-kernel kill request logged, `kernel-die`, `Reattach` unimplemented, phase gate after `/suspend`, `--no-sidecar` behaviour, noisy stderr never echoed) plus `#[ignore] real_sidecar_round_trip` (`RAYITO_SIDECAR_ROOT`)
- [x] 5.13 `cargo test --workspace` green on Windows (unix suite compiled out) and inside the Docker `rust:1.98-slim-bookworm` loop with `python3` present (`m4_code`; the ignored real-sidecar case with the pins installed); `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings` clean on both
- [x] 5.14 `cargo zigbuild --release --target aarch64-unknown-linux-musl -p rayd` produces a statically linked `rayd` (record the size next to M3's 3 868 664 B)

## 6. [sdk] Python client

- [x] 6.1 `_models.py`: `CodeContext`, `OutputMessage`, `Logs`, `ExecutionError`, `Result` (fields, `raw`, `formats()`, `__str__`, `__repr__`, `_repr_*_`), `Execution` (`text`, `to_json`); `_charts.py`: `ChartType`, `ScaleType`, `Chart`, `Chart2D`, `PointData`, `PointChart`, `LineChart`, `ScatterChart`, `BarData`, `BarChart`, `PieData`, `PieChart`, `BoxAndWhiskerData`, `BoxAndWhiskerChart`, `SuperChart`, `parse_chart`; exports in `rayito/__init__.py`
- [x] 6.2 `_code_base.py`: constants, `validate_code`, `resolve_context_id`, `validate_language`, `build_execute_request`, `build_create_context_request`, `execute_deadline`, `result_from_proto` (parsed `json`/`data`/`chart`, raw kept on bad JSON), `error_from_proto` (traceback joined), `context_from_proto`, `ExecutionBuilder` (`feed`, `finish`, callbacks, protocol violations)
- [x] 6.3 `_transport.py`: `KERNEL_GATE_PREFIX`, `is_kernel_gate`, `is_stream_reset` excludes it, `translate_rpc_error` maps it to `SandboxException`; unit tests in `test_transport.py`
- [x] 6.4 `sandbox_sync/code.py`: `CodeClient` (`run_code` on the unary channel via `_open_stream` + `ExecutionBuilder`, `create_context` (90 s default deadline), `list_contexts`, `remove_context`, `restart_context` (90 s)); `sandbox_sync/main.py`: `_code` stub in `_unary_stubs`, `run_code`/`create_code_context`/`list_code_contexts`/`remove_code_context`/`restart_code_context` delegating with the design D12 signatures, `_wait_until_ready` requiring `agent_ready and kernel_ready`, the `run_code` `NotImplementedError` stub removed
- [x] 6.5 `sandbox_async/code.py` and `sandbox_async/main.py`: `AsyncCodeClient` and the same five coroutines over `grpc.aio`; readiness criterion updated
- [x] 6.6 `tests/unit/fake_code.py`: `FakeCodeService` per design D13 (scripted cells, contexts, statuses, `timeout_ms`/`envs`/port recording, `x-access-token` check, `kernel_not_ready_calls` on `FakeRayd` for the readiness test); register it in the `fake_rayd` fixture as `RaydEndpoint.code`; chart fixtures under `tests/unit/fixtures/charts/`
- [x] 6.7 `tests/unit/test_code_base.py`, `test_charts.py`, `test_code_sync.py`, `test_code_async.py`: every case of design D13 "Python SDK unit" (deadline math, validation, request building, proto conversions, chart parsing per type, `formats()`/`_repr_*_`, `ExecutionBuilder`, the acceptance sequence against the fake, timeout, callbacks, envs, context lifecycle incl. default/unknown errors, unary channel, 403 re-mint before the first message, `create()` waits for `kernel_ready`, kernel gate without a `Health` probe, async parity)
- [x] 6.8 `uv run pytest tests/unit`, `uv run ruff check .`, `uv run ruff format --check .`, `uv run mypy src` all clean; `pyproject.toml` and `_version.py` bumped to `0.0.4`

## 7. [image] Dockerfile, zip and publish

- [x] 7.1 `scripts/image_zip.py`: exclusion list (`__pycache__`, `tests`, `.venv`, `.pytest_cache`, `.ruff_cache`, `uv.lock`, `*.pyc`, `*.zip`) applied to any subtree; `Makefile` `image-zip` copies `kernel-sidecar/` to `image/kernel-sidecar/` (rsync-free `python` copy) after `rayd`; `clean` removes it; `test-sidecar`, `dev-run` targets; `lint` and `test` cover the sidecar; `.github/workflows/ci.yml` gains the `sidecar` job and the build job zips the sidecar
- [x] 7.2 `image/Dockerfile` (M4 header): `COPY kernel-sidecar/ /opt/rayito/sidecar/` + `chmod -R a+rX`, `pip install --no-cache-dir -r /opt/rayito/sidecar/requirements.txt` + `pip check`, warm run as `user` building the matplotlib font cache and the IPython profile (`test -f /home/user/.cache/matplotlib/fontlist-*.json`), sidecar import check with `PYTHONPATH`, sanity list extended (`python3 -m ipykernel --version`, kernelspec present); `CMD` unchanged; no `RAYITO_ALLOW_ROOT`
- [x] 7.3 Local loop before publishing: `cargo run -p rayd -- --sidecar-root kernel-sidecar` under WSL2/Docker with the pins in `kernel-sidecar/.venv`, `python scripts/hooks-sim.py --skip-terminate` (`/ready` 503s then 200, `/validate` 503s then 200, `/run` followed by `kernel_ready` true in the `Health` probe), and the SDK's `run_code("x = 42")`/`run_code("x")` against `127.0.0.1:8080` with `TransportSettings` local; `scripts/hooks-sim.py` `READY_RETRY_BUDGET_S` 330 and `/validate` polling
- [x] 7.4 `make image-zip` + `image-publish` → new `rayito-base` version `SUCCESSFUL` + `ACTIVE`; record the zip size, build seconds and `snapshotBuild` memory/code-install/disk sizes next to 4.0's (572 235 776 / 436 166 656 / 23 502 848 B, 123.9 s) here and in `MILESTONES.md`; apply the design D14 knob (trim `sklearn`, then `scipy`, from `0004_warmup.py`) and republish if memory > 1.2 GB

### Notes (rayd + image implementer, 2026-09-15)

- Sidecar: `kernel-sidecar/tests` 50 host tests (protocol + dispatcher) on Windows; 10 real-kernel tests (`-m kernel`) green in `python:3.12-slim-bookworm` as uid 1000 (warm-up 1.4–1.6 s on x86, interrupt < 2 s, restart rotates the connection-file key, reseed changes `random.random()`, `os._exit(0)` → `kernel_died` + recovery). `ruff check`, `ruff format --check`, `mypy src` clean. Pins resolved with `uv pip compile --only-binary :all: --python-platform aarch64-manylinux_2_28` (every binary package has a cp312 aarch64 wheel); `pydantic` 2.13.5 + `pydantic-core` 2.46.5 are pinned too because the vendored `e2b_charts` 1.0.0 imports `pydantic` (not listed in the design's pin list).
- Domain: `rayd-core` 188 tests (was 148). `getrandom` pinned at `=0.4.3` (already in `Cargo.lock` via `tempfile`; 0.3 is not the latest line). `ProtocolError::MissingField` carries a `String`.
- rayd on the Windows host: `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings` (pedantic), `cargo test --workspace` (unix suites compiled out) green. Inside `rust:1.98-slim-bookworm` + `python3` as uid 1000: fmt, clippy pedantic and `cargo test --workspace` green incl. `m4_code.rs` 18/18 (1 ignored); the ignored `real_sidecar_round_trip` passed with the pins in a uv venv (`RAYITO_SIDECAR_ROOT`, `RAYITO_SIDECAR_PYTHON`) in 11.4 s.
- `cargo zigbuild --release --target aarch64-unknown-linux-musl -p rayd` → **4 323 896 B** (M3: 3 868 664 B), `e_machine` 183, no `PT_INTERP`; `cargo tree` for the target has no TLS crate.
- 7.3 local loop (Linux container, debug `rayd` + real sidecar in a uv venv): `hooks-sim.py --only ready,validate,run` → `/ready` 200 after 4 × 503 (4.0 s; sidecar `warmup_ms` 4475), `/validate` 200 `validated` after 1 × 503 (1.0 s; 2 results consumed), `/run` 200 `installed` → `restart_ms` 4053 (x86 emulation, debug build); gRPC through the generated stubs: `Health.kernel_ready` true, `x = 42` / `x` → `42`, `print(x)` → stdout, matplotlib → png (20 282 B) + `e2b/chart`, DataFrame → `e2b/data` + html, `1/0` → `ZeroDivisionError`, `time.sleep(10)` with 2000 ms → `ExecutionTimeout` at 2.01 s and `x` still `42`, `sleep(6)` → one keepalive and `'done'`, `CreateContext(cwd=/tmp, envs)` → `('/tmp', '1')`, list/restart/destroy, `/suspend` + `/resume` → `reseeded 1`; no python processes left after killing `rayd`.
- `/validate` with `--no-sidecar` answers 200 `validate_skipped` (M1 test adapted from `unchanged`).
- Local ARM64 build of `image/Dockerfile` under QEMU (`docker buildx --platform linux/arm64`): every pin installs from aarch64 wheels, `pip check` clean, font cache built as `user`; the image boots (`rayd` root pid 1, sidecar and kernel as `user`), `/ready` 200 after 7 × 503, `/validate` `validated`, `/run` rotation and the same gRPC smoke pass (emulated: `warmup_ms` 15 670, `restart_ms` 15 114).
- 7.4 publish (test account, us-east-1): **6.0** (`rayd-3ba5bfb1da52.zip`, 4 416 309 B; `UPDATED` + `SUCCESSFUL` + `ACTIVE` in **185.3 s**; memory **918 093 824 B**, code install 1 285 054 464 B, disk 39 059 456 B; `chipsetGeneration` 3; build VM `warmup_ms` 9 329 / 9 434, `/ready` 200 after two 503s without the escape; `/validate` cell 250 ms on the restored snapshot). Its real MicroVM measured `run-microvm → kernel_ready` **49.3 s** with `restart_ms` **45 259**: the first kernel start after the restore reads the Python stack from the lazily restored disk (a later `create_code_context` took 2.1 s). Fix, within D9's intent: `/validate` now performs the same default-kernel restart as `/run` before its cell so Lambda samples those pages for prefetch. **7.0** (`rayd-607652950a72.zip`, 4 417 781 B, `rayd` 4 325 368 B; **216.5 s**; memory **920 199 168 B**, code install 1 289 846 784 B, disk 37 994 496 B; `chipsetGeneration` 4; validate VMs `restart_ms` 10 466 / 12 063 before any hint): two fresh MicroVMs measured `run-microvm → agent_ready and kernel_ready` **7.98 s** and **7.67 s** through `Sandbox.create()` with `restart_ms` **2 694** / **2 616** (p50 ≈ 7.8 s: under the D11 8 s rule, no ADR needed; asserted budget 15 s). 6.0 set `INACTIVE`. Memory stays under the 1.2 GB knob: warm-up list untouched.
- Real-VM checks through the SDK on 7.0 (all acceptance cells): first cell 0.11 s, `x` → `42`, `print(x)` in stdout, matplotlib 0.26 s (PNG + `LineChart`), pandas 0.11 s (`data` parsed, html), `1/0` → `ZeroDivisionError` without ANSI, `timeout=2` → `ExecutionTimeout` at 2.11 s with `x` intact, 12 s silent cell `'done'`, `create_code_context` 2.08 s (`/tmp`, envs), isolation `NameError`, `restart_code_context` 2.06 s, envs restored, `ps -o user= -C python3` → `user`, `os._exit(3)` → `KernelDied` then `1+1` in 1.9 s, 20 × `1+1` in 1.97 s, `kill()` true; no MicroVM left running.
- Fixed e2e mismatch (block 12): IPython emits no `execute_result` for a cell whose value is `None`, so `run_code("import os; os.environ.get('M4_RUN')").text` was `None`, not `"None"`; the cell now reads `os.environ.get('M4_RUN', 'unset')` and asserts `"'yes'"` then `"'unset'"` (design block 12 and the delta spec scenario updated to match).

### Review fixes (rayd + sidecar, 2026-09-15)

- [x] R1 `code/execute.rs`: `poll_next` drains the events already queued (`try_recv`) before the timers get a say, so an `end` read late is never rewritten into `ExecutionTimeout` nor followed by a `restart_context`; unit tests over an in-memory `KernelSidecar` (`code/fake_sidecar.rs`, `tokio` `test-util` paused clock): queued end wins, unfinished cell still interrupted, grace restart only for a cell that never ended
- [x] R2 `code/supervisor.rs`: `dispatch_blocked` flag around the `send_timeout` wait (`try_send` first); `note_op_timeout` logs but neither counts nor kills while it is set, and the counter restarts when the stall ends; unit tests (silent sidecar still killed after three; timeouts behind a stall do not) plus `m4_code.rs` `op_timeouts_during_a_stall_do_not_relaunch_the_sidecar` (`big 400`, three `CreateContext` at 200 ms, `sidecar_restarts` 0); `CodeManager::sidecar_restarts()`/`dispatch_blocked()` accessors
- [x] R3 `kernel-sidecar` `executions.py`: `Inbox` (64 channel-message slots via a semaphore; `died`/`abort` sentinels never wait), pumps `await inbox.put`; `tests/test_executions.py` (bounded depth with a blocked `emit`, sentinels pass a full inbox)
- [x] R4 `kernel-sidecar` `protocol.py`: `serialise_mime` total budget `MAX_RESULT_BYTES = 12 MiB` (largest values omitted first); `SidecarServer.emit` backstop `MAX_EVENT_LINE_BYTES = 15 MiB` replacing a `result`/`error` payload by its `rayito/omitted` note (`omit_payload`) and logging `bytes`; `test_protocol.py` (two 7 MiB values, budget order, `omit_payload`) and `test_server.py` (line above the limit leaves omitted)
- [x] R5 Docs: design D2/D5/D6/D8, delta spec (backpressure requirement + three scenarios, mime budget), `ARCHITECTURE.md` sidecar backpressure bullet; `openspec validate m4-code-execution --strict` passes

## 8. [e2e] Green and close

- [x] 8.1 `RAYITO_E2E=1 RAYITO_TEMPLATE=<arn> make test-e2e`: `test_m1_hello.py`, `test_m2_processes.py`, `test_m3_filesystem.py`, `test_m4_code.py` pass; paste the timings of design D15 (`kernel_ready_s` for both sandboxes, first cell, matplotlib, pandas, 20-cell p95, `create_code_context`, `restart_code_context`, timeout latency, 12 s silent cell) and `restart_ms` from the `rayd` log into `MILESTONES.md` M4 "Estado de aceptación"
- [x] 8.2 `AWS_API_NOTES.md` §16: new rows Q34 (snapshot sizes and build time with the scientific stack), Q35 (`run-microvm → kernel_ready` and the default-kernel `restart_ms`; decision rule of design D11 evaluated against 8 s p50, ADR opened for M6 if exceeded), Q36 (`Execute` server-stream through the proxy with 5 s keepalives and 64 KiB chunks)
- [x] 8.3 Docs: `ARCHITECTURE.md` `CodeService` row, "Kernel sidecar" section, domain/ports/adapters tables and hooks table per design D17; `MILESTONES.md` M4 snippet aligned with the e2e; `SECURITY.md` T5 → M4 ✔ and new row T12; `README.md` `run_code` example marked available and the 0.0.4 ↔ M4-image requirement noted
- [x] 8.4 `openspec validate m4-code-execution --strict` passes; acceptance agent runs `openspec archive m4-code-execution --yes`

### Acceptance (2026-09-15, test account, us-east-1)

- R1–R4 landed after 7.0 was built, so acceptance republished: `cargo zigbuild --release --target aarch64-unknown-linux-musl -p rayd` → `rayd` **4 329 272 B** (ARM aarch64, no `PT_INTERP`); `python scripts/copy_sidecar.py kernel-sidecar image/kernel-sidecar` + `python scripts/image_zip.py image image/rayito-image.zip` (30 files, 4 425 299 B); `scripts/publish_image.py` → **8.0** (`rayd-084c93f69ccf.zip`, `UPDATED` + `SUCCESSFUL` + `ACTIVE` in **195.9 s**; memory **925 466 624 B**, code install 1 289 846 784 B, disk 36 929 536 B; `chipsetGeneration` 3; build VM `warmup_ms` 9 682, `/ready` 200 after two 503s; validate VMs `restart_ms` 12 910 / 13 486, `validated`).
- Host before publishing: `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace` 237 tests on Windows (33 + 4 + 12 rayd, 188 rayd-core) and 320 inside `rust:1.98-slim-bookworm` as uid 1000 with `python3` (`m2_process` 25, `m3_filesystem` 30, `m4_code` 19/19 + 1 ignored); `kernel-sidecar` 56 host tests + ruff + mypy; `clients/python` 324 unit tests + ruff + ruff format + mypy.
- `RAYITO_E2E=1 RAYITO_TEMPLATE=<arn> RAYITO_EXECUTION_ROLE_ARN=<role> uv run pytest tests/e2e -v -m e2e -s` → **4 passed in 157.15 s** (M1 + M2 + M3 + M4, first run). D15 numbers: `kernel_ready_s` 3.37 / 6.23 / 6.21 / 6.35 / 6.17 s (fixtures) and 6.17 s (second M4 sandbox) → p50 6.22 s < 8 s (D11: no ADR); `restart_ms` 2 030 / 2 284 / 3 329 / 3 053 / 3 454 from CloudWatch; first cell 0.11 s; matplotlib 0.32 s; pandas 0.11 s; timeout 2.11 s; 12 s silent cell 12.11 s; `create_code_context` 2.09 s; `restart_code_context` 2.10 s; 20 cells 2.10 s (p95 0.11 s); cell after `KernelDied` 1.88 s.
- Q36 probe on one extra MicroVM (raw `Execute` stub): `keepalive` at 5.10 s and 10.10 s on the 12 s cell (2 keepalives, `seq` 0); `print('x' * 3_000_000)` → 46 `OutputChunk` (45 × 65 536 B + 50 881 B) in 0.77 s (3.88 MB/s); that VM's `restart_ms` 4 077, `kernel_ready_s` 8.05.
- `list-microvms` after the run: 0 live MicroVMs. Cost: 1 image version + 6 MicroVMs of < 3 min.

