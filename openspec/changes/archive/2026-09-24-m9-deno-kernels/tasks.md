## 0. [infra] Pre-flight facts (design "Context")

- [x] 0.1 Confirm the Deno pin against the official release:
  - `gh api repos/denoland/deno/releases/tags/v2.9.7 --jq '.assets[] | select(.name=="deno-aarch64-unknown-linux-gnu.zip") | .size, .digest'` prints `39821318` and `sha256:c832298b1ad4422481334855f6003e0f54145762c5a134f20a489511d2f65bbf`
  - `gh release download v2.9.7 -R denoland/deno -p "deno-aarch64-unknown-linux-gnu.zip.sha256sum" -O -` carries the same hash
  - if either differs, stop and update design D2 before continuing
- [x] 0.2 Confirm the Deno kernel facts D3/D4 rely on:
  - `gh api "repos/denoland/deno/contents/cli/js/jupyter_kernel.js?ref=v2.9.7" --jq .content | base64 -d` still shows `Deno.listen({ hostname: this.ip, port: this.port })`, `hmacVerify` and the `interrupt_request` branch calling `op_jupyter_repl_interrupt()`
  - `libs/cli_parser/src/env_vars.rs` at the same tag lists `NO_COLOR`, `DENO_DIR` and `DENO_NO_UPDATE_CHECK`
  - if the transport or interrupt facts changed, stop and amend ADR-013 in design D4
- [x] 0.3 Confirm the Contract agent applied the comment-only proto delta of design D9 to `proto/rayito/v1/code.proto`:
  - the `"javascript" es un nombre reservado` sentence is gone and both comments name `typescript`
  - `buf lint` is clean and `buf breaking` against a scratchpad copy of the previous `proto/` reports nothing
  - regenerate with `cargo build -p rayito-proto`, `cd clients/python && python ../../scripts/gen_python.py` (per its docstring) and `cd clients/typescript && pnpm gen`
  - Verificado aquí (2026-09-23, macOS arm64, reintento de specs-docs): `grep reservado proto/rayito/v1/code.proto` no da nada y los dos comentarios (líneas 31–35 y 57) nombran `typescript`. `buf lint` (buf 1.73.0) → exit 0 sin salida; `git archive dde681c proto` a un directorio temporal y `buf breaking proto --against <copia>/proto` → exit 0 sin salida. Regeneración comprobada sin escribir en el árbol: `buf generate` con los mismos plugins remotos de `buf.gen.yaml` hacia un directorio temporal y `diff -rq` contra `clients/typescript/src/gen` y `clients/python/src/rayito/v1` → idénticos (el delta es sólo de comentarios: `code_pb.ts` los lleva, el gencode Python no cambia). `gen_python.py` no sirve de comprobación (emite gencode 7.35.1, ver `m9-egress-policy` 1.3); el lado Rust lo compila `build.rs` en cada `cargo build` (1.6).
- [x] 0.4 Note in the "Notes" section at the end of this file:
  - the newest ACTIVE `rayito-base` and `rayito-base-poly` versions
  - the `snapshotBuild` of that `rayito-base`, read with the existing CLI/`publish_image.py` output or `get-microvm-image-build`; this becomes `RAYITO_BASE_PREVIOUS_SIZES`
  - its rayd binary size, from `stat -c %s /usr/local/bin/rayd` in one sandbox of it, terminated afterwards
  - placeholders only if anything is copied into tracked files

## 1. [rayd] Language catalog in rayd-core (design D6)

- [x] 1.1 `crates/rayd-core/src/code/language.rs`:
  - `Language::Typescript`, `ALL: [Self; 4]`, `parse("typescript")`, `as_str` → `"typescript"`
  - module doc naming the four wire names and the Deno kernel of `rayito-base-poly`
- [x] 1.2 `crates/rayd-core/src/code/error.rs`: `InvalidLanguage` message `"language must be one of python, bash, javascript, typescript"`; `LanguageUnavailable` text unchanged
- [x] 1.3 Unit tests of D11, which must fail before 1.1/1.2:
  - `parse` accepts `typescript` and still rejects `ts`/`TypeScript`/`typescript `
  - `ALL` has four entries and yields `default-typescript`
  - `ExecuteTarget::resolve(None, Some("typescript"))`
  - `AvailableLanguages::from_ready(["typescript","javascript"])`
  - the exact `InvalidLanguage` string and `LanguageUnavailable(Typescript)` naming `rayito-base-poly`
  - update `crates/rayd/src/grpc/code.rs` tests only where they assert the old message
- [x] 1.4 `crates/rayd/tests/fixtures/fake_sidecar.py`: `KNOWN_LANGUAGES` gains `typescript`, docstring updated
- [x] 1.5 `crates/rayd/tests/m9_deno.rs` (`#![cfg(unix)]`, same harness as `m7_poly.rs`), with the four tests of D11:
  - the lazy `default-typescript` with exactly one `create_context` line
  - `UNIMPLEMENTED` naming `rayito-base-poly` for `typescript`/`javascript` when the fake announces `python,bash`
  - `INVALID_ARGUMENT` for `ts` with the four-name message
  - per-execution `envs` refused on `typescript` with no `execute` line
- [x] 1.6 Gates with your own `CARGO_TARGET_DIR`: `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace --locked`, and the musl cross-build (`cargo zigbuild --release --target aarch64-unknown-linux-musl -p rayd`)
  - verified here on a Linux ARM64 host (see the [rayd] note): clippy clean, the musl build succeeds, and every test of this change passes. The only red items come from other changes or from the host (listed in the note)

## 2. [sidecar] Deno kernelspecs, transport and ANSI strip (design D3, D4, D5)

- [x] 2.1 Write the exact JSON of D3:
  - `kernel-sidecar/jupyter/kernels/rayito-typescript/kernel.json`
  - `kernel-sidecar/jupyter/kernels/rayito-javascript/kernel.json` (same argv/env, `interrupt_mode: "message"`, `display_name`/`language` per file)
- [x] 2.2 `languages.py`:
  - `Transport = Literal["ipc", "tcp"]`
  - `KernelLanguage` gains `transport: Transport = "ipc"` and `strips_plain_text_ansi: bool = False`
  - catalog entries in the order `python`, `bash`, `javascript`, `typescript` with the values of D4
  - module docstring rewritten: Deno instead of ijavascript, why the transport differs, the `void 0` probe
- [x] 2.3 `kernels.py`:
  - `LOOPBACK_IP`, `KernelEndpoint(transport, ip)` and `kernel_endpoint(language, socket_dir)`
  - `KernelContext._start_kernel` passes `transport=`/`ip=` from `kernel_endpoint`; the socket dir is still created 0700 for the connection file and removed on stop
  - `KernelContext` docstring naming Deno and ADR-013
  - no inline comments inside function bodies
- [x] 2.4 `protocol.py`: `PLAIN_TEXT_MIME` and `strip_plain_text_ansi(mime)`, pure, reusing `strip_ansi`
- [x] 2.5 `executions.py` and `kernels.py`:
  - `run_execution(..., *, strip_plain_text_ansi: bool = False)` threaded through `_run_user_cell` to `_on_iopub`, applied to the serialised bundle of `display_data`/`execute_result`/`update_display_data` only
  - `KernelContext._run_cell` passes `self._kernel.strips_plain_text_ansi`
- [x] 2.6 Sidecar tests of D11, which must fail before 2.1–2.5:
  - `test_languages.py`: the catalog, `test_repository_ships_the_deno_templates`, `test_deno_specs_need_the_binary` replacing `test_javascript_needs_node_and_the_kernel_file`, and `test_kernel_endpoint`
  - `test_protocol.py::test_strip_plain_text_ansi`
  - `test_executions.py::test_plain_text_ansi_stripped_only_when_asked`
  - `test_kernel.py::test_deno_typescript_context`, marked `kernel` and skipped unless `RAYITO_TEST_DENO`
- [x] 2.7 Gates: `cd kernel-sidecar && uv run pytest && uv run ruff check . && uv run ruff format --check . && uv run mypy`

## 3. [python] SDK language set and E2B shim mapping (design D7, D8)

- [x] 3.1 `clients/python/src/rayito/_code_base.py`: `SUPPORTED_LANGUAGES` gains `typescript`; `LANGUAGE_ALIASES = {"js": "javascript", "ts": "typescript"}`
- [x] 3.2 Docstrings of `run_code`/`create_code_context` updated per D7 in `sandbox_sync/code.py`, `sandbox_sync/main.py` and their `sandbox_async` twins, plus the module docstring of `cli/_artifact.py` (Spanish prose, identical sync/async wording)
- [x] 3.3 `e2b/_compat.py`:
  - the new `AVAILABLE_KERNELS_REASON`
  - `POLY_KERNELS_REASON`
  - the pure `unimplemented_language(error, feature, language) -> UnimplementedError | None`
  - docstring of `normalized_language_or_unimplemented` updated
  Verificado aquí (2026-09-23, macOS arm64, `clients/python`, reintento): `AVAILABLE_KERNELS_REASON` nombra typescript y `rayito-base-poly`, `POLY_KERNELS_REASON` y `unimplemented_language` según D8.
  Mutación: `unimplemented_language` que siempre devuelve `None` → 3 fallos (base, sync, async); quitar typescript del motivo → 1 fallo.
- [x] 3.4 `e2b/_sync.py` and `e2b/_async.py`: `run_code` and `create_code_context` re-raise an agent `UNIMPLEMENTED` as `unimplemented_language(...)` `from` the native exception and re-raise anything else unchanged; docstrings updated; identical surfaces
  Verificado aquí (2026-09-23, macOS arm64, `clients/python`, reintento): `run_code`/`create_code_context` de `e2b/_sync.py` y `e2b/_async.py` capturan el `InvalidArgumentException` nativo y relanzan `unimplemented_language(...)` `from exc`; el resto se propaga.
  Mutación: sin la reasignación en los cuatro métodos → 2 fallos (`test_kernel_not_shipped_is_unimplemented_from_the_native_error` sync y async).
  Fix de revisión (2026-09-23): la `feature` usa el nombre dado, como piden las dos specs `e2b-compat` y D8 (`create_code_context(language='ts')`, no `'typescript'`), en los cuatro sitios de `e2b/_sync.py` y `e2b/_async.py`; así coincide con la ruta cliente (`'r'`). Las aserciones de `test_e2b_compat_{sync,async}.py` se cambiaron primero: 2 fallos con el código anterior, verde después (143 passed en `test_e2b_compat_{base,sync,async}.py`).
- [x] 3.5 `tests/unit/fake_code.py`: `KNOWN_LANGUAGES` gains `typescript`; the `INVALID_ARGUMENT` text is rayd's new message
- [x] 3.6 Unit tests of D11, which must fail before 3.1–3.4:
  - `test_code_base.py` language table and rejected list (`typescript` moves to accepted, `tsx` added to rejected)
  - `test_code_sync.py`/`test_code_async.py` alias on the wire
  - `test_e2b_compat_base.py` helper and reason
  - `test_e2b_compat_sync.py`/`_async.py` mapping to `UnimplementedError` with `__cause__`
  - verified here: the two native bullets are written; with 3.1 reverted in a scratch copy, 8 tests fail, and with 3.1 applied, 78 pass. The two e2b bullets and 3.3/3.4 stay open for the Parity step, which owns `rayito/e2b/`
  Fix de revisión (2026-09-23): `test_code_base.py::test_result_from_proto_maps_every_mime_field` era intermitente (el orden de un mapa protobuf no está garantizado: 3 de 8 corridas en rojo con `'rayito/omitted' != 'application/vnd.x'`). `result_from_proto` construye ahora `extra` con las claves ordenadas y el docstring de `Result.formats()` lo dice; 15 de 15 corridas verdes.
  Las dos viñetas e2b ya están: `test_e2b_compat_base.py::test_language_gate_forwards_typescript_and_its_alias` y `test_unimplemented_language_maps_only_an_agent_unimplemented`;
  `test_e2b_compat_{sync,async}.py::test_*typescript_alias_reaches_the_agent`, `test_*kernel_not_shipped_is_unimplemented_from_the_native_error` (con `__cause__` y `grpc_code` `UNIMPLEMENTED`) y `test_*other_native_errors_are_not_remapped`. Rojo→verde por las mutaciones de 3.3/3.4.
- [x] 3.7 Gates: `cd clients/python && uv run pytest tests/unit && uv run ruff check . && uv run ruff format --check . && uv run mypy src tests`
  - Gate de Python (2026-09-23, macOS arm64, reintento de specs-docs): `uv run --with pytest-timeout pytest tests/unit --timeout 60` da 2090 passed en 321 s, exit 0; `ruff check .` limpio; `ruff format --check .` 198 ficheros (el `README.md` ya formateado); `mypy src tests` sin errores en 196 ficheros.
  - verified here on the files of this change: ruff check, ruff format --check and mypy are clean. The full-tree gate is still red, and every failure is in a sibling M9 change that is mid-implementation (network, listing, metrics history, lifecycle, transfer)
  Sin marcar. Última corrida (2026-09-23, macOS arm64, reintento): `uv run --with pytest-timeout pytest tests/unit --timeout 60` 2058 passed, 2 failed; los dos fallos son nativos y ajenos al shim: `test_sandbox_sync.py::test_is_running_request_timeout_bounds_the_health_deadline` y su gemelo async (el plazo de `Health` llega como `0.501 <= 0.5` con la máquina cargada; la revisión los vio fallar también aislados; los corrigió después el fixer de python y la corrida completa de hoy da 2090 passed). `ruff check .` limpio, `ruff format --check .` 198 ficheros, `mypy src tests` sin errores en 196 ficheros. Los ficheros del shim (`test_e2b_compat_{base,sync,async}.py`, `test_e2b_v2_{base,sync,async,exports}.py`, `test_packaging.py`) dan 530 passed.
  Corrida del fixer python (2026-09-23, macOS arm64, tras los fixes de revisión): `uv run --with pytest-timeout pytest tests/unit --timeout 60` 2090 passed, 0 failed (332 s; los `is_running` ya no fallan); `ruff check .` limpio; `mypy src tests` sin errores en 196 ficheros; `ruff format --check .` sólo marca `clients/python/README.md` (bloques de código de la doc, área de docs, en edición por otro agente). Queda sin marcar hasta que ese fichero pase el formato.

## 4. [ts] SDK language set (design D7)

- [x] 4.1 `clients/typescript/src/sandbox/code.ts`: `SUPPORTED_LANGUAGES` gains `"typescript"`; `LANGUAGE_ALIASES` gains `["ts", "typescript"]`; JSDoc of `normalizeLanguage` and `RunCodeOptions.language` updated
- [x] 4.2 `tests/unit/fake/code.ts`: `KNOWN_LANGUAGES` gains `typescript`; the `InvalidArgument` text is rayd's new message
- [x] 4.3 `tests/unit/code.test.ts` per D11 (fails before 4.1):
  - routing with `ts`/`TypeScript` and `default-typescript`
  - `tsx` rejected
  - "language not shipped" parametrised over `javascript` and `typescript`
- [x] 4.4 Gates: `cd clients/typescript && pnpm lint && pnpm typecheck && pnpm test && pnpm build`
  - verificado aquí: Gate completo del paquete el 2026-09-23 en este árbol: `pnpm lint` (biome, 114 ficheros, 0 errores), `pnpm typecheck` exit 0, `pnpm test` 35 ficheros / 762 passed, `pnpm build` ok y `pnpm pack:check` ok (lista `package/dist/index.{mjs,cjs,d.mts,d.cts}`). Los fallos de ficheros hermanos que dejaban la casilla abierta ya no existen.
  - reverificado (2026-09-23, reintento): `pnpm lint` (biome, 123 ficheros, 0 errores), `pnpm typecheck` exit 0, `pnpm test` 38 ficheros / 797 passed, `pnpm build` exit 0 y `pnpm pack:check` exit 0.

## 5. [infra] Image layer and download pin gate (design D2, D10)

- [x] 5.1 `image/Dockerfile`:
  - replace the poly conditional `RUN` with D2's exact instruction (one layer, still guarded by the marker)
  - rewrite its Spanish header comment: Deno 2.9.7 pinned by sha256, Python `zipfile` because there is no `unzip`, `/opt/rayito/deno/deno` root-owned 0755, not warmed, inert in `rayito-base`, how to bump
  - update the file's top comment, which says "sus sockets ipc viven en /run/rayito/k", to mention the Deno loopback TCP exception (ADR-013)
- [x] 5.2 `scripts/check_pins.py`:
  - gate 3 `unpinned_downloads` with the constants of D10
  - `DEFAULT_PATHS` gains `image/Dockerfile`
  - `findings_for` applies the gate to files named `Dockerfile`
  - module docstring describes the third gate
- [x] 5.3 `scripts/tests/test_check_pins.py`: the D10 cases (must fail before 5.2), and `main()` over the real tree returns 0
- [x] 5.4 Gates: `python scripts/check_pins.py` prints OK and includes `image/Dockerfile`; `cd clients/python && uv run pytest ../../scripts/tests -p no:cacheprovider`; `uvx ruff==0.16.7 check scripts` and `uvx ruff==0.16.7 format --check scripts` (the Makefile's pin)
- [x] 5.5 Build both zips by hand (no `make` on Windows):
  - verificado aquí (2026-09-23), VM Lima Ubuntu 24.04 aarch64, sin AWS: `cargo auditable zigbuild --release --locked -j 2 --target aarch64-unknown-linux-musl -p rayd` (zig 0.16.0, cargo-zigbuild 0.23.4) → `scripts/check_auditable.py` `.dep-v0: 257 packages, root rayd 0.2.0`; `stat -c %s image/rayd` = 13 755 256 B (ELF aarch64 estático, stripped). `copy_sidecar.py` + `image_zip.py` → `rayito-image.zip` 13 873 692 B (35 entradas), `rayito-image-slim.zip` 13 873 863 B, `rayito-image-poly.zip` 13 873 833 B (36); `marker_variant` da `full`, `slim` y `poly`, y los tres zips llevan `kernel-sidecar/jupyter/kernels/rayito-{javascript,typescript}/kernel.json`. Además, `docker build` nativo arm64 del contenido de cada zip (Docker 29.1.3): las dos builds pasan (dnf con `git-core-2.50.1-1.amzn2023.0.1`, comprobación de herramientas, `pip check`, calentamiento); en la poly, `bash_kernel` 0.10.0, `/tmp/deno.zip: OK` del `sha256sum -c`, `deno 2.9.7` como `user`, `/opt/rayito/deno/deno` root 0755 de 84 971 392 B; en la base no existe `/opt/rayito/deno`. Tamaño de imagen local: base 251 520 873 B, poly 293 678 398 B (+42 MB). Humo local con el contenedor y `rayd` como CMD: `hooks-sim.py` con el drill gRPC verde en las dos; SDK Python contra ese `rayd` (plano de control falso en loopback): celdas python, bash, javascript (0,7 s con arranque del kernel) y typescript (0,5 s) en la poly, `write` con `metadata` + `gzip` y `get_info` devuelven el mismo `metadata`, y `set_timeout(5)` con `on_timeout="kill"` → el contenedor sale con código 124. `RAYITO_RAYD_BYTES_DELTA` = 13 755 256 menos el tamaño de 0.4 (*pendiente de aceptación en AWS*).
  - `cargo zigbuild --release --target aarch64-unknown-linux-musl -p rayd` and copy the binary to `image/rayd`
  - `python scripts/copy_sidecar.py kernel-sidecar image/kernel-sidecar`
  - `python scripts/image_zip.py image image/rayito-image-poly.zip --variant poly` and `python scripts/image_zip.py image image/rayito-image.zip`
  - `marker_variant` gives `poly` and `full`
  - both zips contain `kernel-sidecar/jupyter/kernels/rayito-typescript/kernel.json`
  - record `stat` of `image/rayd` in the Notes; `RAYITO_RAYD_BYTES_DELTA` = this minus the 0.4 size

## 6. [e2e] Real publish, acceptance and measurements (design D12)

- [x] 6.1 Publish `rayito-base-poly`: `uv run --with "boto3>=1.43.82,<2" python scripts/publish_image.py --artifact image/rayito-image-poly.zip --variant poly --bucket <tu-bucket> --base-image-version 1` (the Makefile's `BASE_IMAGE_VERSION`, Q52). Record in the Notes (placeholders only): build seconds, `SUCCESSFUL`/`ACTIVE`, the three `snapshotBuild` sizes. A build failure on the Deno lines means pasting the `stateReason` into the Notes and stopping
- [x] 6.2 Publish `rayito-base` with the marker-less zip and the same flags; record the same numbers
- [x] 6.3 Move `poly_template()` and the `poly_sandbox` fixture from `clients/python/tests/e2e/test_m7_poly_kernels.py` into `tests/e2e/conftest.py` unchanged; delete `test_javascript_is_known_but_not_shipped` (its claim is now false)
- [x] 6.4 Write `clients/python/tests/e2e/test_m9_deno_kernels.py` with the eight tests of D12 (Spanish module docstring, `report()` printing `[m9] …`, every VM terminated in teardown)
  - verified here: written by the [python] implementer. The 13 tests of both files are collected with `-m e2e` and skip without `RAYITO_E2E`. ruff and mypy are clean. They run against AWS in 6.5. `test_e2b_shim_js_ts_on_poly` and the shim half of `test_js_ts_unimplemented_on_base` also need 3.3/3.4
- [x] 6.5 Run `RAYITO_E2E=1 RAYITO_TEMPLATE=rayito-base RAYITO_TEMPLATE_POLY=rayito-base-poly RAYITO_POLY_SIZES=… RAYITO_BASE_SIZES=… RAYITO_BASE_PREVIOUS_SIZES=… RAYITO_RAYD_BYTES_DELTA=… uv run pytest tests/e2e/test_m9_deno_kernels.py tests/e2e/test_m7_poly_kernels.py -m e2e -s`, all green:
  - on failure of `test_javascript_survives_pause_resume`, stop: the change stays open and ADR-013 is amended (design Risks)
  - paste the `[m9]` report lines into the Notes: first/second-cell latencies, Deno RSS, timeout path, the six size numbers, `deno_code`, `deno_memory`
  - Parcial, carril poly (2026-09-23): `RAYITO_TEMPLATE` y `RAYITO_TEMPLATE_POLY` = ARN de `rayito-base-poly` (5.0), `-k "not unimplemented_on_base"` para no tocar la plantilla base mientras corre su carril → **9 passed, 2 failed, 2 deselected en 145 s**. Verdes: los seis tests poly de M9 (incluido `test_javascript_survives_pause_resume`: `kept` = 42, `kernel_state_lost` falso) y los tres poly de M7. Rojos: sólo las bandas de tamaño, sin VM: `test_deno_snapshot_sizes` (`deno_code` 136 863 744 B > 100 MB; `deno_memory` 18 427 904 B, delta de memoria de la base 4 210 688 B y delta de code de la base neto de `rayd` 37 648 488 B > 10 MB por git-core) y `test_m7_poly_kernels.py::test_snapshot_sizes` (code de la base frente a 17.0 neto de `rayd` 53 090 408 B > 10 MB). El binario de Deno ocupa 84 971 392 B en el sandbox, así que el exceso de `deno_code` no es Deno: decisión del orquestador sobre las bandas. Informe `[m9]`: primera celda TS 15,67 s y JS 10,53 s (VMs frías justo tras publicar; 0,49-0,74 s en cuatro VMs posteriores), segunda 1,39 s y 0,10 s; RSS 205 432 KiB; timeout 2,20 s por interrupt por mensaje (estado conservado); `pause()` 1,04 s, `resume()` 0,57 s. Pendientes: `test_js_ts_unimplemented_on_base` y `test_language_unimplemented_on_base` (carril base)
  - Regresión (2026-09-24, carril de regresión): `-k unimplemented_on_base` con `RAYITO_TEMPLATE` = ARN de `rayito-base` 21.0 → **2 passed en 30 s** (`test_js_ts_unimplemented_on_base`: `language typescript is not installed in this image; use rayito-base-poly`, `test -e /opt/rayito/deno` exit 1, shim `UnimplementedError`; `test_language_unimplemented_on_base` verde). Con esto las 11 pruebas con VM están verdes; siguen rojas sólo `test_deno_snapshot_sizes` (136 863 744 B > 100 MB) y `test_m7_poly_kernels.py::test_snapshot_sizes` (53 090 408 B > 10 MB): sin marcar hasta que el orquestador decida las bandas.
  - Bandas revisadas (2026-09-24, aprobado por el responsable, design D12 «Band revision»): con los números reales, `deno_code` 110-160 MB (medido 136 863 744 y 132 603 904 B: `codeInstallSizeInBytes` no es la suma de bytes de los ficheros; el binario mide 84 971 392 B), `|deno_memory|` ≤ 40 MB (medido 18 427 904 y 31 584 256 B: el delta por pares suma el ruido de cuatro builds, Q50), code install de `rayito-base` frente a la anterior neto de `rayd` ≤ 50 MB (medido 37 648 488 y 40 843 368 B: git-core de Q76) y la banda de code de `test_m7_poly_kernels.py::test_snapshot_sizes` frente a 17.0 pasa de 10 a 70 MB (medido 56 285 288 B en 22.0). Con los tamaños de 22.0 (`RAYITO_*_SIZES` y `RAYITO_RAYD_BYTES_DELTA` de la regresión) los dos tests fallaban (`132603904 <= 100000000`, `56285288 <= 10000000`) y ahora pasan sin crear VMs (`pytest --noconftest -m e2e`, 2 passed); con los de 5.0/21.0 también
  - Hecho (2026-09-24, aceptación): con las bandas revisadas, `test_m9_deno_kernels.py` **8/8** y `test_m7_poly_kernels.py` **5/5** dentro de la regresión e2e de Python de 2026-09-24 sobre `rayito-base` 23.0, `rayito-base-caps` 12.0 y `rayito-base-poly` 7.0: **62 passed de 62** (server-timeout 12, observability 4, deno 8, egress 14, transfer 14, M4 1, M5 2, M6 2, M7 poly 5), incluidos `test_javascript_survives_pause_resume` (`kernel_state_lost` falso) y los dos tests de tamaños. Informe `[m9]`: primera celda TS 14,99 s y JS 4,39 s (VMs frías recién publicadas), segunda 0,15 s y 0,09 s; RSS de Deno 205 060 KiB; `timeout=2` vuelve a los 2,22 s por interrupt por mensaje (estado conservado); `pause()` 1,11 s y `resume()` → `kernel_ready` 0,52 s; `deno_code` 132 603 904 B y `deno_memory` 31 584 256 B; `rayito-base` frente a la anterior: memoria +3 158 016 B, code neto de `rayd` +40 843 368 B. Sin ADR-013 que enmendar.
- [x] 6.6 `kernel_ready_s`: run `cd clients/python && uv run python ../../scripts/bench_cold_start.py --template rayito-base --phases a --sequential 5 --out <scratchpad>` and the same with `--template rayito-base-poly`; check `p50(poly) ≤ p50(base) + 1.0 s`; record both p50s. The raw JSON stays in the scratchpad
  - Parcial (2026-09-23): `--template rayito-base-poly --phases a --sequential 5` → `kernel_ready_s` p50 **6,619 s** (p95 9,589, min 5,185), 5 lanzamientos, 0 fallos, ≈ 0,01 USD; JSON en el scratchpad. Falta la mitad `--template rayito-base` (carril base) y la comparación `p50(poly) ≤ p50(base) + 1,0 s`
  - Hecho (2026-09-24, regresión): `--template rayito-base --phases a --sequential 5` (resuelve 21.0) → `kernel_ready_s` p50 **5,807 s** (p95 6,047, min 5,429), 5 lanzamientos, 0 fallos, ≈ 0,0085 USD; JSON en el scratchpad. `p50(poly) 6,619 ≤ p50(base) 5,807 + 1,0` ✓ (diferencia 0,81 s).
- [x] 6.7 TypeScript: extend `clients/typescript/tests/e2e/poly.e2e.test.ts` per D12 (poly test replacing the JS-unimplemented assertion, plus the new base test), then run `RAYITO_E2E=1 RAYITO_TEMPLATE=rayito-base RAYITO_TEMPLATE_POLY=rayito-base-poly pnpm test:e2e` until green
  - Parcial (2026-09-23): el fichero ya tenía el test poly y el nuevo test base de D12. `pnpm test:e2e tests/e2e/poly.e2e.test.ts -t "bash, javascript and typescript"` con las dos variables en el ARN de poly → 1 passed, 1 skipped (15 s): primera celda TS 0,53 s, segunda 0,11 s, primera JS 0,74 s. Falta el test "javascript and typescript are UNIMPLEMENTED on rayito-base" con `RAYITO_TEMPLATE=rayito-base` (carril base)
  - Hecho (2026-09-24, regresión): `pnpm test:e2e tests/e2e/poly.e2e.test.ts` con `RAYITO_TEMPLATE` = `rayito-base` y `RAYITO_TEMPLATE_POLY` = `rayito-base-poly` → **2 passed (2)** en 36,3 s, incluido "javascript and typescript are UNIMPLEMENTED on rayito-base".
- [x] 6.8 Record whether `NO_COLOR` alone removed the escapes. Run in a poly sandbox, as uid 1000: `commands.run("NO_COLOR=1 /opt/rayito/deno/deno eval 'console.log(Deno.inspect(42, {colors: !Deno.noColor}))' | od -c | head -2")`, and the same without `NO_COLOR`. Add a sentence to the Notes
  - Hecho (2026-09-23, poly 5.0, `id -u` = 1000): con `NO_COLOR=1` `od -c` da `4 2 \n`; sin él, `033 [ 3 3 m 4 2 033 [ 3 9 m \n`. `NO_COLOR` sola quita los escapes en ARM64 real, igual que en el contenedor del sidecar; el strip de D5 queda como defensa en profundidad
- [x] 6.9 Teardown check: `list-microvms` for both images shows no live VM created by this run
  - Hecho para lo creado por este carril (2026-09-23): `list-microvms` de `rayito-base-poly` → 0 vivos tras pytest, NO_COLOR, bench y TS. En `rayito-base` había 1 VM `PENDING` (`microvm-<id>`) del carril base, que este carril no creó ni tocó

## 7. [e2e] Records and docs after the measurements (design D13, D14)

- [x] 7.1 Add the `AWS_API_NOTES.md` §16 row of D12 (next free Q number) with the measured numbers, placeholders only (`microvm-<id>`, image names, no account, bucket or ARN)
  - Parcial (2026-09-23): fila **Q77** añadida con builds, los seis tamaños, `deno_code`/`deno_memory`, `kernel_ready_s` de poly, latencias TS/JS, RSS, `NO_COLOR`, camino del timeout y pausa/reanudación. Falta el p50 de `kernel_ready_s` de `rayito-base` (6.6, carril base), marcado *pendiente* en la fila
  - Completado (2026-09-24, regresión): el p50 de `rayito-base` (5,81 s) sustituye al *pendiente* de la fila Q77.
- [x] 7.2 `ARCHITECTURE.md`: ADR-013 "Kernels Deno sobre TCP de loopback con HMAC" (context, decision, consequence, reversibility per D4); the Capa 1 table row "Kernel Jupyter" and the Dockerfile paragraph
  - Verificado aquí (2026-09-23, reintento de specs-docs): ADR-013 con contexto, decisión, consecuencia (iopub legible por procesos locales, residual aceptado en T12, `allow_all`) y reversibilidad (`KernelLanguage.transport`); la fila «Kernel Jupyter» de «Qué corre dónde» (el esquema y la tabla) dice `ipc` para Python/bash y TCP de loopback para Deno; el párrafo del Dockerfile de Capa 1 nombra el pin de Deno 2.9.7 y `check_pins.py`; «Variantes de imagen» nombra poly con Deno. `test_m9_docs.py::test_the_m9_adrs_are_written` rojo → verde.
- [x] 7.3 `SPEC.md`: the §3 kernels row and the §4 non-Python kernels bullet with the R/Java measured reasons (D13)
  - Verificado aquí (2026-09-23, reintento de specs-docs): §3 «Kernels» (python; bash, javascript/js y typescript/ts en poly con Deno, M7/M9), §4 con los tamaños medidos de R y Java, §5 «Kernel Jupyter» con la excepción TCP de ADR-013; `grep reservado SPEC.md` vacío.
- [x] 7.4 `docs/site/docs/kernels.md` per D13 (table, examples, lazy-start latency from 6.5, "JavaScript y TypeScript con Deno", "Agentes anteriores a M9", "R y Java"), then `cd clients/python && uv run --group docs mkdocs build -f ../../docs/site/mkdocs.yml --strict`
  - Hecho salvo la cifra medida (2026-09-23, reintento de specs-docs): tabla de lenguajes (`javascript`/`js` y `typescript`/`ts` = Deno en poly, `UNIMPLEMENTED` en el resto), ejemplos TS/JS con `Deno.jupyter.html`, `npm:` y `Deno.env`, «Arranque perezoso» con los contextos `default-javascript`/`default-typescript`, «JavaScript y TypeScript con Deno» (por qué Deno, el pin y cómo subirlo, TCP de loopback, interrupción por mensaje, `NO_COLOR`/ANSI, permisos), «Por qué no `ijavascript`», «R y Java» y «Agentes anteriores a M9». `mkdocs build --strict` verde. Sin marcar sólo por la latencia de la primera celda Deno, que el texto deja como *pendiente de aceptación en AWS* hasta 6.5.
  - Hecho (2026-09-23): «Arranque perezoso» cita Q77 (primera celda Deno 0,5-0,75 s, siguientes ≈ 0,1 s, 10-16 s en frío tras publicar, ≈ 200 MiB de RSS por kernel) en lugar de *pendiente*. `mkdocs build --strict` verde
- [x] 7.5 `docs/site/docs/e2b-compat.md` kernels row and `README.md` line 73
  - Verificado aquí (2026-09-23, reintento de specs-docs): `e2b-compat.md` mapea `run_code(language="javascript" / "js" / "typescript" / "ts")` a Deno en poly, el kernel ausente fuera de poly y `r`/`java` en la tabla de `UnimplementedError`; `README.md` (y `clients/python/README.md`) ya no dicen «reservado». `test_m9_docs.py::test_javascript_is_no_longer_called_reserved` rojo → verde.
- [x] 7.6 `SECURITY.md`: T8, T10 and T12 appends of D13; `cd clients/python && uv run pytest ../../scripts/tests/test_security_docs.py -p no:cacheprovider` stays green
  - Verificado aquí (2026-09-23, reintento de specs-docs): T8 (`npm:`/`jsr:`/`https:` por el mismo egress), T10 (Deno fijado por versión y sha256, puerta 3 de `check_pins.py`), T12 (residual de ADR-013: puertos en `127.0.0.1`, HMAC en shell/control, iopub legible, `allow_all`). `cd clients/python && uv run pytest ../../scripts/tests` → 184 passed (incluye `test_security_docs.py`).
  - Hallazgo de la revisión sobre la puerta 3 (una instrucción con un `curl` verificado dejaba pasar un segundo `curl`, un `curl | sh`, `wget` y `ADD <url>`): `check_pins.py` ahora exige que cada `curl` escriba con `-o`/`--output` una ruta que compruebe un `sha256sum -c` de la misma instrucción, no más `curl` que comprobaciones, ninguna tubería tras `curl`, y rechaza `wget` y `ADD` con URL. Tests nuevos en `scripts/tests/test_check_pins.py` (`test_a_second_download_in_a_verified_instruction_is_a_finding`, `test_a_download_piped_or_written_to_stdout_is_a_finding`, `test_the_checked_path_must_be_the_downloaded_path`, `test_wget_and_remote_add_are_findings`): 4 failed, 15 passed antes del cambio; 19 passed después; `python scripts/check_pins.py` sigue en OK sobre el `Dockerfile` real. El comentario del `Dockerfile` y T10 describen la regla nueva y su límite léxico (design D10).
- [x] 7.7 `MILESTONES.md` M9 section: the `m9-deno-kernels` line and its acceptance, status with placeholders
  - Verificado aquí (2026-09-23, reintento de specs-docs): sección «M9 — Paridad con E2B» creada con una línea por cambio; la de `m9-deno-kernels` nombra su aceptación (publicar poly y base, los ocho tests de D12, el e2e de TS, la fila §16) y el estado *pendiente de aceptación en AWS*.
- [x] 7.8 Final gates:
  - `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace --locked`
  - the Python and TypeScript gates of 3.7/4.4
  - the sidecar gates of 2.7
  - `buf lint`
  - `python scripts/check_pins.py`, `python scripts/check_license.py`, `python scripts/check_hygiene.py`
  - Pendiente, paso de cierre de M9 (no se difiere, 2026-09-24): se corre sobre el árbol final cuando terminen los refactors sin cambio de comportamiento que están en curso en `crates/` y `clients/*/src`. Última pasada local completa (2026-09-23): Python 2090 passed, TypeScript 831 passed, sidecar 85 passed, `scripts/tests` 184, `check_hygiene`/`check_pins`/`check_license`/`buf lint` OK; faltan los gates de Rust (`cargo fmt`, `clippy -D warnings`, `cargo test --workspace --locked` en la VM Lima como uid 1500) sobre ese árbol.
  - Hecho (2026-09-24, cierre de M9, árbol final): Rust en la VM Lima (Ubuntu aarch64, `CARGO_TARGET_DIR` propio, `-j 2`): `cargo fmt --all --check` exit 0; `cargo clippy --workspace --all-targets --locked -- -D warnings` limpio; `cargo test --workspace --locked` con los binarios como uid 1500 (`setpriv --reuid=1500 --regid=1500 --clear-groups`): rayd lib 172, bin 5, m1 14, m2 25, m3 30, m4 19 (+1 ignored), m5_pty 15, m5_suspend_resume 7, m6_hooks 7, m6_imds 3, m6_limits 5, m7_poly 9, m9_deno 4, m9_egress 1, m9_metrics_history 6, m9_network 4, m9_timeout 11, m9_transfer 21, s3_store 0 (+1 ignored), rayd-core 498: 856 passed, 0 failed, 2 ignored; `m9_egress` como root en `unshare --net` con `RAYITO_REQUIRE_EGRESS_NETNS=1` → 1 passed; `cargo deny check` → advisories, bans, licenses, sources ok; `cargo auditable zigbuild --release --locked --target aarch64-unknown-linux-musl -p rayd` → `rayd` estático de 13 764 024 B y `check_auditable.py` → `.dep-v0: 257 packages, root rayd 0.2.0`; Python (macOS arm64): `uv run --with pytest-timeout pytest tests/unit -q --timeout 60` 2115 passed; `uvx ruff==0.16.7 check` limpio y `format --check` 219 ficheros; `mypy src tests` sin errores en 217 ficheros; sidecar: `pytest` 85 passed (14 deselected), ruff limpio, `mypy src` sin errores; TypeScript: `pnpm lint` (biome, 139 ficheros), `pnpm typecheck` exit 0, `pnpm test` 40 ficheros / 863 passed, `pnpm build` y `pnpm pack:check` exit 0; `buf lint` exit 0; `check_hygiene.py` OK (939 ficheros); `check_pins.py` OK (8 ficheros); `check_license.py` OK; `gen_limits.py --check` exit 0; `scripts/tests` 186 passed; `mkdocs build --strict` (target `docs` del `Makefile`) sin avisos.
- [x] 7.9 `openspec validate m9-deno-kernels --strict --no-interactive` passes. Only the acceptance agent archives, after 6.5–6.7 are green
  - Parcial (2026-09-23): `openspec validate m9-deno-kernels --strict --no-interactive` → valid tras conservar en `code-execution` el escenario `javascript is a known language no image ships` reescrito (Deno sirve `javascript` en poly, Q61) y en `image-variant` el nombre `poly build verifies its kernel`. El archivo queda para el agente de aceptación (6.5–6.7 pendientes de aceptación en AWS).
  - Hecho (2026-09-24): `openspec validate m9-deno-kernels --strict --no-interactive` → valid con 6.5–6.7 verdes; el archivo secuencial simulado sobre una copia de `openspec/` (deno-kernels → file-transfer → server-timeout → sandbox-observability → egress-policy → e2b-v2-surface) archiva los seis sin error (2026-09-24). El archivo real lo hace el cierre de M9, tras los gates de 7.8.

## Notes

(Implementers record measured values here, placeholders only.)

- **[sidecar] 0.1/0.2, verified 2026-09-22.** `gh api repos/denoland/deno/releases/tags/v2.9.7 --jq ...` printed `39821318` and `sha256:c832298b1ad4422481334855f6003e0f54145762c5a134f20a489511d2f65bbf`; the official `deno-aarch64-unknown-linux-gnu.zip.sha256sum` carries the same hash. `cli/js/jupyter_kernel.js@v2.9.7` still has `Deno.listen({ hostname: this.ip, port: this.port })` (lines 402, 491), `hmacVerify` (202, checked at 872/1025/1184) and `interrupt_request` → `op_jupyter_repl_interrupt()` (1060-1061); `libs/cli_parser/src/env_vars.rs@v2.9.7` lists `DENO_DIR`, `DENO_NO_UPDATE_CHECK` and `NO_COLOR`.
- **[sidecar] Red → green (any host).** Each new test was run against a scratchpad copy with the behaviour removed: baseline source → `test_plain_text_ansi_stripped_only_when_asked` (`TypeError: unexpected keyword argument 'strip_plain_text_ansi'`) and `test_unknown_and_unavailable_languages_are_refused` (`unknown language` instead of `language not installed` for `typescript`) fail; no Deno templates → `test_repository_ships_the_deno_templates` and `test_deno_specs_need_the_binary` fail; `kernel_endpoint` forced to `ipc` and `transport="tcp"` dropped → `test_catalog_names_kernels_and_probe_cells` and `test_kernel_endpoint` fail; `strip_plain_text_ansi` made the identity → `test_strip_plain_text_ansi` and `test_plain_text_ansi_stripped_only_when_asked` fail; `KernelContext._run_cell` no longer passing the flag → `test_only_deno_contexts_strip_plain_text_ansi` fails (a test added beyond D11 because only the optional real-kernel test covered that wiring). With the change: `uv run pytest` 81 passed, 3 skipped, 14 deselected; ruff check/format and mypy clean.
- **[sidecar] Real kernels (Linux x86_64 container, `python:3.12-slim`, `requirements.txt` + `requirements-poly.txt`, Deno 2.9.7 `x86_64-unknown-linux-gnu` checked against its official `.sha256sum`).** `RAYITO_TEST_DENO=<deno> python -m pytest -m kernel` → 14 passed, `test_deno_typescript_context` included: Deno kernel start 0.61 s; `40 + 2` comes back `42`; the kernelspec `env` and the context `envs` reach Deno; `while (true) {}` is stopped by the message interrupt over loopback TCP and `x` is still `42` afterwards (state kept). The interrupted Deno cell ends with **no** `error` event (a Python kernel sends `KeyboardInterrupt`); rayd's timeout path synthesises `ExecutionTimeout` on its own, so the D12 timeout acceptance does not depend on it. The same test with `transport="ipc"` forced fails `RuntimeError: Kernel died before replying to kernel_info` (Deno cannot bind `ipc`, ADR-013).
- **[sidecar] `NO_COLOR` probe (same container, jupyter-client 8.10, x86_64).** Raw `execute_result` `text/plain` of `40 + 2` through the `rayito-typescript` kernelspec: with `NO_COLOR=1` → `'42'`; with `NO_COLOR` removed → `'\x1b[33m42\x1b[39m'`. `NO_COLOR` alone removes the escapes here; 6.8 still records the fact on a real ARM64 poly sandbox, and the D5 strip stays as defence in depth.
- **[infra, done by the sidecar agent] 5.1-5.4.** The poly layer of `image/Dockerfile` is D2's instruction verbatim. `scripts/check_pins.py` gate 3 (`unpinned_downloads`): the new tests fail against the unchanged script (5 failed, 6 passed) and pass with it (11 passed). `python scripts/check_pins.py` → `OK 8 fichero(s): ... (…, Makefile, image/Dockerfile)`; scripts tests 164 passed; `uvx ruff==0.16.7 check scripts` clean; `uvx ruff==0.16.7 format --check scripts` reports only `scripts/check_wheel.py`, unformatted since the root commit and outside this change. `m9-e2b-v2-surface` 2.2 adds its dnf gate to the same script later; the docstring then lists four gates.
- **[sidecar] 5.5, partial.** `copy_sidecar.py` + `image_zip.py` into a scratchpad `image/` with a placeholder `rayd`: `marker_variant` gives `poly` and `full`, and both zips carry `kernel-sidecar/jupyter/kernels/rayito-{javascript,typescript}/kernel.json`. The musl `rayd` build, the real zips and the `stat` of `image/rayd` wait for the rayd surface and the Accept step.
- **[rayd] 1.1-1.6, verified 2026-09-23 on a Linux ARM64 host (Amazon Linux 2023, uid 1000).** Red → green: with the `"typescript"` arm of `Language::parse` removed and the old `InvalidLanguage` text restored, `cargo test -p rayd-core --lib code::` → 5 failed (`parse_accepts_the_canonical_names_and_empty`, `names_and_default_context_ids`, `execute_target_follows_the_routing_table`, `available_languages_from_ready_keep_python_and_count_unknowns`, `messages_never_quote_input`); `cargo test -p rayd --test m9_deno` → 4 failed of 4; `m7_poly::language_and_context_are_exclusive` and `m4_code::context_cap_and_bad_inputs` fail on the old message. With the change: 62 passed (rayd-core `code::`), `m9_deno` 4 passed, `m7_poly` 9 passed, `m4_code` 19 passed and 1 ignored. `cargo clippy --workspace --all-targets --locked -- -D warnings` is clean. `cargo zigbuild --release --locked --target aarch64-unknown-linux-musl -p rayd` succeeds and gives a static aarch64 `rayd` of 13,737,264 B. That binary includes the other M9 changes, so it is not the D12 `RAYITO_RAYD_BYTES_DELTA` base. The full `cargo test --workspace --locked --no-fail-fast` passes except for four tests, none of them from this change. `m5_pty::echo_round_trip_size_tty_uid_and_env` fails because the host's `/etc/profile.d/lang.sh` unsets `LC_ALL` in a non-root login shell. `m5_pty::a_stalled_second_subscriber_is_truncated_alone` and `tar_archiver::restore_merges_directories_and_overwrites_files` (a 0644 directory entry unpacked by a non-root user) fail on the same non-root host. That source file is unchanged since the root commit. The two `m5_suspend_resume` timing tests fail under the shared host's load and pass when run alone (7 passed). `cargo fmt --all --check` differs only in `crates/rayd/tests/m9_transfer.rs`, which belongs to `m9-file-transfer` and is still being edited.
- **[accept-prep] 0.4, 6.1, 6.2 (2026-09-23, us-east-1, `<tu-perfil>`).** Pre-M9 (ACTIVE antes de publicar): `rayito-base` **20.0**, `rayito-base-poly` **4.0** (y `rayito-base-caps` 8.0). `snapshotBuild` de 20.0 (`items[0]` de `list-microvm-image-builds`, el que usa `publish_image.py`) → `RAYITO_BASE_PREVIOUS_SIZES=922832896,1321267200,33738752` (la otra build de 20.0, Graviton 3: 928 624 640 / 1 321 267 200 / 39 063 552). `stat -c %s /usr/local/bin/rayd` en un sandbox de 20.0 (`microvm-<id>`, terminado) → **12 525 664 B**. Nuevo `rayd` (`cargo auditable zigbuild --release --locked --target aarch64-unknown-linux-musl -p rayd`, VM Lima ARM64, árbol M9 con el fix de `supervisor.rs`): **13 756 408 B**, `.dep-v0` con 257 paquetes → `RAYITO_RAYD_BYTES_DELTA=1230744`. Zips: `rayito-image.zip` 13 874 844 B (35 ficheros, full), `rayito-image-poly.zip` 13 874 985 B (36, poly). Publicación (`publish_image.py --base-image-version 1`, las tres a la vez): `rayito-base-poly` **5.0** `SUCCESSFUL`/`ACTIVE` en **236,8 s**, `snapshotBuild` **929 153 024 / 1 500 205 056 / 34 295 808** B (Graviton 3; la de Graviton 4: 930 205 696 / 1 500 205 056 / 34 295 808) → `RAYITO_POLY_SIZES`; `rayito-base` **21.0** `SUCCESSFUL`/`ACTIVE` en **216,6 s**, **927 043 584 / 1 360 146 432 / 39 071 744** B (Graviton 4: 935 993 344 / 1 360 146 432 / 39 071 744) → `RAYITO_BASE_SIZES`. Sin fallo en las líneas de Deno. **Aviso para 6.5**: con estos números `test_deno_snapshot_sizes` no pasa tal como está escrito: `deno_code` = (1 500 205 056 − 1 360 146 432) − (Q57: 1 323 397 120 − 1 320 202 240) = **136 863 744 B**, fuera de la banda 70–100 MB de `DENO_CODE_MAX_BYTES`, y `base_code_delta` = 38 879 232 − 1 230 744 = **37 648 488 B** > 10 MB porque `rayito-base` 21.0 incluye además `git-core` (`m9-e2b-v2-surface` D16, 31 452 146 B de RPM). `deno_memory` = 18 427 904 B y el delta de memoria de la base 4 210 688 B sí caen en la banda de 20 MB. Decisión pendiente del orquestador (ajustar las bandas a lo medido o descontar `git-core`).
